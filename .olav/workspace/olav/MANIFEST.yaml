kind: Agent
name: olav
version: "0.11.0"
description: "OLAV Meta Agent — Platform information and routing assistant (no tools; answers help/status queries)"
route_keywords:
  - help
  - what can you do
  - status
  - health check
  - workspace
requires:
  - olav-platform>=0.11
