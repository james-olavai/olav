{
  "scheduled_tasks": [
    {
      "id": "daily_cache_cleanup",
      "enabled": true,
      "description": "Clean up expired cache entries",
      "schedule": "0 2 * * *",
      "schedule_description": "Every day at 2:00 AM",
      "tool": "clean_cache",
      "parameters": {
        "cache_type": "all",
        "age_days": 7
      }
    },
    {
      "id": "daily_health_check",
      "enabled": true,
      "description": "Check system health",
      "schedule": "0 * * * *",
      "schedule_description": "Every hour at the top of the hour",
      "tool": "health_check",
      "parameters": {}
    }
  ],
  "documentation": {
    "title": "Scheduled Tasks Configuration",
    "howto": "Edit this file to add/modify scheduled tasks. Each task needs: id, schedule (cron), tool name, parameters",
    "example_cron": "0 2 * * * = Daily at 2 AM, */10 * * * * = Every 10 minutes, 0 4 ? * SUN = Sundays at 4 AM"
  }
}
