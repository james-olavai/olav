# 103. Services Platform Migration + netops Scope Reduction

**Decision record:** ADR-0014 (Proposed)
**Issue:** follows ISSUE-ARCH-AUDIT-WORKSPACE-DUAL-COPY-DEBT
**Status:** Phase A DONE 2026-06-02 (commit 185393c2); Phase B/C gated on e2e
**Date:** 2026-06-02

## Progress

- **Phase A DONE (commit 185393c2)** — services is now a platform top-level
  agent: `src/olav/data/workspace/services/` (wheel) + `.olav/workspace/services/`
  (runtime), AGENT.md + prompts + root SKILL.md + 5 scripts; removed from devops;
  routing updated in core/devops/admin; manifest + `.gitignore` + olav.md +
  ADR README updated. Naming collision resolved (maintainer: reclaim `services`;
  6 stale guards updated, 1 test skipped pending admin's workspace_health).
  Verified: governance 1021 passed / 1 pre-existing fail; ownership 0 ERROR.
- **Phase B (devops→platform), Phase C (netops scope + ops retirement),
  Phase D (e2e blessing)** — pending; gated on the new e2e suite.
  Note: runtime dev-mirror edits to admin/devops/olav.md are git-ignored and
  regenerated by `olav refresh`; admin itself is not yet in the wheel source
  (separate gap to address with Phase B/C).

## Target ownership model

| Workspace | Owner | Source of truth | Change |
|---|---|---|---|
| `core` | olav | `src/olav/data/workspace/core` | none |
| `services` | olav | `src/olav/data/workspace/services` | **NEW** top-level (was devops sub-agent) |
| `admin` | olav | (platform) | none — stays separate (ADR-0012) |
| `devops` | olav | `src/olav/data/workspace/devops` | **moved out of netops**; drop `services/` sub-agent |
| `netops` | olav-netops | `olav-netops/.olav/workspace/netops` | none |
| `audit` | olav-netops | `olav-netops/.olav/workspace/audit` | none |
| `ops` | — | — | **retired** (only runtime state remains) |

netops domain after migration = **netops + audit only**.

## Current state (branch 0.20.0)

- `services` content: `.olav/workspace/devops/services/` (SKILL.md +
  scripts: register_service, deploy_service, stop_service, docker_compose,
  api_request; references/SERVICES_API_GUIDE.md). **No** `tools/` dir.
- `src/olav/data/workspace/services/` — **missing** (the bug).
- `devops`: authoritative at `olav-netops/.olav/workspace/devops/`
  (AGENT.md + infra/ + scripts/); netops-source AGENT.md already routes
  services out to `--agent services`.
- `ops`: only `netops_init/state/*.json` runtime snapshots; no AGENT.md.
- `workspace_health`: absent on this branch (only in an unrelated worktree).

## Phased plan

### Phase A — services → platform top-level agent
1. Create `src/olav/data/workspace/services/` from the current
   `.olav/workspace/devops/services/` content (SKILL.md, scripts/,
   references/). Add a top-level `services/AGENT.md` (services becomes a
   first-class agent, not a sub-agent).
2. Deploy mirror: `.olav/workspace/services/` (runtime), regenerated by
   `olav init` from the wheel source.
3. Remove `services/` from devops in **both** devops copies; update devops
   AGENT.md to delegate to the top-level `--agent services` (matches the
   netops-source routing already present).
4. Register/verify `services` is discovered as a top-level agent
   (agent discovery scans `.olav/workspace/<name>/AGENT.md`; confirm no
   hard-coded allowlist needs updating in `core/agent_registry.py` /
   `core/workspace.py`).
5. Tests: `test_platform_workspaces_source_includes_services` → green;
   **redirect** `test_workspace_health_tool_batch2.py` to admin (or have
   admin ship `workspace_health`); update `test_claude_md_consistency.py`
   if it asserts the services path.

### Phase B — devops → platform ownership
1. Move `devops` authority to `src/olav/data/workspace/devops/` (platform
   wheel) — OR keep authoritative in a platform location per final
   Decision 4. Update `ownership_manifest.yaml`: `devops` owner
   olav-netops → olav.
2. Re-point any netops-bundle references; ensure `olav skill install`
   (netops) no longer ships devops.

### Phase C — netops scope + ops retirement
1. `ownership_manifest.yaml`: remove `olav-netops/.olav/workspace/devops/**`
   and `.olav/workspace/devops/**` netops-owned rules; remove `ops`
   workspace rules (keep `netops_init/state/` as generated/ignore).
2. Confirm `olav-netops` ships exactly `netops` + `audit`.
3. Regenerate `.gitignore`; `scan_ownership.py` → 0 ERROR.

### Phase D — governance + e2e gate
1. Update workspace SSOT governance tests for the new layout.
2. Run full governance + the **new e2e suite**. Only after e2e green may
   the new workspace layout be blessed as released (per maintainer).

## Open sub-decision (ADR-0014 Decision 4)

`devops` destination is the least-settled point. Default = platform
(olav), because script generation is generic and netops is scoped to
netops+audit. If `devops`'s NetBox/InfluxDB integrations are judged
network-specific, the alternative is to dissolve devops (script-gen →
core, infra queries → services) rather than move it wholesale. Confirm
before Phase B.

## Why plan-only now

Per maintainer: write the plan + ADR first; do not move files. The area
is mid-refactor with conflicting cross-branch states (admin/services/
devops), so a recorded target (ADR-0014) + sequenced plan must exist
before execution, and execution + release blessing are gated on the new
e2e suite.
