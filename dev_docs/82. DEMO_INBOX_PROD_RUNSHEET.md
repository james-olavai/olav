# 82. Demo Runsheet — Production-Grade Inbox Ingest + Fault Analysis + Change Simulation

> **Workspace**: `~/olav-demo-inbox-prod/`
> **Datasets**: 5 weekly bundles — 2026-01-18 / 2026-01-25 / 2026-02-01 / 2026-02-08 / 2026-02-15
> (Australian university, ~339 Catalyst switches, 2 WLCs, ~2100 APs, 4-week span)
> **Bundle source**: `tmp/anon/bundle_2026-01-18` … `bundle_2026-02-15`
> **Date**: 2026-05-30

## Why this runsheet

Earlier runsheets (`dev_docs/71`) walked OLAV against a small synthetic CLAB
lab (6 nodes).  This one drives OLAV against **production-scale real data**
to validate:

1. **Ingest scales** — ~339 hosts × 35 commands × 5 snapshots in one DB.
2. **Cross-snapshot drift analysis** is correct on real weekly change patterns (5-point series).
3. **Fault analyses** surface real operational issues
   (AireOS migration debt, WLC coverage gaps, AP-density imbalance) on
   data we did *not* curate.
4. **Change simulations** (topology blast-radius, migration sequencing)
   produce useful plans without invented data.

The "fault" findings here are **real ops issues** that exist in this
university's network — re-discovering them from raw CDP/inventory data
is the point.

## Acceptance criteria (per chapter)

Each chapter has a green-check at the end:

| Ch | Title | Pass when |
|----|-------|-----------|
| CH1 | Bootstrap workspace | `.olav/databases/main.duckdb` doesn't exist yet; configs in place |
| CH2 | Ingest 5 bundles | OLAV confirms 5 snapshots ingested; `parsed_outputs` accumulates 5 distinct `snapshot_id`s |
| CH3 | Inventory baseline | Response cites 360+ devices, 280+ Cisco, vendor/model breakdown |
| CH4 | Fault — AireOS drift | Response names ~78% APs on AireOS 8.10, ~20% on IOS-XE 17.x; CVE risk cited |
| CH5 | Fault — WLC coverage gap | Response distinguishes managed vs CDP-only WLCs; names 5 unmanaged |
| CH6 | Fault — AP density imbalance | Response names top over-densified edges (≥28 APs); names foo-ehs2-9300-4 |
| CH7 | Drift detection | Response lists added/removed hosts across 5-point series; names at least 1 weekly change |
| CH8 | Sim — dist failure blast radius | Response: 73→99 components, 55 isolated nodes after foo-dist-4500xv-d failure |
| CH9 | Sim — AireOS migration plan | Response outputs per-site batch table; QS4 and foo sites named |
| CH10 | Final report | `exports/reports/inbox_audit_2026-02-15.md` + 2 drawio files on disk |
| CH11 | Change plan — WS-C4500X upgrade | `exports/change_plans/WS-C4500X_BFS_staged_upgrade_plan.md` with 6 stages |

---

## CH1 — Bootstrap workspace

```bash
DEMO=~/olav-demo-inbox-prod
ANON=/home/yhvh/Olav/tmp/anon

mkdir -p $DEMO/.olav/{databases,config/nornir,expertise,inbox}
mkdir -p $DEMO/exports/{reports,change_plans,scripts}

# Config
cp /home/yhvh/Olav/.olav/config/nornir/*.yaml $DEMO/.olav/config/nornir/
cp /home/yhvh/Olav/.olav/config/api.json       $DEMO/.olav/config/api.json

# auth.mode=none (fresh env has no keyring entry)
python3 -c "
import json, pathlib
p = pathlib.Path('$DEMO/.olav/config/api.json')
d = json.loads(p.read_text())
d.setdefault('auth', {})['mode'] = 'none'
p.write_text(json.dumps(d, indent=2))
"

# Symlink workspace agents (stay in sync with dev)
ln -sf /home/yhvh/Olav/.olav/workspace $DEMO/.olav/workspace

# Symlink the 5 canonical bundle directories (NOT tar.gz — survey_bundle needs dirs)
for DATE in 2026-01-18 2026-01-25 2026-02-01 2026-02-08 2026-02-15; do
    ln -sf $ANON/bundle_$DATE $DEMO/.olav/inbox/bundle_$DATE
done

# Verify
ls -la $DEMO/.olav/inbox/
```

**Acceptance**: `ls $DEMO/.olav/inbox/` shows 5 symlinks (`bundle_2026-01-18` …
`bundle_2026-02-15`), each pointing to a directory with `devices/` + `manifest.yaml`.

---

## CH2 — Ingest 5 production bundles

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops \
  "请按时间顺序依次导入 inbox 目录下的 5 个网络快照包：
   bundle_2026-01-18、bundle_2026-01-25、bundle_2026-02-01、bundle_2026-02-08、bundle_2026-02-15。
   对每个包执行 survey_bundle → validate_bundle → ingest_snapshot 完整流程。"
```

OLAV routes to `netops` → `importer` sub-agent → `survey_bundle` + `ingest_snapshot` × 5.

**Expected wall time**: ~25 min total (5 × ~5 min, ~339 devices each).

**Verify**:

```bash
python3 -c "
import duckdb
con = duckdb.connect('.olav/databases/main.duckdb', read_only=True)
rows = con.execute(
    'SELECT snapshot_id[:20], captured_at::DATE, device_count '
    'FROM netops.v_snapshots_auto ORDER BY captured_at'
).fetchall()
for r in rows: print(r)
con.close()
"
# Expected: 5 rows, dates 2026-01-18 … 2026-02-15, device_count ~339 each
```

**Acceptance**: 5 distinct `snapshot_id`s in `netops.parsed_outputs`; device_count
~339 per snapshot.

---

## CH3 — Inventory baseline

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "统计全网设备清单：按厂商和平台分类，列出 top 10 型号，以及通过 CDP 发现的 AP 总数"
```

**Expected response includes**:
- ~295 Cisco devices (cisco_ios + cisco_nxos), ~44 Unknown (CDP-only, un-SSH'd)
- Top models: WS-C3850-12X48U, C9300-48UXM, C9500-48Y4C, C9800-40-K9
- AP count via CDP: ~2100

**Acceptance**: numbers reproducible across re-runs (based on latest snapshot);
vendor `Unknown` count matches CDP-only external devices.

---

## CH4 — Fault analysis #1: AireOS migration debt

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "分析全网 AP 固件版本分布，识别 AireOS 8.10 迁移债务，评估 CVE 风险"
```

OLAV routes to `analyzer` → queries `show cdp neighbors detail` parsed data →
extracts firmware version strings → groups by AireOS/IOS-XE family.

**Expected response includes**:
- AireOS 8.10 (legacy): ~1644 APs (~78%)
- IOS-XE 17.x (new): ~415 APs (~20%)
- CVE risk: AireOS 8.10.185.0 reached end-of-life; fixes only in 17.x
- Migration recommendation: hardware C9130/CW9166 first, then join to 9800 WLC

**Acceptance**: response names the percentage split, cites EOL/CVE risk, and
recommends a migration strategy.

---

## CH5 — Fault analysis #2: WLC coverage gap

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "识别所有 WLC 控制器：哪些已被 OLAV SSH 管理，哪些仅在 CDP 邻居表中可见但未被管理？"
```

**Expected response includes**:
- Managed (SSH): foo-wlc-9800-l, qs4-2p1-wlc1 (Catalyst 9800 IOS-XE)
- Unmanaged (CDP-only): Foo-WLC-8540-1, Foo-WLC-8540-1-Standby,
  Suns-WLC-8540-1, Suns-WLC-8540-1-Standby, foo-wlc-provisioning
- 5 of 7 WLCs outside OLAV visibility
- 8540s likely run AireOS (no IOS-XE upgrade path — hardware EOL)

**Acceptance**: response distinguishes managed vs unmanaged WLCs, names
at least 3 of the 5 unmanaged devices.

---

## CH6 — Fault analysis #3: AP density imbalance

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "找出接入层中连接 AP 数量最多的交换机（20台以上），评估单点故障风险"
```

**Expected over-densified edges (≥28 APs)**:
- foo-ehs2-9300-4: 37
- QS4-B1S1-EDGE: 32
- foo-ehp1-9300 / foo-ps1-3850-1 / foo-ls3-3850: 28 each

**Acceptance**: response names foo-ehs2-9300-4 as top edge, cites single-point-of-failure
risk, recommends stack splitting.

---

## CH7 — Cross-snapshot drift detection

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "对比 2026-01-18 到 2026-02-15 这 5 个周快照的演变趋势：新增设备、下线设备、软件版本升级，按时间序列展示变化轨迹"
```

OLAV routes to `analyzer` → calls `diff_snapshots` → compares `parsed_outputs`
and `topology_links` across the two snapshot IDs.

**Acceptance**: response lists at least 1 added host, 1 removed host, and
1 software version change between the two snapshots.

---

## CH8 — Change simulation #1: foo-dist-4500xv-d chassis failure

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "alpha-dist-4500xv-d 故障后，网络会断成几个部分？"
```

OLAV routes to `reporter` → calls `inspect_blast_radius` (NetworkX weakly-connected
components on the DB topology graph).

> **Note**: `inspect_blast_radius` counts nodes that become **completely
> disconnected** (zero-degree, all edges severed) when `alpha-dist-4500xv-d`
> is removed from the topology graph. Expected numbers depend on the topology
> link count in the demo snapshot.

**Acceptance**: OLAV response cites network partition count and mentions
`alpha-dist-4500xv-d` or `4500xv` in the analysis.

---

## CH9 — Change simulation #2: AireOS → IOS-XE migration plan

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "生成 AireOS 到 IOS-XE 的分批迁移计划：按建筑站点分组，每站点每批最多 5 台 AP，输出各站点需要的批次数"
```

OLAV routes to `analyzer` → queries legacy AP list → groups by site prefix →
computes batch count per building.

**Expected**: per-building batch table — QS4 site: ~200 legacy APs → ~40 batches;
foo site: ~800 legacy APs → ~160 batches. Total ~150-300 migration windows
depending on parallelism.

**Acceptance**: response produces a per-site rollout schedule with at least
QS4 and foo sites named, batch count derivable from AP count / 5.

---

## CH10 — Final report

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "生成完整的网络审计报告：1) 全网拓扑 drawio 图；2) QS4-2P1-BORDER-S1 2跳局部视图 drawio；3) 包含 CH3-CH9 所有发现的 Markdown 报告。保存到 exports/"
```

OLAV routes to `topology` sub-agent → `build_adjacencies_view` + `render_topology_drawio`
→ `format_and_export` × 3.

**Acceptance**: 3 files on disk:
- `exports/topology/inbox_full_topology.drawio` — opens cleanly in draw.io
- `exports/topology/inbox_border_s1_2hop.drawio`
- `exports/reports/inbox_audit_2026-02-15.md` — all numbers from CH3-CH9 present

---

## Hard rules

- **No fabricated facts.** Every number in the final report must come
  from OLAV's DB queries — no paraphrasing.
- **Snapshots are immutable.** Both bundles' `content_sha256` recorded
  in `bundle_ingests` — re-running produces identical numbers (modulo `ingested_at`).
- **Change simulations are read-only.** CH8/CH9 do NOT push to any
  device — they compute reports against the in-DB topology graph only.

---

## Total budget

| Activity | Time |
|---|---|
| CH1 (bootstrap) | 5 min |
| CH2 (ingest 5 bundles) | **~25 min** (5 × ~5 min, ~339 devices each) |
| CH3-CH6 (4 fault analyses) | ~10 min |
| CH7 (drift) | ~5 min |
| CH8-CH9 (simulations) | ~15 min |
| CH10 (report + drawio) | ~10 min |
| CH11 (change plan) | ~10 min |
| CH12 (admin health check) | ~3 min |
| CH13 (audit explorer) | ~8 min |
| CH14 (devops script gen) | ~5 min |
| CH15 (memory injection) | ~5 min |
| **Total** | **~1h51m** |

---

## CH11 — Change Plan: Catalyst 4500X Distribution IOS-XE Upgrade

**触发依据**: CH7 发现接入层已从 17.9.6 升至 17.12.5，但全部 WS-C4500X-32
distribution/wifi/server 层仍停留在 03.11.10.E（2021 年发布，距当前 latest stable
03.11.12.E 差两个 maintenance release）。版本割裂导致 VSS/LACP 报文格式不一致，
存在 CDP capability mismatch 和 QoS policy 解释差异风险。

### 分阶段升级计划（OLAV 自主生成）

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "制作所有 WS-C4500X-32 设备的分阶段 BFS 升级变更计划（alpha-dist-4500xv-a、alpha-dist-4500xv-d、alpha-dist-4500xv-m 等共 14 台），叶节点优先，保存到 exports/change_plans/WS-C4500X_BFS_staged_upgrade_plan.md"
```

OLAV routes to `analyzer` → SQL topology queries → `inspect_blast_radius` per stage
→ `format_and_export(mode='append')` × 6.

**验收**: `exports/change_plans/WS-C4500X_BFS_staged_upgrade_plan.md` 存在，包含：

| 阶段 | 设备 | blast radius 等级 |
|---|---|---|
| Stage 1 | foo-dist-4500xv-b, c | Low |
| Stage 2 | foo-dist-4500xv-e, f | Low |
| Stage 3 | foo-dist-4500xv-g, h | Low |
| Stage 4 | foo-dist-4500xv-i, j, k | Low |
| Stage 5 | foo-dist-4500xv-l, n, d | High（d: 35 接入交换机） |
| Stage 6 | foo-dist-4500xv-a, m | Critical（核心汇聚，影响 16+25 接入交换机）|

### 升级前验证

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "列出所有 WS-C4500X-32 设备当前的 IOS 版本、CDP 邻居数量和上行接口状态，作为升级前 baseline"
```

**验收**: 返回 14 台设备的版本（应为 03.11.10.E）、邻居数和接口状态；数字存档用于升级后对比。

### 升级后验证

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "对比升级前快照和升级后快照，确认所有 WS-C4500X-32 已升至 03.11.12.E，CDP 邻居无丢失"
```

**验收**: 所有 14 台返回 ✓ OK；任何 ✗ FAILED 触发回滚流程。

### 回滚标准

满足以下任一条件立即回滚至 03.11.10.E：
1. 升级后 CDP 邻居数量减少 >1（邻居丢失）
2. 任意 uplink 接口 `link_status != 'up'`（拓扑中断）
3. 升级后 5 分钟内发生 BGP/OSPF flap（路由不稳定）
4. AP 关联数在 WLC 侧下降 >10%（Stage 5 专用指标）

### C9500 Border 版本一致性检查

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "检查所有 C9500 设备的 IOS-XE 版本一致性，识别版本差异（suns-border-9500 已升至 17.15.3）"
```

---

## CH12 — Admin: 平台运行状态健康检查

**适用场景**: 演示开始或结束时展示 OLAV 自身的可观测性——平台不只分析网络，也能自我诊断。
不依赖 demo 快照，任何时间点均可演示。

```bash
cd ~/olav-demo-inbox-prod
olav --agent admin "OLAV 平台当前运行状态是否正常？检查关键服务健康状态"
```

OLAV routes to `admin` → queries platform service registry → LLM / DB / workspace 状态检查。

**预期**: 响应包含关键服务（LLM、database、workspace）的健康状态，明确标注 OK / Warning / Error。

**验收**: 响应中出现 health / status / running / healthy / 正常 / 平台 等关键词，无 Python traceback。

---

## CH13 — Audit: 自主网络异常探索

**适用场景**: 展示 OLAV `audit` 代理的自主分析能力——不给具体问题，让 agent 自主发现异常。
演示"开卷考试"模式：agent 自行决定查什么、查多深。

```bash
cd ~/olav-demo-inbox-prod
olav --agent audit "探索最新快照的网络数据，找出值得关注的问题或异常，输出发现摘要"
```

OLAV routes to `audit` orchestrator → spawns `author` sub-agent → SQL 扫描多个视图
（BGP summary、interface brief、OSPF neighbor）→ 汇总发现报告。

**预期**: 输出包含至少一个具体设备名或版本号，并说明发现的问题或异常。

**验收**: 响应中出现设备名（alpha/beta/cisco/9300/3850/4500/AP/WLC）等网络要素，
有"发现/found/issue/问题/异常/result"等关键词，无 Python traceback。

---

## CH14 — Devops: 自动化脚本生成

**适用场景**: 展示 OLAV 不局限于分析——可以直接生成可执行的运维脚本。
不依赖 demo 快照，纯 LLM 能力展示。

```bash
cd ~/olav-demo-inbox-prod
olav --agent devops "生成一个 Python 脚本，通过 SSH 批量备份网络设备的运行配置（show running-config），设备列表从文件读取"
```

OLAV routes to `devops` sub-agent → 直接生成包含 SSH 连接逻辑的 Python 脚本。

**预期**: 输出包含可运行的 Python 代码，含 `def`/`import`/`for` 等语法结构，
引用 paramiko / netmiko / subprocess 等 SSH 库。

**验收**: 代码块中出现 Python 关键字（`def `, `import `, `for `, `open(`），
并包含 ssh / backup / 备份 / script 等语义关键词。

---

## CH15 — Memory Injection: 用户知识库自动注入

**适用场景**: 展示 OLAV 的 org-scope 专家知识库（Expert KB）功能——
将组织特有的运维规范（变更窗口、联系人、SLA）写入知识库后，
任何后续 agent 会话都能自动检索并引用，无需每次在 prompt 中重复说明。

### 步骤 1 — 写入专家知识文件

创建 `~/.olav/expertise/alpha_change_window.expert.yaml`（或放在 workspace 路径下）：

```yaml
schema_version: 1
topic: alpha_site_change_window
scope: org
keywords:
  - alpha
  - change window
  - maintenance window
  - 变更窗口
  - 维护窗口
  - tuesday
  - 周二
  - "22:00"
body: |
  Alpha site (alpha.net.demo.internal) maintenance window:
  Every Tuesday 22:00-02:00 CST. Emergency contact: james.chen@wwt.com.
  All changes to alpha-dist and alpha-ehs2 devices must be scheduled
  within this window. No exceptions without CAB approval.
```

### 步骤 2 — 导入到 LanceDB

```bash
cd ~/olav-demo-inbox-prod
olav kb import-experts .olav/workspace
```

**预期**: 输出 `Imported N expert(s)` — 专家条目写入 LanceDB 向量索引。

### 步骤 3 — 验证自动召回

```bash
cd ~/olav-demo-inbox-prod
olav --agent netops "alpha 站点的变更维护窗口是什么时候？联系人是谁？"
```

OLAV routes to `netops` → auto-recall middleware 在 org-slot 检索专家 KB →
向量相似度匹配命中 `alpha_site_change_window` → 注入 system prompt → LLM 回答。

**预期**: 响应包含 "Tuesday / 周二 / 22:00" 和联系人邮箱 `james.chen@wwt.com`，
内容来源于知识库而非 demo 数据库快照。

**验收**: 响应中出现 tuesday/周二/22:00/维护/变更/窗口/change window/maintenance 之一，
以及联系人信息（james.chen 或 wwt.com），无 Python traceback。

**关键展示点**: agent 从未在 demo 数据库中存储过这个变更窗口——它来自用户侧知识库注入，
体现了 OLAV "活知识"与"静态快照"双轨并行的架构优势。
