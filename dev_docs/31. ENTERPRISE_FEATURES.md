# 企业版功能清单（BSL Commercial Tier）

> **实测校准（2026-08-04）**
>
> 校准前本文档 **62 个 checkbox 全部未勾、0 个已勾**——从落地至今没有人维护过，因此
> 它既不反映已完成的工作，也不反映实际在做的工作。本次按**代码取证**逐子项核对
> （grep 关键符号 + 确认是 `def`/赋值而非 docstring 提及），结果：
>
> | | 数量 |
> |---|---|
> | 已实现 `- [x]` | **52** |
> | 部分实现 `- [~]` | 2 |
> | 未实现 / 缺验收标准 `- [ ]` | **37** |
>
> 计数在 2026-08-04 当天两次变化，避免误读：**第一次校准**是 20/2/40（当时只覆盖
> AAA + 审计合规两节）；随后 **删除过期的 `ent_batfish_analyzer` 节**（-1 已勾、-10 未勾），
> 并**补入 `ent_twin_*` + `ent_cab_*`**（+33 已勾、+9 未勾——后者是**验收标准缺失**，
> 不是功能未实现）。所以现在的 52/37 与第一次的 20/40 不可直接比较。
>
> | 功能级 | 项 |
> |---|---|
> | 完整实现（设计先行清单） | 5：`ent_dataset_redaction` / `ent_dataset_sft` / `ent_dataset_trajectory` / `ent_dataset_atif` / `ent_dataset_encryption` |
> | 完整实现（代码先行，验收标准待补） | 6 组：`ent_twin_fleet` / `ent_twin_intent` / `ent_twin_build` / `ent_twin_run` / `ent_twin_prod` / `ent_cab_tcf` |
> | 部分实现 | 2：`ent_sso_providers` 仅 LDAP；`ent_dataset_access_token` 仅一个字符串字段 |
> | 未开始 | 5：`ent_auth_rate_limit` / `ent_admin_dual_control` / `ent_rbac_policy_management` / `ent_log_integrity` / `ent_log_retention` |
> | 已删除 | 1：`ent_batfish_analyzer`——不是未实现，是过期设计，能力在开源 netops 侧 |
>
> **更重要的发现：本文档漏掉了 olav-ent 实际代码量的 68%。** `lab/` + `lab/twin/` +
> `cab/` 共 40 个模块（数字孪生 / CAB 变更记录）在校准前一个字都没提——ent 的开发
> 重心早已转到那条线，而这份 AAA + 审计合规清单基本停在原地。已补「未在本清单内的
> 已实现能力」一节。
>
> 末尾「开源版 vs 企业版快速参照」表的 ✅ 表示**分层归属**（该功能属于哪一层），
> **不表示已实现**；本次为该表补了「状态」列以消除误读。

**Updated:** 2026-04-17（内容）／2026-08-04（实测状态校准）  
**对应设计文档：** `olav_aaa.md`、`log_rector.md`、`plugin_rector.md`、`audit_dataset_export.md`、`encrypted_dataset_control.md`、`dev_docs/00. issues.md` ARCH-06/14（开源 fallback 路径）

> **分界原则**：安全基线（基础认证、凭据脱敏、安全 Cookie）属于**开源版，必须实现**；  
> 合规增强（SSO、审计哈希链、长期保留、训练数据导出、高级速率限制）属于**企业插件**。  
> 企业插件通过 Python entry points 发现，不在开源目录中（见 `plugin_rector.md`）。  
> 参照：HashiCorp Vault / Teleport / GitLab 同一切割模式。

> **RBAC 分界补充**：
> - **默认内置 / 开源基线**：固定三角色（admin/user/readonly）+ 默认 skill 权限矩阵 + admin-only workspace lifecycle
> - **企业增强**：可编辑 RBAC 策略、用户组、审批流、策略版本化、UI/CLI 管理工具
> - 因此，企业版卖点不是“有没有 RBAC”，而是“能否灵活管理和审计 RBAC”

> **命名规则**：从本版开始，所有企业功能统一使用 `ent_` 前缀作为功能标识；旧的 `E-A1`、`E-4.5` 等编号仅作为历史别名保留在注释中，不再作为新设计主标识。

---

### AAA 企业层（auth_rector.md 对应项）

#### ent_sso_providers（原 E-A1） 企业 SSO 集成（olav_aaa.md §3 Tier 2）

- [~] `LDAPAuthProvider`：ldap3 包激活 ✓（`auth/ldap_provider.py`），**group → role 映射 ✗**（无 `group_to_role` / `role_map`）
- [ ] `ActiveDirectoryAuthProvider`：msal 包，AD group 到 OLAV 角色映射
- [ ] `OIDCAuthProvider`：authlib 包，JWT 验证 + claims 映射
- [~] `api.json` auth.mode 切换 ✓（`auth_mode` 已接线），但只有 `"ldap"` 可用——`"ad"` / `"oidc"` 无实现

#### ent_auth_rate_limit（原 E-A2） 高级速率限制与暴力破解保护（olav_aaa.md GAP-2/3）

> **实测 2026-08-04：未实现（无 `list-blocked-ips` / `auth_rate_limited` / IP 锁定）**

- [ ] 连续失败 ≥ 5 次 → 10 分钟 IP 锁定（内存 + DuckDB 持久化）
- [ ] `auth_rate_limited` 事件写入 audit.duckdb
- [ ] `olav admin list-blocked-ips` + `olav admin unblock <ip>`

#### ent_admin_dual_control（原 E-A3） Admin 强制轮换与双人确认（olav_aaa.md GAP-3）

> **实测 2026-08-04：未实现（无 `rotate-token`、无双人确认）**

- [ ] Admin token 默认 90 天 `expires_at`，到期前 7 天 CLI 警告
- [ ] `olav admin rotate-token`
- [ ] Admin 操作（add-user/revoke）记录独立 `admin_action` 事件，含操作者身份
- [ ] 两人确认（Two-Person Integrity）标志位，高危操作可选启用

#### ent_rbac_policy_management（新增） 企业 RBAC 策略管理（olav_aaa.md D6 / §7.2）

> **实测 2026-08-04：未实现（无 rbac CLI / 用户组 / 策略版本化）**

- [ ] `admin rbac list/grant/revoke` CLI
- [ ] 用户组 / 组成员 / 组到 skill 权限映射
- [ ] RBAC 策略版本化与回滚
- [ ] 高危授权变更审批流 / 双人确认
- [ ] WebUI 权限矩阵管理页
- [ ] 所有 RBAC 变更写入独立 `admin_action` / `rbac_change` 审计事件

---

### 审计日志与数据集导出企业层（log_rector.md 对应项）

> **本节范围仅限「审计**日志**」（audit log / trail），不含「审计**agent**」。**
> 这两件事在本系统里同名而不同物，是命名混淆的根源：
>
> | | 归属 | 状态 |
> |---|---|---|
> | 审计 **agent**（`audit-orchestrator` + author/runner/explorer/profiles；跑健康检查 Profile、合规报告、SLA、开放式调查） | **netops 开源**（`olav-netops/.olav/workspace/audit/`） | 已实现 |
> | 审计 **trail** 记录（`audit_runs`/`audit_events`/`audit_messages`/`audit_tool_calls`）+ `olav log` 24h 查询窗口 | **core 开源**（`core/audit_recorder.py`） | 已实现 |
> | trail 防篡改与长期留存 | **ent**（`ent_log_*`） | 未实现 |
> | trail → 训练数据导出（SFT/trajectory/ATIF）+ 脱敏 + 加密 | **ent**（下方 `ent_*_export` 等） | 已实现 |
>
> 因此本节内部实际是**两组不相干的关注点**：`ent_log_*`（轨迹可信与留存，属合规控制）
> 与 `ent_*_export` / `ent_dataset_*`（轨迹变现为训练数据，**不是**合规控制——它增加而非
> 减少数据外流面）。前者的 ID 已于 2026-08-04 从 `ent_audit_*` 改为 `ent_log_*` 对齐
> CLI 动词面（`olav log verify` / `olav log archive`）；**后者一组的 ID 尚未重命名**
> 已于 2026-08-04 一并重命名为 `ent_dataset_*`（待决问题 8 关闭）——依据是代码自己的
> 词汇（`audit_dataset_export.py` / `dataset_encryption.py` / `memory_dataset_export.py`）
> 与 CLI 动词面 `olav log export sft|trajectory|atif|raw`。旧 ID 作为历史别名保留在
> 各功能标题里。

#### ent_dataset_redaction（原 ent_field_redaction / E-1） 字段级脱敏引擎（log_rector.md §7.2–7.5）

- [x] 高风险字段名规则（password / token / api_key / community 等）
- [x] IP/hostname/username 稳定哈希映射
- [x] 设备配置块级替换规则
- [x] 可配置白名单与保留策略
- [x] `redaction` 字段元数据写入（`applied`, `policy_version`, `fields_redacted`）
- [x] 脱敏后再导出门控

#### ent_dataset_sft（原 ent_sft_export / E-2） SFT Chat JSONL 导出（log_rector.md §9.2）

- [x] `audit_to_sft_jsonl()` 函数
- [x] 从 `audit_runs` + `audit_messages` 派生规范主格式 `sft.jsonl`
- [x] 过滤失败 run / 高风险操作

#### ent_dataset_trajectory（原 ent_trajectory_export / E-3） Tool-use Trajectory JSONL 导出（log_rector.md §9.2）

- [x] `audit_to_tool_trajectory()` 函数
- [x] 从 `audit_events` 重建 `instruction → reasoning → tool_call → tool_result → final` 轨迹，产出规范主格式 `trajectory.jsonl`
- [x] 脱敏后再导出

#### ent_dataset_atif（原 ent_atif_export / E-4） ATIF / Harbor 风格轨迹导出（log_rector.md §9.2）

- [x] 产出可选分析格式 `atif.jsonl`
- [x] 对接 LangSmith 或内部分析工具
- [ ] `_legacy_archived/memory-lancedb-pro/` 中已存参考实现，待整合

#### ent_dataset_encryption（原 ent_dataset_export_encryption / E-4.5） 训练集导出加密控制（encrypted_dataset_control.md）

- [x] 使用 `Google Tink` 作为企业版训练集导出加密库
- [x] 增加 `dataset_export.encryption_mode = disabled | optional | required`
- [ ] `dev/test` 允许明文导出，`prod` 强制加密
- [x] 对规范主格式和训练派生格式统一产出 `.enc` 文件
- [x] 统一使用带 `encryption` 对象的 `manifest.json`
- [ ] `required` 模式下禁止 `--no-encrypt`
- [x] 预留 `key_ref` 以对接 KMS / Vault

#### ent_dataset_access_token（原 ent_local_train_one_time_token / E-4.6） 可选本地训练一次性 token 控制（encrypted_dataset_control.md）

- [ ] 增加 `dataset_export.local_train_access_mode = none | one_time_token | managed_only`
- [ ] 支持一次性 token 签发、短 TTL、单次使用、绑定 `export_id`
  > **实测（2026-08-04）：未实现。** 全仓只有 `dataset_encryption.py:277` 的一个
  > 字符串字面量 `"access_mode": "one_time_token"` 写进 manifest —— 没有签发、
  > TTL、单次使用、`export_id` 绑定的任何代码。首轮探测曾据此误判为已实现，靠追
  > `def`/赋值才发现。
- [ ] 记录 token 签发和使用审计
- [ ] 明确 `one_time_token` 仅提高访问门槛，不作为 DRM 能力宣传
- [ ] 对“尽量不让客户自己训练”的场景，优先提供 `managed_only`

#### ent_log_integrity（原 ent_audit_hash_chain / E-5） 审计日志防篡改（log_rector.md §8.5）

**实现机制：** SHA-256 哈希链（`prev_event_hash` + `event_hash` 逐行链式签名）。
**验证入口：** `olav log verify --run <run_id>`。

> ID 命名说明：锁定「保证什么」（audit log integrity —— 安全领域标准词，审计师与
> 采购方都认）而非「怎么实现」。若日后从哈希链换成签名快照 / Merkle 树 / 外部公证，
> 这个 ID 不需要跟着变。也避开了两个已被占用的词：`chain` 在本仓已是 LangChain
> （7 个核心依赖）和 audit-runner 的 `cascade_chain`（故障级联链，就在 audit 域内）；
> `audit` 则会与开源的**审计 agent** 混淆（见本节开头）。

> **实测 2026-08-04：未实现（无 `prev_event_hash` / `event_hash` / `olav log verify`）**

- [ ] `audit_events` 表增加 `prev_event_hash` + `event_hash` 列
- [ ] 写入时计算 SHA-256 链式签名
- [ ] 验证工具 `olav log verify --run <run_id>`

#### ent_log_retention（原 ent_audit_archive / E-6） 审计日志长期保留与归档（log_rector.md §8.4，olav_aaa.md GAP-5）

> **实测 2026-08-04：未实现（无保留策略 / `olav log archive` / Parquet 归档）**

- [ ] 可配置保留策略（`api.json` `audit.retention_days`，推荐 365 天）
- [ ] 归档到外部存储（S3 / GCS），导出格式 Parquet
- [ ] `olav log archive --before <date>`
- [ ] 开源版默认 24h 查询窗口，超出部分自动压缩不删除

---

### 诊断与分析：Batfish 属开源 netops，不是企业功能

> **本节原有的 `ent_batfish_analyzer` 于 2026-08-04 删除（原 82 行设计已移除）。**
>
> 删除原因：**该能力已在开源侧实现，本节的分层声明是错的。** olav-netops（BSL 开源）
> 已有完整 Batfish 栈：
>
> | 组件 | 位置 |
> |---|---|
> | `batfish_q` 通用 question runner（`@tool`） | `netops/simulator/tools/batfish_q.py` |
> | `batfish_capability` 厂商支持预检 | `olav_netops/core/sim/batfish_capability` |
> | question / capability 目录（steering guide） | `netops/simulator/references/batfish_{question,capability}_catalog.guide.yaml` |
> | simulator 子 agent | `netops/simulator/SKILL.md` |
> | pybatfish 依赖声明 | olav-netops `[sim]` extra |
>
> 被删掉的那节声明了 4 个 `inspect_batfish_*` inspector（traceroute / route_lookup /
> policy_test / compare_snapshots）——**它们一个都不存在**；同时那张对比表把
> 「ACL reachability / config what-if / 跨 snapshot differential reachability」标为
> 企业版独有，而这些恰恰是 `batfish_q` 能回答的 Batfish question，且在开源侧。
> 也就是说这一节不是「未实现的企业功能」，而是**一份从未更新的过期设计**。
>
> **ent 侧真实的 pybatfish 用途只有一处**：数字孪生的 T0 门禁
> （`lab/twin/t0_gate.py` + `design.py`），且 t0_gate 的 docstring 明写
> 「**Absence is never a blocker**」——缺 pybatfish 时报
> `reason = "pybatfish is not installed"` 并放过，不阻断设计。它由 olav-ent 的
> `[batfish]` extra 声明（2026-08-04 补上，此前无任何声明，运维无从知道可以装）。
> 该门禁属于孪生能力，应记入下方 `ent_twin_*` 清单而非独立的分析引擎功能。
>
> 若日后确实要做企业级 Batfish 增强，前提是先说清它相对开源 `batfish_q` 的**增量**
> 是什么——原设计没有回答这个问题，这正是它过期而无人察觉的原因。

### 数字孪生企业层（`ent_twin_*`，dev_docs/112 / 113）

> **补记 2026-08-04（待决问题 10）。** 这一节此前**完全不存在**，而它覆盖的 33 个模块
> 是 olav-ent 最大也最新的一块（`lab/twin/` 25 + `lab/` 8，占 ent 代码 56%）。功能清单
> 长期只描述 AAA + 审计合规那一半，导致完成度比例被误读为 ent 的成熟度。
>
> **本节的勾选口径与上文不同**：这些能力是**代码先行**的（`dev_docs/112`/`113` 是设计
> 文档，不是验收清单），所以按「模块已存在」勾选，并单独列出**尚缺的验收项**。这与
> `ent_dataset_*` 那种「设计先行、逐项打勾」的清单性质不同，也是它此前无法被纳入本
> 文档的原因。

**能力定位：** 用现网真实配置构建 containerlab 数字孪生，在孪生上验证变更，再推生产。

#### ent_twin_fleet 现网侧输入（设备 → 真实配置与平台）

- [x] `fleet.py` 设备名 → 真实配置 + 平台解析
- [x] `scanner.py` 镜像扫描 + 资格门禁（dev_docs/96 §2）
- [x] `capability.py` 已发现命令能力缓存（§9.1）
- [x] `profiles.py` 平台 profile（一平台一文件，零平台专属代码）
- [x] `resolve.py` 设备 → clab kind 解析
- [ ] 验收项：平台覆盖矩阵——哪些 platform family 已端到端验证过

#### ent_twin_intent 意图与变更计划

- [x] `intent.py` 声明式 lab 验证契约（§2.2）
- [x] `author.py` 从 LLM 已知的少量信息推导完整 spec
- [x] `changeplan.py` 变更计划 → lab 验证（管线缺失的第一段）
- [x] `bucket.py` 确定性分层归桶（§5）
- [ ] 验收项：意图 schema 版本化与向后兼容策略

#### ent_twin_build 配置切片与孪生装配

- [x] `parse.py` 层次化 CLI 配置解析（§5.2）
- [x] `slice.py` 依赖闭包切片（§6.1）
- [x] `synth.py` 合成对端生成（§7）
- [x] `remap.py` 接口重映射（prod 名 → lab 名，确定性双射）
- [x] `assemble.py` 把切片脚手架渲染为可部署产物
- [x] `startup.py` / `design.py` 渲染节点可部署配置 / greenfield 设计
- [ ] 验收项：切片正确性判定标准——漏切与过切如何界定和检测

#### ent_twin_run 部署、验证与演练

- [x] `clab_client.py` clab REST 生命周期客户端（containerlab-api-server v0.5.x）
- [x] `runner.py` 部署 + 分层确认（§3 C/D、§8，Path A）
- [x] `predicates.py` 逐层判定谓词库（§8.1）
- [x] `roundtrip.py` 往返校验——推下去的配置是否真的生效
- [x] `drill.py` 失效演练——断链路，看架构是否存活
- [x] `t0_gate.py` T0 门禁——花 lab 时间之前先做语义检查（可选 pybatfish，**缺失不阻断**）
- [ ] 验收项：分层确认的失败语义——哪一层失败对应什么处置

#### ent_twin_prod 生产侧访问与推送

- [x] `node_access.py` 生产节点访问（§8.0）
- [x] `node_cli.py` 节点 CLI——SSH 推配置 + 跑 show（§8.2）
- [x] `lab/deploy_lab.py` / `deploy_and_push.py` clab 拓扑部署（含强制 post-deploy workaround）
- [x] `lab/push_node_config.py` 推 SR Linux CLI 配置
- [x] `lab/deploy_io.py` / `_images.py` / `_paths.py` 部署 I/O、镜像单一来源、路径解析
- [ ] 验收项：生产推送的审批与回滚路径——`db_write_gate` 是否覆盖此路径未验证

---

### CAB 变更记录企业层（`ent_cab_*`）

> 同为补记（待决问题 10）。7 个模块，此前不在清单内。

#### ent_cab_tcf TCF（Test Case File）变更记录

- [x] `tcf_schema.py` TCF schema 的 Pydantic 模型
- [x] `tcf_io.py` TCF 文件读写
- [x] `tcf_diff.py` TCF spec ↔ lab 结构化 diff
- [x] `tcf_lab.py` lab 侧 TCF 编排
- [x] `prod_cli.py` sim 侧确定性 prod-form CLI 生成（R94.1）
- [x] `intent_registry.py` 意图单一来源（ISSUE-ARCH-40）
- [x] `lab/spec_footer.py` 向 CAB markdown spec 追加 Lab Validation Footer
- [ ] 验收项：TCF schema 的版本化与迁移策略
- [ ] 验收项：diff 的判定口径——什么算「lab 与 spec 一致」

### 这两节仍缺的东西（诚实标注）

- **无分层归属原则**：本文档开头的「分界原则」没有覆盖孪生 / CAB——它属于企业层的依据
  目前只是「代码在 olav-ent 里」，而不是一条明确的切割原则。销售侧因此说不清它**为什么**
  是企业功能。
- **快速参照表未加对应行**：本次刻意未加——分界原则未定之前加进去会是猜测。
- **9 项验收标准缺失**（上面各 `- [ ]`）：它们不是「未实现的功能」，而是「已实现但没有
  判定通过与否的标准」。

---

## 未在本清单内的已实现能力（2026-08-04 补记）

**这是本次校准最重要的发现**：olav-ent 实际有 59 个 Python 模块，其中 **40 个（68%）
在本清单里从未被提及**。开发重心早已从「AAA + 审计合规」转向「数字孪生 / 变更验证」。

| 域 | 模块数 | 内容 | 对应设计文档 |
|---|---|---|---|
| `lab/twin/` | 25 | 数字孪生：拓扑装配、意图起草、能力探测、变更计划、T0 门禁、节点访问、演练 | `dev_docs/112` |
| `lab/`（twin 之外） | 8 | clab 部署、镜像解析、配置推送、spec footer | `dev_docs/112` |
| `cab/` | 7 | TCF 变更记录：schema、IO、diff、意图注册、lab/prod CLI | — |
| `api/` | 5 | Web API + WebGUI（含 langgraph auth 适配） | 清单里作为「已迁出 OSS」提及，无实施项 |
| 顶层 | 10 | `daemon` / `daemon_svc` / `web_svc` / `db_write_gate` / `benchmark` / 各 dataset 导出 | 部分对应 `ent_*_export` |

**含义**：不要用本清单的完成度（19/62）判断 ent 的成熟度——它只覆盖了 AAA 与审计
合规那一半，而那一半确实基本没动；真正在推进的孪生/CAB 那一半没有对应的功能清单与
验收项。**建议为 `lab/twin` + `cab` 补一份同规格的 `ent_twin_*` / `ent_cab_*` 清单**，
否则这 40 个模块处于「有代码、无验收标准、无分层归属声明」的状态——销售侧也无法说清
它属于哪一层。

### 已知缺陷（校准时实测）

| 缺陷 | 状态 |
|---|---|
| `olav.enterprise.memory_dataset_export` 在干净安装上 **ImportError** | 依赖 `olav.core.memory._is_env_transient_failure`，该符号在当前 core 里 0 处，只存在于未合并的 `feat/dataset-export-memory-dpo` 分支。ent 自己的 `KNOWN_UNIMPORTABLE` 已认领 |
| olav-ent 测试**不在任何 forge 的 CI 里** | ISSUE-ENT-TESTS-NOT-IN-CI。实证代价：2026-08-04 移动 skillpack 打断 8 个测试，无任何机制报告 |
| `unit/` 下混了 3 个会挂死的 live 测试 | ISSUE-ENT-LIVE-TESTS-IN-UNIT-HANG，`--timeout` 杀不掉；这是接 CI 的**前置阻塞** |
| 2 个测试写死 `/home/yhvh/Olav/`（少 `code/`） | ISSUE-ENT-TESTS-HARDCODED-ABS-PATHS，覆盖 5 个文件 |
| 当前测试基线 | **384 passed / 2 failed**（排除 3 个 live 文件），2 个失败即上一行 |

## 开源版 vs 企业版快速参照

> ✅/— 列表示**分层归属**（该功能属于哪一层），不表示已实现；
> 「实测状态」列才是完成度。

| 功能 | 开源 | 企业 | 实测状态 (2026-08-04) |
|---|---|---|---|
| OS Identity / Token auth | ✅ | — | —（开源基线） |
| 三角色（admin/user/readonly） | ✅ | — | —（开源基线） |
| 默认 skill 粒度 RBAC 矩阵 | ✅ | — | —（开源基线） |
| admin-only workspace lifecycle | ✅ | — | —（开源基线） |
| 命令黑名单 | ✅ | — | —（开源基线） |
| GAP-1 凭据脱敏（audit_messages） | ✅ 必须 | — | —（开源基线） |
| GAP-4 安全 Cookie 标志 | ✅ 必须 | — | —（开源基线） |
| 24h 审计窗口 + `olav log` | ✅ | — | —（开源基线） |
| RBAC 策略编辑 / 用户组 / 审批（ent_rbac_policy_management） | — | ✅ | ❌ 未实现 |
| LDAP / AD / OIDC（ent_sso_providers） | — | ✅ | 🟡 仅 LDAP |
| 速率限制 / 暴力破解锁定（ent_auth_rate_limit） | — | ✅ | ❌ 未实现 |
| Admin 轮换 + 双人确认（ent_admin_dual_control） | — | ✅ | ❌ 未实现 |
| 审计哈希链（ent_log_integrity） | — | ✅ | ❌ 未实现 |
| 长期保留 + 归档（ent_log_retention） | — | ✅ | ❌ 未实现 |
| 高级脱敏引擎（ent_dataset_redaction） | — | ✅ | ✅ 已实现 |
| SFT / Trajectory / ATIF 导出（ent_dataset_sft / ent_dataset_trajectory / ent_dataset_atif） | — | ✅ | ✅ 已实现 |
| 训练集导出加密控制（ent_dataset_encryption） | — | ✅ | 🟡 主体已实现 |
| 本地训练一次性 token 控制（ent_dataset_access_token） | — | ✅ | ❌ 未实现 |
| Batfish 配置层分析（`batfish_q` + `batfish_capability` + question/capability guide） | ✅ **netops 开源** | — | ✅ 已实现（开源侧） |
| └ 孪生 T0 门禁复用 pybatfish（`lab/twin/t0_gate`，缺失不阻断） | — | ✅ | ✅ 已实现（记入 `ent_twin_*`） |

---

## 许可与分发（`ent_license_*`）—— 设计草案

**Added:** 2026-08-04 ｜ **状态：设计草案，未实现，末尾待决问题定稿后再动代码**

### 立场与硬约束

**现有法律立场已经决定了保护模型。** core 与 olav-netops 是 **BSL-1.1**（源码可见 +
使用限制 + change date），olav-ent 的 `LICENSE` 是 **OLAV Enterprise Commercial
License v1.0**。也就是说这套产品从一开始就选择了「靠许可条款保护，不靠保密保护」
——与 HashiCorp Vault / Teleport / GitLab 同一模式（本文开头的分界原则已经引用了
它们）。后文关于代码混淆的结论与这个既有立场一致，不是新的取舍。

> 附带缺口：`olav-ent/pyproject.toml` **没有 `license` 字段**（core 与 netops 都有
> `license = {text = "BSL-1.1"}`）。因为 ent 不上 PyPI，影响仅限于 wheel 元数据缺
> 少商业许可声明，但交付给客户的 wheel 里应当能自证许可归属。

**离线可验证是硬约束，不是偏好。** 气隙部署是旗舰场景——`[local-embed]` extra
整个存在的理由就是 offline / air-gapped。任何以 phone-home 为运行前提的方案会直接
砍掉最有价值的那类客户。因此：**签名令牌离线校验为主，联网仅作可选增值**
（自动续期、自动 true-up、用量遥测），永不作为启动条件。

### `ent_license_token` — 许可令牌

离线可验证的签名令牌，公钥随 olav-ent 分发，`olav license install <file>` 落地，
全程不联网。

**加密原语无需新依赖**：olav-ent 已声明 `tink>=1.14.1`（原为加密数据集导出引入）
与 `PyJWT>=2.8.0`。建议 Ed25519 + JWS —— 选 JWS 而非私有二进制格式，是因为客户能
自行解开查看内容，对企业采购是透明度加分项。

令牌字段（草案）：

| 字段 | 说明 |
|---|---|
| `lic_id` | 许可证唯一 ID，用于审计与吊销名单 |
| `customer` | 客户标识 |
| `tier` | 层级（决定 `ent_*` 功能开关位） |
| `features` | 显式功能开关，允许按项售卖而非只按层级 |
| `device_quota` | 设备额度（见 `ent_license_device_scale`） |
| `not_before` / `not_after` | 有效期 |
| `binding` | 绑定策略与参数（见 `ent_license_node_lock`） |
| `rebind_quota` | 允许的自助重绑定次数 |

### `ent_license_device_scale` — 设备规模授权

**计数口径不能取库存表。** `netops.devices` 是客户自己可写的 DuckDB，改一行就绕过。
正确的计量点是**设备被实际操作的出口**，而这套代码里这些出口已经很窄：SSH 采集
（`execute_cli_parallel` / `take_snapshot`）、配置推送（node_cli push）、快照。

**口径建议**：滚动窗口（默认 30 天）内**实际发生过采集或推送的不同设备身份数**。
身份优先取序列号 / chassis ID，退化到 `hostname + mgmt IP`。落库存**加盐哈希**而非
明文——计数可核对，但即使启用遥测也不泄露客户网络清单。

这个口径对客户也更公平：库存 500 台、实际只管 80 台就按 80 计，销售侧更好谈。

**超额处置：只告警，不阻断。**

| 用量 | 行为 |
|---|---|
| ≥90% | 告警 |
| ≥100% | 每次运行显示超额 + 写审计 |
| ≥150% | 仅限制**新增**设备，不限制已在管设备的操作，且给足宽限期 |

理由不是宽容，是风险：网络运维工具在变更窗口里因许可超额拒绝执行，会直接导致投诉
升级甚至丢单。超额事件写入 `audit.duckdb`——既是续约时的真实证据，也是客户可自查
的透明记录，与本仓 @tool 判据 #3「关键审计必须落审计表」一致。

### `ent_license_node_lock` — 一机一码

**这套架构下硬绑定会失控**：运行环境包含容器与 VM（仓库内即有 docker overlayfs 与
containerlab）。容器里的 `/etc/machine-id` 每容器一份、重建即变——硬绑它等于每次
重新部署都要客户重新申请授权，支持成本不可接受。

**方案 A（推荐）：模糊指纹 + 自助重绑定。**

指纹取多源元组：`/etc/machine-id`、DMI product UUID
（`/sys/class/dmi/id/product_uuid`，注意需 root）、主网卡 MAC、根盘序列号、
CPU 型号 + 核数、主机名。匹配用 **N-of-M**（如 6 取 4）：换网卡、迁移 VM、扩容 CPU
都不误伤，整机更换则失配。再叠**重绑定配额**（如许可期内 3 次自助重绑），能消掉
绝大多数支持工单。

**方案 B（更省事，值得认真考虑）：绑部署实例而非机器。** 首次运行生成实例 ID 写入
客户自己的 DuckDB，令牌约定「同时活跃实例数 ≤ N」。对 HA / 蓝绿 / 容器编排友好得多，
代价是防复制弱一些。若客户以直签企业为主，这个折中通常更划算——企业客户不会为省一份
授权去做批量克隆，风险收益不成比例。

**必须设计的两个失效模式**：

- **时钟篡改**：调回系统时间可绕过到期。对策是本地记录「观察到的最大时间戳」高水位，
  发现时钟大幅倒退则拒绝运行并要求人工确认。
- **气隙续期**：必须有纯文件投放的续期路径，不能只有联网续期。

### `ent_license_metering` / `ent_license_audit` — 强制点接线

这套代码已有的钩子，不需要新开侵入式检查：

| 钩子 | 位置 | 用途 |
|---|---|---|
| `db_write_gate` | `core/db_write.py:125`（try/except 引入 `olav.enterprise.db_write_gate`） | 写入闸门，天然收口点 |
| 认证分派 | `core/auth/provider.py` → `olav.enterprise.auth.*` | 已有的 enterprise 挂载点 |
| `olav.enterprise_commands` entry point | 已存在（`log_export` 走它） | 加 `olav license` 系列命令 |
| `olav doctor` | 已报 15~19 项检查并带 `fix` | **展示**许可状态：到期 / 超额 / 指纹失配 |

**强制逻辑放在 olav-ent 内部**，挂在 enterprise 功能入口上，不进 core。理由：core 是
BSL 源码可见单元，把许可检查写进去既违反边界（CLAUDE.md 禁止 core 硬编码 enterprise
语义），又等于白送一份可 patch 的检查代码。没装 olav-ent 就没有 enterprise 功能可
授权，逻辑自洽。

> **来自本仓血泪史的强提醒**：CLAUDE.md 记录了四个「TDD 全绿但从未被任何入口触达」
> 的功能，其中两个躺了几个月。许可检查若只有单测、没有从真实 CLI / middleware 路径
> 走通的集成测试，它极可能成为第五个——而这个功能一旦是死的，**不会有任何报错**，
> 只会在某天发现所有客户都在免费使用。按 CLAUDE.md「Definition of Done: wiring must
> be verified from a real entry point」，这是必须项而非加分项。

### 代码保护：真实威胁模型

先直接回答那个必然会被问到的问题——**「不做 pyc，客户逆一下代码和 skill 不就能自己
用了？」**

这个担心是对的，但它推不出「所以要做 pyc」：**做了 pyc，这个担心一个字都不会缓解。**
理由见下面三层，其中第三层解释了为什么这个问题本身问错了层次——你担心的是「代码
**和 skill**」，而 skill 才是命门，它恰恰是编译动不了的那部分。

**结论：不要编译成 `.pyc`。** 三层理由，第三层是这套架构特有的、也是决定性的。

**（1）保护力接近零。** `.pyc` 是字节码，`decompyle3` / `pycdc` 可还原为可读
Python。它拦住的是不会写代码的人，而不会写代码的人本来也不会去逆许可检查。

**（2）成本很实：版本锁死。** pyc 带 magic number，绑定具体 CPython 小版本。
`requires-python = ">=3.11"` 而实测环境已在 3.14——意味着 3.11/3.12/3.13/3.14 各需
一份 wheel，客户升级 Python 即失效；同时 traceback 失去源码行，远程支持难度陡增。

**（3）它会直接打断产品机制**：

- **docstring 就是 LLM 的工具契约。** `@tool` 的 docstring 是喂给模型的 API 描述
  （CLAUDE.md：每个 tool schema 每次调用花 150~300 token，且有
  `test_docstring_budget` 守卫）。要真正剥离字符串需 `-OO`，而 `-OO` **删除所有
  docstring** —— 等于撕掉 agent 的工具说明书。不用 `-OO` 则 docstring 原样留在 pyc
  里，混淆价值更低。
- **workspace 脚本是当源码执行、当文档阅读的。** `execute_skill_script` 起子进程跑
  `<skill>/scripts/*.py` 纯文件；SKILL.md 的 `scripts:` 描述注入系统提示。
  `services/lab/scripts/*.py` 正是这一类。编译后要么跑不了，要么运维再也读不懂
  agent 在做什么——而「运维能读懂脚本」是本设计刻意保留的性质。

**最关键的一点：真正值钱的资产根本无法编译。** 这是个 LLM 产品，核心资产是
**skill / prompt / guide**——`SKILL.md`、`prompts/*.md`、`references/*.guide.yaml`、
tier 默认值、WHAT-not-HOW 的提示工程结论。这些**必须是明文**，因为消费方是模型本身。
`.pyc` 保护的是最不值钱的 Python 管道，对最值钱的提示资产**零作用**。所以「做不做
pyc」这个问题本身就问错了层次。

### 威胁分层

**先分层再投入。最典型的错误是把工程资源花在赢不了的那层，而便宜有效的那层还没做。**

| 层 | 威胁 | 占实际商业损失 | 什么能挡住 | 现状 |
|---|---|---|---|---|
| **T1** | 内部超量部署、随手复用（买 100 台额度用在 400 台；一份授权装五个部署） | **绝大多数** | 许可令牌 + 绑定 + 审计 | **尚未实现** |
| **T2** | 竞争对手决心克隆 | 少但致命 | 进程内**无任何手段**可阻止；靠法律（BSL use restriction + 商业许可）+ 迭代速度 | 法律侧已就位 |
| **T3** | 客户员工好奇心、读代码学习 | ≈0 | 无需处理 | — |

T2 值得再说一句：他们能读代码、能抄 prompt、能重建。pyc / 加密 / 混淆对他们只是几小时
的延迟。把预算投在这里的**唯一效果是延迟 T1 的落地**。

### 决策：做三件，不做两件

**做 ① 许可令牌 + 绑定 + 审计**（前述 `ent_license_*`）。挡 T1，成本中等。**这是唯一
值得优先投入的地方**，因为 T1 是真实损失的主体且目前完全没有防护。

**做 ② 按客户水印 skill / prompt 资产 —— 仅限 olav-ent。** 泄露变得**可归因**，威慢
作用强于混淆——对手知道拿出去用会被追到源头。前提是 ent **按客户交付 wheel**，per-customer
构建可行。**不要推广到 core**，原因见下节「为什么 core 不做水印」。落点与「不影响模型
行为」的验证方式列为待决问题 6。

> 更正（2026-08-04）：本节初稿曾引用 `demo_watermark.py` 作为仓库内已有的水印先例。
> **该文件不存在** —— 它只是 `pyproject.toml:360` 里 pyright `exclude` 的一条陈旧条目，
> 指向一个已删除的文件。水印在本仓**没有任何现成实现**，要从零做（也顺手清掉那条
> exclude）。

**做 ③ 仅把许可 / 指纹 / 计量模块 Cython 化。** 这是编译**唯一挣回成本**的地方：模块
小、无 docstring 职责（不是 `@tool`）、运维不需要读它。它防的是「把
`if not licensed: return` 那一行 patch 掉」这种最省力的攻击。仍属抬高门槛而非安全
边界——公钥在二进制里，决心足够总能改。

**不做 ① 全量 `.pyc`。** 理由见上一节：保护力≈0、版本锁死（3.11~3.14 各一份 wheel）、
且会撕掉 agent 的工具说明书与运维可读性。

**不做 ② 加密 skill 资产 + 内存解密。** 除非确认愿意放弃「运维能读懂 agent 在做什么」
这个刻意保留的设计性质。而且能启动这个进程的人就能从内存 dump 出来，防护提升有限，
代价却是中高。

### 为什么 core 不做水印（「避免被白嫖」问的不是这个问题）

**结论：不要在 olav / olav-netops 注入水印或许可检查。** 不是因为成本，而是因为它对
「白嫖」这个目标**结构上无效**。

**先看清在现有许可下「白嫖」是什么。** `LICENSE` 的 Additional Use Grant §A.2
**明确免费授权**：任何组织可以在生产环境用 olav 管理**自己拥有或租用的**基础设施，
承包商在其指挥下操作也允许。**所以企业拿 core 免费管自己的网络不是白嫖，那正是设计
意图** —— core 是漏斗，ent 是变现层。这里没有需要「避免」的东西。

**真正需要付费的 §B 场景，全部在技术上不可检测**：

| §B 禁止的用法 | 水印/指纹能检测吗 | 为什么 |
|---|---|---|
| MSP 管第三方客户设施 | **否** | MSP 的安装与企业自用的安装**完全一样**；设备数也不区分（大企业比小 MSP 设备更多） |
| CSP 托管 / 对外提供 API | 否 | 同上，进程内看不到「服务对象是谁」 |
| 外包运维 | 否 | 与 §A.2 允许的「承包商代客户自用」在技术上无法区分 |
| 打包转售进商业产品 / 硬件 | 否（但市场可见） | 是商标与市场行为，不是软件事件 |
| 卖 olav 实施 / 调优服务 | 否 | 根本不是软件事件 |

§B 违规的判定依据是**被管设施归谁所有**和**存在什么商业关系**——这些事实活在进程之外。
往 core 里加任何标记都不会让它们变得可见。

**还有两个结构性障碍**：

1. **core 走 PyPI 单一制品**（`twine upload` 手动发布），全世界拿到的是同一份 wheel。
   按客户水印需要按客户构建，而 core **没有按客户交付的通道**。ent 有，所以 ent 能做。
2. **往 BSL 源码可见的 core 加许可检查或遥测，是经典的信任毁灭动作**，而且直接违反
   CLAUDE.md 的边界规则（core 不得硬编码 enterprise 语义）。core 的摩擦会直接缩小
   ent 的漏斗。

**真正对 §B 有用的三件事**（都不是水印）：

- **输出物署名**：报告 / 导出 / 图表带「Generated by OLAV v<version>」。这是最接近
  「水印」且对 core 合理的做法，但它的用途是**法律取证而非技术阻止** —— MSP 把 OLAV
  生成的报告交付给它的客户时，这些制品会流出 MSP 之外、成为可发现的证据。这是 §B 唯一
  现实的发现通道。
- **商标**：BSL 不授予商标权利。改名转售是商标问题，不是代码问题。
- **商业侧**：企业需要 indemnity / SLA / 支持，MSP 需要能对客户说明自己跑什么。§B 的
  实际发现渠道是市场（销售对话、RFP、竞品宣传），不是代码。

### 更值得关注的问题：资产切分，不是混淆

实测本仓提示资产的许可分布（2026-08-04）：

| 层 | 许可 | `*.guide.yaml` | `SKILL.md` | `references/*.md` |
|---|---|---|---|---|
| core（`src/olav/data/workspace`） | BSL 公开 | 16 | 16 | 24 |
| netops（`olav-netops/.olav/workspace`） | BSL 公开 | 36 | 12 | 14 |
| **ent**（`skillpack/`） | 商业许可 | **2** | **1** | **3** |

**54 个 guide 里 52 个、29 个 SKILL.md 里 28 个在公开的 BSL 层。** 前面论证过提示资产
才是 LLM 产品的核心资产——而它**93% 都在免费层**，并且 §A.2 明确允许企业在生产中免费
使用。

**这不是混淆能解决的问题，是产品线决策**：如果这些提示工程结论不打算免费给出去，杠杆
在于**把哪些 guide / 子 agent 放进 ent**，而不是给 core 加水印。反过来说，如果 core
的丰富度正是获客的关键（很可能是），那就应当**明确承认这是有意的投入**，并把变现完全
押在 ent 的功能与支持上——两种选择都成立，但必须是选择，而不是默认。

列为待决问题 7。

### 最强的实际护城河不在保密

**自我改进闭环**：guide / memory 随部署持续积累（L1/L2 capture、reflection）。被窃取
的快照是**贬值资产**——几个月后就落后于持续更新的版本，而更新与支持偷不走。这比任何
混淆都实在，也和 BSL「源码可见、靠条款与迭代取胜」的既有立场自洽。

### 待决问题（定稿后才动代码）

1. **绑定粒度**：方案 A（一机一码，N-of-M 指纹 + 重绑定配额）还是方案 B（绑部署实例，
   活跃实例数上限）？B 的支持成本低一个数量级。
2. **设备计量口径**：接受「滚动 30 天内实际操作过的设备数」，还是坚持按库存计？
3. **超额策略**：确认「只告警不阻断」为底线，还是需要硬上限？
4. **遥测通道**：是否需要可选联网上报用量以支持自动 true-up；气隙客户如何 true-up。
5. **是否 Cython 化许可模块**：若客户群存在转售风险则值得；若均为直签企业客户，
   建议省下这份复杂度。
6. **水印方案**：水印落在 guide / prompt 的哪一层，如何做到不影响模型行为。
   （仅 ent；core 不做——见「为什么 core 不做水印」。）
7. **资产切分**：当前 93% 的提示资产在公开 BSL 层。这是有意的获客投入，还是应当把
   部分高价值 guide / 子 agent 迁入 ent？变现押在功能+支持，还是也押在提示资产？
8. ~~**`ent_*_export` 一组是否也重命名为 `ent_dataset_*`**~~ —— **已完成（2026-08-04）**：6 个 ID 全部改名，`dev_docs/34` 的固定顺序开关列表同步（顺序不变，仅换名），旧 ID 保留为历史别名。
9. ~~**`ent_batfish_analyzer` 整节已过期**~~ —— **已删除（2026-08-04）**，替换为一段说明该能力归属开源 netops 的记录，并把孪生 T0 门禁归入 `ent_twin_*`。对照表同步改为「netops 开源已实现」。
10. ~~**`lab/twin` + `cab` 缺功能清单**~~ —— **已补（2026-08-04）**：新增
    `ent_twin_*`（5 组 / 33 模块）与 `ent_cab_*`（1 组 / 7 模块）两节，按「模块已存在」
    勾选并单独列出 9 项缺失的**验收标准**。仍缺两样、已在节末诚实标注：分层归属原则
    未定，以及快速参照表未加对应行。
