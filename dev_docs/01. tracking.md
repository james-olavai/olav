# OLAV Development Tracking

**Last Updated:** 2026-04-02 (rev 2)
**Version:** v0.21
**Purpose:** 记录当前可执行阶段、硬门禁和证据要求。

---

## 1. Current Architecture State

**采集层**: SSH (Scrapli/Netmiko) → TextFSM → `netops.parsed_outputs` → `netops.topology_links`

**数据库现状**:
- `netops.devices`: 6 devices
- `netops.topology_links`: 32 links
- `netops.parsed_outputs`: 6828 rows
- `netops.oc_outputs`: 存在但稀疏 — 无自动填充管道（OC transform pipeline 已废弃）

**ops agents**: orchestrator / diff / topology / probe / sim / lab

**Lab agent 架构**: `execute_sql` → `run_python_simulation` (build YAML + translate configs + httpx push) → `deploy_lab` → `exec_on_node` 验证

**已废弃/删除**:
- gNMI stack (`gnmi_collector.py`, `srl_exporter.py`, `gnmi_collect.py`, `push_gnmi_config.py`)
- OC transform pipeline (`oc_pipeline.py`, `oc_mapping_agent.py`, `generate_oc_transforms.py`, `renormalize_oc_outputs.py`, `oc_transforms` table)
- ops-oc subagent (`.olav/workspace/ops/oc/`)
- yang_leaves corpus bootstrap scripts
- `push_config_api.py` (lab sandbox handles via httpx directly)
- `oc_transform_runner.py`, `transform_sandbox.py` (Ghost code — 无引用，随 OC pipeline 废弃)

---

## 2. Execution Order

| Phase | 状态 | 里程碑 |
|-------|------|--------|
| Phase 0-2 | 🟢 Completed | — |
| Phase 3 | 🟢 Completed（核心域 98.77% ≥ 95% gate） | — |
| Phase 4 | 🟢 Completed（4 模块部署 + 测试通过） | — |
| Phase 5 | 🟢 Completed（62/62 unit tests + 真实 E2E） | — |
| Phase 6 | 🟢 Completed（CLAB CAB validation，cEOS BGP E2E） | — |
| Phase 7 | 🟡 **当前焦点**（Platform Gap 收尾 + 测试修复） | **M1** |
| Phase 8 | ⬜ 待启动（Doc-Driven Testing + 文档站发布） | **M1** |
| Phase 9 | ⬜ 待启动（NetOps Lab E2E + 协议扩展 + NetOps 发布） | **M2** |
| Phase 10 | ⬜ 待启动（Enterprise 功能 + 商业主站） | **M3** |

---

## 3. Global Rules

1. 严格阶段制：未过门禁不得前推
2. 先文档后实现：先确认 contract 再改代码
3. 未验证不算完成：必须有 unit + 真实设备 E2E
4. 真实环境失败默认按代码问题处理
5. 禁止引入非必要抽象层（KISS 原则）

---

## 4. Phase 7 — Ghost Audit + Protocol Expansion

**状态**: 🟡 当前焦点

**目标**:
1. Ghost code 审计：无 owner/无测试/无用途代码识别与删除
2. `api_registry` 边界确认：`src/olav/core/api_registry.py` 是否仍需要
3. 协议收集器扩展：IS-IS / EVPN / VXLAN / MPLS LDP 命令补充
4. Lab agent E2E 验证：exec API 配置推送 → OSPF/BGP 收敛完整流程

**参考文档**:
- `dev_docs/12. PLATFORM_NETOPS_OWNERSHIP_MAP.md`

---

## 5. Protocol Collection Gaps

| 协议 | 采集状态 | 阻塞原因 |
|------|---------|---------|
| STP/MSTP | ✅ 18 ports | — |
| IS-IS | ❌ 0 rows | 缺 `show isis neighbors` |
| EVPN | ❌ 0 rows | 缺 `show evpn instance` |
| VXLAN | ❌ 0 rows | 缺 `show vxlan vtep` |
| MPLS LDP | ❌ 0 rows | 缺 `show mpls ldp neighbor` |
| EIGRP | ❌ 未实现 | Phase 7 后启动 |

---

## 6. Platform Gap Backlog (updated 2026-04-02 rev 2)

规划已有、代码未实现的平台功能。按对 skill 自主开发闭环的阻塞程度排序。

### P0 — 直接阻塞 agent skill 开发闭环

| ID | 功能 | 规划来源 | 状态 |
|----|------|---------|------|
| GAP-01 | `olav skill install` 支持裸 MANIFEST.yaml（无需 workspace.yaml） | §18.2.4 | ✅ |
| GAP-02 | skill install 后自动写入 PLATFORM.md `agents:` | PLATFORM.md 设计 | ✅ |
| GAP-03 | M5 `DockerComposeBackend` + `LifecycleManager`（容器健康等待/修复） | §16.7, plan M5 | ✅ |
| GAP-03b | `stop_service` / `list_services` 平台工具（停止 + 状态查询） | — | ✅ |
| GAP-04 | M5 `response_extractor.py` schema-aware 响应裁剪 | §16.5.4, plan M5 | ⬜ |

### P1 — 影响多 workspace 使用体验

| ID | 功能 | 规划来源 | 状态 |
|----|------|---------|------|
| GAP-05 | `olav workspace use` 同步更新 PLATFORM.md `active:` | PLATFORM.md 设计 | ✅ |
| GAP-06 | `olav skill install <git-url>` git clone 流程 | §18.2.1 | ⬜ |
| GAP-07 | `olav skill install --merge-into <ws>` SkillPack 追加 | §18.2.3 | ⬜ |

### P2 — 已有框架但功能不完整

| ID | 功能 | 现状 | 状态 |
|----|------|------|------|
| GAP-08 | `olav registry register` schema fetch 重试/等待 | 无重试，netbox 500 直接失败 | ✅ |
| GAP-09 | `service_call()` JWT token 过期自动刷新 | `_jwt_login()` 存在但无刷新 | ✅ |
| GAP-10 | Skill 级 Python venv 隔离 | 当前所有 skill 共用 uv 项目 venv，跨 skill 包冲突无解 | ✅ |
| GAP-11 | Sandbox 工具层精简（删除 3 个纯 shell 封装：ping/traceroute/port_scan；其余 @tool 经 E2E 评估后保留） | 见 `dev_docs/19. SANDBOX_TOOL_REFACTOR.md`（2026-04-02 修订版） | ⬜ |
| GAP-14 | `call_api.py` → `service_call("clab", ...)` 迁移（`call_api` 含 hardcoded IP，clab 已注册 services.yaml） | `ops/lab/tools/call_api.py`, `deploy_lab.py`, `exec_on_node.py` | ⬜ |
| GAP-12 | Creator tools 5 个工具缺少 unit test | E2E 验证通过但无 unit test | ⬜ |
| GAP-15 | 文档站建设：MkDocs+Material+Cloudflare Pages；Doc-Driven Testing（代码审计→Claim→骨架→测试→修复/收窄→发布） | `dev_docs/22. WEB_AND_DOCS_SITE.md` | ⬜ 依赖 P0+P1 完成后启动 |

---

## 7. Claude Code Improvement Backlog — ✅ 已完成 (2026-04-02)

全部 5 项完成，17 新增单元测试 green，1132 unit total pass。详见 ADJ #35。

---

## 8. ADJ Log

| # | 日期 | 摘要 | 类型 |
|---|------|------|------|
| 42 | 2026-04-02 | 主站+文档站技术路线确定：MkDocs+Material+Cloudflare Pages；单仓库布局（`docs/` → 文档站，`web/` → 主站）；Doc-Driven Testing 方法论（Claim→Test→Fix or Narrow）；docs/ 现状审计（14个文件，✅/⚠️标注）；新增 `dev_docs/22. WEB_AND_DOCS_SITE.md` | D |
| 41 | 2026-04-02 | 普遍适用性修复：`client.py` 写门尊重 `readonly_post_paths` 白名单（POST /query 不再被拦截）；`_SCHEMA_PROBE_PATHS` 扩展至 17 条（含 InfluxDB `/api/v2/swagger.json`、SpringDoc `/v3/api-docs`、K8s `/openapi/v2`）；`deploy_service.py` 兼容 `.yaml` 扩展；新增 `stop_service` + `list_services` 平台工具（core workspace）；Creator system.md 增加 Step 4b（读实际生成文件提取真实函数名，防幻觉）| C |
| 40 | 2026-04-02 | InfluxDB/NetBox skill 质量修复 + Creator 6 项质量提升：NetBox 5 个工具文件重新生成（补 @tool 装饰器，187 个函数）；InfluxDB `readonly_post_paths` 机制 + `ServiceConfig` 字段新增；`register_api_service` 新增 @tool 验证 + `ServiceRegistry.reload()` 单例缓存修复；`create_skill_workspace` 自动生成 MANIFEST.yaml + prompts/system.md；Creator system.md 增加 Step 3 `readonly_post_paths` + Step 4 `@tool` 检查 + 3 条 ❌ 防护规则；1129 tests pass | C |
| 39 | 2026-04-02 | 多用户并发修复：`AuditEventRecorder` 从持久连接改为短生命周期连接 + 模块级 threading.Lock + 8 次指数退避；不再持久化 `_conn`；1149 tests pass | C |
| 38 | 2026-04-02 | 平台核心工具迁移：8 个平台工具从 `ops/` 移至 `core/` workspace；`_build_subagents()` 向所有 subagent 注入 core tools；所有 subagent 升级为 CompiledSubAgent；1149 tests pass | C |
| 37 | 2026-04-02 | ISSUE-P1-DEEPAGENTS-TOOL-ISOLATION 根本修复：`delegate_tool.py` 新建 `build_delegate_tool()`，绕过 deepagents `SubAgentMiddleware`，子 agent 仅持有 SKILL.md 声明工具；所有 subagent（含零工具）改为 CompiledSubAgent；orchestrator 注册 `olav_delegate` 工具；1149 tests pass | C |
| 36 | 2026-04-02 | Ghost Audit 部分完成：删除 `oc_transform_runner.py`、`transform_sandbox.py`（无引用 Ghost code）；修复 `test_register_service_retries` 测试（缺少 `_discover_schema_url` mock）；修复 `main.parsed_outputs` compat view（由空 BASE TABLE 重建为 VIEW）；`v0_12_schema_split` 迁移改为 `CREATE OR REPLACE VIEW`；GAP-09 JWT 自动刷新标记为 ✅（401重试逻辑已实现）；1132 unit total pass | C |
| 35 | 2026-04-01 | CC Optimization Backlog 全部完成（含 CC-05）：CC-01 CompiledSubAgent 补齐 SummarizationMiddleware+AnthropicPromptCachingMiddleware；CC-02 AsyncDuckDBSaver.list_threads() + /threads/search；CC-03 .claude/settings.local.json 精简至 4 条；CC-04 approval_rules.yaml 声明式危险规则；CC-05 orchestrator.md 新增 TOOL PARTITIONING 章节（Scratchpad vs 领域工具）。17 new tests green，1097 unit total pass | C |
| 34 | 2026-03-31 | §20 安全加固三项：6.1 DuckDB monkey-patch（sandbox 强制 read_only=True）；6.2 OLAV_SANDBOX_NETNS=1 unshare --net 隔离；6.3 service_call 写方法审批门；删除 hermes.md 消息网关设计；新建 20.SECURITY_MODEL.md；17 tests green | C |
| 33 | 2026-03-31 | 平台只读安全强化：`ServiceConfig.readonly_only=True`（默认）→ tool_generator 只生成 GET；clab `readonly_only=false`；`sandbox_guard.py` 扫描 HTTP/DB 外部写操作；`execute_in_sandbox` 预执行检查，24 tests green | C |
| 32 | 2026-03-31 | GAP-10 Skill venv 隔离：`_read_requires_packages` + `_create_skill_venv` (uv) + `resolve_sandbox_python` + `execute_in_sandbox`；`sandbox.py` 新建；11 unit tests green | C |
| 31 | 2026-03-31 | Platform Gap P0 实施：GAP-01 skill install 支持裸 MANIFEST.yaml；GAP-02 install 写入 PLATFORM.md agents；GAP-03 DockerComposeBackend + LifecycleManager (M5)；GAP-05 workspace use 同步 PLATFORM.md active；GAP-08 registry register retry。159 tests pass | C |
| 30 | 2026-03-31 | PLATFORM.md 三层注册模型实施：PlatformRegistry.load()、discover_valid_agents() 读 PLATFORM.md、_get_orchestrator_prompt() 注入 platform context；workspace validate 修复死代码；.olav/workspace/PLATFORM.md 生成 | C |
| 29 | 2026-03-31 | 架构大幅简化：完整删除 gNMI stack、OC transform pipeline、ops-oc subagent；lab agent 重设计为 sandbox-first（httpx exec API push）；push_config_api.py 删除；LAB_REFERENCE.md 新建；SKILL.md v5.0；ops-lab SKILL.md 同步；protocol_platform_map.yaml gnmi 节删除；test_lab_tools.py TestPushGnmiConfig → TestPushConfigApi | C |
| 28 | 2026-03-30 | OC pipeline 全量集成（已废弃，见 ADJ 29） | C |
| 27 | 2026-03-29/30 | Ops agent workspace 全面对齐：lab/probe/diff/topology/sim/orchestrator prompt 更新 | C |
| 26 | 2026-03-28 | Phase 6.7 agent-driven OC config extraction pipeline（67/67 tests） | C |
| 25 | 2026-03-28 | CLAB cEOS live E2E 验证通过（2/2 BGP Estab）；关键行为记录 | C |
