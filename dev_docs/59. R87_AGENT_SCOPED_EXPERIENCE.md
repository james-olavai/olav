# R87 — Agent-Scoped Experience Library

**Last updated**: 2026-04-27
**Status**: Phase 1 + Phase 1.5 shipped (open source); Phase 2-4 deferred to **enterprise edition**.

**Lineage**:
* `dev_docs/57. MEMORY_DRIVEN_USAGE_GUIDES.md` — R84 design doc
* `dev_docs/58. R84_MEMORY_DRIVEN_GUIDES_TRACKING.md` — R84/R85/R86 tracking
* `dev_docs/94. BUSINESS_MODEL.md` — commercial direction

---

## Context

R84-R86 built three layers of memory-driven knowledge that all live
in a **single global LanceDB store**, surfaced uniformly to every
agent:

| category | purpose | scope today |
|---|---|---|
| `schema_knowledge` | DB views auto-discovered at ingest | global |
| `value_distribution` | per-column value frequencies | global |
| `query_pattern` | captured-from-success SQLs | global |
| `usage_guide` | curated workflow imperatives | global |
| `format_guide` | curated save-call shapes | global |

All agents see the same pool.  This is a **start**, but Chapter 5
testing exposed three structural limits:

### Limit 1 — vendor-bias of the LLM

When ops-lab validated the SRL eBGP CAB spec, the agent saw `bgp
neighbor` state = `active` and reported PASS.  In SR Linux,
`active` means **TCP is failing** (hard fault).  The LLM applied
its **Cisco-flavored prior** (where `active` is "trying").
Generic memory entries can't fix this — the model's training data
is overwhelmingly Cisco-shaped, so vendor-specific
counter-knowledge is the only fix.  But that knowledge would only
be relevant when the active workflow involves SRL containers — for
core-agent / writer-agent it's noise.

### Limit 2 — partial-success poisoning

`query_pattern_capture` writes successful execute_sql calls
directly to memory.  But "successful" only means "tool didn't
error".  In R84 CC-1c, Q3's captured SQL was single-view (queries
one of 5 interface views), which the diversifier surfaced for
later runs — agent then "answered fast" but with **incomplete
data**.  Predicate failed.  We saw partial-success poison the
recall.

### Limit 3 — agentic learning is currently a hope, not a system

The vision in `dev_docs/94` is "users inject experience".  But
right now, the only auto-learning mechanism is `query_pattern_capture`,
which has the poisoning problem above.  There's no review,
no confidence model, no rollback, no per-agent isolation.

R87 addresses all three.

---

## Architecture: scoped + tiered + curated

### The full design (4-tier confidence)

```
┌──────────────────────────────────────────────────────────────┐
│  GLOBAL CURATED CONTENT (R84/R85/R86, already shipped)       │
│  scope='global', confidence=1.0                              │
│  ├── schema_knowledge   (auto from ingest)                   │
│  ├── value_distribution (auto from ingest)                   │
│  ├── usage_guide        (curated *.guide.yaml)               │
│  └── format_guide       (curated *.format.yaml)              │
├──────────────────────────────────────────────────────────────┤
│  AGENT-SCOPED CURATED EXPERTISE  (R87 Phase 1, this doc)     │
│  scope='<agent_name>', confidence=1.0                        │
│  └── expert_knowledge   (curated *.expert.yaml)              │
│      ↑ vendor / platform-specific corrective knowledge       │
│        — fixes the LLM's training-data bias.                 │
│        Examples: SRL eBGP gotchas, IOS-XR EVPN quirks,       │
│        Junos commit-confirmed semantics.                     │
├──────────────────────────────────────────────────────────────┤
│  AGENT-SCOPED LEARNED EXPERIENCES (R87 Phase 2-4, FUTURE,    │
│                                     ENTERPRISE EDITION)      │
│  scope='<agent_name>', confidence=0.0~1.0 progressive        │
│  ├── pending          (auto-capture, conf=0.3)               │
│  │   recall middleware FILTERS OUT (conf < 0.5 default)      │
│  ├── approved         (curator review pass, conf=0.5-0.7)    │
│  ├── seasoned         (N reuses w/o user override, conf=0.8) │
│  └── authoritative    (cross-agent validated, conf=1.0)      │
└──────────────────────────────────────────────────────────────┘
```

### Phase 1 (open source, this doc) — per-agent curated expertise

**Goal**: scoped curated knowledge that fixes vendor-bias.  No
auto-learning yet.

* New memory category: `expert_knowledge`
* New file schema: `*.expert.yaml`
* Scope semantics: `scope='global'` OR `scope='<agent_name>'`
  - Recall middleware filter: `scope IN ('global', current_agent)`
  - Per-agent expertise stays out of other agents' recall
* New CLI: `olav kb import-experts <dir> --agent <name>`
* New `_CATEGORY_QUOTAS["expert_knowledge"] = 1` (borrow 1 slot
  from schema_knowledge: 3→2; total stays 13)
* First curated content: `bgp_validation_srl.expert.yaml`
  (scope='ops-lab') — corrects the Cisco-flavored BGP state
  decoder

**Validation gate**: Chapter 5 retest after Phase 1.  Expected:
agent recognizes `active` as TCP failure (per SRL knowledge),
reports CAB FAIL with specific diagnosis ("missing
ebgp-default-policy" / "no IGP route to peer loopback").
Without Phase 1 (R86 baseline): agent reports CAB PASS or
incorrect FAIL with no actionable diagnosis.

**Why this is open source**: vendor-specific quirks are community
knowledge — TAC engineers, network architects, certification
holders all contribute.  OLAV provides the schema + import path;
the value flow goes through customers/community, not vendor.

### Phase 2-4 (enterprise edition, FUTURE)

**Phase 2 — pending pool + manual curator CLI** (~2 days work):
* `AutoCaptureMiddleware` writes to `scope='pending'`,
  `confidence=0.3`
* Recall middleware default filter: `confidence >= 0.5` (pending
  invisible)
* `olav curator list/inspect/approve/reject/merge` CLIs (manual
  human review)

**Phase 3 — automated curator agent** (~3-5 days work):
* New subagent `core/curator` (or extend `audit/curator`)
* Curator's static_context: `principles.expert.yaml` shipping with
  the platform — what makes a capture worth approving
* Curator runs on a schedule (cron) or threshold (>N pending)
* Decisions: approve+promote / reject / hold-for-evidence /
  cluster+merge / GC stale

**Phase 4 — cross-agent validation propagation** (~1 week):
* ops-lab CAB PASS/FAIL retroactively boosts/demotes ops-analyze's
  spec-generation captures
* Reward signal flows backward through delegation chain
* Time-window + debounce to prevent oscillation

**Phase 2-4 are deferred to enterprise edition** for these
reasons:

1. **Curator quality is the moat**.  An OLAV deployment with a
   well-tuned curator gets smarter over time; one without
   stagnates.  This is exactly the high-touch "knowledge
   distillation" service `dev_docs/94` describes.
2. **Operational complexity**.  Pending pool + curator agent +
   audit logging + rollback is enterprise-grade ops.  Open source
   users get curated content (Phase 1); enterprise gets the
   self-improving loop.
3. **Privacy / multi-tenancy**.  Per-deployment learned content
   needs isolation tooling (PII sanitization, opt-in community
   contribution).  That's an enterprise feature.
4. **Time to validate Phase 1 first**.  Before committing to a
   curator agent, we need bench data showing per-agent expertise
   actually moves the needle.  If Phase 1 doesn't pay off,
   Phase 2-4 are dead.

---

## Phase 1 detailed design (open source, current work)

### `*.expert.yaml` schema

```yaml
schema_version: 1
topic: bgp_state_decoder_srl     # human-readable identifier
scope: ops-lab                   # which agent surfaces this
vendor: srl                      # OPTIONAL — for filter / display
platform_family: nokia           # OPTIONAL
keywords:
  - srl
  - sr-linux
  - bgp
  - established
  - active
  - cab
body: |
  # SRL-specific BGP State Decoder (NOT Cisco)
  ...
```

Required: `topic`, `scope`, `keywords`, `body`.
Optional: `vendor`, `platform_family`, `schema_version` (defaults to 1).

`memory_id` = `expert_<scope>_<topic>` — idempotent upsert.

### Module: `src/olav/core/memory/expert_kb.py`

Mirrors the structure of `guide_kb.py` and `format_kb.py`.
Discovery glob: `<workspace_root>/<agent>/expertise/*.expert.yaml`
(sibling to `guides/`, `formats/`).

### CLI: `olav kb import-experts <dir>`

Sibling to `import-guides` / `import-formats`.  Scans for
`*.expert.yaml`, primes each with category=`expert_knowledge`,
scope from YAML's `scope` field.

### Recall middleware change (small but semantic)

In `_gather_candidates`:
* Was: `scope='global'` (or omitted)
* Becomes: `scope IN ('global', current_agent_name)`

This requires the recall middleware to know which agent is
currently running.  The orchestrator passes `current_agent` when
constructing AutoRecallMiddleware (already happens in
`_build_subagents`).

### Quota update

```python
_CATEGORY_QUOTAS = {
    "schema_knowledge": 2,      # was 3 (R86); -1 for expert_knowledge
    "value_distribution": 4,    # unchanged
    "query_pattern": 2,         # unchanged
    "usage_guide": 3,           # unchanged
    "format_guide": 1,          # unchanged
    "expert_knowledge": 1,      # NEW
}
# Total: 13 (recall_top_k for large tier; budget unchanged)
```

Borrowing the slot from `schema_knowledge` (currently
under-utilised since most queries don't need 3 schema entries —
the matched one is usually clear from the query verb).

### First curated content

`olav-netops/.olav/workspace/ops/lab/expertise/bgp_validation_srl.expert.yaml`:

```yaml
schema_version: 1
topic: bgp_validation_srl
scope: ops-lab
vendor: srl
platform_family: nokia
keywords: [srl, sr-linux, sr_cli, nokia, bgp, established, active, cab]
body: |
  # SRL-specific BGP State Decoder (NOT Cisco)
  ...
```

(Body content as drafted in the chat session; covers state-name
semantics, failure mode decoder, SRL gotchas, verification
sequence, CAB pass criteria.)

### Validation gate

**Re-run Chapter 5** after Phase 1 lands.  Compare:

| metric | R86 baseline | Phase 1 expected |
|---|---|---|
| ops-lab recognizes `active` ≠ Established | mixed | always |
| ops-lab provides actionable diagnosis | sometimes (Cisco-flavored) | SRL-specific |
| ops-lab reports CAB FAIL with `ebgp-default-policy` cause hint | no | yes |
| ops-lab pulls SRL knowledge into recall block | n/a | yes (1 slot) |

If Phase 1 doesn't move these needles, the per-agent scoping
mechanism alone isn't enough — would need to investigate prompt
adherence rather than ship Phase 2-4.

---

## Risks & open questions

### Risk 1: scope=ops-lab but agent uses execute_sql for general queries
ops-lab uses `execute_sql` to gather generic info (not just lab).
The `bgp_validation_srl` expertise would also surface for those
queries (because keywords match).  Whether that's good or bad —
needs bench observation.

Mitigation: tighten keywords to lab-specific phrases (`sr_cli`,
`clab-`, `digital twin`).

### Risk 2: who writes the first curated YAML
The user (us, in this session) has to write it.  Quality depends
on human-driven SRL expertise.  Long-term, OLAV team / community /
vendor partnerships supply the content; for now, hand-rolled.

### Risk 3: prompt adherence still required
Even with SRL knowledge in recall, the small model has to
**actually apply** it.  We've seen agents skip recall content
(R84 CC-1c).  The bench is the only way to know.

If Phase 1's bench shows recall surfaces but agent ignores → need
prompt-level reinforcement (`usage_guide` for ops-lab saying
"when SRL platform involved, consult `expert_knowledge`").

### Open question 1: should other vendor knowledge be added before bench?
Or just the SRL one for now (smallest test surface)?

Recommendation: **just SRL** — focus the Chapter 5 gate.  Other
vendors come after the architecture validates.

### Open question 2: scope inheritance
ops-lab is a subagent of ops-orchestrator.  Should
`scope='ops-orchestrator'` be visible to ops-lab too?  Hierarchy?

Recommendation: **flat scope, no inheritance** for Phase 1.  Each
agent sees `('global', '<own_name>')`.  Cleaner mental model.
Hierarchy can be Phase 1.5 if needed.

### Open question 3: when agent has no expertise files, anything bad?
Recall just returns 0 expert_knowledge entries.  Quota slot is
filled from global ranking (existing fallback path).  No
regression — recall_top_k still saturates at 13 from the other
categories.

---

## Decision log

| date | decision | rationale |
|---|---|---|
| 2026-04-27 | R87 architecture documented | Capture the design before code |
| 2026-04-27 | Phase 1 → open source, Phase 2-4 → enterprise | Curator agent is the moat; OS gets the corrective layer, EE gets the self-improving loop |
| 2026-04-27 | First curated content: `bgp_validation_srl.expert.yaml` | Chapter 5 already exposes the gap; tightest validation gate |
| 2026-04-27 | Defer self-feedback phase | Need bench evidence Phase 1 works first |

---

## Files / artefacts inventory (Phase 1)

**To be added** (Phase 1 implementation):
* `src/olav/core/memory/expert_kb.py` — module
* `src/olav/cli/commands/kb.py` — `cmd_import_experts` subcommand
* `src/olav/core/memory/middleware.py` — scope-aware recall filter
* `tests/unit/test_expert_kb.py` — module contract
* `tests/unit/test_recall_scope_filter.py` — recall scope behaviour
* `olav-netops/.olav/workspace/ops/lab/expertise/bgp_validation_srl.expert.yaml`
  — first content

**To be modified**:
* `src/olav/core/memory/middleware.py` `_CATEGORY_QUOTAS` — add
  `expert_knowledge`, decrement `schema_knowledge`
* `src/olav/core/memory/middleware.py` `_gather_candidates` —
  scope filter

**Reuse (do NOT modify)**:
* `src/olav/core/memory/guide_kb.py` — UsageGuide pattern
* `src/olav/core/memory/format_kb.py` — FormatGuide pattern
* `src/olav/core/memory/__init__.py` — store API + scope field
* `src/olav/cli/commands/kb.py` — existing import-guides /
  import-formats infrastructure

---

## Phase 1.5 design — scope vocabulary + user injection

**Status**: Designed 2026-04-27 — not yet implemented. Trigger:
during R87 Phase 1 closed-loop validation we hit two structural
gaps that Phase 1 doesn't address:

1. **No "shared" tier** — `srl_spec_generation_rules` is needed by
   ops-analyze (writes spec) AND ops-lab (validates spec). Phase 1
   forces a choice (one scope) → either duplication or wrong-agent
   recall.
2. **No user-injection path** — `dev_docs/94` business model
   requires "users inject experience" but Phase 1 only ships
   platform-authored YAMLs. There's no recall slot reserved for
   user content, no on-disk path the user can write to, no
   provenance tracking.

Phase 1.5 closes both gaps without touching the Phase 2-4 curator
machinery (which stays enterprise).

### Three-tier scope vocabulary

| scope value | meaning | who can write | recall visibility |
|---|---|---|---|
| `<agent_name>` | agent-specific | platform/extension authors | only that agent |
| `shared:<domain>` | cross-agent within a domain | platform/extension authors | all agents in that domain |
| `org` | universal organizational | **users only** | all agents, all domains |

`<domain>` is the skill name (`ops`, `audit`, `ent`). `shared:ops`
is visible to all `ops-*` subagents; `shared:audit` to audit
subagents; etc.

**Why no multi-value scope (`scope: [ops-lab, ops-analyze]`)?**
Multi-value devolves into copy-pasted lists with no abstraction.
Forcing the author to choose between `<agent>` / `shared:<domain>`
/ `org` is a cleaner mental model.

### Two-axis source provenance

`source` field — auto-populated at import time, not authored:

| source | meaning | provenance |
|---|---|---|
| `shipped` | came with the package | author = OLAV team / extension maintainers |
| `user` | locally injected | author = end user / org |

Used for:
* Audit (`olav kb list-experts --source=user` shows my customizations)
* Conflict signaling (when shipped + user knowledge collide)
* Bench testing (compare model behavior with/without user injections)

### Directory layout

```
# Shipped — versioned with the code
<plugin>/.olav/workspace/<agent>/expertise/             → scope: <agent>
<plugin>/.olav/workspace/shared/<domain>/expertise/     → scope: shared:<domain>

# User-injected — NOT versioned with the code
~/.olav/expertise/                                       → scope: org (cross-project)
<project>/.olav/expertise/                               → scope: org (per-project)
```

`shared/<domain>/expertise/` is a NEW directory pattern (sibling
to per-agent dirs). `discover_experts` recursively scans both
patterns and infers scope from path:

```python
# discover_experts(workspace_root) returns list of (path, inferred_scope)
# path matches "<root>/<agent>/expertise/*.expert.yaml" → scope=<agent>
# path matches "<root>/shared/<domain>/expertise/*.expert.yaml" → scope=shared:<domain>
```

YAML's `scope` field (if present) overrides the inferred default —
useful when YAML is symlinked / copied across paths.

### Recall filter (replaces Phase 1's vector-only approach)

When agent `X` in domain `D` runs:

```sql
-- Phase 1 (current): vector relevance only, no scope filter
WHERE category = 'expert_knowledge'

-- Phase 1.5: hard scope isolation
WHERE category = 'expert_knowledge'
  AND scope IN ('X', 'shared:D', 'org')
```

This solves the "vector-relevance occasionally surfaces wrong-agent
expertise" issue we observed in R87 closed-loop validation.

### Quota allocation — split the expert_knowledge slot

Currently `_CATEGORY_QUOTAS["expert_knowledge"] = 1`. Phase 1.5
splits into 3 sub-slots inside the recall block:

```python
_CATEGORY_QUOTAS["expert_knowledge"] = {
    "agent":  1,   # at least 1 own-agent entry
    "shared": 1,   # at least 1 cross-agent entry
    "org":    1,   # at least 1 user-injected entry
}
# Total: 3 (was 1) — borrow 2 slots from value_distribution
# (currently 4, dropping to 2 — diminishing returns past 2 entries)
```

User-injected (`org`) ALWAYS gets 1 slot when present — concrete
implementation of "users own their workflow". Empty slots fall
through to vector ranking (no waste).

### Conflict handling — surface, don't auto-resolve

When user `org` knowledge contradicts shipped `shared` knowledge
(example: user says "we use BGP md5", shipped says "SRL doesn't
support md5, use TCP-AO"), DON'T programmatically resolve. Instead:

1. **Surface both** in the recall block
2. **Sort `source=user` first** (user's content visible first)
3. **Optional explicit override**: YAML field `precedence: override`
   tells recall to put it ahead of shipped on tied vector scores

LLM gets both viewpoints in context and reasons about the conflict
— more rigorous than if-else resolution in code, and the user can
see what's clashing.

### CLI: olav kb import-experts (extended)

```
# Default behavior (Phase 1.5): scan both user dirs
olav kb import-experts                          # scans ~/.olav/expertise/ + <project>/.olav/expertise/

# Explicit dir (Phase 1 still works)
olav kb import-experts <dir>

# Audit
olav kb list-experts --source=user              # only my injections
olav kb list-experts --scope=shared:ops         # only ops-domain shared
olav kb list-experts --conflicts                # show shipped/user collisions
```

### Re-classify existing 6 expert YAMLs

Current state (all Phase 1, scope=`<agent>`):

| YAML | current scope | Phase 1.5 scope | rationale |
|---|---|---|---|
| `bgp_validation_srl` | ops-lab | ops-lab (keep) | lab-only verification |
| `ospf_validation_srl` | ops-lab | ops-lab (keep) | lab-only verification |
| `srl_config_push_gotchas` | ops-lab | ops-lab (keep) | lab-only deploy |
| `clab_topology_srl` | ops-lab | **DELETE** | misclassified Type B → moved to R88 generator tool |
| `srl_spec_generation_rules` | ops-analyze | **shared:ops** | analyze writes spec, lab debugs failures using same rules |
| `junos_vs_ios_bgp_translation` | ops-analyze | **shared:ops** | translation rules used by analyze (writing) and lab (verifying) |

So 3 stay as-is, 1 moves to a tool (R88), 2 promote to `shared:ops`.

### Implementation slice (Phase 1.5)

1. Update `expert_kb.discover_experts` to scan `shared/<domain>/`
   pattern and infer scope from path
2. Update `expert_kb.from_yaml` to validate scope vocabulary
3. Update `_gather_candidates` to filter `scope IN (current_agent,
   shared:domain, org)`
4. Update `_CATEGORY_QUOTAS` to use the dict form for `expert_knowledge`
5. Add `source` field at import time (`source=shipped` if path is
   under a plugin dir; `source=user` if under `~/.olav/expertise`
   or `<project>/.olav/expertise`)
6. Add CLI flags: `--source=user`, `--scope=...`, `--conflicts`
7. Migrate 2 YAMLs to `shared/ops/expertise/`
8. Delete `clab_topology_srl.expert.yaml` (replaced by R88-A tool)

### Validation gate (Phase 1.5)

* Re-run Chapter 5 — ops-lab still recognizes SRL `active` as TCP fail
* Re-run ops-analyze spec generation — `srl_spec_generation_rules`
  surfaces from `shared:ops` (was `ops-analyze` only)
* New test: ops-lab recall block includes the same rules (now visible)
* Negative test: ops-audit recall block does NOT include
  `bgp_validation_srl` (scope=`ops-lab` only, ops-audit is in same
  domain but not the same agent)

### Decision log addendum

| date | decision | rationale |
|---|---|---|
| 2026-04-27 | Three-tier scope: `<agent>` / `shared:<domain>` / `org` | Multi-value scope leads to copy-paste lists; forced choice creates abstraction |
| 2026-04-27 | `org` reserves 1 quota slot when present | Business-model promise: users own workflow, must always be visible |
| 2026-04-27 | Surface conflicts, don't auto-resolve | LLM reasons over conflict better than if-else; user can audit |
| 2026-04-27 | Move `clab_topology_srl` to R88 generator tool | Type B knowledge (data transformation) belongs in tool layer, not memory |
| 2026-04-27 | Phase 1.5 SHIPPED: scope-vocabulary validator + path-inference + recall scope filter + 3-slot sub-quota + reclassified YAMLs (5 expert entries: 3 `ops-lab`, 2 `shared:ops`) | Closes the two structural gaps Phase 1 left (no shared tier, no user-injection path); commits in `feat(memory): R87 Phase 1.5` |
| 2026-04-27 | Phase 1.5 architecture closing: `precedence` field + `olav kb list-experts` CLI | Last architectural items — schema now supports explicit override marking; CLI gives users an audit surface |

---

## Phase 1.5 in-vivo validation — Chapter 4 → Chapter 5 chain (2026-04-27)

After shipping the architecture, ran the full demo chain against the
real CLAB digital twin to see whether Phase 1.5's scope filter and
shared:ops promotion actually changed LLM behavior.

### Chapter 4 — ops-analyze produces CAB spec

| run | result | observation |
|---|---|---|
| ch4 v1 (vanilla prompt) | partial | spec produced (`cab_r1_r4_ebgp_ch4.md`) but contains only Junos/IOS prod configs — no SRL Lab Substitution Table, no SRL CLI ``config_lines``. The shared:ops `srl_spec_generation_rules` knowledge did NOT surface (vector relevance: prompt mentions no SRL/lab). |
| ch4 v2 (explicit SRL hint) | tool-call protocol fail | small model emitted literal ``<xai:function_call>`` control tokens inside `format_and_export`'s `subdir` arg → ``FileNotFoundError``. Different LLM bug class — prompt protocol confusion. |

### Chapter 5 — ops-lab validates the ch4 v1 spec

| signal | result |
|---|---|
| Agent calls `recall_memory("junos_vs_ios_bgp_translation")` | ✅ Phase 1.5 scope filter working — ops-lab CAN see shared:ops knowledge |
| Agent uses recalled translation knowledge correctly | ❌ agent fell back to `web_search` despite prompt prohibition; produced SRL configs with wrong YANG paths (``connectivity-endpoint``, deprecated ``group type external``, missing ``peer-group``, wrong ``afi-safi`` placement) |
| `generate_clab_topology` produces correct yaml | ✅ R88-A worked — `r1:e1-1 ↔ r4:e1-1`, lowercase nodes, correct mapping |
| Lab deploys + topology established | ✅ deploy_and_push_lab succeeded; veth + interfaces UP |
| BGP reaches Established | ❌ configs never committed — SRL YANG rejected every variant the agent tried (~12 push iterations) |
| Final verdict | ❌ CAB FAIL with **sharp, actionable diagnosis**: "SRL config parse failures: connectivity-endpoint path, BGP group type external unsupported, neighbor peer-group mandatory error, afi-safi must be enabled per-neighbor" |

### What this reveals

1. **Phase 1.5 architecture works** — scope filter, sub-quota,
   shared:ops promotion all functioning at the recall layer.
   `recall_memory` with the right query lights up the right entry.

2. **R88-A architecture works** — generate_clab_topology produced
   correct yaml across the stack; topology / interfaces / veth all
   verifiably correct.

3. **Diagnosis quality is excellent** — agent named the exact 4
   YANG paths that failed; this is the kind of FAIL evidence that
   Phase 1's design promise was about. R86 baseline would have
   reported "BGP not up" with no specific cause.

4. **Knowledge → tool-arg gap persists** — same class of small-model
   adherence issue we saw in R88-B v1. Surfacing the right knowledge
   isn't enough; the LLM has to apply it during tool-arg synthesis.
   The agent had `srl_spec_generation_rules` in recall AND called
   `recall_memory` for `junos_vs_ios_bgp_translation` AND was
   prompted to translate — and still produced syntactically wrong
   SRL configs.

5. **ops-analyze doesn't auto-include SRL Lab Substitution** —
   shared:ops scope means `srl_spec_generation_rules` _can_ surface
   for ops-analyze, but vector relevance only fires when the prompt
   itself mentions SRL/lab. The CAB-spec contract should always
   include SRL substitution when downstream is the digital twin,
   but there's no prompt-level enforcement of that today.

### Two follow-up items implied (not yet implemented)

* **F1** — usage_guide forcing ops-analyze to include SRL Lab
  Substitution Table on every CAB spec output. Either a new
  `cab_spec_must_include_srl_substitution.guide.yaml` for ops-analyze,
  or update `ops/analyze/SKILL.md` flow to require it.
* **F2** — closer look at recall ranking for procedural-translation
  knowledge: small model didn't trust the recalled translation
  rules (fell back to `web_search` despite prompt prohibition).
  Likely the recall block presents the YAML body but the agent
  treats it as background hint rather than authoritative source.
  Investigate whether prepending "AUTHORITATIVE — apply verbatim"
  to expert_knowledge entries helps.

### F1 attempt + outcome (2026-04-27, REOPENED)

Tried three iterations of F1:

1. **Standalone usage_guide** (`cab_spec_srl_required.guide.yaml`,
   intent=`cab_spec_generation`) — failed: keywords like "lab
   validation" / "digital twin" / "srl" pulled the orchestrator's
   routing toward ops-lab when the user said "save the spec".
   Agent went full Chapter 5 deploy path. Guide deleted.

2. **CAB_SPEC_FORMAT.md update** with detailed SRL section + table
   template — failed: ops-analyze's static_context_mode is
   `on_intent`, so CAB_SPEC_FORMAT.md is lazy-loaded and the agent
   never reads it during spec generation. Reference doc kept
   (it's correct for any human or agent that DOES load it).

3. **Inline SRL skeleton in `prompts/system.md`** — partial: agent
   used the new lab-link IP scheme (172.16.99.0/30, direct
   peering) but still didn't emit the SRL `set / interface ...`
   block. format_and_export call sometimes botched (passed a
   dict containing `path` instead of the markdown string).
   Variable LLM behavior across runs.

**Status reopened (2026-04-27):** F1 is **NOT** "deferred to
tuning" — that framing was too generous. ops-analyze is failing
at a **basic correctness requirement**: the spec it produces is
missing the SRL section that ops-lab needs to validate. This is
the agent's primary deliverable, not a polish item. The fact that
three prompt iterations didn't fix it means we need to re-examine
the failure mode, not write it off.

Reframed problem: ops-analyze must reliably produce a CAB spec
that includes BOTH a prod CLI section AND a SRL Lab Substitution
+ SRL CLI section — every time. Today it doesn't. This needs to
be solved before Chapter 4 → Chapter 5 demo chain can be claimed
"complete".

Investigation directions still on the table:
* Force-load `CAB_SPEC_FORMAT.md` via `static_context_mode:
  always` for change-plan intents only
* Check whether the recall block actually surfaces
  `srl_spec_generation_rules` for ops-analyze in a real run
  (Phase 1.5 scope filter is correct in theory; observe in vivo)
* Test whether prepending "AUTHORITATIVE — apply verbatim" to
  expert_knowledge text bodies changes adherence
* Consider whether the dict-vs-string `format_and_export` bug is
  upstream (small-model artifact) or downstream (tool ergonomics)

Until F1 actually lands, ops-analyze auto-generation is unreliable
for the lab-validation chain. **Demo runsheet must use a
hand-authored spec** (reference: `exports/reports/cab_r1_r4_ebgp_v3.md`)
for Chapter 5 to pass.

---

## F3 — Lab → Spec validation footer feedback loop (NEW, 2026-04-27)

**Origin**: Chapter 4 → Chapter 5 closed loop ran on 2026-04-27.
Chapter 4 produced a defective spec (missing SRL section).
Chapter 5 found the defect and produced a high-quality CAB FAIL
report with 4 specific YANG rejection paths and a clear
recommendation. **But** the FAIL report landed in a SEPARATE
file (`cab_r1r4_ch4_ch5_report.md`), with no link back to the
original spec (`cab_r1_r4_ebgp_ch4.md`). Anyone re-opening the
spec — human or future agent — has no way to know it was
validated, failed, and why. The lab's high-quality diagnosis
is wasted after the session ends.

This is a real architectural gap, separate from Phase 1.5
(recall layer) and Phase 4 (curator-driven self-improvement).
Phase 4 was deferred to enterprise edition for good reasons
(curator agent + confidence model + multi-tenant isolation),
but the **simplest possible feedback channel** — lab writes a
footer on the spec — needs none of that. It's the zero-cost
prelude to Phase 4.

### Design

When ops-lab finishes a CAB validation and renders a verdict,
it appends a `## 🤖 Lab Validation Footer` to the original
spec file (the one referenced in the user's prompt). The footer
includes:

* Decision (PASS / FAIL / BLOCKED)
* Lab name + snapshot used
* Why it failed (specific issues — pulled from the structured
  report's `issues` field)
* Recommendation for the next sim revision
* Path to the full lab report file

Original content stays intact; the footer is purely additive.

### Worked example (PASS case)

```markdown
... (sim's original spec content)

---

## 🤖 Lab Validation Footer — appended by ops-lab on 2026-04-27 12:30

**Decision**: ✅ PASS  **Lab**: `cab_r1r4_v3_r88b_v5`  **Snapshot**: `2026-03-30_2210`

**Evidence**:
- BGP r1: established (172.16.99.2 AS65001), [Rx/Active/Tx]=[1/1/1]
- BGP r4: established (172.16.99.1 AS65000), [Rx/Active/Tx]=[1/1/1]
- Routes exchanged: 4.4.4.4/32 ↔ 1.1.1.1/32 (1 each side)
- Configs committed cleanly, no dry_run_failures

**Full lab report**: `exports/reports/cab_r1r4_v3_r88b_v5_report.md`
```

### Worked example (FAIL case — what the ch4→ch5 run should
have produced)

```markdown
... (sim's original spec content)

---

## 🤖 Lab Validation Footer — appended by ops-lab on 2026-04-27 13:52

**Decision**: ❌ FAIL  **Lab**: `cab_r1r4_ch4_ch5`  **Snapshot**: `2026-03-30_2210`

**Why this spec failed lab validation**:

- Spec contains only Junos / IOS CLI — ops-lab needs SRL CLI to push to digital-twin containers.
- On-the-fly translation tripped 4 SRL YANG rejections:
  - `connectivity-endpoint` path is invalid in SRL v24.10.1
  - `bgp group type external` not supported (group type inferred from peer-as mismatch)
  - `neighbor` requires explicit `peer-group` reference
  - `afi-safi ipv4-unicast` must be enabled (default is rejected)

**Recommendation for sim revision**:

- Add `## SRL Lab Substitution Table` mapping prod R1/R4 → lab r1/r4 (lowercase, `ethernet-1/1`)
- Add `## Config Lines — node r1 (SRL CLI)` + same for r4 using the 23-line SRL skeleton
- Use a NEW /30 for lab link IPs (e.g. `172.16.99.0/30`), not prod IPs

**Full lab report**: `exports/reports/cab_r1r4_ch4_ch5_report.md`
```

### Implementation plan

**Where in ops-lab**:

The natural integration point is between `format_and_export`
(writes the standalone lab report) and `destroy_lab`. Add an
explicit step:

* **Step 5b — annotate spec** (NEW): if the user-provided spec
  path is known and the spec file exists on disk, append the
  validation footer to it. Use `format_and_export` with mode
  `append` (or a small `append_to_file` helper — choose the
  simpler one available).

**Where in ops-lab prompt** (`ops/lab/prompts/system.md`):

Update the mandatory tool sequence:
* 5a. `format_and_export` writes the standalone lab report
  (existing).
* 5b. **NEW** — if the originating spec file path is in scope,
  append a Validation Footer with decision + diagnosis +
  recommendation. FAIL footers are mandatory; PASS footers are
  recommended (cheap to write, helps anyone re-reading).

**Where in tooling**:

Two options:
* (i) Add an `append` mode to `format_and_export` if it doesn't
  already support it.
* (ii) Create a tiny `append_to_file` MCP tool — 20 LOC, no
  validation, just appends a markdown block to a file path
  with optional separator.

### Open questions

* **Scope of edit** — only the FAIL case mandates an annotation,
  or PASS too? (Recommend: both — PASS shows "this spec is
  validated"; FAIL shows "this spec needs revision".)
* **Idempotency** — if the lab is re-run on the same spec, does
  the new footer replace or append? (Recommend: append; multiple
  footers form an audit trail across revisions.)
* **Spec path discovery** — the lab agent gets the spec path
  from the user prompt today. Should there be a structured
  way to pass it (e.g., a `--spec` arg to a higher-level
  command)? Out of scope for F3 v1.

### Boundary vs Phase 4 (enterprise)

| F3 (this) | Phase 4 (enterprise) |
|---|---|
| Lab annotates the spec file | Curator agent reviews captures, promotes/demotes confidence |
| Markdown append, no schema | Pending pool, confidence model, audit log |
| Visible to next reader (human or agent re-reading file) | Visible to future recall as boosted/demoted memory |
| Zero new infrastructure | New subagent + cron + reward propagation |
| Open source | Enterprise edition |

F3 is the simplest possible feedback channel. Phase 4 is the
self-improving system. F3 doesn't depend on or block Phase 4;
they compose if/when Phase 4 ships.

### Decision log addendum

| date | decision | rationale |
|---|---|---|
| 2026-04-27 | F1 reopened — not "deferred to tuning" | Failure to produce SRL section is a basic correctness bug, not polish; sim's primary deliverable is broken |
| 2026-04-27 | F3 — lab writes Validation Footer back to spec | Closes the artifact-level feedback loop; zero-cost prelude to Phase 4; spec becomes self-documenting after lab validation |

### Comparison: R88-B v5 (full PASS) vs ch4→ch5 (FAIL)

| factor | v5 (PASS) | ch4→ch5 (FAIL) |
|---|---|---|
| Spec source | hand-authored v3 with full SRL Lab Substitution + correct SRL CLI | ops-analyze ch4 with prod-only Junos/IOS configs |
| ops-lab translation step required | no | yes (Junos/IOS → SRL) |
| Phase 1.5 scope filter active | yes (R88-A) | yes (this run) |
| Translation knowledge available | n/a | yes (`shared:ops/junos_vs_ios_bgp_translation`) |
| Agent applied translation correctly | n/a | no (web_search + wrong YANG paths) |

The PASS-vs-FAIL diff is entirely about whether the spec arrives
already in SRL form. F1 closes that gap.
