# 101. OLAV Mini Demo Runsheet — the 12-minute network-engineer wow

**Date**: 2026-07-18
**Version**: v0.23.1 (public PyPI: `olav` + `olav-netops`)
**Duration**: ~10–13 min (6 beats)
**Companion to**: `dev_docs/100. DEMO_RUNSHEET_V3.md` — that is the full ~50 min
capability sweep; **this is the tight cut**: only the beats that make a network
engineer lean in, each verified end-to-end on a small local model this cycle.

> **Why a mini runsheet.** The full V3 sweep is comprehensive but long. For a
> cold audience (a network engineer who's never seen OLAV), the story lands in
> ~11 minutes if you show exactly six things: it installs itself, ingests a
> real fleet with no SSH, answers in plain language + exports, draws the core,
> **writes a BGP change and then formally validates it**, and runs the morning
> NOC inspection from a versioned Profile. Everything below was run on
> `gemma4-31b` and produced correct output — no frontier model required.

---

## Setup (off camera)

**LLM backend.** `gemma4-31b-it-qat` at an OpenAI-compatible endpoint
(`http://<host>:11433/v1`, `api_key: local`, `llm.timeout: 600`). This is the
headline claim — a **local ~30B model** runs the whole demo. Do NOT use a
"flash"/tiny model for the agentic beats: in testing `deepseek-v4-flash`
flailed (146 tool calls vs gemma4's 16 on the same task). gemma4-31b or a
frontier model; nothing in between is needed.

**Demo data.** A 339-device campus/DC fleet already imported (Ch1 shows the
import live, or pre-load it). Batfish image cached locally so the
self-service deploy is fast on camera.

**One health line before you hit record:**

```bash
olav doctor          # must be all 9 ✓ — workspace/agents/subagents/tools/memory/recall
```

---

> **Two contexts, in execution order.** Everything runs from one machine.
> **On the host terminal** you do exactly two things: import the fleet, then
> launch the TUI. **Inside the TUI** you do the rest — asking in plain English
> and switching agent context with `/workspace <name>`. Lines below are marked
> `[terminal]` or `[TUI]` so the operator knows where they are.

## Ch0 — A clean machine installs and configures itself (2 min) ★ · `[terminal]`

```bash
mkdir ~/olav && cd ~/olav
python3 -m venv .venv && source .venv/bin/activate
pip install olav "olav-netops[sim]"  # [sim] = pybatfish, needed by Ch4's formal validation
olav skill install olav-netops        # bootstraps on first run (asks once for the LLM key), deploys the network agents
olav doctor                           # 9 zero-LLM checks, all ✓
```

**Why they care.** Zero-touch. No `git clone`, no editing YAML. `olav doctor`
proves the whole stack is wired (agents, sub-agents, tools, experience layer,
recall, workspace integrity) before you ask it anything — an ops person trusts
a tool that self-diagnoses.

## Ch1 — Ingest a real fleet, no SSH (2 min) ★ · `[terminal]`

The one data step on the CLI (it's a long parse — run it, then open the TUI):

```bash
olav --agent netops "Import the network backup at <path-to-archive>.tar.gz"
olav                       # launch the TUI — everything from here on happens inside it
```

**Why they care.** You hand it a raw collector dump (a `.tar.gz`); the importer
extracts + normalises + parses (TextFSM) + builds the topology automatically.
339 devices land with zero device access — safe against production backups.

## Ch2 — Ask in English, get a CSV (1.5 min) · `[TUI]`

Type these straight into the TUI prompt (the default orchestrator routes them):

```
How many devices and what platforms did we just import?
List every device with its model and mgmt IP, and export it to CSV
```

**Why they care.** The NL question becomes SQL over the parsed inventory and
drops a real `exports/*.csv` — no query language to learn, the data is exact,
not hallucinated.

## Ch3 — Draw the core network as a draw.io diagram (1.5 min) ★ · `[TUI]`

```
Draw the core network topology as a draw.io diagram
```

Open the resulting `exports/diagrams/core_topology_*.drawio` in diagrams.net.
**Why they care.** A scoped, correct topology (Cisco stencils, real
adjacencies) for a Confluence page — the agent scopes it to the core layer
(~16 nodes), not the whole fabric.

## Ch4 — Write a BGP change and formally validate it (3 min) ★★★ · `[TUI]`

Switch to the services agent to stand up the validation engine, then to netops
to draft-and-validate — all inside the TUI via `/workspace`:

```
/workspace services
Deploy batfish/allinone, ports 9996 9997
/workspace netops
Write a change plan to add a redundant eBGP uplink on the alpha border router, mirroring the existing AARNet peer
```

**Why they care.** `/workspace` swaps agent context inside the same TUI
session — no new terminal, no re-launch. That single change-plan request then
fans out into three verified sections, stacked in the reply:

- **Draft** — analyzer reads the device's **actual** running-config (not a
  template), finds the real `router bgp <asn>` + the existing neighbour's
  style, and emits mirroring CLI + rollback, saved to `exports/change_plans/`.
- **Pre-check (reporter)** — device/interface/IP sanity against live state +
  an independent blast-radius number ("removing this isolates N nodes").
- **Formal verification (Batfish)** — subnet/overlap conflicts, BGP
  compatibility, reachability — a real verdict, on the agent's **own**
  self-deployed Batfish.

An AI that writes a change **and then formally proves it out** from one
sentence is a different category of tool. (The `Deploy batfish…` step must
finish before the change-plan line; if Batfish isn't up the workflow still
returns the draft + pre-check and tells you how to bring it up. The deploy
asks for a one-key confirmation — that HITL gate is by design.)

## Ch5 — Scheduled-style audit inspection (1.5 min) ★★ · `[TUI]`

```
/workspace audit
Run the interface_health audit profile and give me the findings
```

**Why they care.** This is the Day-2 ops loop: a **Profile** (a YAML file of
SQL health checks with severity rules — `.olav/workspace/audit/profiles/`) is
executed deterministically against the state DB, and the agent returns an
executive summary plus a saved report (`exports/audit_reports/*.md` + a
findings JSON). Profiles are plain files: engineers review them, extend them
via the audit author agent ("add a check for X"), and can schedule them with
`olav cron` — the same inspection your NOC runs every morning, but the
checks themselves are versioned, auditable artifacts. Two tool calls,
zero-LLM evaluation — the LLM only narrates findings it can cite.

---

## Closing line

> "Local 30B model. A production backup in, and out came: answers, a diagram, a
> validated BGP change — and the morning NOC inspection as
> a versioned, auditable Profile. No cloud, no SSH to prod, nothing to babysit."

## Beat → capability → verified-artifact map

| Beat | Capability                               | Artifact you can show                                           |
| ---- | ---------------------------------------- | --------------------------------------------------------------- |
| Ch0  | zero-touch install + self-diagnosis      | `olav doctor` — 9 ✓                                         |
| Ch1  | offline ingest + TextFSM + topology      | 339 devices in DuckDB                                           |
| Ch2  | NL → SQL → export                      | `exports/*.csv`                                               |
| Ch3  | topology render                          | `exports/diagrams/*.drawio`                                   |
| Ch4  | self-service Batfish + change plan drafted, pre-checked & Batfish-validated (one workflow) | `exports/change_plans/*.md` with Draft / Pre-check / Formal verification sections |
| Ch5  | Profile-driven audit inspection          | `exports/audit_reports/interface_health_*.md` + findings JSON |

## Notes / honesty

- Every beat above was run on `gemma4-31b` this cycle and produced correct
  output. Batfish models **BGP** well and EIGRP poorly — keep the Ch4
  change-plan + validation beat on **BGP**, not EIGRP.
- **Ch4 ordering matters**: `/workspace services` + `Deploy batfish…` must
  complete *before* the `/workspace netops` change-plan line — that command
  triggers the analyzer→reporter→simulator workflow whose final step validates
  against a running Batfish. (If Batfish isn't up the workflow still returns
  the draft + pre-check.)
- **`/workspace <name>` is the in-TUI agent switch** (netops / services /
  audit / admin / core). Only the import (Ch1) runs on the host terminal;
  everything else is typed into the TUI. An unknown name lists the valid
  ones. `/workspace` and `/agents` are the same command.
- Ch5 must use **interface_health** (or device_inventory): this dataset has no
  BGP/OSPF TextFSM views, so `bgp_health`/`ospf_health` profiles error out
  (`v_show_ip_bgp_summary_auto` does not exist). Interface/inventory views are
  fully populated.
- For the full capability sweep (KB, conversational HITL memory, drift,
  blast-radius, audit profiles, syslog) see `dev_docs/100. DEMO_RUNSHEET_V3.md`.
