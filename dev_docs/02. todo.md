# OLAV 执行清单

**Last Updated:** 2026-04-02
**Version:** v0.20

---

## 0. 使用规则

1. 本文件只保留当前可执行的 next actions
2. 完成项移入 tracking ADJ Log，不在此堆叠
3. 与 `tracking.md`、`issues.md` 冲突时，以 tracking 为准

---

## 优先级总览（重构 2026-04-02）

```
P0  平台稳定 + pip 发布就绪        ← 最高优先，一切其他工作的基础
P1  文档网站（Cloudflare/GitHub Pages）
P2  Creator Agent 完整化            ← 核心能力
P3  NetOps 专属任务（CLAB E2E、协议扩展）
```

---

## 1. P0 — 平台稳定 & pip 发布就绪

> 目标：`pip install olav` 在干净 venv 中可用，通过全部 unit test

### 1.1 打包最终确认

1. ⬜ 运行 `uv build` → 验证 `dist/*.whl` 可生成，无 build error
2. ⬜ 在干净 venv 中 `pip install dist/olav-*.whl` → `olav --help` 正常
3. ⬜ 版本号对齐：`pyproject.toml version=0.12.0`，`__init__.py __version__` 一致
4. ⬜ `CHANGELOG.md` 补全 0.12.0 条目（当前 1.0.0 节不对应实际版本）

### 1.2 平台核心缺口修复

5. ⬜ **ISSUE-P1-DEEPAGENTS-TOOL-ISOLATION 根本修复**：实现 `olav_delegate(subagent_name, task)` 工具，绕过 deepagents `SubAgentMiddleware`，提供真正隔离的子 agent 调用（FilesystemMiddleware 不注入）；无工具 subagent 也改为 CompiledSubAgent 模式（见 `issues.md`）
6. ⬜ **GAP-04** `response_extractor.py`：schema-aware 响应裁剪（M5 规划，影响大型 API 响应处理）
7. ⬜ **GAP-11** Sandbox 工具层精简：删除 13 个可被 sandbox 替代的 `@tool`（见 `dev_docs/19. SANDBOX_TOOL_REFACTOR.md`）
8. ⬜ `enterprise/dataset_encryption.py` 中 `NotImplementedError` 占位符处理（明确废弃或实现）
9. ⬜ `olav skills create` CLI 模板中 3 处 `TODO` 占位符替换为实际模板内容

### 1.3 Ghost Audit 收尾（Phase 7）

9. ✅ `oc_transform_runner.py` / `transform_sandbox.py` 已删除
10. ⬜ 确认 `api_registry.py` 引用是否合理（3 处：`platform/services/client.py`、`service_registry.py`、`api_registry.py` 自身）
11. ⬜ `src/olav/core/` 剩余遗留文件全面扫描

---

## 2. P1 — 文档网站

> 目标：在 Cloudflare Pages 或 GitHub Pages 上部署，Markdown 自动渲染，支持搜索

### 2.1 静态站点框架搭建

1. ⬜ 选型并初始化（推荐 **MkDocs + Material theme**，纯 Markdown 驱动，零 JS 构建）
2. ⬜ 创建 `mkdocs.yml`，映射 `docs/` 目录树
3. ⬜ 配置 GitHub Actions / Cloudflare Pages 自动部署（push to main → deploy）

### 2.2 文档内容补全

4. ⬜ `docs/olav/` — 补全缺失章节：**Creator Agent 使用指南**（全新）
5. ⬜ `docs/olav/` — 补全：**Skill 开发指南**（GAP-06/07 覆盖 git install 流程）
6. ⬜ `docs/olav/` — 补全：**平台扩展开发指南**（如何开发 Domain Extension）
7. ⬜ `docs/cn/` — 现在只有 `01_README.md`，补全全部中文对应文档
8. ⬜ `docs/netops/` — ARCHITECTURE.md 已有，补全 Creator/Discovery workflow 文档

---

## 3. P2 — Creator Agent 完整化

> 目标：`config/creator` 成为可用的核心能力，能端到端生成并部署 skill

### 3.1 工具层验证与修复

1. ⬜ 为 5 个 creator tools 补充 unit test（`read_api_schema`、`write_skill_code`、`generate_skill_config`、`read_file`、`write_file`）
2. ⬜ E2E 验证：输入 NetBox OpenAPI schema → 生成 skill 文件 → 可正常加载

### 3.2 基础设施补全（Creator 的下游依赖）

3. ⬜ **GAP-06** `olav skill install <git-url>`：实现 git clone + workspace 注册
4. ⬜ **GAP-07** `olav skill install --merge-into <ws>`：SkillPack 追加模式

### 3.3 Prompt & SKILL.md 深化

5. ⬜ `config/creator/prompts/system.md` 深化：增加 OLAV 平台约定（SKILL.md v5 格式、工具命名规范、沙箱安全要求）
6. ⬜ `config/creator/SKILL.md` 补充 WSDL/Postman 格式支持文档

---

## 4. P3 — NetOps 专属（不阻塞平台发布）

### Lab Agent E2E（需 CLAB 访问）

1. ⬜ 确认 CLAB token 获取方式
2. ⬜ `run_python_simulation` sandbox 推配置 → 验证接口 IP 落地
3. ⬜ OSPF / iBGP 收敛验证
4. ⬜ agent 自主完成全流程

### 协议收集器扩展

5. ⬜ 扩展 `commands` 表：IS-IS / EVPN / VXLAN / MPLS LDP
6. ⬜ 验证 ntc-templates TextFSM 模板存在
7. ⬜ 重跑 snapshot → 验证视图有数据

### Test Coverage

8. ⬜ unit test：`_is_ip`, `_strip_domain`
9. ⬜ 真实设备 snapshot 后回归验证 topology ETL

---

## 5. Deferred（平台 1.0 之后）

1. EIGRP 实现
2. Repo 物理拆分（olav-core / olav-netops 独立仓库发布）
3. SecuritySidecarPlugin（LLM 旁路安全监控）
4. OLAV Enterprise（`olav-ent`）正式化
