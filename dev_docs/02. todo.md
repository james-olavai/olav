# OLAV 执行清单

**Last Updated:** 2026-04-02 (rev 3)
**Version:** v0.22

---

## 0. 使用规则

1. 本文件只保留当前可执行的 next actions
2. 完成项移入 tracking ADJ Log，不在此堆叠
3. 与 `tracking.md`、`issues.md` 冲突时，以 tracking 为准

---

## 优先级总览（修订 2026-04-02 rev 3）

```
P0  发布就绪                  ← 阻塞 pip publish 的硬性缺口（3项）
P1  平台质量收尾              ← 不阻塞发布但影响可靠性（5项）
P2  平台功能扩展              ← 有需求但不是必须（4项）
P3  NetOps 专属               ← 不影响平台核心（7项）
```

---

## 1. P0 — 发布就绪（最小化阻塞集）

> 目标：`pip install olav` + `olav --help` 在干净环境正常，全部测试绿

1. ⬜ **GAP-13** 修复 `test_run_single_query_writes_run_start`：`AuditEventRecorder._conn` 已删除，测试改为读 DB 文件验证（快，1个测试）
2. ⬜ **打包验证**：`uv build` → `dist/*.whl` 生成无错；干净 venv `pip install` → `olav --help` 正常
3. ⬜ **版本对齐**：`pyproject.toml version`、`__init__.py __version__`、`CHANGELOG.md` 0.12.0 条目三处对齐

---

## 2. P1 — 平台质量收尾

> 目标：核心功能有测试覆盖，技术债不积压

4. ⬜ **GAP-12** Creator tools unit test：5 个 creator tools 补 unit test（`create_service_config`、`register_api_service`、`create_skill_workspace`、`extract_schema_reference`、`list_platform_services`）— 已 E2E 验证，补单元测试完成质量闭环
5. ⬜ **GAP-11**（修订版）删除 3 个纯 shell 封装：`ping_device.py`、`traceroute.py`、`port_scan.py` → 更新 probe SKILL.md + system.md 说明用 `run_shell`（其余 10 个 @tool 经评估保留，详见 SANDBOX_TOOL_REFACTOR.md）
6. ⬜ **GAP-14** `call_api` → `service_call` 迁移：更新 `deploy_lab.py` + `exec_on_node.py` 改用 `service_call("clab", ...)`，删除 hardcoded IP 的 `call_api.py`
7. ⬜ `src/olav/core/` Ghost Audit 收尾：全面扫描剩余遗留文件（`api_registry.py` 已确认活跃）
8. ⬜ `olav skills create` CLI 模板中 3 处 `TODO` 占位符替换为实际模板内容

---

## 3. P2 — 平台功能扩展

> 目标：扩大平台能力边界，不阻塞 P0/P1

9. ⬜ **GAP-04** `response_extractor.py`：schema-aware 响应裁剪（大型 API 响应无自动截断，影响 LLM context）
10. ⬜ **GAP-06** `olav skill install <git-url>`：git clone + workspace 注册
11. ⬜ **GAP-07** `olav skill install --merge-into <ws>`：SkillPack 追加模式
12. ⬜ `enterprise/dataset_encryption.py` `NotImplementedError` 占位符：明确废弃或实现

---

## 4. P3 — NetOps 专属（不阻塞平台发布）

> 目标：完成 Lab Agent E2E 验证 + 协议采集扩展

### Lab Agent E2E（需 CLAB 访问）

13. ⬜ 确认 CLAB token 获取方式（`POST /api/v1/auth/login`）
14. ⬜ 验证 `exec_on_node` 推配置 → 接口 IP 落地（走 @tool，不走 sandbox）
15. ⬜ OSPF / iBGP 收敛完整流程（deploy → push → verify → 自主修正）

### 协议收集器扩展

16. ⬜ 扩展 `commands` 表：IS-IS / EVPN / VXLAN / MPLS LDP + 验证 ntc-templates 模板
17. ⬜ 重跑 snapshot → 验证 `v_isis_adjacencies` 等视图有数据

### Test Coverage

18. ⬜ unit test：`_is_ip`、`_strip_domain`（topology ETL）
19. ⬜ 真实设备 snapshot 后回归验证 topology ETL

---

## 5. Deferred（平台 1.0 之后）

1. 文档网站（MkDocs + Material + Cloudflare Pages）
2. EIGRP 实现
3. Repo 物理拆分（olav-core / olav-netops 独立仓库发布）
4. SecuritySidecarPlugin（LLM 旁路安全监控）
5. OLAV Enterprise（`olav-ent`）正式化
6. WSDL / Postman 格式支持（Creator Agent）
