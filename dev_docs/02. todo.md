# OLAV 执行清单

**Last Updated:** 2026-04-03 (rev 6)
**Version:** v0.12.0

---

## 0. 使用规则

1. 本文件只保留当前可执行的 next actions
2. 完成项移入 tracking ADJ Log，不在此堆叠
3. 与 `tracking.md`、`issues.md` 冲突时，以 tracking 为准

---

## 优先级总览（修订 2026-04-03 rev 6）

```
🔴 P0  发布阻塞         — ISSUE-P0-DEEPAGENTS-CLI-MISSING（31 E2E 失败）
✅ P1  平台质量收尾     — 全部完成
✅ P2  平台功能扩展     — 全部完成
✅ DOC 文档站           — 完成（EN/ZH 双语 23 对，mkdocs build 通过）
P3  NetOps 专属         ← 等待 M2 阶段
```

**当前状态：文档就绪，平台被 deepagents-cli 依赖问题阻塞。**

---

## 0.5 🔴 发布阻塞（立即修复）

> 详见 `dev_docs/00. issues.md` — ISSUE-P0-DEEPAGENTS-CLI-MISSING

1. ⬜ **确认 `deepagents-cli` 在 pyproject.toml dependencies 中声明**
2. ⬜ **`uv sync` 或 `uv add deepagents-cli`** 安装到运行环境
3. ⬜ **重跑 E2E**: `uv run pytest tests/00_e2e_acceptance_test.py -q` → 预期 ≥165 passed
4. ⬜ **LDAP 测试降级**: 在 TestLDAPAuthClaim 中加 `pytest.importorskip("ldap3")`

解除后 E2E 预期：**169 passed（或 167 passed + 2 skipped）**

---

## 1. DOC — 文档站（✅ 已完成）

> 详细方案见 `dev_docs/22. WEB_AND_DOCS_SITE.md`（Doc-Driven Testing 方法论）
> 执行指令见 `dev_docs/24. DDD_VERIFICATION_TASK.md`

1. ✅ **Step A** 代码审计 → 44 条 Claim 清单（`docs/reference/claim-registry.md`）
2. ✅ **Step B** Claim 审阅 + ID 分配（L1 ×1, L2 ×43, 全部 ✅ Verified）
3. ✅ **Step C** `docs/` 完整改造：EN/ZH 双语 23 对文件 + 每页 Claim 声明表
4. ✅ **Step D** CLI 验证 + E2E 测试：44 条 Claim 全部 Verified，50 个 TestClass
5. ✅ **Step E** 修复或收窄：完成
6. ⬜ **Step F** 部署：`mkdocs build` ✅ 通过 → **待 Cloudflare Pages 连接**

---

## 2. P3 — NetOps 专属（M2 阶段）

> 目标：完成 Lab Agent E2E 验证 + 协议采集扩展

### Lab Agent E2E（需 CLAB 访问）

7. ⬜ 确认 CLAB token 获取方式（`POST /api/v1/auth/login`）
8. ⬜ 验证 `exec_on_node` 推配置 → 接口 IP 落地（走 @tool，不走 sandbox）
9. ⬜ OSPF / iBGP 收敛完整流程（deploy → push → verify → 自主修正）

### 协议收集器扩展

16. ⬜ 扩展 `commands` 表：IS-IS / EVPN / VXLAN / MPLS LDP + 验证 ntc-templates 模板
17. ⬜ 重跑 snapshot → 验证 `v_isis_adjacencies` 等视图有数据

### Test Coverage

18. ⬜ unit test：`_is_ip`、`_strip_domain`（topology ETL）
19. ⬜ 真实设备 snapshot 后回归验证 topology ETL

---

## 6. Deferred（平台 1.0 之后）

1. 文档网站多版本（MkDocs versioned docs）
2. EIGRP 实现
3. WSDL / Postman 格式支持（Creator Agent）
4. SecuritySidecarPlugin（LLM 旁路安全监控）— M3 前可选
