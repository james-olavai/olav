# OLAV 执行清单

**Last Updated:** 2026-05-09 (rev 8 — V2 work stream + CAB Patch K-N closed; new R-AGENT-HIERARCHY work stream from `dev_docs/73`)
**Version:** v0.19.0 (0.11.0 branch)

---

## 0. 使用规则

1. 本文件只保留**当前可执行**的 next actions
2. 完成项移入 `00. issues.md` 关闭记录或 commit message
3. 与 `00. issues.md` / commit history 冲突时，以 commit history 为准

---

## 优先级总览

```
✅ V2 demo runsheet — 14/14 chapters closable
✅ CAB analyzer→lab→TCF — Patch L+M+N shipped (rev 230-231); end-to-end PASS verdict
✅ Patch D' (Steps 1-5) — tool registration scope decoupled from impl sharing
🔴 R-AGENT-HIERARCHY — Phase A/B/C agent refactor + 3 over-engineering follow-ups
🔴 ISSUE-CAB-DATA-NOT-GROUNDED (P0) — depends on R-AGENT-HIERARCHY Phase C
🔴 ISSUE-CAB-PROD-CLI-INCOMPLETE (P0) — also closed by Phase C tcf_writer
P3  Future       — variance instrumentation, CI capture of T1l/Ch7/Ch9
```

---

## 1. R-AGENT-HIERARCHY work stream (active)

Design + rationale: **`dev_docs/73. AGENT_HIERARCHY_REFACTOR.md`**.
Trigger: 2026-05-09 three failed CAB-emission experiments on
gemma4:31b nothink. The LLM was carrying multi-responsibility
agent contexts AND the orchestrator prompt was bloated.

Patch D' Steps 1–5 (2026-05-08) decoupled tool registration from
implementation sharing — this removes the 2026-05-01 reason that
forced `devops_*` / `netops_*` agents to top level.

### Phase A — Workspace consolidation (~1 unit) ✅ DONE 2026-05-09 (`ad99eee`)

Goal: 7 scattered top-level agents → **3 user-facing
orchestrators** (`audit`, `devops`, `netops`).  No prompt edits
beyond AGENT.md path adjustments.

**Phase A shipped** — see commit message for V1-V6 verification
results.  V7 (gemma4 in-vivo routing tests) deferred to Phase B/C.

| # | Step | File-level change |
|---|---|---|
| A1 | Create `devops/` orchestrator AGENT.md | merge route_keywords from devops_scripts + devops_infra; subagents: scripts/SKILL.md + infra/SKILL.md |
| A2 | Move `devops_scripts → devops/scripts`, promote AGENT.md → SKILL.md | mv + content transform |
| A3 | Move `devops_infra → devops/infra` same way | mv + content transform |
| A4 | Rename `ops → netops` (the source-tree netops_ops orchestrator) | mv + AGENT.md `name:` field |
| A5 | Move `topology → netops/topology` | mv + promote to SKILL.md |
| A6 | Move `command_learner → netops/learner` | mv + promote to SKILL.md |
| A7 | Update `ownership_manifest.yaml` for moved paths | yaml edits |
| A8 | `olav refresh` → rebuild PLATFORM.md + route table | runtime regen |
| A9 | `olav kb import-guides .olav/workspace` → reprime memory under new agent IDs | one cmd |
| A10 | In-vivo verify: 3 representative prompts per agent route correctly | qualitative |

**Verification gate A:** `olav list` shows 3 user-facing
orchestrators (audit / devops / netops); each prompt routes to the
right sub-agent; memory recall keys updated.

### Phase B — Split `ops/analyze` → `analyser` + `sim` (~½ unit)

Today's `analyze` carries 5 responsibilities (TCF emission /
routing analysis / simulation / topology viz / drift).  Today's
2026-05-09 CAB experiments showed gemma4:31b nothink can't reliably
hold all five contexts.

| # | Step | File-level change |
|---|---|---|
| B1 | Create `netops/analyser/` (read-side: drift, BGP investigation, topology Q&A) | new dir + SKILL.md |
| B2 | Create `netops/sim/` (write-side: what-if + change-plan PROSE) | new dir + SKILL.md |
| B3 | Migrate `analyze/tools/run_python_simulation.py` to both (or share via core) | reuse decision |
| B4 | Update guides: `drift_detection.guide.yaml` → analyser; `change_plan.guide.yaml` → sim | per-intent split |
| B5 | Delete `netops/analyze/` after migrations | cleanup |
| B6 | `olav kb import-guides` reprime + verify routing | regression check |

**Verification gate B:** memory recall for "diff snapshots t1 vs
t2" → analyser's guide; recall for "add eBGP between R1 and R3" →
sim's guide.  Both sub-agents start with `run_python_simulation`.

### Phase C — CAB workflow decoupling: prose plan → deterministic TCF writer (~1.5 units)

This is the actual fix for **ISSUE-CAB-DATA-NOT-GROUNDED**.  LLM
outputs prose; deterministic Python composes TCF.

| # | Step | File-level change |
|---|---|---|
| C1 | New `src/olav/core/cab/tcf_writer.py` — `render_tcf_from_change_plan(plan_text) → dict` | pure Python, no LLM |
| C2 | Move `_db_facts(...)` from emit_tcf to tcf_writer (DB grounding) | refactor |
| C3 | Per-intent + per-platform CLI templates for ebgp_direct (Junos / IOS) | template registry |
| C4 | New `render_tcf` skill-script in netops/sim/ | thin wrapper |
| C5 | sim's SKILL.md: forbid TCF emission; require `## Change Summary` block | prompt edit |
| C6 | Drop `emit_tcf` from sim's tools list | tool whitelist |
| C7 | Orchestrator HITL hook: pause after sim returns prose, wait for user, then call render_tcf | orchestrator change |
| C8 | Rewrite `change_plan.guide.yaml` to teach prose summary, not emit_tcf args | guide rewrite |

**Verification gate C:** gemma4:31b nothink with `r1-r3-ebgp`
request produces parseable prose plan with grounded facts (R1+R3
both AS 65000 → eBGP feasibility blocker explicitly stated, NOT
hallucinated as 65001/65003).  HITL → render_tcf produces valid
TCF YAML that ops-lab consumes (verdict=PASS or explicit
feasibility-block).  Closes:
- ISSUE-CAB-DATA-NOT-GROUNDED (P0)
- ISSUE-CAB-PROD-CLI-INCOMPLETE (P0) — CLI from templates is complete
- ISSUE-CAB-LLM-EMIT-SEMANTIC-ERRORS (P2)

---

## 2. Over-engineering follow-ups (audit findings 2026-05-09, scoped from `dev_docs/73`)

### F1 — `ops/prompts/orchestrator.md` token diet (30 min, P3)

101-line orchestrator.md.  Audit:
- 27 lines hard rules + delegation prose ✓ keep
- 14 lines delegation table ✓ keep
- 6 lines anti-routing footnote ✓ keep
- **16 lines schema cheatsheet** ← bloat (orchestrator only does
  single-row device lookups per Patch D' tools whitelist)
- **12 lines "Specialists" section** ← redundant with delegation
  table
- 9 lines operational guidelines ✓ keep

Target: ~40 lines.  Delete schema cheatsheet, fold "Specialists"
into delegation table commentary.

### F2 — `ops/lab/prompts/system.md` token diet (45 min, P3)

181-line single prompt — biggest in netops.  Patch L (2026-05-07)
folded the 5-step lab validation into `validate_tcf_in_lab.py`.
The sub-agent's runtime path is now "call execute_skill_script
once".  Suspect 60–70% of the prompt is historical workflow
narration that the skill-script subsumes.

Target: ~60 lines.  Keep "what is lab / when invoked / how to
read results"; delete multi-step workflow descriptions.

### F3 — `audit/auditor` split into `designer` + `auditor` (~1 unit, P3)

SKILL.md description literally says: "Audit Executor + Profile
Author — runs Profiles, renders reports, **and drafts/extends
Profile files**".  Was merged from v0.18.0 designer in Round 17
(Sprint 3 Step B).  281 lines of total prompts.

| Mode | Tools | Belongs to |
|---|---|---|
| Run | `map_engine`, `render_report` | new `auditor/` (run-only) |
| Author | `save_profile`, `append_jobs`, references/PROFILE_AUTHORING.md | new `designer/` |

Defer until evidence shows mixed-context wrong-tool picks; not on
the daily run path.

### F4 — `audit/curator` 4-domain split (defer, P3)

Curator owns schema discovery / TextFSM learning / trace analysis
/ scaffolding.  4 distinct workflows.  But SKILL.md prompt is small
(8 lines + curator README), each workflow has its own tools, Patch
D' guard limits wrong-tool risk.

**Defer.**  Revisit if local-LLM tests show curator picking wrong
tool across these 4 workflows.

---

## 3. Future / Deferred (long-term)

### 3.1 CI 化测试

V2 章节验证现在是手工跑：

```bash
olav --agent netops_ops "..."   # ad-hoc
```

未来应该自动化：
- T1l (TCF emission) — 检查 spec.tcf.yaml 写出 + Pydantic 校验通过
- Ch7 (BGP Idle layered) — 检查 root cause text 含 "admin down" / "interface"
- Ch9 (audit list) — 检查 12 profile 名都在输出里

写成 `tests/integration/test_v2_chapters.py`，每 chapter 1 个 test，
N=3 跑取 majority。

### 3.2 Variance instrumentation

T1h (Patch F) returned 0 emit_tcf calls; T1i (same setup, retry)
returned 31. DeepSeek 同 prompt variance 大。validation 应统计
multi-run success rate，不是 single-run。

实施方案：在 demo7 加个 `tools/v2_chapter_runner.py` 跑同一 prompt
N=3 次，agg success rate 输出 markdown table。

### 3.3 长 reference 全部审查 (跟已 ship 的 Patch J 同思路)

| Reference | 行数 | 拆分建议 |
|---|---|---|
| `infra/references/netbox_dcim_api.md` | 853 | NetBox DCIM CRUD per-resource guides (~10 个) |
| `infra/references/netbox_ipam_api.md` | 326 | NetBox IPAM 同上 |
| `ops/lab/references/LAB_REFERENCE.md` | 313 | CLAB lifecycle 5 个 guide |
| `infra/references/netbox_circuits_api.md` | 199 | 同 DCIM 模式 |

总潜在收益：~1.7 K rows of long-form ref → ~30 个 small guides，
意图召回精度提升一个量级。

### 3.4 Release tag 0.12.0

V2 work stream 是个完整里程碑。建议：
- `git tag 0.12.0-rc1` 在 0.11.0 顶
- 跑全 V2 sweep on 干净 demo
- 提到 main → 0.12.0 release

但建议**先做 R-AGENT-HIERARCHY Phase A+C** 再切 0.12.0 — Phase C
关闭两个 P0，0.12.0 该带这个修复。

---

## 4. Reference

* `dev_docs/00. issues.md` — 当前 issue tracker
* `dev_docs/03. milestones.md` — M1/M2/M3 release roadmap
* `dev_docs/73. AGENT_HIERARCHY_REFACTOR.md` — R-AGENT-HIERARCHY 设计 + 验证 gate
* `dev_docs/72. V2_VALIDATION_RETROSPECTIVE.md` — V2 work stream 复盘
* `dev_docs/71. DEMO_RUNSHEET_V2.md` — 跑通的 demo 脚本
* `dev_docs/65. R90_TCF_STRUCTURED_CHANGE_CONTRACT.md` — TCF schema 设计（Phase C 涉及修改）
