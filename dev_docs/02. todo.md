# OLAV 执行清单

**Last Updated:** 2026-03-31
**Version:** v0.19

---

## 0. 使用规则

1. 本文件只保留当前可执行的 next actions
2. 完成项移入 tracking ADJ Log，不在此堆叠
3. 与 `tracking.md`、`issues.md` 冲突时，以 tracking 为准

---

## 1. 当前优先级

### P1 — Lab Agent E2E 验证（需 CLAB 访问）

1. ⬜ 确认 CLAB token 获取方式（REST login 或 SSH）
2. ⬜ `run_python_simulation` 内 httpx exec API 推配置 → 验证接口 IP 正确落地
3. ⬜ `exec_on_node` 验证 OSPF 收敛：`show network-instance default protocols ospf neighbor`
4. ⬜ `exec_on_node` 验证 iBGP 会话：`show network-instance default protocols bgp neighbor`
5. ⬜ agent 自主完成配置差距发现 + 修正 + 再验证全流程

### P1 — Phase 7 Ghost Audit

1. ⬜ 阅读 `dev_docs/12. PLATFORM_NETOPS_OWNERSHIP_MAP.md` — 确认 Phase 7 归属边界
2. ⬜ Ghost code 审计：识别无 owner/无测试/无用途代码（重点：`src/olav/core/` 中的遗留文件）
3. ⬜ 确认 `api_registry.py` 是否仍被主动使用，或可删除

### P2 — 补充 test coverage

1. ⬜ unit test：`_is_ip`, `_strip_domain`（`tests/` 目录）
2. ⬜ 下次真实设备 snapshot 后回归验证 topology ETL 输出

### P2 — 协议收集器扩展（不阻塞 Phase 7）

1. ⬜ 扩展 `commands` 表：IS-IS / EVPN / VXLAN / MPLS LDP show 命令
2. ⬜ 验证对应 ntc-templates TextFSM 模板存在
3. ⬜ 重跑 snapshot → 验证 `v_isis_adjacencies` 等视图有数据

---

## 2. Deferred（Phase 7 后）

1. EIGRP 实现（view + IR + StateProjector）
2. Repo 物理拆分与发布工作流
3. IS-IS / EVPN / VXLAN / MPLS LDP E2E（依赖收集器扩展完成）
