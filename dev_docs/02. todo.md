# OLAV 执行清单（与 tracking/issues 同步）

**Last Updated:** 2026-03-21  
**Version:** v0.14 phase-3-recovery

---

## 0. 使用规则

1. 本文件是当前执行清单，唯一目标是让 Phase 3 恢复过 gate
2. 与 `dev_docs/01. tracking.md`、`dev_docs/00. issues.md` 冲突时，以 tracking 为准
3. 在 Phase 3 未恢复为 Completed 前，禁止启动 Phase 4-7 开发项

---

## 1. 当前优先级

### P0（必须先完成）

1. ISSUE-P0-OPENCONFIG-COVERAGE
2. ISSUE-P0-PHASE-GATE-DRIFT

### P1（P0 完成后立即执行）

1. ISSUE-P1-TOPOLOGY-L3-NORMALIZATION
2. ISSUE-P1-GIT-BASELINE-HYGIENE

### Deferred（阶段恢复后再启）

1. semantic engine（Phase 4）
2. sandbox integration（Phase 5）
3. CLAB validation（Phase 6）
4. ownership/ghost closure（Phase 7）
5. repo 拆分与发布工作流

---

## 2. P0 执行清单

### 2.1 ISSUE-P0-OPENCONFIG-COVERAGE

**目标**：建立可复核映射覆盖率并达标，证明 07 contract 在运行时成立。

执行项：

1. 定义覆盖口径：platform/command/field 三层
2. 固化 DuckDB 查询：总量、已映射量、覆盖率、按平台分布
3. 补齐映射元数据缺口（`schema_catalog` 维度）
4. 更新文档证据：07/08/13 ADJ + tracking gate 记录
5. 复跑 gate：unit + real device E2E + real LLM API E2E

已落地进展（2026-03-21 更新）：

1. 覆盖率审计脚本已落地：`scripts/audit_openconfig_coverage.py`
2. 元数据回填脚本已落地：`scripts/backfill_schema_catalog_from_mapping_rules.py`
3. 缺口清单脚本已落地：`scripts/report_openconfig_mapping_gaps.py`
4. LLM 映射生成脚本已落地：`scripts/generate_openconfig_mappings_with_llm.py`
5. 当前基线：`fields 3859/10482 (36.82%)`，`platform-commands 850/938 (90.62%)`
6. 当前 backlog：`tmp/openconfig_mapping_gaps_20260321.csv`（剩余 6623 字段）
7. 单元测试通过：45/45 OpenConfig 相关核心测试 PASS ✓
   - schema engine: 9 PASS
   - bootstrap YANG: 27 PASS
   - mapping rules: 9 PASS

完成定义（DoD）：

1. 覆盖阈值有明确定义并实际达标（当前 36.82% < 需要 90%）
2. DB 查询可复现并可独立复核
3. 不再以 `mapping_rules` 作为默认真相源
4. 单测覆盖核心逻辑并全部通过

---

### 2.2 ISSUE-P0-PHASE-GATE-DRIFT

**目标**：消除“阶段结论与证据漂移”。

执行项：

1. 持续维持 Phase 3 = Reopened，直到证据完备
2. 建立 gate 执行记录表（命令、结果、证据路径、责任人）
3. 完成一次全量 gate 复跑并写入 13
4. 同步 tracking/issues/todo 三文档状态与用词

完成定义（DoD）：

1. 三文档状态一致
2. gate 证据完整且可复核
3. 满足条件后才允许把 Phase 3 改回 Completed

---

## 3. P1 执行清单

### 3.1 ISSUE-P1-TOPOLOGY-L3-NORMALIZATION

**目标**：修复 L3 endpoint 命名质量，消除数字化 destination token。

执行项：

1. 修复 endpoint normalization 逻辑
2. 增加单测（覆盖异常 token、跨协议样本）
3. 在真实数据上回归并输出修复前后样本
4. 更新 13 中对应 evidence block

完成定义（DoD）：

1. L3 endpoint 输出可读、稳定
2. 回归测试通过

---

### 3.2 ISSUE-P1-GIT-BASELINE-HYGIENE

**目标**：建立可追溯基线，避免“口头完成”。

执行项：

1. 按 issue 分批组织变更
2. 每批变更绑定对应 gate 证据
3. 文档与代码同批提交，避免证据滞后

完成定义（DoD）：

1. 关键变更均可追溯到提交
2. gate 证据可按提交重放复核

---

## 4. Gate 运行模板（执行时必填）

```markdown
### Phase 3 Gate Run - YYYY-MM-DD HH:MM

1. Test Pre-Audit
- scope:
- contract checks:
- stale test handling:

2. Unit
- command:
- result:

3. Real Device E2E
- command:
- result:
- evidence paths:

4. Real LLM API E2E
- command:
- result:
- evidence paths:

5. Runtime DB Evidence
- key query 1:
- key query 2:
- key query 3:

6. Failure Attribution
- code defect / external / precondition:
- proof:

7. Phase Decision
- pass / fail:
- next action:
```

---

## 5. 暂停项（不允许当前启动）

1. Repo 物理拆分与发布
2. 新仓库初始化与 tag/release 流程
3. Phase 4+ 的任何新功能开发

这些事项在 Phase 3 gate 恢复通过后再重新排期。
