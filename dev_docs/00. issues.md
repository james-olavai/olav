# 活跃问题追踪 (Issues)

**Last Updated:** 2026-04-11 (rev 93)
**Version:** v0.14.0
**Focus:** 全局 agent 注册表实现完成 — 无活跃 issues

---

## 活跃 Issues

_无活跃 issues_

---

### 已知 xfail（不阻塞）

| ID | 类型 | 说明 |
|----|------|------|
| LAB-LOOPBACK-MISCONFIGURED | lab 数据 | SW1/SW2 Loopback0 IP = 4.4.4.4（与 R4 相同） |
| LAB-BGP-R2-R4-STATE | lab 状态 | R2→4.4.4.4 BGP Established；测试设计为 Idle |
| LAB-BGP-NL-DOWN | lab 状态 | NL BGP-down 查询当前无 down 邻居 |

---

## 已关闭 Issues（149 条）

### v0.14.0（04-11）— 全局注册表 + 多用户 + 遗留清理 + 集成测试修复

| Issue | 摘要 |
|------|------|
| NO-GLOBAL-AGENT-REGISTRY | `olav refresh` 命令：扫描 workspace/*/AGENT.md → 重写 PLATFORM.md + 更新 olav/prompts/system.md 路由表；auto-hook 到 `olav init` + `olav skill install`；`audit_workspace` 扩展 `_check_platform_md_stale()`；24 个单元测试全绿 |
| INGEST-TABLE-NOT-REGISTERED | `IngestManager.bulk_load()` 加入 `_load_table_plugins()` entry-point 自动发现；integration test 改为显式调用 `_register_all()` |
| DEVOPS-E2E-AUTO-ENABLED | `_devops_e2e_enabled()` 改为只查 `DEVOPS_E2E_ENABLED` 环境变量，防止 API key 自动触发 LLM 超时 |
| DEAD-IMPORTS-RUNTIME-CRASH | 删除 config/knowledge/ 死工具 + config_evolve schema_engine 引用 |
| DEAD-PYPROJECT-DEPS | 移除 pygnmi/xmltodict/pyang/yangson 4 个死依赖 |
| DEEPAGENTS-CLI-DEP-CONFLICT | deepagents-cli 加入正式依赖，uv override 解决冲突 |
| CLAIM-REGISTRY-NOT-SYNCED | registry 新增 C-KB + C-L4，83→119 条 |
| M4-INIT-AUTO-CREATE-USER | olav init 自动建 admin + token + auth.mode=token |
| M4-SESSION-SYNC | sessions 表 + olav sessions CLI |
| M4-THREAD-OWNERSHIP | stream_run 403 校验 |
| M4-ADD-USER-LINUX-VALIDATION | pwd.getpwnam 校验 |
| M4-SYSLOG-NO-CONSUMER | search_logs core skill |
| M4-DOC-KB/SERVICES/USERS | 3 篇文档重写/修正 |
| OC-NETCONF-LEGACY | rm netconf_collector + ncclient + phase2 gate |
| SCHEMA-ENGINE-MISSING | rm 4 个 OC discovery tools |
| OC-SCHEMA-INFRA | rm schema_cache + schema_mutation_service (544 行) |
| DB-DIRTY-REBUILD | main.duckdb 全量重建 |
| KB-TAGS/SEARCH/GRAPH/SYNC 修复 | 7 个 KB 生产环境 bug |

### v0.13.0（04-10）— M3 UKS + 测试体系

| Issue | 摘要 |
|------|------|
| UKS-P1~P4 | C-KB-01~28 全绿 |
| E2E-CLAIMS-RUBBER-STAMPED | 24 伪测试 → gates/ + 真实 E2E |
| CI-RUNS-ZERO-TESTS | test.yml workflow |
| PHASE-TESTS-MASQUERADE | 86 伪 E2E → gates/ |

*(+ 120 条 v0.10~v0.12 历史 issues — 详见 git log)*
