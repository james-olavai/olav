# 活跃问题追踪 (Issues)

**Last Updated:** 2026-03-31
**Version:** v0.17

---

## 🟡 活跃 Issues

### ISSUE-P1-LAB-AGENT-E2E

**优先级**: 🟡 P1
**状态**: Open — 待 CLAB 访问验证
**影响阶段**: Digital Twin / CAB Validation
**发现日期**: 2026-03-31

#### 问题描述

Lab agent 已重设计（exec API sandbox 方案），但完整 E2E 流程（部署 → 推配置 → 协议收敛 → 自主差距修复）尚未在真实 CLAB 上运行验证。

#### 待验证

1. ⬜ CLAB token 获取正常（`POST /api/v1/auth/login` → token）
2. ⬜ `run_python_simulation` sandbox 内 httpx exec API 推配置 → R3 获得正确接口 IP（10.1.13.3）
3. ⬜ OSPF 在 SRL lab 内收敛（R2/R3/R4 互通）
4. ⬜ iBGP 会话建立（R2-R4）
5. ⬜ agent 自主完成：deploy → push config → 验证 → 发现差距 → 修正 → 再验证

#### 解除门禁条件

agent 自主完成完整流程；最终协议状态与 DB 期望值一致；无人工干预。

---

### ISSUE-P1-TOPOLOGY-L3-NORMALIZATION

**优先级**: 🟡 P1
**状态**: 代码已修复，unit test 待补充
**影响阶段**: Phase 3 质量收尾
**发现日期**: 2026-03-21

#### 已修复

- ✅ `_is_ip()` 辅助函数（标准库 `ipaddress`）
- ✅ `_strip_domain`：IP 原样返回，FQDN 取第一段
- ✅ 未解析 IP dst_dev 加 `[X.X.X.X]` 括号标注
- ✅ ETL 重跑：L2=436 links, L3=11 links（清洁数据）

#### 剩余

1. ⬜ 补充 unit test：`_is_ip`, `_strip_domain`（`tests/` 目录）
2. ⬜ 下次真实设备 snapshot 后回归验证

---

### ISSUE-P2-PROTOCOL-COLLECTION-GAPS

**优先级**: 🟢 P2
**状态**: Open — 不阻塞 Phase 7
**影响阶段**: 协议可见性
**发现日期**: 2026-03-20

#### 问题描述

IS-IS / EVPN / VXLAN / MPLS LDP 在 `netops.parsed_outputs` 中无数据，对应 `v_*_auto` 视图为空。原因：命令采集列表未包含这些协议的 show 命令。

#### 修复要求

1. ⬜ 扩展 `commands` 表：添加 `show isis neighbors`、`show evpn instance`、`show vxlan vtep`、`show mpls ldp neighbor`
2. ⬜ 对应 TextFSM template 确认存在（ntc-templates）
3. ⬜ 重跑 snapshot → 验证 `v_isis_adjacencies` 等视图有数据

---

## 🟢 已关闭 Issues（摘要）

| Issue | 关闭日期 | 摘要 |
|------|------|------|
| ISSUE-P1-OC-UNKNOWN-MODULE | 2026-03-31 | OC transform pipeline 已废弃，oc_outputs 不再由 transform 填充，问题失效 |
| ISSUE-P1-OC-MAPPING-AGENT | 2026-03-30 | oc_mapping_agent 实现后随 OC pipeline 一起废弃 |
| ISSUE-P1-OC-COVERAGE-EXPANSION | 2026-03-27 | 覆盖率 100%（已废弃的 pipeline 内） |
| ISSUE-P0-OPENCONFIG-CONTRACT | 2026-03-22 | 核心域 98.77% gate |
| ISSUE-P0-SNAPSHOT-PERFORMANCE | 2026-03-21 | P95≤6min, P50=3.4min |
| ISSUE-P1-LAB-AGENT-EFFECTIVENESS | 2026-03-31 | push_gnmi_config 方案废弃，替换为 exec API sandbox 方案（见 ISSUE-P1-LAB-AGENT-E2E） |

---

## 📋 新 Issue 模板

```markdown
### ISSUE-<ID>

**优先级**: 🔴/🟡/🟢 P0-P3
**状态**: Open/In Progress/Blocked/Closed
**影响阶段**: Phase N
**发现日期**: YYYY-MM-DD

#### 问题描述

#### 待验证 / 修复要求

#### 解除门禁条件
```
