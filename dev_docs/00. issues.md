# 活跃问题追踪 (Issues)

**Last Updated:** 2026-04-02
**Version:** v0.18

---

## 🟡 活跃 Issues

### ISSUE-P1-DEEPAGENTS-TOOL-ISOLATION

**优先级**: 🟡 P1
**状态**: 部分缓解，根本修复待定
**影响阶段**: 所有 Agent（Creator / Ops / Quick / Config）
**发现日期**: 2026-04-02

#### 问题描述

deepagents 框架与 OLAV 的 SKILL.md 工具声明模型之间存在结构性冲突，导致：

1. **SKILL.md 被忽略**：`SkillsMiddleware` 只把 SKILL.md 内容追加到 system prompt（文档化），不会限制实际可用工具。deepagents 仍为每个 agent 自动注入 `FilesystemMiddleware`（`ls / read_file / write_file / edit_file / glob / grep / execute`）和 `TodoListMiddleware`（`write_todos`），无论 SKILL.md 是否声明。

2. **task() 子 agent 工具集不可控**：通过 `task()` 委托启动的子 agent，除自身声明工具外，还会收到 deepagents 默认全工具栈。在 Creator E2E 测试中，子 agent 使用了 `run_python_code / glob / web_search` 等 SKILL.md 中未声明的工具，绕过了平台管道（`register_api_service` 等 bridge 工具未被调用）。

3. **父 agent 状态传递给子 agent**：`SubAgentMiddleware` 在调用子 agent 时，将父 agent 的完整状态（files 积累、skills metadata、所有 state keys 除 messages 外）复制给子 agent。父 agent 若 context 已膨胀（如大量工具返回值），子 agent 一开始就在高负载 context 下运行。

4. **工具返回值无边界 → context 膨胀 → task() 逃跑**：当工具返回大量无截断数据（如 `execute_sql` 的大型 JSON 字段、`run_shell` 的完整日志），LLM 因 context 接近上限倾向于调用 `task()` 委托，而委托出去的子 agent 缺乏专用工具，进一步降级处理。

#### 已缓解措施（2026-04-02）

- ✅ `read_api_schema`：改为返回紧凑 `tags_summary`（13 tags + 计数），不再返回 1166 条完整 endpoint + 参数（原始 ~500KB → 现在 ~2KB）
- ✅ `execute_sql`（4个workspace）：`_sanitize_value()` 增加字段截断（>800字符截断 + `…[truncated]` 标记）
- ✅ `run_shell`：stdout 截断至 10KB，stderr 截断至 2KB，增加 `truncated` 标记
- ✅ Creator system prompt：明确禁止使用 `task()` 委托，要求 agent 直接执行全部 6 步
- ✅ Creator E2E 验证通过：agent 正确调用 6 步 bridge 工具，生成 24 个 circuits 函数文件

#### 根本修复待定

| 修复项 | 难度 | 说明 |
|--------|------|------|
| OLAV 自定义 `task()` 替代实现 | 高 | 提供真正隔离的子 agent 调用，不继承父 state |
| 所有 subagent 改用 `CompiledSubAgent` 模式 | 中 | 已对有工具的 subagent 生效，无工具 subagent 仍走默认全栈 |
| 所有高风险工具加返回大小上限 | 中 | `take_snapshot` / `diff_configs` / `recall_memory` 待处理 |
| SKILL.md `exclude_tools` 字段 + deepagents PR | 高 | 需要 deepagents 上游支持黑名单机制 |

#### 解除门禁条件

所有 agent 的核心工具返回值有明确上限；CompiledSubAgent 覆盖所有有工具声明的 subagent；task() 委托在正常流程中不再作为 LLM 的"逃跑路线"被触发。

---

### ISSUE-P1-LAB-AGENT-E2E

**优先级**: 🟡 P1
**状态**: Open — 待 CLAB 访问验证
**影响阶段**: Digital Twin / CAB Validation
**发现日期**: 2026-03-31

#### 问题描述

Lab agent 已重设计（exec API sandbox 方案），但完整 E2E 流程（部署 → 推配置 → 协议收敛 → 自主差距修复）尚未在真实 CLAB 上运行验证。

#### 待验证

1. ⬜ CLAB token 获取正常（`POST /api/v1/auth/login` → token）
2. ⬜ `run_python_simulation` sandbox 内 httpx exec API 推配置 → R3 获得正确接口 IP（10.1.13.3）
3. ⬜ OSPF 在 SRL lab 内收敛（R2/R3/R4 互通）
4. ⬜ iBGP 会话建立（R2-R4）
5. ⬜ agent 自主完成：deploy → push config → 验证 → 发现差距 → 修正 → 再验证

#### 解除门禁条件

agent 自主完成完整流程；最终协议状态与 DB 期望值一致；无人工干预。

---

### ISSUE-P1-TOPOLOGY-L3-NORMALIZATION

**优先级**: 🟡 P1
**状态**: 代码已修复，unit test 待补充
**影响阶段**: Phase 3 质量收尾
**发现日期**: 2026-03-21

#### 已修复

- ✅ `_is_ip()` 辅助函数（标准库 `ipaddress`）
- ✅ `_strip_domain`：IP 原样返回，FQDN 取第一段
- ✅ 未解析 IP dst_dev 加 `[X.X.X.X]` 括号标注
- ✅ ETL 重跑：L2=436 links, L3=11 links（清洁数据）

#### 剩余

1. ⬜ 补充 unit test：`_is_ip`, `_strip_domain`（`tests/` 目录）
2. ⬜ 下次真实设备 snapshot 后回归验证

---

### ISSUE-P2-PROTOCOL-COLLECTION-GAPS

**优先级**: 🟢 P2
**状态**: Open — 不阻塞 Phase 7
**影响阶段**: 协议可见性
**发现日期**: 2026-03-20

#### 问题描述

IS-IS / EVPN / VXLAN / MPLS LDP 在 `netops.parsed_outputs` 中无数据，对应 `v_*_auto` 视图为空。原因：命令采集列表未包含这些协议的 show 命令。

#### 修复要求

1. ⬜ 扩展 `commands` 表：添加 `show isis neighbors`、`show evpn instance`、`show vxlan vtep`、`show mpls ldp neighbor`
2. ⬜ 对应 TextFSM template 确认存在（ntc-templates）
3. ⬜ 重跑 snapshot → 验证 `v_isis_adjacencies` 等视图有数据

---

## 🟢 已关闭 Issues（摘要）

| Issue | 关闭日期 | 摘要 |
|------|------|------|
| ISSUE-P1-OC-UNKNOWN-MODULE | 2026-03-31 | OC transform pipeline 已废弃，oc_outputs 不再由 transform 填充，问题失效 |
| ISSUE-P1-OC-MAPPING-AGENT | 2026-03-30 | oc_mapping_agent 实现后随 OC pipeline 一起废弃 |
| ISSUE-P1-OC-COVERAGE-EXPANSION | 2026-03-27 | 覆盖率 100%（已废弃的 pipeline 内） |
| ISSUE-P0-OPENCONFIG-CONTRACT | 2026-03-22 | 核心域 98.77% gate |
| ISSUE-P0-SNAPSHOT-PERFORMANCE | 2026-03-21 | P95≤6min, P50=3.4min |
| ISSUE-P1-LAB-AGENT-EFFECTIVENESS | 2026-03-31 | push_gnmi_config 方案废弃，替换为 exec API sandbox 方案（见 ISSUE-P1-LAB-AGENT-E2E） |

---

## 📋 新 Issue 模板

```markdown
### ISSUE-<ID>

**优先级**: 🔴/🟡/🟢 P0-P3
**状态**: Open/In Progress/Blocked/Closed
**影响阶段**: Phase N
**发现日期**: YYYY-MM-DD

#### 问题描述

#### 待验证 / 修复要求

#### 解除门禁条件
```
