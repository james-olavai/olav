# 活跃问题追踪 (Issues)

**Last Updated:** 2026-04-12 (rev 104)
**Version:** v0.14.2 → v0.14.3 pending
**Focus:** Runsheet 验收完成 — 待发布 v0.14.3

---

## 活跃 Issues

| ID | 优先级 | 类型 | 说明 | 状态 |
|----|:------:|------|------|------|
| RUNSHEET-AGENTS-MISSING | P1 | 功能缺失 | `audit-designer`, `audit-auditor`, `devops` agents 不在任何 skill 包中；Ch6(audit)/Ch8(devops) 无法按 runsheet 原文执行 | 待设计 |
| RUNSHEET-PARSEDOUTPUTS-CRITERION | P2 | 文档 | DEMO_RUNSHEET Ch2 criterion 写 "72+ rows in parsed_outputs"，实际最多 49（OSPF 设备无 OSPF、running-config 无模板）；应改为 "40+" | 待修复 |
| RUNSHEET-ACTIVE-WORKSPACE-DEFAULT | P2 | UX | 全新 `olav init` 后 `api.json` 默认 `active_workspace: "quick"`；但无 `quick` agent 内置，导致 `olav "query"` 报 RuntimeError。应默认指向 `core` | 待修复 |
| RUNSHEET-ADMIN-ARGPARSE | P3 | CLI | `olav admin add-user bob --no-verify` 在 argparse 层被拒绝（unrecognized arguments）；需改用 `olav admin "add-user bob --no-verify"` 作为 workaround。runsheet 文档应更新 | 待修复 |
| RUNSHEET-CH9-NO-INFRA-AGENT | P2 | 功能缺失 | Ch9 安全测试要求 `infra` agent（readonly_only service, API write block, HITL approval）；该 agent 不在任何 skill 中，测试仅通过 `ops` agent 无写工具间接覆盖 | 待设计 |

---

### 已知 xfail（不阻塞）

| ID | 类型 | 说明 |
|----|------|------|
| LAB-LOOPBACK-MISCONFIGURED | lab 数据 | SW1/SW2 Loopback0 IP = 4.4.4.4（与 R4 相同） |
| LAB-BGP-R2-R4-STATE | lab 状态 | R2→4.4.4.4 BGP Established；测试设计为 Idle |
| LAB-BGP-NL-DOWN | lab 状态 | NL BGP-down 查询当前无 down 邻居 |

---

## 已关闭 Issues（167 条）

| 版本 | 数量 | 摘要 |
|------|:----:|------|
| v0.14.2 | 4 | slash cmd dispatch + TextFSM parsing + topology ETL field names + fallback table name |
| v0.14.0 | 43 | UKS + 多用户 + OC 清理 + LLM E2E + 发布准备 |
| v0.13.0 | 28 | 测试体系重构 + C-NE DDD |
| v0.10~0.12 | 92 | 平台基础 + 历史 issues |

*(详见 git log)*
