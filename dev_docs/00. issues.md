# 活跃问题追踪 (Issues)

**Last Updated:** 2026-05-07 (rev 231 — **CAB Patch M+N shipped — analyzer→lab→TCF 完整闭环可用**. Patch M `7816d79` 加确定性 prod→SRL 翻译器（command map 5 类 + 两阶段 IP map own-interface + neighbor-fallback + forgiving regex match）；Patch N 给 `TvtRow` 加 `evidence_check_ids` schema 字段 + 3-tier fallback（exact / device-in-description / index-zip）。**In-vivo demo7 端到端首次 PASS**：`exports/cab/r1-r3-ebgp/spec.tcf.yaml` verdict=PASS / 2 post_check 全过 / 4 TVT 行 link by_device_in_description / lab destroyed / TCF 写回 8-phase journal。整个分析→实验→TCF 闭环现在产出可用的 PASS verdict——之前所有真实 spec 都假阴 FAIL。29 个新单测覆盖翻译器 15 + composite 14。Closes ISSUE-CAB-POSTCHECK-NEEDS-TRANSLATION + ISSUE-CAB-TVT-CHECK-LINK-MISSING. | rev 230 — **CAB Patch L shipped + 2 downstream gaps登记**. Patch L `39f964d` 把 5-step lab validation pipeline 折成单一 `validate_tcf_in_lab(spec_path)` Python composite + 同名 skill_script，关闭 ISSUE-CAB-AGENT-DRIVEN-LAB-VALIDATION-LOOPS。Lab agent MANDATORY TODO 10→2，agent 只调一次 `execute_skill_script("validate_tcf_in_lab.py")`，server-side 顺序跑 load → topology → SRL → save × N → deploy → exec post_checks → record back → destroy。In-vivo demo7 端到端验证：CLAB 真实部署成功、verdict 写回 TCF、journal 6 phase、destroy 成功。7 个新单测 mock service_call 仅一处覆盖 PASS/FAIL/regex/skip_destroy/skip_record/missing_spec/deploy_fail_still_destroys。**新登 2 个下游 gap (Patch L 暴露的 pre-existing CAB 设计漏洞)**: **(1) ISSUE-CAB-POSTCHECK-NEEDS-TRANSLATION** P1 — analyzer 写的 post_check.command 是 prod-form (`show bgp summary` Junos/IOS 形式)，lab 是 SRL 数字孪生需 `sr_cli` 翻译；expected_pattern 用生产 IP `10.1.13.x` 但 lab 用 `172.16.99.x`；composite 直接 exec → 100% FAIL on real specs（假阴），demo7 实测两 check 都因 docker exec "show: not found" 失败；修复 Patch M 加确定性 prod→SRL 翻译器 + IP 替换。**(2) ISSUE-CAB-TVT-CHECK-LINK-MISSING** P2 — TvtRow.test_id (`TC-01..04`) 与 post_check.check_id (`PC-01..02`) 没有 schema link 字段，composite 启发式匹配 0 条 → tvt 行不更新；修复 Patch N 加 `TvtRow.evidence_check_ids` 字段 + index fallback + device-based fallback 三层兜底。两个 gap 之前 manual 流靠 lab agent LLM 临场智能补救，Patch L 移除编排时暴露成具体阻塞。 | rev 229 — **CAB analyzer↔lab pipeline E2E validation: server-side PASS, agent-driven FAIL**. Server-side 5-step pipeline (tcf_load_for_lab → generate_clab_topology → generate_srl_lab_config → deploy_and_push_lab containers booted ✓ → tcf_record_lab_run FAIL verdict written back) verified end-to-end. Discovered 4 real bugs + 1 agent-behavior pattern. **(1) ISSUE-LAB-PATHS-HARDCODED-PRE-RENAME** P1 — `olav.core.lab._paths.py` marker still points to pre-rename `.olav/workspace/ops/lab/...`; agent rename commit `b3c7da5` didn't scan `*.py` string literals; fix: marker → glob `.olav/workspace/*/lab/...`. **(2) ISSUE-LAB-DEFAULT-SERVICES-NOT-PRIMED** P2 — `olav init` ships empty services.yaml; CLAB must be manually registered before deploy works. **(3) ISSUE-CLAB-TOPOLOGY-MGMT-SUBNET-AUTOSELECT** P2 — `generate_clab_topology` doesn't read `lab/config/config.json::mgmt_subnet`, lets CLAB auto-select clashing subnet (172.21.x clash with llama_default). **(4) ISSUE-R89-SRL-RENDER-MISSING-CANDIDATE-MODE** P2 — `generate_srl_lab_config` outputs bare `set / ...` but SRL YANG requires `enter candidate` wrapper. **(5) ISSUE-CAB-AGENT-DRIVEN-LAB-VALIDATION-LOOPS** P2 — agent on "validate spec in lab" emits 77 tool calls / 0 deploy invocations, loops tcf_load_for_lab + re-emits emit_tcf + disk-hunts; same shape as T1 but with multi-step pipeline. **Key insight**: agent repeating single-tool calls (T1) was solved by forgiving signature (Patch F+G); multi-step pipeline (CAB lab) needs **atomic-tool wrapper** (Patch L candidate `validate_tcf_in_lab(spec_path)` that calls all 5 scripts server-side). Two failure modes, two different fix shapes. | rev 228 — **V2 optimization phase complete: Patch I + J + K shipped + olav-doc fault-handling chapter**. Three follow-ups to the V2 demo runsheet validation arc, all small + non-blocking. **Patch I** `8160971` — system.md token diet: removed two duplicated sections (schema cheatsheet + TCF emission "emit FIRST") that AutoRecall already surfaces via memory guides. analyze/prompts/system.md 9,129 → 4,099 chars (-55%, **~1,250 tokens/turn** saved — bigger than the 550 predicted because Patch E grew the TCF section to fight the disk-hunt reflex which AutoRecall now handles). **Patch J** `dd9360a` — split ROUTING_EXPERT_GUIDE.md (368 lines, ~12 KB on_intent) into 5 small intent-keyed *.guide.yaml memory entries: bgp_best_path_python_algorithm / ospf_shortest_path_dijkstra / ecmp_detection / convergence_time_estimation / path_shift_analysis_between_snapshots. Per-intent semantic precision verified (top-1 scores 0.75-0.81). Per-turn injection on BGP/OSPF intent: ~12 KB → ~2 KB. Reference file shrunk to 42-line routing table pointing at memory-guide names. **Patch K** `9cb0b18` — emit_tcf content-correctness gates: (a) DB-side ASN cross-check against netops.v_show_ip_bgp_summary_auto.local_as — caught T1l-style invented ASNs (R3 spec=65003 vs prod=65000 → warning); (b) sim_passed:bool|None kwarg — None=soft warning recommend feasibility sim, False=loud warning, True=silent. Both gates advisory (never block). Offline 3/3 scenarios verified. **olav-doc commit** `82ab1de` (local) — extending-memory.{en,zh}.md augmented with fault-handling override worked example (BGP-Idle 90/7/3 priority) + 7-row pitfalls table + usage_guide vs expert_knowledge boundary. Total memory guide count: 14 → 19 (5 new from Patch J). Active task list now empty: 0 P0/P1, 1 P2 (gemma4 thinking-off, model-side), 4 P3 (minor). | rev 227 — **V2 Ch9 ✅ Closed by accumulation** — DeepSeek V4 Flash retest of "List all available audit profiles" returns complete 12-profile list (matches disk 100%) in 9 tool calls. No new code needed; closure comes from cumulative effect of (a) ce6edca R100/S4 virtual-FS deny, (b) 21e3c2f list_profiles.py skill script + SKILL.md teaching, (c) DeepSeek model upgrade — agent goes through glob/delegate fallback chain but lands on list_profiles skill script for the answer. **All V2 demo runsheet chapters now closed**: Ch1-6 ✅, Ch7 ✅ (5-layer mitigation + DeepSeek), Ch8 ✅ (Patch D-G arc), Ch9 ✅ (this), Ch10-14 ✅. | rev 226 — **V2 Ch8 (CAB / TCF emission) functionally working**. 4-commit fix arc Patch D+E+F+G after unified-sandbox refactor exposed that ``tcf_emit_from_sim`` fails ADR-0008 condition #2 (sandbox-external write target) and was over-folded into the sandbox prologue. **Patch D** (`3de7498`): emit_tcf back to @tool with thin Pydantic wrapper. **Patch E** (`32a119f`): AutoRecall ``change_plan.guide.yaml`` + behavioral "emit FIRST, refine LATER" imperative in system.md + orchestrator.md. **Patch F** (`0fb6936`): forgiving signature — implementation_json/rollback_json accept str|list[dict], cli auto-wrapped if str, missing phase defaulted to 1, ASN coerced to int, output_dir double-nesting stripped. **Patch G** (`f27da3f`): post_check_json/tvt_json silent-degrade malformed blocks to "[]" with warning rather than failing whole spec write. **T1 reproducibility track**: T1 (baseline) 0/4 → T1g (Patch E) 4 emit_tcf calls but 0 written specs → T1j (Patch F debug) 1 spec written wrong path → **T1l (Patch G)** 1 spec written correct path on 2nd emit ✅. Real-trace debugging via emit_tcf JSONL log (T1j) was the breakthrough — exposed actual failure modes (post_check shape mismatch + path doubling) that offline tests had completely missed. Demo7 spec content: 2 devices Junos+IOS, prod-aligned ASNs+loopbacks, 2-phase implementation, 2 rollback CLI blocks, 2 PostCheck regex assertions, 4 TVT rows, required_tests. Ops-lab can consume directly. | rev 225 — **V2 Ch7 ✅ Closed**: unified sandbox refactor (`c91a272`) + DeepSeek V4 Flash. **Ch7 实测通过**: 12 tool calls in <480s，第 2 SQL 即给出正确根因 "R1 ge-0/0/0 admin down → BGP 卡 Idle"，正是 V2 fault inject 注入的故障。Hybrid: `deepseek/deepseek-v4-flash` (284B MoE, 13B active, 1M ctx, paid) + 本地 bge-base-en + cohere rerank-4-fast cloud. **Unified sandbox**: 5 skill scripts (diff_sql_state / diff_topology_drift / diff_routing_drift / diff_configs / tcf_emit_from_sim) 折成 run_python_simulation prologue 全部 import，删 4-mode 决策 + 2-level dispatch；agent 写 Python 直接调函数；-210 LOC。**5 层 mitigation 累积通过**: (1) layered guide slim、(2) ops re-org 6→3 sub-agents、(3) recursion_limit 75→200、(4) unified sandbox、(5) DeepSeek V4 Flash 切换。Guide rewrite 不再 P0。 | rev 224 — adaptive endpoints (Patch A/B/C) + cloud LLM Ch7 retest + recursion limit fix。**5 commit 链**：`ec3f49b` Patch A+B (cloud reranker 自适应 + per-section api_key 优先级翻转)、`95a076b` Patch C (LLM-only None-vec write guards，让 `embedding.mode='none'` 真正可用)、`19cfe3e` recursion_limit 75→200 + env `OLAV_RECURSION_LIMIT` 可调、olav-doc `1f98622` 三档部署模式 + reranker 章节 + LLM-only 文档化。**新增 49 个测试**全过 (test_reranker_strict_factory +6 / test_api_key_precedence +6 / test_commit_to_memory +2 / test_autorecall_no_embed +3)。**Cloud LLM Ch7 retest 关键发现**: `nvidia/nemotron-3-super-120b-a12b:free` (120B MoE) 用 OpenRouter，配本地 bge-base-en-v1.5 + cloud rerank cohere/rerank-4-fast。Ch6 11 tool calls / 240s actively diagnosing；Ch7 14 tool calls / 600s 完成结构化 plan 但陷 plan→write_todos 循环。**结论**: 120B cloud LLM 仍未通过 Ch7 → 瓶颈是 layered guide 的 instructional 结构，**不是模型能力**。修复路径明确：guide 改 prescriptive 3-step recipe + verdict。**新登 2 issues**: ISSUE-LANGGRAPH-RECURSION-LIMIT-TOO-LOW (P2, ✅ Closed) + ISSUE-OPENROUTER-FREE-TIER-RATE-LIMIT-VOLATILE (P3, 🟡 运维) — 后者文档化 Google free-tier 共享池 429 频发，建议生产换 NVIDIA/Tencent provider 或 BYOK。 | rev 223 — agent re-org Step 1+2 + 命名标准化 + V2 retest。三 commit 链路 (`950c79b` Step 1: devops/infra 从 ops sub-agent 升级到 top-level workspace；`9306c89` Step 2: core/admin 折入 services agent，5 工具迁移；`b3c7da5` 命名加 domain prefix `netops_*` / `devops_*`，audit/core/services 平台级保持不变；附 `071506a` 修 Step 2 残留 broken symlink 解锁 wheel build；`9572050` 记录 retest)。**累积效果**：ops orchestrator 从 6 sub-agents 减到 3，core 从 5 sub-agents 减到 4。**V2 Ch7 retest 重大改善**：分层 BGP 故障诊断从 "0 tool calls in 8 min (stuck)" → "12 tool calls in 8 min (actively diagnosing L1→L3 + live execute_cli)"；synthesis 仍慢（gemma4:31b 64K ctx 模型上限），但 prompt-side 已不阻塞。**V2 Ch9 retest 无变化**：audit list profiles 仍 glob-loop，确认与 rename 无关，根因是 gemma4 偏好 deepagents 虚拟 FS 工具，待 FilesystemPermission deny on `/profiles*` 修复。**新 8 agent 终态**：3 平台级 (core, services, audit) + 3 netops (netops_ops, netops_topology, netops_learner) + 2 devops (devops_scripts, devops_infra)。`olav skill install` 报告 6 workspaces，`olav list` 全 8 agents 验通。 | rev 222 — R83.2 收尾 + 关闭 R81/R82 batch + 新 3 issue。R82-R83 关闭 8 项 issue：**ISSUE-NL-CLI-SILENT-FINAL** P0 ✅ (`b625e62`)、**ISSUE-WRITER-WRONG-PATH** P1 ✅ (`b625e62`)、**ISSUE-AGENT-SCHEMA-CACHE** P2 ✅ (`e520789` + `cf583e9` schema cache via SKILL.md frontmatter)、**ISSUE-PORT-NAME-CANONICALIZATION** P1 ✅ (`219e712` netutils + reject regex)、**ISSUE-LLDP-NUMERIC-PORTID** P2 ✅ (`219e712` reject `^\d+$`)、**ISSUE-VIEW-SNAPSHOT-ACCUMULATION** P1 ✅ (`cf583e9`)、**ISSUE-EXPORTS-DOUBLE-PREFIX** P2 ✅ (`cf583e9`)、**ISSUE-AGENT-PARSED-JSON-NAV** P2 ✅ (`fe37b95` 改架构方向：DuckDB-native zero-ETL 自动建 52 per-command auto-views，agent 用 `DESCRIBE` + `SELECT * FROM v_show_<cmd>_auto`)。R83 数据架构升级：(a) per-command auto-views 用 DuckDB `unnest(from_json(p.parsed_data, json_structure(...)))`，每命令一 view，零硬编码 vendor mapping；(b) state canonicalization 移到 ingest-time `field_normalizer.normalize_fields`，BGP `Estab` → `Established`、OSPF `FULL/DR` → `Full/DR` 在 `parsed_outputs.parsed_data` 入库前规范；(c) 删 L1 view_recipes machinery（`recipe_seeds.load_recipe_seeds` / `_bgp_branch` / `_ospf_branch` / `_BRANCH_BUILDERS` / `build_all_views` / `rebuild_views_for_command`），view_builder 610→250 LOC；(d) `take_snapshot` 全套对齐 `/netops_init`（`is_cli_error` 边界过滤 / `finalise_ingest` view 重建 / `populate_devices` Device ETL 公共化到 `device_etl.py`）；(e) **新 P0 query_pattern_capture 中间件**：每次 agent 调用后扫 `state["messages"]` 找 `execute_sql` ToolMessage，解析 JSON content `{"data": [...], "sql": "..."}`，按 `len(data)` 评分（DESCRIBE / information_schema / COUNT 拒绝评分），top-scoring SQL 写入 `category="query_pattern"` 的 memory，AutoRecallMiddleware 注入 `<relevant-memories>` 上下文给小模型 few-shot；(f) introspection-first prompt（`db_query/SKILL.md` 4 步 workflow + `core/prompts/system.md` cheatsheet）。`Demo4 双轮验证：Q1 4→3、Q2 5→4、**Q3 17→2 (-88%)**、Q4 ops 25→27（ops agent memory 没生效）。**3 个新 issue 登记本轮**：**ISSUE-INTERFACE-STATE-NOT-NORMALIZED** P0：cisco_ios `show interfaces status` 的 `connected`/`notconnect`/`disabled` 没规范化到 RFC 2863 ifOperStatus `up`/`down`，agent 写 `WHERE status NOT LIKE '%up%'` 把 16 个 `Connect`（实为 up）误返回为 down——是 P1 R83.2 state canonicalization 漏掉的接口枚举；**ISSUE-MEMORY-POISONING** P1：`query_pattern_capture` 仅按 `len(data)` 评分，瑕疵 SQL（如 Q3 错误 WHERE 但返回 16 行）成为 winner 写入 memory，下次同问 AutoRecall 召回坏模板，agent 直接复用——memory 不是修复，反而是放大错误的载体。修复方向：(a) 多模板存储而非 winner-take-all，(b) 后置验证（agent 显式标记成功 SQL），(c) time-decay 加速旧瑕疵衰减；**ISSUE-OPS-MEMORY-NO-EFFECT** P3：Q4 ops agent 第二次调用 25→27（无收益），子代理（ops/audit/lab）的 `scope` 隔离让 core 的 query_pattern memories 不被 ops 看到——下轮调研 scope 配置。| rev 221 — Round 81: fresh-demo runsheet 三 bug 全部数据驱动修复 + 登记四个新 issue。Round 81 修复落地（commit `380286f`）：(1) ip_address 权威化——nornir hosts.yaml 的 `hostname` 字段是 SSH target，是"如何到达设备"的唯一权威；`_load_host_metadata` 增 `mgmt_ip`，Device ETL 直接消费，loopback 转入 `metadata.loopback_ip`；(2) Junos `show interfaces terse` 自定义 seed TextFSM——ntc-templates 不带这个 parser，shipped seed 落到 `seed_templates/juniper_junos/show_interfaces_terse.textfsm`，`commands_sync` 注册为 `custom_textfsm` 自动入 discovery list；(3) raw 内容层 CLI 错误检测——新 `parse_helpers.is_cli_error()` 在 `_collect_cmd` 入 staging 前拒绝 `% Invalid input detected at '^' marker.` / `unknown command.` 等设备 CLI 错误响应，**未用 `blacklisted_commands.yaml`**（命令在某些 platform/version 合法，命令级黑名单是错的抽象层）；on-disk forensic dump 保留。Demo 验证（fresh venv + 本地 wheel + dev API + complete nornir + model=deepseek/deepseek-v4-flash）：6 设备 mgmt IP 全对、R1 `show interfaces terse` 22 行结构化、parse coverage 32%→**75%**、unsupported 错误数 52→**3**。新登记 4 个 issue 待后续 Round 处理：**ISSUE-NL-CLI-SILENT-FINAL** (Bug 4, P0)：agent 工具调用后无 final assistant text 打印到 stdout，用户只见 `🔧` 图标，文件路径也不知道，demo UX 重伤；**ISSUE-WRITER-WRONG-PATH** (Bug 5, P1)：writer subagent 把 `devices.csv` 写到项目根目录而不是 `exports/reports/`，违反 `references/OUTPUT_EXPORT_RULES.md`；**ISSUE-AGENT-SCHEMA-CACHE** (Bug 6, P2)：deepseek-v4-flash 没有有效利用 `references/DB_SCHEMA.md`，第一次 `execute_sql` 用了不存在的 `device_type` 字段需要重试——可能通过 schema cache + memory hint 改进，每次 `execute_sql` 走前注入"上次哪些 query 成功"或"agent 自检 columns"；**ISSUE-EMPTY-CMD-LEARNING** (Bug 7+8 合并, ⏸️ Wontfix)：原想做 per-fleet learned skip-cache，用户挑战"是否过度设计"后自评收回——36 次空 SSH 在 nornir 并行下 <3s，占总耗时 <10%，但 skip-cache 需新表 + 4 处状态机 + device_hash 触发器 + TTL 退让 + 测试面，新 failure mode 风险（合法命令被静默跳过）远大于收益。唯一保留的 actionable：ntc-templates 解析失败 log 级别 WARN→DEBUG（1 行改动）。重启条件：fleet 规模 ≥ 100 设备或 snapshot 频率达分钟级。| rev 220 — Round 77: ARCH-27 真正收尾 + ISSUE-NORNIR-INVENTORY-METADATA-LOSS。用户洞察："nornir 和 ntc-templates 的 platform 字符串天然 1:1 对应（`cisco_ios` / `juniper_junos` / `arista_eos` …，都是 netmiko driver 名），直接检索即可；不需要 `DISCOVERY_COMMANDS_IOS/_JUNOS` 这类硬编码 list"。验证：`ntc_templates/templates/` 966 个 `.textfsm` 文件，glob `cisco_ios_*.textfsm` 得 143 条命令（含 `show_ip_ospf_neighbor` 单数），`juniper_junos_*.textfsm` 得 21 条。infrastructure 已经在 `olav_netops.core.commands_sync:sync_commands()`（扫 ntc + 自定义 textfsm + PaC + overlays 写入 `netops.commands`）——但 `netops_init/run.py` **从不调用**它，且自己保留 `DISCOVERY_COMMANDS_UNIVERSAL/_IOS/_JUNOS/_PLATFORM_COMMANDS` 三份硬编码 list。后果：(a) 新装 Arista/NXOS 设备一行代码都不跑；(b) OSPF 单复数 bug 靠 `_NTC_FILENAME_ALIASES` 遮羞。附带登记 **ISSUE-NORNIR-INVENTORY-METADATA-LOSS**：`_populate_devices` 把 `site/role/metadata` 直接写 NULL，hosts.yaml 里 `data.role=border/core`、`data.aliases=['核心路由器']`、`groups=[test]` 全部丢弃；agent 无法做"列出所有 core 路由器"或自然语言别名匹配。| rev 219 — Round 76 跨仓 backup commands 迁移: `src/olav/core/ingest_manager.py::_load_backup_commands` 改为优先查 `netops.commands WHERE backup_only=true`(R73 SSOT),YAML 降级为 pre-R73 fallback。`find_backup_commands_yaml` 保留(governance pins + fallback)。验证:341 tests passed(netops 333 + governance 8),DB 路径返回 10 backup commands,YAML fallback 返回 11。| rev 218 — Round 75 清理: `command_registry.py` 420→94 LOC(-77%),`CommandRegistry` 类 + 9 个方法全部删除,两个 entry-points(`reload_hook` / `get_config_commands`)改为 thin shim 调 R73 `sync_commands` + 直接查 `netops.commands WHERE backup_only=true`。测试重写:`test_command_registry.py` 7 类 → 3 类 / 10 用例全绿;`commands_db` fixture 更新到 R73 9 列 schema;`test_cli_builtin.py` R71c 直调路径 stub。netops 完整测试套件 333 passed / 0 failed。跨仓 `ingest_manager.find_backup_commands_yaml` 迁移留 ARCH-22 C 下一轮。| rev 217 — Round 73/74 审计: 发现 R73-SCHEMA-DRIFT critical 回归(execute_cli / search_commands 查旧 commands 表 schema,白名单 fail-open)并立即修复;登记 R73-DEAD-CODE low-severity(command_registry.py 孤儿 + backup_only_commands.yaml 冗余,留 Round 75 清理)。验证其他 closed issues(ARCH-24/25/27/22C/23/NETUTILS/GLUE-AUDIT)原 fix 全部成立,未被 R73/R74 破坏。R74 新增 Batfish snapshot 导出(Stage 3.8)`exports/snapshots/<date>/batfish/configs/<host>.cfg`,Junos 用 set 格式,manifest.json + latest-batfish symlink。| rev 216 — Round 72: 新登 **ISSUE-LEARNER-BATCH-CUT** + **ISSUE-INGEST-NORMALIZATION**。R71 批量 learner 砍掉架构:netops_init Stage 3.5 删 `learn_commands` 调用,替换为 parse coverage classifier(parsed / raw-only / unsupported),持久化 `.olav/config/unsupported.json`,明确告知用户哪些 `/learn_cmd` 对 sim/lab 必要。`command_learner` skill 作为 `/learn_cmd` 后端保留。Agent 端加 raw fallback 策略:view → parsed_outputs → raw_output_store + LLM reasoning。Ingest-time normalization:`textfsm_parse.parse_output` 后加 `_normalize_fields` 使用 netutils canonicalize interface/IP/ASN/MAC,使 parsed_outputs 写入前就规范化,view_builder CASE 简化。验证 fresh `~/olav-demo6`:/netops_init ≤ 90s,parse coverage 报告,用户按需 `/learn_cmd` BGP 后 v_bgp 填充,agent raw fallback 能回答 ad-hoc。| rev 215 — Round 70 再追补: 新登 **ISSUE-LEARNER-ARCH** 架构违规 —— netops_init 持有学习逻辑(`run.py` 直接 `from olav_netops.core.auto_learn import ...`)违反 ADR-0002;auto_learn + pac_learn 串联是历史叠加(Round 67 引入 PaC 时没重构 auto_learn);`/learn_cmd` slash command 通过 ops agent 做 NL 委托,对单命令合适但对 batch 31 命令会更慢,不能简单让 netops_init 调它。正确架构:抽 `command_learner` skill(单入口 `learn_commands(samples, budget, max_workers)`),batch 和 interactive 两路共用;内部 router 按输入结构决定 TextFSM vs Python DSL,删 D5 串联浪费。| rev 214 — Round 70 追补: AUTO-LEARN-PERF 状态从 Closed → 🟡 Partial,补根因拆解 D1-D5(demo5 fresh install 实测 20min timeout 复现):D1 串行设计无并发(主因;31 对 × 3 retry × 15s = 23min 理论上界);D2 无失败缓存(re-run 等于 first run);D3 无 wall-clock 预算(CI 失控);D4 retries=3/5 边际收益低(ReAct 真正学到的很少);D5 auto_learn + PaC 串联重复(两者目标重叠仅 DSL 不同)。建议 Round 71 做 D1+D3 快进胜利(首装 23min → 5min)。ADR-0002 边界 3 处 V1+V2+V3 修复落地:V1 sandbox prologue 改 `olav.sandbox_prologues` entry-point,olav-netops 通过 `sandbox_prologue.network_model_prologue` 注册;V2 `catalog.py` `_TOPICS` 改为 `olav.catalog_topics` entry-point + `TableRegistry.all_tables()` fallback,olav-netops 贡献 `get_catalog_topics`;V3 `find_backup_commands_yaml` 路径泛化为 glob `workspace/*/*_init/config/` + `config/domains/*/`。olav 核心目录 `from olav_netops` 导入 = 0。新增 92 个单测(test_view_builder=35、test_populate_devices=15、test_parser_learner_safety=42);test_migration.py 删除(v0.12 迁移 8 版本前);test_cli_builtin 改 asyncio.run;test_command_registry YAML fixture 改用 OLAV_BACKUP_COMMANDS_PATH env。测试 227 pass / 0 fail。| rev 213 — Round 70: ARCH-29 正式实施 + ARCH-24 完整删除 —— "no garbage" 约束。`olav-netops/.olav/workspace/topology/` sub-skill 新建（query_topology 从 ops/tools 迁入，新增 discover_recipe / save_recipe / rebuild_views / list_recipes 四工具）。`view_recipes_seed.yaml` 单体拆分为 8 个 per-protocol YAML 至 `recipes/builtin/`；recipe_seeds loader 扫 builtin + `~/.olav/config/recipes/user/`。新 `~/.olav/config/topology.yaml` 用户意图文件（只一个 protocols: list）+ `topology_intent.py` loader。netops_init Stage 3.6 从 ARCH-24 L3 ETL 块替换为意图 coverage gap WARN；take_snapshot._run_one 加 post-hook `rebuild_views_for_command` 纯 SQL 刷视图。**ARCH-24 删除**：etl_discovery / etl_registry / etl_contract / observed_canonical / l3_topology_etl / protocol_keywords 共 ~1240 LOC + BgpSessionsTable / OspfAdjacenciesTable DDL + 5 frozen ETL artifacts + view_recipes_seed.yaml 单体 + `compare_view_vs_table` drift 助手。ARCH-25 `ast_safety_check` 内联到 parser_learner.py。Fresh ~/olav-demo4 venv 全新 SSH 回归 V1-V8。 | rev 212 — Round 69 收尾: 登记 **ISSUE-ARCH-29: netops/topology sub-skill —— 意图驱动 + LLM 起草 recipe + pipeline 集成**。用户两点澄清：(1) skill **归 netops domain**（不是 olav core），路径 `olav-netops/.olav/workspace/topology/`；(2) 用户配置**极简**：`~/.olav/config/topology.yaml` 只写 `protocols: [bgp, ospf, cdp_lldp, bfd, ...]`，agent ReAct 自动扫 parsed_outputs 起草 recipe YAML，`save_recipe` 验证后冻结到 `view_recipes` DB + `recipes/user/*.yaml`。**关键分界**（vs 被否决的 ARCH-24）：LLM 产 YAML 声明 ≠ Python 执行代码，可读可编辑无 AST 风险，用户可事后 override canonical。**双入口**：pipeline 模式（netops_init Stage 3.5 cov check + 3.7 纯 SQL build_all_views；take_snapshot post-hook 刷 view）+ agent 模式（interactive discover）共用同一 `view_recipes` 表。**首次发现 2-3 LLM 调用，之后所有 snapshot 0 LLM**。Pydantic 三层：内置 typed + 自定义 `custom_concepts: dict` + 可声明 Literal override。Round 70 实施，含 skill 目录拆分、CLI 脚手架、Stage 3.5 新增。 | rev 211 — Round 69: ARCH-28 进入 In Progress，平行验证模式。新建 `view_builder.py`（读 view_recipes → `CREATE OR REPLACE VIEW v_bgp_neighbors_auto` 等，state 跨 vendor 归一用 SQL CASE 声明式）+ `schemas/topology.py` Pydantic 模型（BGPSession/OSPFAdjacency/L2Link/TopologySnapshot，`extra=forbid` + `Literal` enum）+ `query_topology` agent tool。netops_init Stage 3.7 调 `build_all_views` 并对比 ARCH-24 物化表 log 漂移。Scope: BGP+OSPF+L2。Fresh ~/olav-demo3 venv 全新 SSH 采集验证，通过后清 ~/olav-demo2。ARCH-24 机器暂保留（Round 70 canary 后真删）。 | rev 210 — Round 68 收尾: 登记 **ISSUE-ARCH-28: view_recipes-driven SQL views 替代 ARCH-24 物化 ETL**。用户洞察：ARCH-06 `view_recipes` 表已 seed（15 rows, per-vendor field_mappings 就位）但 runtime view builder 从未实现；Round 67 建 ARCH-24 LLM-生成 ETL 实质是重复造轮子。Path B: (1) `view_builder.py` 读 `view_recipes` 生成 `CREATE OR REPLACE VIEW v_bgp_neighbors_auto UNION ALL 各 vendor`；(2) state 跨 vendor 归一用 SQL CASE 表达式（`WHEN state ~ '^\d+$' THEN 'Established'`）声明式替代 observed_canonical 全套；(3) Pydantic schema 在 agent 输出边界；(4) NetworkX 仅做图算法；(5) ARCH-25 PaC parser 保留（parse 层真难题）。Path B 自动解决 Round 68 P0 三大语义 gap。可弃用 ~1240 LOC ARCH-24 机器。实验阶段：手写 view SQL → 查 demo2 → 对比 bgp_sessions → Pydantic 包装 → 确认。 | rev 209 — Round 68 续2: ARCH-27 + GLUE-AUDIT 进入 In Progress，合并交付。新建 `platform_canonical.py`（4 处 vendor 别名 3-in-1；`SCRAPLI_PLATFORM_MAP` 保留为合法 driver 映射）+ `universal_parse.py`（3 级优先级：PaC Python parser → custom TextFSM → ntc-templates，修复 G7 "take_snapshot 看不见 PaC 产物" 根本问题）。`discovery_commands.yaml` 改 per-platform 结构并修正 `show ip ospf neighbor` 单数拼写；`run.py` 删 `DISCOVERY_COMMANDS_*` + `_CMD_ALIASES`，从 yaml + CommandRegistry 交叉验证推导。Device ETL 的 Junos `show interfaces terse` 手写 regex → 正规 PaC seed parser（shipped 到 wheel data/seed_parsers/）。8 处 bare except → logger.warning。fresh ~/olav-demo2 venv 全新 SSH 采集端到端回归。 | rev 208 — Round 68 续: 登记 **ISSUE-ARCH-27**（Discovery 命令列表硬编码 —— 应从模板文件名反推；`DISCOVERY_COMMANDS_IOS` 三份列表 + `_CMD_ALIASES` alias map 是 OSPF 复数 bug 真正根源，`command_registry.py` 已有能力但 `netops_init/run.py` 从不调用，两套并存靠 alias map 掩盖）。完成 `netops_init + take_snapshot` 系统审计 → 新登 **ISSUE-NETOPS-GLUE-AUDIT: 13 处硬编码与粘合剂**：4 处 vendor 别名表重复（`_VENDOR_ALIASES` / `_JUNOS_PLATFORMS` / `_normalise_platform` / `SCRAPLI_PLATFORM_MAP`）、LLM 自学路径覆盖缺口（`_try_parse_textfsm` 不查 PaC parsers、Device ETL Junos 手写 regex 绕过 PaC、vendor 字段嗅探）、magic heuristics（loopback 关键字 + 10.x 跳过启发、unknown device → cisco_ios fallback）、7 处 `except Exception: pass`、两处 textfsm lookup 逻辑不一致。整治路线建议 Round 68-70 分批。 | rev 207 — Round 68: ARCH-26 + NETUTILS-INTEGRATION 进入 In Progress —— 合并交付 "数据多数派 de facto canonical" 方案：新建 `observed_canonical.py` 模块；`etl_contract.py` / `parser_contract.py` 版本分别 bump v2（强制现有 frozen artifacts 失效）；`etl_discovery.py` prompt 注入现有 DB 分布（`SELECT state, COUNT(*) FROM bgp_sessions GROUP BY state`）告诉 LLM dominant form + netutils 作协助库（AST allowlist 加 `netutils`，prompt 提示 `canonical_interface_name` / `asn_to_int`）；ETL validator 新增 `validate_against_observed` 软告警，outlier → reflection hint。contract 不引入 `*_ALLOWED` / `*_MAP` 任何硬编码。 | rev 206 — Round 67 续5: ARCH-26 设计再进一步 —— 用户指出 "canonical 也不该硬编码，通俗法：看现在 ETL 里主要用的是什么，就用什么"。重写 ARCH-26 为 **数据多数派 de facto canonical** 方案：新建 `observed_canonical.py` 模块在运行时 `SELECT field, COUNT(*) FROM target_table GROUP BY field`，取 argmax 作为 dominant form，渲染进 LLM prompt 让生成的 parser/ETL 向其收敛。Bootstrap：第一轮 observed 为空，LLM 自由发挥；后续 run 以第一轮结果为 canonical 自增强。不新增 `*_ALLOWED` 常量、不新增 `BGP_STATE_MAP`、不新增任何硬编码映射。netutils 作协助库（如 dominant form 恰好与 `canonical_interface_name` 一致则 LLM 直接调），不当 oracle。 | rev 205 — Round 67 续4: ARCH-25 E2E 验证发现规范化 gap —— `bgp_sessions.state` 有 `"Establ"` / `"Established"` / `"0"` 三种值，破坏简单 WHERE 查询。(1) 新登 **ISSUE-ARCH-26: Canonical-form contract + LLM-driven normalization**（别再硬编码映射表）—— 核心主张：LLM 自学 ETL 的价值就是让 agent 自己发现 mapping、屏蔽底层差异；禁止再写 `BGP_STATE_MAP = {...}` / `_VENDOR_ALIASES` 类硬编码。正确模式：contract 只声明 canonical allowed-values（不具体映射），validator 强制枚举，prompt 告诉 LLM 归一化规则，多样本让 LLM 自动发现 "Establ" ↔ "Established" 等价。(2) 新登 **ISSUE-NETUTILS-INTEGRATION**: netutils 虽声明为依赖但只进了 analysis sandbox，netops_init 管道（collect/parse/ETL）完全没用 —— 归入 ARCH-26 同轮修复，作为 LLM prompt 允许调用的库（不是 wrapper）。ARCH-26 实施后 `PARSER_CONTRACT_VERSION` / `ETL_CONTRACT_VERSION` bump 自动让现有 frozen artifacts 失效触发 re-learn。 | rev 204 — Round 67 续3: 新登 **ISSUE-ARCH-25: Parser-as-Code (PaC) Learner** —— 用户指出 "手写一个 Junos BGP 模板只解决本地模拟器 R1，换设备版本又崩"，架构正确解法是改造 learner 本身。ARCH-25 用**多样本** + Python 函数（非 TextFSM）+ 结构预扫描反射 + quarantine，全面复用 ARCH-24 的沙箱 / AST / header / sha256 基础设施。新增 3 模块 `parser_contract.py` / `parser_registry.py` / `parser_learner.py`；集成到 `netops_init/run.py` 作为 Stage 3.5b（Stage 3.5 auto_learn TextFSM 失败后兜底）。`ISSUE-NETOPS-PARSE-JUNOS-BGP` 的方案 A（手写模板）deprecated，改由 ARCH-25 学习器层面解决，Junos BGP 即 ARCH-25 第一个 test case。 | rev 203 — Round 67 续2: ARCH-24 complete-validation 暴露 2 个 upstream 缺陷（不在 ARCH-24 scope）——(1) 新登 **ISSUE-NETOPS-COLLECT-OSPF-CMD**: `show ip ospf neighbors`（复数）在 5 处文件用到但 IOS 拒绝，实际 cisco_ios 正确命令是 `show ip ospf neighbor`（单数），导致 `ospf × cisco_ios` L3 ETL 学习永远无法触发；(2) 新登 **ISSUE-NETOPS-PARSE-JUNOS-BGP**: ntc-templates 的 `juniper_junos_show_bgp_summary.textfsm` 不兼容 Junos 18.4R3-S3 真实输出（header `Last Up/Dwn` vs 模板期望 `Last Up/Down`；`inet.0` 后 stats 在下一行而模板期望同一行），实测 R1 真实 BGP 数据（2 peer）无法 parse，auto_learn 3-5 次重试也学不出来，导致 `bgp × juniper_junos` L3 ETL 学习永远无法触发。方案 A：写 `.olav/templates/juniper_junos/show_bgp_summary.textfsm` 覆盖模板（auto_learn 的 load_custom_template 优先）；L3 ETL 端 P2 副作用路径：让 LLM ETL 可选接受 raw_output fallback，bypass TextFSM 空白区。 | rev 202 — Round 67 续: (1) ISSUE-ARCH-06 从 Closed 重开为 🟡 Partially Implemented —— P1 view_recipes seeds 已 ship 但 P2 runtime ETL 从未落地（`topology_engine.py` 仍 L2-only；BGP/OSPF 虽在 `network_model.py:927-1107` 按 query-time 重建但无 SQL 持久化）；(2) 新登 ISSUE-ARCH-24: L3 Topology Self-Learning ETL（LLM-frozen，以 CDP/LLDP ETL 为 few-shot exemplar，per-(protocol,vendor) 生成 Python 提取器，AST 白名单 + sandbox 执行 + NetworkX oracle ±20% gate 后冻结到 `.olav/templates/etl/<proto>_<vendor>.py`）—— 新建 5 模块 (`protocol_keywords.py`, `etl_contract.py`, `etl_registry.py`, `etl_discovery.py`, `l3_topology_etl.py`) + 2 DDL (`bgp_sessions`, `ospf_adjacencies`) + `netops_init/run.py` Stage 3.6 集成；(3) 新登 ISSUE-AUTO-LEARN-PERF: Stage 3.5 17 min 阻塞 demo/CI —— 加 `--skip-auto-learn` / `--auto-learn-retries N` CLI flags 解耦 LLM 路径（并行化留下一轮）。 | rev 201 — Round 67: Web 登录流 3 项 auth 缺陷修复 + demo 部署元数据对齐。(1) `src/olav/api/static/login.html:24` `&#9632;` placeholder → `⚡ OLAV` (与 index.html:154 品牌一致)；(2) `src/olav/api/server.py:64-115` `_require_auth` 重写为 FastAPI `Depends` + `Request`，加 `olav_session` cookie fallback（旧实现构造 fake empty Request，Bearer/cookie/无凭证一律 401，`POST /threads` 等全部 endpoint 不可用）；(3) `src/olav/api/server.py:279-303` `GET /` 加 cookie 有效性校验，陈旧 cookie → `delete_cookie` + 302 `/login`（此前 SPA 正常渲染但每 API 调用 401，UI 看似"发不出消息"）；(4) `dev_docs/42. DEMO_RUNSHEET.md:122,127` 删过时 `embedding.mode=api`+OpenRouter→401 警告，改为正确使用方式说明（实测 `openai/text-embedding-3-small` + `base_url=https://openrouter.ai/api/v1` 1536-dim 正常）；(5) `olav-netops/workspace.yaml:2` `version: "0.17.2"` → `"0.19.0"`（untracked 本地文件，与 pyproject.toml + __init__.py 对齐，`olav skill install` 报告现为 `netops v0.19.0`）。Playwright 端到端：302 set cookie → 200 GET / → 200 POST /threads → 200 runs/stream (SSE)，0 console errors。未 pin governance 测试（follow-up）。 | rev 200 — Round 65: ARCH-23 R65 deployment hygiene — `.olav/workspace/core/` tool layout now matches packaged `src/olav/data/workspace/core/`. 17 tools relocated out of monolithic `core/tools/` into 5 sub-agent homes (admin 11 / api_query 2 / db_query 2 / remote 2 / writer 2); orchestrator retains 3 真跨域 (execute_sql / recall_memory / web_search). 15 governance tests updated for new paths + 1 relaxed (test_step_e 7→3, test_tool_dedup_phase2 symlink-retirement, test_round39 describe_table-canonical-in-db_query). `load_reference.py` walks up 3 levels for refs; `tool_help.py` `_TOOL_ROOTS` adds 5 sub-agent dirs; `api_request.py` (api_query/) gains `_COMPACT_LIST_CAP=50` compact envelope (ARCH-18 #2). Services symlinks re-pointed to new homes. T0 32/32 PASS; Governance 677→**679 passed / 1 skipped**; Tier 1 from 6 FAIL → 1 FAIL (T1-53 env-dep, unrelated). | rev 199 — Round 64: ARCH-23 Closed — packaged 5 核心工具按语义迁入 {admin, db_query}/tools/，orchestrator 余 3 @tool (`execute_sql` / `recall_memory` / `web_search`)；admin SKILL.md tools 白名单 +4、db_query +1；installer-source pin 与 ARCH-22 D allowlist 随迁移更新；deployment AGENT.md 加 subagents: 5 条 + writer/SKILL.md & references/ 补齐；顺手修 T0 B11（nornir resolver 优先级 0 `collect/` + 两份 config.yaml 的 host_file/group_file/defaults_file 由 probe→collect）。T0 30→**32/32**，Governance 679→**677 passed / 3 skipped / 0 failed**（净增 2 pin 减 2 skip）。| rev 198 — Round 63: T2 run-5~10 驱动的 6 项 runtime + hygiene 修复 + 1 条新 Open issue 登记。(1) netops_init ARCH-20 Phase 3 陈旧 `from olav.core.topology_engine` import 同步（olav-netops packaged 副本）；(2) netops_init `from .checkpoint` relative import 在 `spec_from_file_location` 下失效 → try/except ImportError + sys.path fallback at `.olav/workspace/ops/netops_init/run.py:287` + `:787`；(3) `src/olav/core/llm.py` LLMFactory 加 base_url → provider 自动映射（openrouter / together / groq / deepseek / perplexity），修 `ValueError: Unable to infer model provider for 'x-ai/grok-4.1-fast'`；(4) `tests/ci/tier2_integration.sh` `OLAV_DEV_CONFIG=<dir>` 自动 resolve 到 api.json；(5) `olav-netops/.olav/workspace/` 删除 Sprint-3 前陈旧目录 `ops/{probe,analysis,diff}/` + 从 canonical 同步 `collect/` `analyze/` `services/` `audit/`；(6) `olav-doc/docs/netops/quick-agent/` 删目录 + 12 处坏链改到 `core-agent/index.md`。新 2 个 governance 文件 5 条 pin：`test_llm_provider_autodetect.py` + `test_netops_init_standalone_load.py`。T0 B11 `ops/probe/config/nornir/` 路径笔误 sed 修复。新登记 ARCH-23：core 5-subagent MVC 重构 in-flight（scaffold 已 ship，tool-migration 未做）—— T0 A18 FAIL 即该重构进度信号。Governance 674→**679 passed / 1 skipped / 0 ERROR**。T2 run-10 Group 1+2+T2-11 全绿。T3 13/13 / T4 29/29。)
**Version:** v0.18.1

---

## Round 27 完成后：全部 Issue 状态一览

| 类别 | 数量 | 示例 |
|---|---|---|
| ✅ **Closed** (有 code + pin test) | ~24 条 | CORE-01/02/03/04/06 · NETOPS-01-06 · ARCH-20 P1/P2/P3 · ARCH-08 P1 · ARCH-12 · ARCH-13 core · ARCH-22 A1/B/F · RAW-01-04 · **R63**（6 项 runtime + hygiene fix）· **ARCH-23 (R64)** |
| ✅ **Fixed** (有 code + Round pin test) | ~2 条 | ARCH-22 C3 (OLAV_WEB_PORT) · ARCH-22 C4 (IP docstring) |
| 🟡 **Partially Implemented** (部分实现 + 详细矩阵) | ~10 条 | ARCH-06 · ARCH-10 · ARCH-11 · ARCH-16/17/18/19 · ARCH-21 · CORE-05 · WRITER-01 |
| 🔴 **Not Started** (design complete, 0 LOC + scoping) | 2 条 | RAW-05 · ARCH-14 NoM |
| ⏸️ **Deferred** (显式延后 + 阻塞原因) | ~8 条 | ARCH-22 A2/A4/C1/C2/C5/E · Sprint 3 Step C/D-后半/E |
| ℹ️ **Mitigated / Future** | 3 条 | P1-DEV-ENV-MASKS-PRODUCTION-BUGS · ARCH-09 Future · ARCH-22 D |

**0 条"Open 无状态说明"**。所有 issue 都有：(1) 当前状态标签；(2) 具体证据（file path + test）或 scoping 清单；(3) 下一步决定（下一轮候选 / 独立 design / XL long-term）。

---
**Focus:** Data pipeline raw fallback + writer subagent + 小模型优化 + **核心平台审计**

---

## CI Test Progress

| Tier | Total | Pass | Fail | Warn | Skip | Last Run |
|---|---|---|---|---|---|---|
| 0 | **32** | **32** | **0** | 0 | 0 | 2026-04-19 (R64: A18 PASS ARCH-23 closed; B11 nornir resolver collect/ priority 0) |
| 1 | **54** | **52** | 0 | 0 | 2 | 2026-04-17 (T1-21b→SKIP, T1-53 TUI echo pipe) |
| 2 | **40** | **39** | 0 | **1** | **0** | 2026-04-20 (R66d with full dev env: CLAB API restarted via host SSH, all 6 SKIPs unblocked (Group 4 + 6), T2-25 WARN→PASS via R66c EXPORTS_DIR fix; only T2-21 audit summary WARN remains, pure LLM variance) |
| 3 | **13** | **13** | 0 | 0 | 0 | 2026-04-19 (R63 run-1, 13/13 clean) |
| 4 | **29** | **29** | 0 | 0 | 0 | 2026-04-19 (R63 run-1, 29/29 clean) |

**Tier 1 SKIP (2):** T1-21b (olav-ent not installed), T1-47 (loopback env)
**Tier 2 WARN (1 post-R66d):** T2-21 (audit summary LLM variance — Gitea #5/#9). T2-25 promoted to PASS via R66c EXPORTS_DIR + broader script detection fix.
**Tier 2 SKIP (0)** — all groups active. CLAB API restored on 192.168.100.12:8080 (Docker container had been stopped post-reboot due to missing `/tmp/shadow_clab` tmpfs file; recreated + started).
**Tier 2 dev config hook:** `OLAV_DEV_CONFIG=.olav/config/api.json` + `OLAV_DEV_NORNIR_DIR=.olav/workspace/ops/collect/config/nornir/` unlocks LLM + SSH + CLAB + Registry groups from the in-repo dev configs.
**Tier 2 CLAB lifecycle (T2-15~18 full pass):** topology deploy → node config push → BGP convergence verify → lab destroy — the complete ARCH-14 sim/lab loop end-to-end against real containerlab.

---

## 🚀 交付就绪清单 — 给开发团队（rev 157 新增）

> **TL;DR:** 以下表格按 **sprint + 依赖顺序** 列出全部开放 issue，含 size / owner / blocker。
> 每条 issue 详见下方 `### ISSUE-XXX` 条目。TDD 实施细节见 `dev_docs/51. SMALL_MODEL_GOVERNANCE_ROADMAP.md`。

### Sprint 1（Next 2 Weeks）— 阻塞清理，必做

| # | Issue | Size | Owner | 依赖 | 影响 |
|---|---|---|---|---|---|
| 1 | **ARCH-20 Phase 1**: 删 v0.17 残留 7 顶层 agent + LEGACY redirects | M | platform | — | 解锁 ARCH-21 + ARCH-17 |
| 2 | **ARCH-20 Phase 2**: 工具去重 (diff_configs 6→2 等) | L | platform | Sprint 1 #1 | 解锁 ARCH-21 |
| 3 | **ARCH-20 Phase 3**: Platform/Netops 边界修复（topology_engine 迁出） | L | platform + netops | Sprint 1 #1 | 解锁企业版集成 |
| 4 | **ARCH-22 A (部分)**: `.olav/workspace/` 部署 `.disabled` 清理 + `olav refresh` hook | S | platform | — | 可与 #1-3 并行 |
| 5 | **CORE-01**: 单例线程安全 (ConfigLoader/get_store/get_router) | M | platform | — | 生产稳定性（独立可并行） |

**Sprint 1 验收:** `tests/governance/test_v018_compliance.py` + `test_platform_netops_boundary.py` Tier-0 绿；ARCH-20 全部 Phase 完成。

---

### Sprint 0 前置（1 Week）— 测试基础设施 + model_tier

| # | Issue | Size | Owner | 说明 |
|---|---|---|---|---|
| 0a | 测试基础设施 (`tests/governance/` + token_meter + budget_models) | M | platform + QA | 所有后续 sprint 的前置 |
| 0b | **model_tier 配置维度** | M | platform | ARCH-16/17/18/19 的"宪法级"基础 |

---

### Sprint 2（2 Weeks）— 小模型底座

| # | Issue | Size | Owner | 依赖 |
|---|---|---|---|---|
| 6 | **ARCH-17 P1**: `static_context: mode` 三档机制 + quickref 拆分 | L | platform + netops | Sprint 0b |
| 7 | **ARCH-18 #1**: Tool docstring 瘦身 + `tool_help()` | M | platform | Sprint 0b |
| 8 | **ARCH-18 #2**: 返回值默认压缩 (api_request / diff_configs) | M | platform | Sprint 0b |
| 9 | **ARCH-18 #3 + ARCH-19 #A**: AutoRecall hybrid + 档位驱动 | M | platform | Sprint 0b |
| 10 | **ARCH-19 #B**: 本地 LLM prompt cache 对接 (vLLM/llama.cpp/Ollama) | L | platform | — |

---

### Sprint 3（1-2 Weeks）— v0.18.1 边界终态

| # | Issue | Size | Owner | 依赖 |
|---|---|---|---|---|
| 11 | **ARCH-21**: v0.18.1 spec 重组（core 瘦身 + subagent 合并 + services 独立） | XL | platform + netops | Sprint 1 全部 |

---

### Sprint 4（2-3 Weeks）— 新能力（并行）

| # | Issue | Size | Owner | 依赖 |
|---|---|---|---|---|
| 12 | **ARCH-06**: recipe 种子库 10-15 条 | L | netops + data | — |
| 13 | **ARCH-14 P1+P2**: NoM lazy proxy L1+L2+L3+policy 只读 | XL | netops + data | — |
| 14 | **ARCH-11**: evidence 引用链规范 + linter | M | platform | — |
| 15 | **ARCH-13**: `diff_snapshots` 工具 | M | netops | — |
| 16 | **RAW-05**: 5 P2 消费者 raw fallback（take_snapshot / anomaly / 3 audit） | M | netops | — |

---

### Sprint 5（1 Week）— UX 收尾

| # | Issue | Size | Owner |
|---|---|---|---|
| 17 | **ARCH-12**: `olav catalog` 三级 drill-down | M | platform + UX |
| 18 | **ARCH-19 #C**: `tool_help(name)` 完善 | S | platform |
| 19 | **ARCH-08**: Nornir inventory environment 标签 | S | netops |

---

### 生产可靠性修复（穿插进行，可任意 sprint 做）

| # | Issue | Size | Owner | 说明 |
|---|---|---|---|---|
| R1 | **CORE-02**: 核心模块 20+ 处 `except: pass` 加日志 | S | platform | 可分批提交 |
| R2 | **CORE-03**: ingest_manager SQL 参数化 | S | platform | 独立 PR |
| R3 | **CORE-04**: tool_discovery sys.path 污染 | S | platform | 配合 ARCH-20 |
| R4 | **CORE-05**: api_registry print() → logger | S | platform | 独立 PR |
| R5 | **CORE-06**: config 编码异常记录 | S | platform | 独立 PR |
| R6 | **NETOPS-01**: Nornir 连接池泄漏（`nr.close()`） | M | netops | **P1，生产阻塞** |
| R7 | **NETOPS-02**: `datetime.now()` vs `utcnow()` 统一 | S | netops + data | 独立 PR |
| R8 | **NETOPS-03**: CommandRegistry hot-reload 锁 | S | netops | 独立 PR |
| R9 | **NETOPS-04**: netops_init 断点续跑 | L | netops | 大环境才体感 |
| R10 | **NETOPS-05**: safe_cmd 路径遍历防御 | S | netops | 独立 PR |
| R11 | **NETOPS-06**: SSH stage 粗粒度错误处理 | S | netops | 独立 PR |
| R12 | **ARCH-22 B**: COMPATIBILITY_CUTOFF 升 v0.15 + 删旧 migrations | S | platform | 独立 PR |
| R13 | **ARCH-22 C/D/E/F**: 硬编码散件 + legacy 标注 + bridge 重构 | M | platform | 分批 |

### Future（暂不排期）

| # | Issue | 说明 |
|---|---|---|
| F1 | **ARCH-09**: Syslog 驱动的只读自动诊断 | 标 Future；排在 ARCH-10 + ARCH-11 完成后 |
| F2 | **ARCH-14 P3**: LLM-as-interpreter policy 求值 | 开源版 fallback；企业版走 `ent_batfish_analyzer` |
| F3 | **WRITER-01**: T2-14 工具调用计数（CI 测试方法问题） | 非功能 bug，可 backlog |

---

### Size 定义

| | 含义 |
|---|---|
| **S** | < 1 day（1 个 PR 单点修复） |
| **M** | 1-3 days |
| **L** | 1 week |
| **XL** | 2 weeks+（通常需拆子任务）|

### Owner 定义

- **platform** — `src/olav/` core/agents/tools
- **netops** — `olav-netops/` + `.olav/workspace/ops/`
- **data** — 解析 / ETL / recipe
- **QA** — CI / test infra
- **UX** — CLI / TUI 体验

### 交付 readiness 审视

✅ 全部 30 个开放 issue 已分配 sprint + size + owner
✅ 强依赖关系显式标注
✅ 生产可靠性修复有单独通道（可独立于治理主线）
✅ TDD roadmap (`dev_docs/51.*`) 覆盖 sprint 0-5 大框架，每 sprint 的具体测试 spec 可查
⚠️ CORE/NETOPS 组的 R1-R13 未在 `51.*` 的 TDD 中展开 —— 但这些多为局部修复，Red-Green 模式 trivial（测试 = "before: leaks; after: no leak"）

---

## 治理主题 Overlay（rev 153 新增）

> **目的:** 12+ ARCH issue 平铺无 treetop view 违反自身渐进式披露原则。
> 本节为 cross-cutting 视图，每条 issue 仍独立 atomic 追踪；本节只做归类 + 路线图。
> **不合并 issue**（保留发现历史 + 单独 close 能力）。
>
> **可执行实施清单见 `dev_docs/51. SMALL_MODEL_GOVERNANCE_ROADMAP.md`**（TDD 格式，按 sprint 组织，含每条 ARCH 的 Red→Green→CI 三步）。

### 4 个治理主题

| 主题 | 包含 issue | 共同根因 |
|---|---|---|
| **T1 小模型适配** | ARCH-16（反 fan-out 原则）、17（static_context 失控）、18（tool/return/recall 优化）、19（现有机制配置缺口）；ARCH-12 修订（drill-down）、ARCH-14 lazy proxy | 4-8K 有效注意力窗口的小模型部署 |
| **T2 边界与组件治理** | ARCH-20（v0.18 迁移未完成）、21（v0.18.1 收紧）；ARCH-08（lab/prod 环境标签） | v0.17→v0.18 迁移欠债；spec 自身需收紧 |
| **T3 数据/分析栈** | ARCH-06（recipe）、10（多源检索，已收敛）、13（snapshot diff）、14（NoM）；企业版 `ent_batfish_analyzer`。**RAW-01/02/03/04 已完成 ✓**（rev 156），RAW-05 仍 open | live / config / cross-snapshot 三层栈未完整 |
| **T4 诊断质量** | ARCH-11（引用链）、12（catalog）、09（syslog 自动诊断 future） | 只读诊断的可信度 + 发现性 |

### Sprint 路线图（按依赖排序）

```
Sprint 1 (清债务):    ARCH-20 Phase 1+2  (删 v0.17 残留 + 工具去重)
                              │
                              ▼
Sprint 2 (小模型底座): ARCH-17 P1  +  ARCH-18 #1-#3  +  ARCH-19 #A/#B
                              │
                              ▼
Sprint 3 (边界终态):   ARCH-21 (v0.18.1 spec：core 瘦身 + subagent 合并 + services 独立)
                              │
                              ▼
Sprint 4 (新能力，可并行): ARCH-06 recipe || ARCH-14 NoM P1+P2 || ARCH-11 引用链 || ARCH-13 diff
                              │
                              ▼
Sprint 5 (UX 收尾):    ARCH-12 catalog drill-down + ARCH-19 #C tool_help
```

### 强依赖关系（不能跳过）

| 依赖 | 原因 |
|---|---|
| ARCH-20 → ARCH-21 | 必须先消除 v0.17 残留才能定 v0.18.1 |
| ARCH-17 → ARCH-21 | core 瘦身需要 spec 终态确定（否则要重做） |
| ARCH-11 → ARCH-09 | 引用链规范是自动诊断报告的前置 |
| ARCH-11 → ARCH-14 P3 | NoM trace 输出必须带 evidence |
| ARCH-06 / ARCH-14 → 企业版 ent_batfish_analyzer | 开源 fallback 路径必须先就位 |

### 工作复用（多 issue 共享同一段实施）

| 工作项 | 解决的 issue |
|---|---|
| Tool docstring 瘦身 + `tool_help()` 二级查询 | ARCH-18 #1, ARCH-19 #C, ARCH-21 |
| 模型档位驱动配置 (`model_tier`) | ARCH-16, 17, 18, 19 全部 |
| `static_context: mode: always/on_intent/lazy` 三档机制 | ARCH-17, 12 |
| 工具去重 + 单一权威源 | ARCH-20, 21, 17 |

### 治理优先级矩阵

| | 必做（P1） | 应做（P2） | 可做（P3） |
|---|---|---|---|
| **T1 小模型** | ARCH-17 P1 | ARCH-18, 19 | ARCH-12 修订 |
| **T2 边界** | ARCH-20 Phase 1-3 | ARCH-21, 08 | ARCH-20 Phase 5 |
| **T3 数据** | — | ARCH-06, 14 P1-P2, 13 | ARCH-14 P3 |
| **T4 诊断** | — | ARCH-11 | ARCH-12, 09 future |

---

## 活跃 Issues

> **Gitea tracker:** http://192.168.100.50:3000/admin/olav/issues
>
> 每条 issue 单独 atomic 追踪。所属主题见上方"治理主题 Overlay"。

---

### ISSUE-LAB-PATHS-HARDCODED-PRE-RENAME — `olav.core.lab._paths.py` marker 仍指 `.olav/workspace/ops/lab/...` (P1, 🔴 Open 2026-05-07)

**症状**: 在 demo7（命名标准化后 agent 目录是 `netops_ops/`）跑 `deploy_and_push_lab`，立即报：
```
FileNotFoundError: '/home/yhvh/olav-demo7/.olav/workspace/ops/lab/config/config.json'
```

**根因**: agent 命名标准化（commit `b3c7da5` 2026-05-01，把 `ops/` → `netops_ops/`）只改了 workspace 目录 + workspace.yaml，**没同步**更新 `src/olav/core/lab/_paths.py`：

```python
# _paths.py:35
_MARKER = Path(".olav") / "workspace" / "ops" / "lab" / "config" / "config.json"
```

`olav_root()` 用 marker 文件向上 ascend 找 workspace root；marker 路径里的 `ops/` 在新布局里不存在 → 找不到 root → fallback 到 cwd → 拼出错误路径。

**这是 rename 工作流的盲点**: 改 workspace.yaml + AGENT.md 的 frontmatter `name:` 容易；但 **Python 代码里硬编码的目录字符串** 没有自动迁移。Patch B (per-section api_key) + 命名标准化 commit 都没扫描 `*.py` 里的字符串字面量。

**修复路径**:

* P1 短期: 改 `_MARKER` 用 glob 而非固定路径，例如：
  ```python
  _MARKER_PATTERNS = [".olav/workspace/*/lab/config/config.json"]
  ```
  walk 时用 `Path.glob` 找任何含 lab 配置的 agent 目录
* P0 长期: 把 lab config 路径暴露成 `OLAV_LAB_CONFIG_PATH` env + workspace-level config registry，去掉路径硬编码

**Workaround (验证用)**: `ln -s netops_ops .olav/workspace/ops` — 让旧路径仍可解析。临时方案，不进 commit。

**影响**: 所有 lab 相关功能（`deploy_lab` / `push_node_config` / `destroy_lab`）在 rename 后的部署上**完全 broken**，没人发现是因为没人 V2 Ch8 跑过 lab deploy 的 server side 流程（之前都停在 spec.tcf.yaml 写出阶段）。

---

### ISSUE-LAB-DEFAULT-SERVICES-NOT-PRIMED — `olav init` 不预注册 containerlab service (P2, 🔴 Open 2026-05-07)

**症状**: 全新 demo7 上跑 `deploy_and_push_lab` → `Service 'containerlab' not found in .olav/config/services.yaml. Available: []`

**根因**: `olav init` 创建 services.yaml 时只 ship 空的 `services: {}`。CLAB API endpoint（demo7 上 `192.168.100.12:8080`）需要用户**手工**写一个 services.yaml 条目并填入 username/password。文档里也没有"必须先 register containerlab"这一步。

**对比 expected**: demo7 这种 V2 demo 部署应该开箱即用 — `olav init` + `olav agent install /path/to/olav-netops` 之后 lab 就能跑。

**修复路径**:

* `olav init` 检测 lab 配置文件 `lab/config/config.json` 是否存在 → 自动从中提取 base_url + auth 信息往 services.yaml 写入 default 条目
* 或：`olav agent install netops` 时自动 prime 一份 services.yaml 模板（用户填 password）

**Workaround**: 手写 services.yaml — 见这次 validation 的 `dev_docs/00 § ISSUE-LAB-PATHS-HARDCODED` 同段记录。

---

### ISSUE-CLAB-TOPOLOGY-MGMT-SUBNET-AUTOSELECT — generate_clab_topology 不带 mgmt block，subnet 自动选可能冲突 (P2, 🔴 Open 2026-05-07)

**症状**: `deploy_and_push_lab` 第一次成功调到 CLAB API，但 CLAB 拒绝部署：
```
deployment failed: requested subnet(s) 172.21.53.0/24 overlap an existing
Docker network. Existing: llama_default:[172.21.0.0/16] ...
```

**根因**: `generate_clab_topology.py` 输出的 CLAB YAML 不含 `mgmt:` 块。当 yaml 缺 mgmt subnet 时 CLAB 自动从默认池（172.20+）找空闲，但默认池没排除已存在的 docker networks（如开发机上常见的 `llama_default`、`*-default`）。

而 demo7 的 `lab/config/config.json` **明明定义了** `"mgmt_subnet": "172.20.50.0/24"`，但 generate_clab_topology 不读这个字段。两边各管各的。

**修复路径**: 让 `olav.core.lab.topology.generate_clab_topology` 读 `lab_config_path()` 的 `mgmt_subnet` + 一个 `network` 名（用 `change_id` 派生，避免多 lab 同名），把 mgmt block 注入 yaml 头部。

```yaml
mgmt:
  network: olav-mgmt-<change_id>
  ipv4-subnet: <mgmt_subnet from config>
```

**Workaround (本次验证用)**: 手工 sed 注入 mgmt block 到生成的 yaml — 见 commit pipeline 调试代码。

---

### ISSUE-R89-SRL-RENDER-MISSING-CANDIDATE-MODE — generate_srl_lab_config 输出缺少 SRL CLI mode-entry (P2, 🔴 Open 2026-05-07)

**症状**: 拉到 `deployed: true`（容器起来了）后 commit 阶段报：
```
At line 2: Parsing error: Unknown token 's'.
Options are ['!', '!!', '!!!', '#', '/', 'acl', ..., 'enter', ..., 'set', ...]
```

那个 `'s'` 是 `set` 的第一个字符。SRL CLI 在顶层不能直接 `set / interface ...`，必须先 `enter candidate`，然后才能编辑 config tree，最后 `commit save`。

**根因**: `olav.core.lab.srl_render.generate_srl_lab_config` 渲染的 SRL CLI 是裸 `set / ...` 命令，没用 mode-entry/exit 包裹。CLAB 容器的 SRL CLI 是顶级 mode → 拒绝 `set`。

**修复路径**: srl_render 输出格式改为：
```
enter candidate
set / interface ethernet-1/1 admin-state enable
set / network-instance default protocols bgp ...
...
commit save
```

或改用 SRL 的 `--commit-confirm` 一次性 push 模式（需要客户端支持）。

**修复时机**: P2 — 因为 ops-lab 现在 dry_run_failures 把这个 caught 并写回 TCF (验证流程没破)。但任何想真正 commit 配置的 V2 Ch8 流程都会卡这。

**Workaround**: 没（需要 R89 translator code 改）。

---

### ISSUE-CAB-AGENT-DRIVEN-LAB-VALIDATION-LOOPS — agent 跑 "validate spec.tcf.yaml in lab" 陷入 emit_tcf+investigation 循环 (P2, ✅ Closed by Patch L `39f964d` 2026-05-07)

**症状**: 给 ops orchestrator 这个 prompt:
> "Validate the TCF spec at exports/cab/r1-r3-ebgp/spec.tcf.yaml in the lab. Deploy the topology, push the prod CLI, run post_check assertions, then record the verdicts back into the TCF."

实测（DeepSeek V4 Flash, 600s timeout）:
* 77 个 tool calls 内：13 个 `execute_skill_script`（12 个全是 `tcf_load_for_lab.py`，循环重复）+ **7 个 emit_tcf（重复发射！）** + 27 个 execute_sql（重新 investigate 数据）+ 8 个 glob/ls disk-hunt
* **0 个** deploy_and_push_lab / push_node_config / tcf_record_lab_run 调用
* 最终 timeout，spec.tcf.yaml 的 `lab:` block 没被填

**根因（agent 行为模式）**:

跟 T1 (TCF emission) saga 同款 ZONE-OUT 模式，但场景**更复杂**：现在是 multi-step deterministic pipeline，agent 需要按顺序调 5+ skill_scripts。Failure mode 有 4 层：

1. **不收敛 to next step**: 调完 tcf_load_for_lab，没有 internal "I'm done with step 1, now go to step 2" 状态 → 又调一次 tcf_load_for_lab。LLM agent 的 stateless 性质 + 长 multi-step pipeline 缺乏明确 transition cue 是根因。
2. **混淆 emit 跟 validate**: 7 个 emit_tcf 调用证明 agent 把 "validate spec in lab" 误当成 "emit a new spec"。两个动词在 LLM 的 token space 里太接近。
3. **disk-hunt 反射没消失**: 8 个 glob/ls 调用还是在找 "the lab tool"，跟之前 T1 saga 模式一致。
4. **task() 用得不够**: 只 2 次 `olav_delegate`，没有把 "lab validation" 整体打包成对 ops-lab sub-agent 的一次 task() 委托 → 转用 server-side pipeline。

**为什么之前 T1 (emit_tcf single-step) 修复（Patch D+E+F+G）不能直接复用**:

T1 修的是 single-tool 调用（emit_tcf 自己），通过 forgiving signature + AutoRecall change_plan guide 把 0/4 → 1/2 success rate。CAB lab validation 是 **5-step pipeline**，没有 "lab_validate" 单一 @tool — agent 必须自己编排 5 个 skill_script 调用 + 数据传递。这是更高的 cognitive load。

**修复路径（按 ROI 排序）**:

* **L1 (最简，~30 min)**: 加 `cab_lab_validation.guide.yaml` AutoRecall guide，里面写完整 5-step minimum-viable pipeline + "调一次每个 script，不要循环"。预期把循环减少。
* **L2 (中等, ~60 min)**: 把 5 个 skill_scripts 折成一个**单一 @tool** `validate_tcf_in_lab(spec_path: str)`，内部 server-side 顺序调 load → topology → srl → deploy → record。Agent 只需调一次。等于把 multi-step pipeline 物化成原子操作。
* **L3 (深层, 数 hour)**: 设计 langgraph state machine 让 agent 显式持续 step state；超出 OLAV 现行 deepagents 框架。

**Workaround (本次验证)**: server-side 直调 5 个 scripts。这种模式适合 demo / CI / 自动化流水，**不适合 user 直接 agent 交互**。

**关闭 — Patch L `39f964d` (2026-05-07)**: 选 L2 路径 — 把 5 步 pipeline 物化成单一 Python 函数 `olav.core.cab.validate_tcf_in_lab(spec_path)` + 同名 skill_script 包装。Lab agent 的 MANDATORY TODO 从 10 条压到 2 条，agent 只调 `execute_skill_script("validate_tcf_in_lab.py", {"spec_path": ...})` 一次，composite server-side 顺序跑 load → topology → SRL → save × N → deploy + push → exec post_checks → record back → destroy。In-vivo demo7 实测端到端跑通：lab 真实部署成功、verdict 写回 TCF、journal 6 条 phase 记录、destroy 成功。错误处理保证即使中途失败也尝试 destroy_lab，不会泄漏。**新发现 2 个下游 gap** 单独登记为 ISSUE-CAB-POSTCHECK-NEEDS-TRANSLATION + ISSUE-CAB-TVT-CHECK-LINK-MISSING（见下）。

---

### ISSUE-CAB-POSTCHECK-NEEDS-TRANSLATION — post_check.command 是 prod-form CLI，lab 无法直接执行 (P1, ✅ Closed by Patch M `7816d79` 2026-05-07)

**症状**: Patch L composite 在 demo7 真实跑 `exports/cab/r1-r3-ebgp/spec.tcf.yaml`，post_check 100% FAIL。actual 字段都是同一个 docker exec 错误：
```
OCI runtime exec failed: exec failed: unable to start container process:
exec: "show": executable file not found in $PATH
```

**根因**: Analyzer (sim) 写的 post_check 是**生产形式**的 CLI：
* `PC-01 (R1) command: 'show bgp summary'` (Junos 形式)
* `PC-02 (R3) command: 'show ip bgp summary'` (IOS 形式)
* 期望 pattern `'10.1.13.3.*65000.*Established'` 用的是**生产 IP** 10.1.13.x

但 lab 是 SR Linux 数字孪生：
* SRL 不认 bare `show`，需 `sr_cli "show network-instance default protocols bgp neighbor"`
* SRL lab 用 `lab_subnet=172.16.99.0/30`，没有 10.1.13.x 链路 IP
* 即使 BGP 真的在 lab 内 established，pattern 也匹配不上

**为什么之前 manual 流能跑通**: lab agent 用 LLM 智能临场翻译每个 post_check（`show bgp summary` → `sr_cli "..."`），但这正是 Patch L 想消除的"agent 编排"模式。Composite 直接 exec 拷贝就暴露了这个 schema gap。

**修复方向 — Patch M (确定性翻译器)**:

1. **Command 翻译**：在 `olav.core.cab.tcf_validate` 加 `_translate_command_to_srl(prod_command, vendor)`：
   * `show\s+(ip\s+)?bgp\s+summary` (Junos/IOS) → `sr_cli "show network-instance default protocols bgp neighbor"`
   * `show\s+(ip\s+)?route` → `sr_cli "show network-instance default route-table ipv4-unicast"`
   * `show\s+interfaces` → `sr_cli "show interface"`
   * 不在 known-pattern map 里的命令 verbatim 透传 + warn

2. **Pattern 翻译**：从 r88_args + r89_args 构建 prod→lab IP 映射:
   * 简单情况（ebgp_direct, 2 设备）: peer-of-A 是 B；A 在 lab 上的 peer 是 B 的 lab_ip = `lab_subnet[i+1]`
   * 把 expected_pattern 里的 prod IP 替换成对应 lab IP；ASN 不变（intent 保持）
   * Established 关键字 case-insensitive 匹配（已实现）

3. **wired into composite**: phase 5 verify 之前对每个 post_check 跑 translate，journal 里记 "translated: <orig> → <new>" 留 audit trace。

**ROI**: 解锁 Patch L 的 PASS verdict — 没这个 patch，所有真实 spec 都 FAIL on lab side（假阴），CAB 流程失去价值。优先级 P1 因为 Patch L 已 ship 但功能未闭合。

**Workaround**: 暂用 `skip_record=True` 走 dry-run + 手工读 post_check_results.actual 判断；或让 sim 提前生成 lab-form post_check（schema change，更大的工作量）。

**关闭 — Patch M `7816d79` (2026-05-07)**: 新模块 `olav.core.cab.postcheck_translate` 提供确定性翻译器：(1) command map 覆盖 `show (ip) bgp summary|neighbors` / `show (ip) route` / `show ospf neighbor` / `show interfaces` / `show version` → SRL `sr_cli` 等价形式；已是 SRL 或 unknown 命令 verbatim 透传；(2) 两阶段 prod→lab IP map：先扫 own-interface 赋值（`family inet address X/N` Junos / `ip address X Y` IOS），再用 `neighbor <IP>` fallback（real spec 接口配置在 baseline 不在 change，只能从 neighbor 行反推）；(3) pattern 替换 bare 和 regex-escaped 两种形式，`re:` prefix 保留；(4) `_match_pattern` 加 forgiving regex fallback——bare pattern 含 `.*`/`\s`/`\d` 时 substring 失败后试 regex match，让 analyzer 习惯写 `a.*b` 但不加 `re:` 的常见偏差能 pass。In-vivo demo7 实测：`exports/cab/r1-r3-ebgp/spec.tcf.yaml` 完整流程现在 PASS，2 个 post_check 都过，verdict 写回 TCF。

---

### ISSUE-CAB-PROD-CLI-INCOMPLETE — analyzer 写的 prod implementation 不可生产部署 (P0, 🔴 Open 2026-05-07)

**症状**: Patch L+M+N 已让 lab 端 PASS，但写回 TCF 的 `implementation` block 是 LLM analyzer 自由写的 fragment——直接贴到生产设备上**不能 commit**。

实测 `exports/cab/r1-r3-ebgp/spec.tcf.yaml` analyzer-emitted vs 已有确定性生成器 `derive_prod_cli_from_tcf`:

| 项 | analyzer (3 行/block) | `generate_junos_ebgp_config` (12 行) |
|---|---|---|
| 接口 IP | ❌ 缺 | `set interfaces ge-0/0/0 ... address 172.16.99.1/30` |
| Loopback IP | ❌ 缺 | `set interfaces lo0 ... address 1.1.1.1/32` |
| Router-ID | ❌ 缺 | `set routing-options router-id 1.1.1.1` |
| **本地 AS** | ❌ **缺**（Junos 无法 commit） | `set routing-options autonomous-system 65001` |
| local-address | ❌ 缺 | `set protocols bgp group ... local-address 1.1.1.1` |
| 导出策略 | ❌ 缺（默认全 reject，BGP up 但无路由） | `set policy-options policy-statement bgp-export ...` |
| **IOS address-family** | ❌ **缺**（IOS BGP session 永远 Idle） | `address-family ipv4 ... neighbor X activate` |
| IOS update-source | ❌ 缺 | `neighbor 172.16.99.1 update-source Loopback0` |
| IOS network 通告 | ❌ 缺 | `network 3.3.3.3 mask 255.255.255.255` |

**IP plan 也错位**: analyzer 用 `10.1.13.x` 当 prod IP，但 `intent.lab_subnet=172.16.99.0/30`。`derive_prod_cli_from_tcf` 让 prod 和 lab 共用同一 /30（数字孪生 1:1 等价）才对——LLM 幻觉了一个不同的 prod 子网，Patch M 的"prod IP→lab IP 翻译器"实质上是给这个幻觉打补丁。

**根因（架构错位）**: Lab 侧 R88-A `generate_clab_topology` + R89 `generate_srl_lab_config` 是**完全确定性**渲染（intent + structured fields → 完整可部署 config）；但 Prod 侧把 implementation 创作权完全交给 LLM，`tcf_emit_from_sim` 直接接受 `implementation_json` verbatim 写入 TCF。已有的 `derive_prod_cli_from_tcf` 完整生成器**从未被 emit 路径调用**。

**修复方向 — Patch O**:

* **核心改动**: `tcf_emit_from_sim` 默认调 `derive_prod_cli_from_tcf(tcf)` 生成 implementation + rollback；LLM 只填 `intent.type` + `devices[i].prod_loopback/prod_asn/prod_intf` structured fields；"prod CLI 创作权"从 LLM 收回到 Python。`_PROD_SUPPORTED_INTENTS` 命中时强制确定性；不命中时 fallback 到 LLM 路径但加完整性 lint。
* **完整性 lint** (`validate_prod_cli_completeness`): 当 LLM 路径触发，按 platform 校验：
  - Junos: 每非空 block 含 `routing-options autonomous-system` (eBGP 必需) + 末尾 `commit` 或显式 `assumes-baseline` 元数据 flag
  - IOS: BGP block 含 `address-family` + 每 neighbor 后 `activate` + block 末尾 `end` / `write memory`
* **lab 侧已用 prod CLI 的影子 push**（可选, P1）: lab 在 SRL 推送之外，再起一个 Junos/IOS container 跑 `commit check` 验证 prod-form CLI 能解析（vendor 真实语法 lint）。
* **数字孪生 IP parity**: 让 sim 默认让 prod 和 lab 共用 `lab_subnet`，Patch M 的 IP 翻译器降级为兼容补丁不再是常用路径。

**优先级 P0** 因为这是 CAB 流"agent→lab→prod"闭环的最后一英里——之前所有 patch 都是修流程，这个修的是输出物本身。当前 verdict=PASS 但 implementation 不能跑生产 = 假阳性。

---

### ISSUE-CAB-LLM-EMIT-SEMANTIC-ERRORS — LLM emit 产出 schema-valid 但语义错的 prod CLI (P2, 🔴 Open 2026-05-07)

**Context**: Ollama 端到端 CAB 测试（qwen3.6:27b + qwen3-vl-embedding-2b + qwen3-reranker-0.6b 全 local）暴露的真实 LLM-emit 缺陷。

**症状**: 给 sim agent prompt: "emit_tcf 给 R1 (juniper, AS 65001, lo 1.1.1.1) ↔ R3 (cisco, AS 65000, lo 3.3.3.3)"，agent 产出:

```yaml
implementation:
  - device: R1
    cli:
      - set protocols bgp group EBGP-R3 type external
      - set protocols bgp group EBGP-R3 neighbor 3.3.3.3 remote-as 65000  # ← 错
  - device: R3
    cli:
      - router bgp 65000
      -  neighbor 1.1.1.1 remote-as 65001                                  # ← 错
```

`neighbor 3.3.3.3`（R3 的 loopback）和 `neighbor 1.1.1.1`（R1 的 loopback）**作为 BGP 邻居 IP 是错的**——直接 eBGP 应该用对端的**链路 IP**（如果有 lab_subnet 那就用 lab_subnet 里的 host IP）。用 loopback 当 neighbor 需要 multihop 配置（`set protocols bgp group X multihop ttl N`）+ 需要 IGP 路由 loopback → loopback，eBGP-direct intent 没这些。

**根因**: Lab review (Patch O'-A) 是 schema-level 规则——查"有没有 AS 号"、"有没有 address-family"——但不查"neighbor IP 是 loopback 还是 link IP"这种**意图级语义**。R89 SRL render 也不会 catch，因为它根据 r89_args 自己生成完整 SRL，根本不读 prod CLI。

**为什么 cloud LLM (DeepSeek V4 Flash) 没这个问题**: cloud 模型经验更多，知道 ebgp_direct 该用 link IP 而非 loopback。本地 27B 模型 (qwen3.6:27b) 推理深度不够，看到"R1 neighbor R3"就抓 R3 的 loopback 当 ip。

**修复方向 — Patch P (review 加意图级语义检查)**:

1. 在 `tcf_review.py` 新增 intent-aware rule:
   ```python
   def _check_neighbor_not_loopback(cli, device, intent_type, peer_loopbacks):
       if intent_type != "ebgp_direct": return []
       for ip in _extract_neighbor_ips(cli):
           if ip in peer_loopbacks:
               return [ProdReviewFinding(
                   severity="warn",
                   device=device, category="semantic_mismatch",
                   description=(
                       f"neighbor {ip} is the peer's loopback. ebgp_direct "
                       f"intent uses link IPs (lab_subnet hosts). "
                       f"Loopback peering needs multihop + IGP. Confirm intent."),
                   suggested_fix=f"use a host from intent.lab_subnet instead of {ip}",
               )]
       return []
   ```

2. 同样问题更广泛存在于 sim 端：每次 emit_tcf 都该把"derive_prod_cli_from_tcf 输出 vs LLM 写的 implementation"做 diff 给 review，让 deterministic generator 当语义 oracle。这是 **Patch O 重启的入口**——之前砍掉确定性接管是正确的（保留弹性），但用它做**校验** vs LLM emit 是合理的。

**ROI**: 加一条 intent-aware rule 半小时；接 generator 当 oracle 1-2 小时。**P2** 因为 cloud LLM 不出这问题、且 lab CLAB 部署会因 BGP 不收敛 catch（数字孪生兜底），但本地小模型场景必须修。

**验证测试用例** (Ollama qwen3.6:27b 实测复现):
- spec_path: `exports/cab/r1-r3-ebgp-ollama/spec.tcf.yaml` (revision_count=3 after patches)
- Lab will likely deploy + push but BGP stays Idle (loopback peering without multihop + IGP)

---

### ISSUE-CAB-EMBED-DIM-MIGRATION-DESTRUCTIVE — 切换 embed model 强制 destroy 全部 memory (P2, 🟡 Workaround 2026-05-07)

**症状**: 把 demo7 从 cloud (BAAI/bge-base-en-v1.5 768-dim) 切到 Ollama (qwen3-vl-embedding-2b 2048-dim) 时，启动报错：
```
Embedding dim mismatch on table 'memory': stored=768, embedder=2048.
Refusing to start to avoid silent data loss. Set OLAV_ALLOW_DESTRUCTIVE_DIM_MIGRATION=1
to opt in to the old drop-and-recreate behaviour (destroys ALL rows in 'memory').
```

意思是切换 embed 模型 = **删全部 memory** + 重新 prime。如果用户已经攒了大量 query_pattern / operational_event memory，这等于全损。

**Context**: ISSUE-DIM-MISMATCH-DESTROYS-DATA (#238 已 closed) 加的安全机制——拒绝静默 drop——是对的。但工作流上现在是"切 embed → 必失忆"。

**修复方向**: 三条路：
1. **Re-embed migration tool**: 加 `olav memory migrate-embed --new-model X` 命令，**保留** memory 行的 text，重新 embed 成新 dim。LanceDB 不能 alter dim，要建新表 + copy。复杂但保留所有积累。
2. **多 embedder 索引并存**: 不同 dim 的 embedding 各有独立 vector 列（`emb_768`, `emb_2048`），新 embedder 来时给老行算新 embedding。空间翻倍但不丢数据。
3. **Document the limitation**: 在 issue 里写明"切 embed model 等于换 memory"，让用户做决策。最小工作量但用户体验差。

短期建议 #3，中期做 #1。

---

### ISSUE-AUTORECALL-GUIDE-COMPETITION — 新 guide 把已 working guide 挤出 top-3 (P0, 🔴 Open 2026-05-07)

**症状**: 同一份 emit_tcf prompt，今早跑得通，今晚跑不通。Bisect 出根因：

今晚 sim agent prompt: `"emit_tcf for change_id=r1-r3-baseline2..."`

AutoRecall 召回 top-5（manually verified）：
1. **`guide_ops_cab_revise_after_lab_findings`** ← 我今天加的，包含关键词 "TCF" "CAB" "patch TCF" "tcf_patch_block"
2. `guide_core_bgp_path_selection_and_state_machine`
3. `guide_core_schema_introspection_via_describe_table`
4. `guide_ops_change_plan_emit_tcf` ← **正确 guide，应在 #1**
5. `guide_ops_simulation_what_if`

AutoRecall 默认 top-3 → emit_tcf prompt 只看到 **cab_revise + bgp_path + schema_introspection**，**完全没有 change_plan_emit_tcf**。Agent 按 cab_revise 教导用 `execute_skill_script` (revision 路径) 而不是 emit_tcf @tool (fresh emit 路径)。Result：disk-hunt + recursion timeout / 假 yaml 输出。

**根因**: `cab_revise.guide.yaml` 关键词跟 `change_plan_emit_tcf.guide.yaml` 严重重叠：

```yaml
# cab_revise.guide.yaml keywords (今天加):
- "patch TCF"
- "fix lab findings"
- "二次修订"

# change_plan_emit_tcf.guide.yaml keywords (已 working):
- "change plan"
- "emit TCF"  
- "TCF spec"
- "CAB"
```

两个都包含 "TCF" 概念，semantic embedding 把 cab_revise 排得更高（因为它的全文比 change_plan 短，向量更密）。

**第二层根因 (PLATFORM bug)**: `olav skill install` 是 upsert-only — **不清除 source 已删的旧 memory entries**。今晚我把 cab_revise.guide.yaml 从 source 移走，重 install 报告 "guides primed: 19"（少了一个），但 memory 里那个 entry **依然存在**。直到我手工 `store.delete_memory('guide_ops_cab_revise_after_lab_findings')` 才真正消失。

实测验证（手工删 cab_revise memory entry 后）:
- top-3 变成 bgp_path / schema / change_plan_emit_tcf
- emit_tcf 现在排第 3，被 top-3 覆盖到
- 但 agent 仍不可靠（rank 3 < rank 1，模型不强烈跟随）

**真实修复路径**:

1. **立即 (P0)**: 删除 `cab_revise.guide.yaml` 完全；它的功能（教 sim 用 patch_block）应该走 system.md 路径而非 AutoRecall guide（避免与 change_plan 竞争）
2. **平台修复 (P0)**: `olav skill install` 加 prune 阶段——比对当前源文件与已存 memory，删除 orphan entries。现在的 upsert-only 是 silent corruption hazard
3. **AutoRecall 健壮性 (P1)**: 当 prompt 含明确关键词（"emit_tcf"）时，给精确匹配的 guide 加 score boost，不让 generic guide 抢 rank

**为什么早上 qwen3 跑通**: 早上是 cab_revise 加进来**之前**的最后一次成功 run。后续 reprime 把 cab_revise 装进 memory 后，所有 emit_tcf 工作流（cloud + local 都受影响）开始坏。这印证了"OLAV 是为本地小模型设计"这一目标——但今天的 regression 是我自己引入的而不是模型问题。

**ROI**: 立即删 cab_revise 5 min，prune 修复 ~1 hour，加 boost ~30 min。先做前两个。

---

### ISSUE-CAB-GEMMA4-AGENT-UNUSABLE — gemma4 两个变体在 OLAV agent loop 都不可用 (P3, 🟡 Documented 2026-05-07)

**Context**: 用户要求测试 gemma4 在 OLAV CAB 流程下的表现，对比 qwen3.6:27b 验证哪个 local 模型更合适。Stack 同上（embed/rerank 不变），只切换 LLM。

**实测两个 gemma4 变体两种失败模式**:

| 模型 | 失败模式 | 实测输出 |
|---|---|---|
| `gemma4:31b` (19.9 GB) | **Plan-without-execute 循环** | 输出格式化 "## SESSION INTENT / SUMMARY / ARTIFACTS / NEXT STEPS" 计划文档，列出"step 1: Execute the SQL query"，但**从不真正发起 execute_sql tool call**，session 结束时计划仍未执行 |
| `gemma4-26b:latest` (21.2 GB) | **Chat template 泄漏 + 数据幻觉** | 输出 `<|tool_response>` 特殊 token 重复 24 次（应被 inference engine 剥离的 chat template 标记），然后**编造**出 `node-01` / `node-02` 假设备名，DB 里实际是 R1, R2, R3, R4, SW1, SW2 |

**Raw API 健全性 (排除模型本身能力问题)**:

两个 gemma4 变体在直接 `curl /v1/chat/completions` 单工具测试下都返回正确 `finish_reason: tool_calls` + 正确 `tool_calls` 数组。在 OLAV 完整 agent loop 下（system prompt + 多 tools + AutoRecall memory injection）都失败。

**与 qwen3.6:27b 对照（同一 stack 同一日测试）**:

| 测试 | qwen3.6:27b | gemma4:31b | gemma4-26b |
|---|---|---|---|
| Raw tool-call API | ✅ | ✅ | ✅ |
| Smoke (SQL list devices) | ✅ 6 devices 正确返回 | ❌ Plan-only, no exec | ❌ 假数据 |
| Sim emit_tcf | ✅ 6-9min | (没跑到) | (没跑到) |
| 3-patch revise via cab_revise.guide | ✅ 直接引用 guide 原文 | (没跑到) | (没跑到) |

**根因分析** (revised 2026-05-07 after thinking-toggle experiments):

**最初诊断错了一半**: 第一次说 gemma4 "把 thinking 当 content 输出"是 training-design 问题。**实际是 wire protocol 错配**：

- Gemma 模型在 GGUF 里有独立 `<thinking>` block 设计 + `thinking` 字段
- **只有 Ollama native `/api/chat` 端点**正确解析 chat template，把 thinking 放进独立字段
- **OpenAI-compat `/v1/chat/completions` 端点**把 thinking block 当 raw content 流出来 → plan-as-content 假象
- gemma4-26b 的 `<|tool_response>` 泄漏更直接证明 chat template 在 OpenAI-compat 路径下没被正确应用

**修订验证（同日）**:

切到 native API（model_provider=ollama, langchain-ollama 走 `/api/chat`）实测：
- 单工具 SQL smoke test: ✅ gemma4:31b 真调 execute_sql + 真返回数据
- 关 thinking via OLAV_DISABLE_THINKING=1: tokens 69→14 (5×)，单工具仍工作
- **但**多工具 emit_tcf：开 thinking 时 disk-hunt + plan + 重试 + 超时；关 thinking 时 200 次错误工具死循环

所以**真正的 gemma4 问题**有两层：

1. **基础设施层** (易修): OLAV 默认走 OpenAI-compat /v1，对 Ollama 模型该走 native /api/chat 才能正确解析 chat template。**已经被 llm.py:93-99 自动探测覆盖**——只要 base_url 含 `:11434` 或 "ollama" 字样且 model_provider 不显式指定，会自动用 ollama provider。
2. **模型能力层** (难修): 即便 native API + thinking 配置正确，gemma4:31b 在 OLAV 完整 system prompt + 10+ tools 下**工具选择能力不足**：
   - 关 thinking → 选错工具死循环（`execute_skill_script` 而非 `emit_tcf`）
   - 开 thinking → thinking 溢出 + 多 grep/recall_memory 干扰 → 超时

**这与 ChatOllama `reasoning=False` 文档警告一致**: "thinking is load-bearing for ReAct/tool-call adherence on Gemma-class models"。模型用 thinking 内部空间做工具选择推理，关掉就裸跑导致选错。

**这与之前 V2 Ch7 的发现一致**: gemma4:31b 在分层故障诊断 prompt 下也是 0 tool calls / 8 min stuck（dev_docs/00 § ISSUE-V2-CH7-LAYERED-FAULT-DIAGNOSIS-STUCK，已 closed by DeepSeek V4 Flash）。今天的 CAB 测试**复现了相同 failure mode**——证明这不是单一 prompt 触发，是 gemma4 + OLAV agent harness 组合的系统性问题。

**结论**: gemma4 系列**不应作为 OLAV 推荐 local LLM**。Local 推荐 qwen3.6:27b（或更大 GX-Telecom Qwen3.6-35B 待测）。Gemma3 / Gemma2 系列也不建议（同流派）。

**修复方向**: 不修。这是模型选择问题，不是平台 bug。文档化让用户避坑：

```yaml
# olav-doc 应加章节: Local LLM compatibility
verified working:
  - x-ai/grok-4.1-fast (cloud, paid, 推荐生产)
  - deepseek/deepseek-v4-flash (cloud, paid)
  - qwen3.6:27b (Ollama local, 慢但功能完整)
verified broken:
  - gemma4:31b — plan-without-execute (2026-05-07)
  - gemma4-26b — chat template leak (2026-05-07)
  - gemma4-26b NEMO/Telecom variants — untested but suspect same family
```

---

### ISSUE-CAB-OLLAMA-AGENT-LATENCY — Local thinking model 把 CAB session 从秒级拉到分钟级 (P3, 🟡 Workaround 2026-05-07)

**症状**: 同一 CAB session：
* DeepSeek V4 Flash (cloud, paid): emit_tcf + 3 patches ≈ 30-60s
* qwen3.6:27b (Ollama local, thinking model): emit_tcf 单步 ~3-5 min, 3 patches ~3 min, 总 6-9 min

**Throughput 实测**:
- 26 tok/s LLM inference
- ~25 ms embed call
- Cold start 模型 load: ~40s 一次性

**根因**: 三因素叠加：
1. Thinking model: 每 turn 50-100 token 的 reasoning 在工具调用前消耗
2. 27B 参数 + 单 GPU 推理 vs cloud LLM 的多卡集群
3. Orchestrator → sub-agent delegate 链路有时双层（`olav_delegate` ×2 重复观测到一次），多 turn 翻倍

**这不是平台 bug**，是 LLM stack 选择的硬延迟。但用户体验角度需要：
- (a) 文档明确"local LLM 期望延迟 5-10×cloud"
- (b) 评估非 thinking 模型选项（Qwen2.5-coder:32B、Llama-3.3-70B 等）
- (c) Patch agent prompts 减少不必要的 delegate 跳转（orchestrator 直接 emit 而非 delegate to ops-analyze who delegates again）

非阻塞但作为 demo 体验记录。

---

### ISSUE-CAB-UX-GAPS-AFTER-PATCH-O — Patch O' user-role audit 暴露的 UX 缺口 (P2, 🔴 Open 2026-05-07)

**Context**: Patch O'-A + O'-B 已 ship (`45f224b`)。完整 CAB approver session 在 demo7 走通：sim emit v0 → lab review (8 findings: 3 blocker + 5 warn) → operator 决策（接受 4 baseline warn，修 3 blocker + 1 export policy warn）→ sim patch_block × 3 → lab full validate **verdict=PASS**, 4 baseline warns 留记录, tvt 4/4 PASS, revision_count 0→4.

**实测发现的真实 UX gaps**:

1. **Operator 直接调 Python API**: `tcf_patch_block(spec, device, add_lines=[...])` — 真实 operator 应该用 `/sim revise` slash command 包一层接自然语言指令。当前要写 JSON args，门槛高。需要 UI/CLI 层封装。
2. **Findings 没有 ID + acknowledge 机制**: 每次 review 重跑都重新生成全部 findings。如果 approver 决定"这 4 warn 我接受为 baseline"，下次 lab 跑还是会再报。需要 `ProdReviewFinding.finding_id: str` (deterministic hash) + `tcf.acknowledged_findings: list[str]` 让 review 跳过已确认项。
3. **没有 diff view**: revision_count=4 看得见，但每次改了什么看不见。需要 schema `revision_history: list[RevisionDiff]` 或 UI 层算 git-style diff 展示。
4. **没有"operator approved" 终态**: lab.verdict=PASS 只代表 lab 通过，不代表 CAB 决议批准部署。需要 `tcf.cab_status: pending|approved|rejected_by_operator`. + `cab_approved_by/at` 字段。
5. **Review 是规则的不是 LLM 的**: ebgp_direct + Junos/IOS 全 cover；其他 intent / vendor 只能给 info "go extend tcf_review"。需要 follow-up 加 LLM-backed Tier C reviewer 处理 unknown 情况。

**优先级各异**:
* (1) UX 包装 — P1，需要 UI 配合做 slash command；agent 侧先加 `/sim revise <自然语言>` 翻译到 patch_block call。
* (2) Acknowledge 机制 — P1，否则 review 噪音永不消除；schema + review filter 一起改。
* (3) Diff view — P2，UI 层可独立加。
* (4) cab_status 终态 — P1，部署门控的最后一英里。
* (5) LLM Tier C reviewer — P2，覆盖率扩展。

**ROI**: 4 个 P1 加起来 ~4 小时。建议作为下一轮 CAB 工作的内容。

---

### ISSUE-CAB-LAB-PROD-REVIEW-REACT — lab agent 翻译命令前先 ReAct 审计 prod CLI 完整性 (P1, ✅ Closed (deterministic Tier A) by Patch O'-A `45f224b` 2026-05-07)

**Context**: ISSUE-CAB-PROD-CLI-INCOMPLETE 提了一个 Patch O 主路径（确定性生成器接管）+ Tier A/B/C 级联检测。用户反馈"太复杂"，提出更简单的替代设计。

**简化设计**:

不要重写 `tcf_emit_from_sim`、不要加多层级联。直接在 lab agent 流里加**一步 ReAct 审计**：

```
Phase 0    tcf_load_for_lab          (existing)
Phase 0.5  prod_cli_review (NEW)     ← lab 拿到 implementation 后，
                                       走 ReAct 思考"这份 CLI 推到真实设备上能跑吗"，
                                       缺什么、哪些是 baseline 假设、哪些是 blocker
Phase 1    generate_clab_topology    (existing)
Phase 2~5  ... (existing)
Phase 5    post_check translation    ← Patch M
Phase 6    record back to TCF        ← 写入 lab.prod_review_findings (NEW schema field)
```

**Phase 0.5 ReAct 流程** (单次 LLM call 即够，不需 multi-step ReAct loop):

输入:
* `tcf.implementation` 各 device 的 CLI block
* `tcf.intent.type` + `device.platform`
* lab agent expert memory（`bgp_validation_srl.expert.yaml` 等已有 vendor 知识）

输出 (新 schema `ProdReviewFinding`):
```python
class ProdReviewFinding(BaseModel):
    severity: str       # "blocker" | "warn" | "info"
    device: str
    category: str       # "missing_command" | "assumes_baseline" |
                        # "ip_plan_mismatch" | "syntax_warning"
    description: str    # "Junos eBGP 缺 routing-options autonomous-system"
    suggested_fix: str  # "set routing-options autonomous-system 65001"
```

**新 schema 字段**:
```python
class ExecutionRecord:
    ...
    prod_review_findings: list[ProdReviewFinding] = []
```
反映在 TCF YAML 里就多一个 `lab.prod_review_findings:` 章节，CAB approver 一目了然知道这份 implementation 部署前要先确认什么。

**和 Patch O 的对比**:

| 维度 | Patch O (确定性生成接管) | Patch O' (lab ReAct 审计) |
|---|---|---|
| 实现复杂度 | 改 `tcf_emit_from_sim` + 加 lint + 调 `derive_prod_cli_from_tcf` | 加一个 Python helper + 一个 schema 字段 + composite phase 0.5 |
| 修改面 | sim 侧 + lab 侧 | 只改 lab 侧 |
| 错误处理哲学 | 默认确定性强制，LLM 只是 fallback | LLM 创作 + 报告，operator 决策 |
| 假阳性/假阴性 | 假阴接近 0 (确定性) | LLM peer review 标准漏报率 |
| 操作员负担 | 低（自动生成完整 CLI） | 中（要看 findings，自己决策 baseline） |
| 适合场景 | 标准化 intent (ebgp_direct 等) | 复杂 / 非确定性 / 探索性 intent |
| 当前 supported intent 覆盖率 | 100%（ebgp_direct 已有生成器） | N/A（不替换、只补） |

**核心 trade-off**:

- Patch O **消除**问题：CLI 由生成器写就一定完整。但要重构 emit 路径，sim 侧 LLM 角色降级。
- Patch O' **暴露**问题：CLI 还是 LLM 写，但 lab 给 CAB approver 一份"这份 spec 部署到真机前要补 X、确认 Y"清单。Operator 仍是决策者。

**两者不互斥**: Patch O' 可以独立实施，对已有架构干扰小；Patch O 之后 review findings 自然变少（生成器路径不会有缺失）但 review 仍有价值（可发现意图错位 / 跨 device 对称性 / 数值越界等 LLM-only 能看的问题）。

**Peer-review 角色思考再补充**:

之前讨论"analyzer 自检 vs lab 先看"时漏掉一个根本点：**lab 是天然的"独立验证者"**——它对 sim 的输出无 prior，对设备真相有 prior（vendor expert memory）。让 sim 自检等于让作家自审稿；让 lab 看 = 编辑审稿。架构上 lab 看更对。Patch O' 把这个角色显式化。

**未决问题（"再思考"环节）**:

1. ReAct 单次 LLM call vs full multi-step ReAct loop：单次 LLM 拿 implementation + intent + expert memory 一次性输出 findings 应该够用。Multi-step ReAct（"先列出已有 CLI / 再查 vendor reference / 再对比 intent / 再列 gaps"）准确率更高但成本翻数倍。建议先单次试，不够再加。
2. 是否 block 部署：建议**不 block**。findings 是 advisory，CAB approver 决定。`severity=blocker` 时 lab 仍跑（毕竟 lab 是 SRL 数字孪生，prod CLI 完不完整不影响 lab 起 BGP），但写入 findings + verdict 改为 `PASS_WITH_BLOCKERS`。
3. Findings 多语言提示：当前 OLAV agent 都是英文 prompt + memory；findings 应该跟 agent 系一致英文，但 CAB approver 看的可能是中文界面，UI 层翻译。
4. 实施 Patch O' 之后是否还要 Patch O：分两步走更稳——先 O' 看 review 输出质量；如果 LLM review 漏报率高，再上 Patch O 把 ebgp_direct 这类标准 intent 改成确定性接管。

**ROI 估计**: ~2 小时（schema 加 1 字段 + 1 helper + 1 phase + 几个测试）。比 Patch O (~1 day + 大量测试) 一个数量级小。

---

**架构哲学澄清 (2026-05-07，对前面所有 patch 的重读)**:

CAB 本质是 **human-in-the-loop**。TCF 是人 / sim / lab 共享的 artifact，**人是唯一决策者**，agent 是按需 invoke 的工具：

- 没有 sim↔lab 自动 loop / 没有 max_revisions / 没有 no-progress detector
- 没有 sim-vs-lab 仲裁 / 没有 ESCALATED 状态
- 没有 cab_iterate composite 编排

人的工作流（典型 session）:

1. `/sim plan eBGP R1↔R3` → sim emit TCF v1
2. 人在编辑器 / UI 读 TCF v1
3. 人选下一步（**人决策，不是 agent**）：
   - 直接改 yaml 某行
   - `/sim revise: 在 R1 加 AS 65001`（sim 做**定向 patch**，不重写）
   - `/lab validate spec` → lab 写 findings，人再读
   - approve / 拒绝
4. 人觉得 OK → 部署；不 OK → 回 step 3

Agent 提供的是**数据 + 单步工具**，UI 把它们组装成 review 体验。

**Patch O' 在这框架下的最终定位**:

- **A 部分（lab 写 findings）保持**: 给人提供材料
- **B 部分**: sim 加 "patch 模式"（`tcf_patch_block(spec, device, add=[...], del=[...])`），保证人手编辑不会被覆盖
- **简化 revision tracking**: `revision_count`/`last_revised_at`/`last_revised_by` 三个字段足够审计，**不**追踪 "哪个 finding 触发哪次 revision"（那是收敛思维）
- **砍掉**: `RevisionEntry.triggered_by_findings`、`assumes_baseline` 仲裁字段、auto-loop / max-iter / no-progress detector、`verdict=ESCALATED`

ROI 重估: A 部分 ~2h（不变），B 部分 ~3h（patch_block helper + sim prompt 加 surgical-edit 工作流 + 测试）。两块独立可上。

---

### ISSUE-CAB-TVT-CHECK-LINK-MISSING — tvt.test_id 与 post_check.check_id 没有 schema 链接 (P2, ✅ Closed by Patch N `7816d79` 2026-05-07)

**症状**: Patch L composite 跑完后 `lab.verdict + lab.journal` 完整写回，但 `tvt[i].actual_lab` / `tvt[i].status` 全部保持 `None / PENDING`（除两条因更早 dry_run 失败被前置流程标 BLOCKED）。

**根因**: 实际 analyzer-emit spec 中：
* tvt: `TC-01`, `TC-02`, `TC-03`, `TC-04`（test plan 维度，按测试编号命名）
* post_check: `PC-01`, `PC-02`（runtime check 维度，按检查编号命名）

两套 ID 没有 schema 级别的 link 字段。Composite 当前用 `test_id == check_id` 启发式匹配，匹配 0 条 → 直接 skip TVT 更新。

**为什么 manual 流没暴露**: lab agent 之前用 LLM 智能映射（"TC-01 BGP up R1" 对应 "PC-01 R1 BGP check"），手填 `tvt_test_ids` / `tvt_actual_lab` / `tvt_status` 三个并行数组传给 `tcf_record_lab_run.py`。Patch L composite 没有 LLM，需要 deterministic 链接。

**修复方向 — Patch N (双层兜底)**:

1. **Schema 字段（长期）**：给 `TvtRow` 加 `evidence_check_ids: list[str] = []`，sim emit 时填上对应 post_check.check_id。让链路显式化、reviewer 可读、composite 直接消费。
2. **Index fallback（即时）**：当 `test_id == check_id` 无匹配 AND `len(post_check) == len(required_tests)` AND 两边 device 顺序一致，按 index zip 配对。Composite 在 journal 里记 "tvt-link-strategy: by_index"。
3. **Device-based fallback**：看 tvt.description 里是否含设备名（`"BGP up R1"` 里的 `R1`），与 post_check.device 匹配。可选第三层。

**优先级 P2** 因为 verdict + journal + post_check_results 已经够 reviewer 判断 PASS/FAIL；TVT 行更新更多是 audit-trail completeness。但 CAB approver 看到 TVT 全 PENDING 会困惑，仍值得修。

**Workaround**: 让 sim 在 emit 时刻意把 `tvt[i].test_id` 改成与对应 `post_check[j].check_id` 同名（例如都叫 `bgp_up_r1`）。

**关闭 — Patch N `7816d79` (2026-05-07)**: 三层 fallback 链路：(1) 新增 `TvtRow.evidence_check_ids: list[str]` schema 字段（默认 `[]` 兼容老 spec），sim emit 时显式填上对应 check_id；(2) `test_id == check_id` exact match（clean spec 走这条）；(3) device-name match in tvt.description（demo7 实测的"BGP up R1" → check on R1 的 case 全 4 行 link 成功）；(4) index-zip across `required_tests` × `post_check_results` 当 cardinalities 一致时。Strategy 用了哪一层记录在 journal `tvt_link.result_summary.strategy`，reviewer 能看见推断路径。In-vivo demo7 实测：4 个 TC-* test 行全部 link 到对应 PC-* check 并写入 actual_lab + status=PASS。

---

### ISSUE-V2-CH7-LAYERED-FAULT-DIAGNOSIS-STUCK — agent 在分层诊断 prompt 下不调工具 (P2, ✅ Closed 2026-05-05)

**关闭 — DeepSeek V4 Flash 实测通过 (2026-05-05, commit `c91a272`)**:

切换到 OpenRouter `deepseek/deepseek-v4-flash` (284B MoE, 13B active, 1M ctx) + 本地 bge-base-en + cohere rerank-4-fast。同一 Ch7 prompt 实测：

* **12 tool calls in <480s, exit clean (0)**
* 第 2 个 tool call (`SELECT * FROM netops.v_show_interfaces_terse_auto WHERE device_name='R1'`) 即给出**正确根因**:
  > R1 是 Juniper 设备。关键发现：`ge-0/0/0.0` (IP: 10.1.12.1/24) — 这是连接 BGP 对等体 10.1.12.2 的接口，但它的 link_state 是 down，且物理口 ge-0/0/0 也是 admin down + link down。
* 这正是 V2 Ch7 fault inject script 注入的故障（接口 admin down → BGP 无法建立邻居 → 状态卡 Idle）

**结论 — root cause 是模型 + prompt 综合**:
* gemma4:31b @ 64K ctx (Round 88-93)：0 tool calls / 8 min — 卡在不动
* Nemotron-120B @ 1M ctx 但 free-tier (2026-05-04)：14 tool calls / 600s — 写了 plan 但陷入 write_todos 循环未结论
* **DeepSeek V4 Flash @ 1M ctx 付费 (2026-05-05)：12 tool calls / 480s + 正确根因结论** ✅

DeepSeek V4 Flash 的 13B-active MoE 既快又准 + 1M ctx 让 plan 不必频繁压缩 + 付费 quota 没 free-tier 共享池 throttle，三因素叠加让 layered guide 的 instructional 结构终于不再是瓶颈。

**层叠 mitigation 路径回顾**:
1. R88 layered guide slim (2,486 → 1,838 chars, -26%) — 必要不充分
2. agent re-org Step 1+2 (`950c79b` + `9306c89`) — ops orchestrator 6→3 sub-agents — 必要不充分
3. recursion_limit 75→200 (`19cfe3e`) — 必要不充分
4. unified-sandbox refactor (`c91a272`) — 删除 4-mode 决策 + 2-level dispatch — 必要不充分
5. **DeepSeek V4 Flash 切换** — 临门一脚，让前 4 步累积修复终于通过

**guide rewrite 不再 P0**: 当前 prompt structure 在 13B-active 模型上够用了。如果以后回到 32B 以下本地模型，guide 改 prescriptive 仍然是值得的优化，但不再阻塞 demo。

**Cloud LLM retest 排除模型瓶颈 (2026-05-04, commits ec3f49b + 19cfe3e)**:
切换到 OpenRouter `nvidia/nemotron-3-super-120b-a12b:free` (120B MoE, 12B active) + 本地 `BAAI/bge-base-en-v1.5` embed + OpenRouter `cohere/rerank-4-fast` reranker（hybrid Layer）。同一 Ch7 prompt 实测：

* **第 1 次**: `GraphRecursionError 75` — langgraph 默认 limit 不够，12 tool calls 就撞顶。修复 ✅ commit `19cfe3e` 提到 200 + env `OLAV_RECURSION_LIMIT` 可调。
* **第 2 次（limit=200）**: 14 tool calls in 600s，agent 写了完整结构化 plan (SESSION INTENT / SUMMARY / NEXT STEPS) 然后**陷入 plan→write_todos→re-plan 循环**，未给最终根因。

**结论 — root cause 不在模型**: 120B cloud LLM (Nemotron) 比 31B 本地 (gemma4) 强一个量级，仍未通过 → 瓶颈是 `troubleshoot_layered_l1_to_l4.guide.yaml` 的 **instructional 结构**（教 agent "怎么 plan"），不是 ctx/能力上限。

**修复路径（确认）**: 把 guide 从 instructional 改成 **prescriptive 3-step terminal recipe + verdict**:
1. `SELECT platform FROM netops.devices WHERE hostname = '<host>'` → 决定 view 名
2. `SELECT * FROM <interface_view> WHERE device_name = '<host>' AND state != 'up'` → 直接 verdict
3. 路由 + ARP + log search 是分支条件，明确步骤号不要"agent 自由发挥"

预计 30-60 min。是 V2 demo runsheet 唯一明显 broken 的章节，应作为 work stream 收尾。

**进一步缓解 (2026-05-01 post-rename retest, commits 950c79b + 9306c89 + b3c7da5)**:
Step 1 (devops/infra → top-level) + Step 2 (admin fold) + 命名标准化 累积 reclaim 让 ops orchestrator 显著 lighter。同 prompt 下 Ch7 实测：
* **240s 内**: 4 个 execute_sql tool calls (devices → interfaces_terse → arp → ip_route) — 完整 L1→L3 layered query
* **480s 内**: 12 个 tool calls (execute_sql ×9 + search_logs ×2 + execute_cli ×1) — 走完 DB lookup 进入 live verification
* 依然 timeout 不完成 final synthesis，但从 "0 tool call (stuck)" 提升到 "actively diagnosing"

剩余瓶颈：gemma4:31b 在 64K ctx 下 synthesis 阶段慢，每 tool turn ~30-40s。升 70B+ 或扩 ctx 仍是终态修复。

**之前的部分缓解**:
* `troubleshoot_layered_l1_to_l4.guide.yaml`: 2,486 → 1,838 chars (-26%) — 改 narrative prose → ACTION-oriented "FIRST emit a tool_call, do NOT inline-recite"
* 实测后果（rename 前）: 仍 0 tool call。问题深 — 不是单 guide 内容，是 ops orchestrator + AutoRecall + tools' Pydantic schema 综合 30-40K prompt 让 gemma4:31b 64K ctx 几乎到顶，加 thinking-on overhead，agent 在前 8 min 没机会输出任何 tool_call

**根因（实证）**: gemma4:31b 在 OLAV 多 sub-agent + AutoRecall 重 prompt 下接近能力上限。直接简单查询（"how many devices?" → 1 SQL）work；触发 layered diagnostic AutoRecall 召回的多 turn 流（"BGP Idle 排查"）→ stuck。

**修复路径（不动 OLAV，仅运维）**:
* 升级到 70B+ 模型（gemma4-70B / qwen3.6-72B）
* 或扩 ctx 到 128K per slot (`--parallel 1 --ctx-size 131072`)，给 model 更多 thinking 余量
* 或用 cloud LLM (Anthropic / OpenAI) 端到端跑 Ch7

**修复路径（OLAV side, 长期 P3）**:
* 引入 plan-and-execute middleware: 第一步轻 prompt 决定路由 → 进 sub-agent 隔离 ctx → 返回汇总 → 不让 ops orchestrator 一次扛全部
* 进一步 slim ops orchestrator: 删 cross-workspace `core/writer` 引用 + 缩 sub-agent description 字段（已部分做）



**状态**: 🔴 Open · **优先级**: P2
**发现日期**: 2026-05-01

**症状**:
* 配置: gemma4:31b + Ollama /v1 + thinking on (默认)
* prompt: "R1 上 BGP 邻居 10.1.12.2 是 Idle 状态，从已有的 DB 数据排查可能根因"
* 实测: 240s 超时，**audit_tool_calls 0 entries**
* 同模型同配置下，"Analyze BGP convergence" 和 "Which interfaces on R3 are down?" 都正常调工具
* LLM endpoint 健康（curl 413ms 响应）

**假设**:
分层诊断 prompt 触发 AutoRecall 召回 `troubleshoot_layered_l1_to_l4` (1.8K chars) + `bgp_path_and_state_reference` (2.3K chars) + `take_snapshot_when_db_stale` (1.7K chars) 三大教学型 guides。模型陷入"读完该读什么再行动"的 thinking-overhead，迟迟不发 tool_call。

**对比 Ch6 Q1 ("Analyze BGP convergence")**：相对 open-ended，agent 自由探索，AutoRecall 召回中等量；表现良好。
**vs Ch7 ("R1 BGP 邻居 Idle 排查根因")**：精确诊断指令 + 触发完整 layered diagnostic pipeline → guide 注入大量 → agent 不动了。

**变通**:
* 用更具体的 prompt: "Show R1 interface ge-0/0/0 link state from netops.v_show_interfaces_terse_auto" (绕过分层框架直接 SQL)
* 或在 ops orchestrator 加 "if user reports specific BGP state issue, call execute_sql FIRST against interface views, THEN reason"

**对 Ch7 demo 影响**: V2 Ch7 layered fault diagnosis 这个章节当前 demo 不可重现。fault inject scripts 自身工作正常（已实测 inject + revert），但 agent diagnosis 那一步卡住。

**P2 候选修复**: 进一步 slim layered guide bodies，或改成 "minimal trigger + reference link to deep guide" 模式让 model 不 inline 阅读全文。

---

### ISSUE-LANGGRAPH-RECURSION-LIMIT-TOO-LOW — deepagents 多 middleware 栈下 75 unit 不够 (P2, ✅ Closed 2026-05-04 commit 19cfe3e)

**症状**: cloud LLM (Nemotron-3 120B) Ch7 实测 12 tool calls 就撞 `langgraph.errors.GraphRecursionError: Recursion limit of 75 reached`。

**根因**: langgraph 默认 `recursion_limit=75` 在 `cli/main.py:1101`。OLAV 的 deepagents 栈每个 turn 触发 ≥6 个 middleware (AutoRecall + Skills + SubAgent + Filesystem + Summarization + TodoList)，每个都是 graph node。一个 tool call 实际消耗 6-8 recursion units，所以 ~10 tool calls 就到顶。

**修复 (commit `19cfe3e`)**:
```python
_rec_limit = int(os.environ.get("OLAV_RECURSION_LIMIT", "200"))
config = {"configurable": {"thread_id": ...}, "recursion_limit": _rec_limit}
```
默认 200 给 layered 诊断流足够 headroom；env `OLAV_RECURSION_LIMIT=400` 可应付特殊深 task (e.g. multi-stage TCF lab validation)。真正的死循环仍在新 limit 兜底。

---

### ISSUE-OPENROUTER-FREE-TIER-RATE-LIMIT-VOLATILE — Google AI Studio 共享池极易 429 (P3, 🟡 运维注意 2026-05-04)

**症状**: 配 `google/gemma-4-31b-it:free` (或其它 Google 系 free model) on OpenRouter，5 秒间隔的 2 次 smoke test 均 429:
```
"google/gemma-4-31b-it:free is temporarily rate-limited upstream.
Please retry shortly, or add your own key to accumulate your rate limits"
```

**根因**: OpenRouter free-tier Google 模型走共享 Google AI Studio 池，被全平台用户挤满。文案明示需要 BYOK (bring-your-own-key 自己开 Google AI Studio 账号填 key) 才能逃脱共享池。

**变通**:
* **换 provider 系列**: `nvidia/nemotron-3-super-120b-a12b:free` (NVIDIA 自托管，远没那么挤) — 实测 0 rate-limit 跑通 Ch6 + Ch7 (600s 14 tool calls)
* `tencent/hy3-preview:free` / `inclusionai/ling-2.6-1t:free` / `poolside/laguna-m.1:free` 都验证可用
* 或开 Google AI Studio 自己账号拿 key，用 OpenRouter BYOK 模式

**对部署影响**: free-tier model 在 demo / 教学场景**不要选 Google**。生产应付费。

---

### ISSUE-V2-CH9-AUDIT-LIST-PROFILES-NO-TOOLCALL — audit agent 不调 read_profile 直接返回空 list (P3, ✅ Closed 2026-05-07)

**关闭 — DeepSeek V4 Flash 实测通过 (2026-05-07)**:

切换到 DeepSeek V4 Flash 之后，"List all available audit profiles" 实测：
* 9 tool calls in <240s, exit 0 ✅
* 5 个 glob/ls 尝试（agent 反射）+ 2 olav_delegate（一个 wrong subagent name `audit`，一个对 `audit-auditor`）+ 1 `list_profiles({})` skill script（最终成功路径）+ 1 execute_sql
* **返回完整 12 profile 列表**（与 disk truth 100% 匹配，含 BGP-focused 11 + drift 1）
* 输出 polished markdown table + 按 naming 模式的元数据分析 + 后续 action suggestions

通过累积修复（不需要新 patch）:
1. ce6edca (2026-05-01): R100/S4 deny 虚拟 FS read+list — 当 deepagents 支持时启用
2. 21e3c2f (2026-05-01): list_profiles.py skill script + audit/auditor SKILL.md 教学段
3. DeepSeek V4 Flash 切换 (2026-05-05): 比 gemma4:31b 强一档，能在多次 glob 失败后真正 pivot 到 skill script

agent 行为模式: **glob fallback → delegate → skill script → 答**。中间步多了点（5 glob + 2 delegate 都返回空），但最终通过 list_profiles skill script 给出正确答案。Quality > efficiency at this stage.

**之前的部分修复 (2026-05-01)**:
* 加 `audit/auditor/scripts/list_profiles.py` skill script — 直接 `ls audit/profiles/*.md` + 取第一行 H1。 实测 stdin pipe `python list_profiles.py` 返回 12 profiles + filename + size 干净 ✅
* 更新 `audit/auditor/SKILL.md` tools list + `## Listing available profiles` 教学段
* 更新 `audit/prompts/orchestrator.md` 加 LIST routing 规则 + "DO NOT call ls/glob/grep" 警告

**剩余问题**:
gemma4:31b **忽视 prompt 警告**，agent 仍用 deepagents 内置 `ls('/profiles')` / `glob('**/*.yaml')` / `grep` 而不 task("audit-auditor")。这与 R100/S4 同类（model 偏好简单虚拟 FS 工具）。 

**Post-rename 重测 (2026-05-01)**: 同 prompt "List all available audit profiles" 仍 240s timeout，trace 显示 3 次 glob 尝试 (`**/*.yaml` / `**/profiles/**` / `**/*profile*.yaml`) — 与 rename 前完全一致。重命名不影响此 issue，根因仍是 gemma4 偏好 deepagents 虚拟 FS 工具。

**未来 fix**:
* 添加 `FilesystemPermission(operations=["read"], paths=["/profiles*"], mode="deny")` 强制 model fallback 到 task delegation
* 或在 audit orchestrator 移除 ls/glob 工具暴露（R85 写类同思路）



**状态**: 🔴 Open · **优先级**: P3
**发现日期**: 2026-05-01

**症状**:
* `olav --agent audit "List available audit profiles"` 返回 `[]`
* `audit_tool_calls` 0 entries —— agent 没调 `read_profile.py` 也没调任何 listing 工具
* 看 `/.olav/workspace/audit/profiles/`：实际有 N 个 *.md profile 文件

**假设**:
audit agent 的 system prompt 没明确教 "list profiles" 这个动词的工具映射。或者 audit/auditor/SKILL.md 的 tools 列表里没暴露 list-profiles 工具（只有 `read_profile`，是读单个 profile）。

**变通**: 用 `ls .olav/workspace/audit/profiles/` 直接看（绕过 agent）。

**修复**: audit/auditor/SKILL.md 加一个 `list_profiles` 工具或在 prompt 里注明 "to list profiles, run `ls` on profiles dir via run_shell"。

---

### V2 Ch1-Ch14 batch test summary (2026-05-01) — gemma4:31b on Ollama /v1

| Ch | 状态 | 备注 |
|---|---|---|
| Ch1 install | ✅ | (earlier session) |
| Ch2 collect | ✅ | (earlier session) |
| Ch3 Q1-Q3 (core NL) | ✅ | (earlier qwen3.6) |
| Ch4 KB declarative | ✅ | (earlier) |
| Ch5 R102 multi-turn | ✅ | Playwright browser, fs draft persistence, BFD rule committed |
| Ch6 Q1 (BGP analysis) | ✅ | gemma4: 6 SQL + describe_table → "all Established" |
| Ch6 Q3 (R3 down ifaces) | ✅ | gemma4: 1 SQL + table format → "all up" |
| Ch6 Q2 (R5 change plan) | ⏸ | 未测 |
| Ch7 layered fault diagnosis | 🟡 | **post-rename retest (2026-05-01)**: 0 → 12 tool calls in 8min（从 stuck 升级到 actively diagnosing），synthesis 仍 timeout — see ISSUE-V2-CH7-... |
| Ch8 CAB | ⏸ | 未测 |
| Ch9 audit list | ✅ | DeepSeek V4 Flash 9 tool calls / 12 profiles 全部正确返回 — closed 2026-05-07 |
| Ch10 services list | ✅ | list_services → "no services registered" |
| Ch11 syslog search | ✅ | search_logs(hours=1, query='test') → "No matching" |
| Ch12 devops scripts | ✅ | exports/backup_routers.sh (1.1KB, R3+R4) |
| Ch13 write security | ⏸ | 未测 |
| Ch14 multi-user / training | ⏸ | 未测 |

**关键发现**:
1. **gemma4:31b 是当前最佳模型** — 比 qwen3.6:27b 在 OLAV 多 sub-agent 树下显著更稳，1-shot tool-call adherence 高
2. **简单 single-agent 工具流 (Ch10/11/12) 100% work**
3. **复杂多 sub-agent + 长 guide 注入 (Ch7) 仍可能 0-tool-call hang**（不是 LLM 端问题，是 agent prompt 太重模型不动）
4. **layered guide bodies 即使 slim 后还是教学型偏长，触发 thinking-overhead 不动**

**Post-rename 更新 (2026-05-01)**:
* Step 1+2 agent 折叠 + 命名标准化让 ops orchestrator 6 → 3 sub-agents 累积。Ch7 retest 实测从 stuck 跨过门槛能 actively diagnose（12 tool calls / 8 min L1→L3 layered + execute_cli），但 synthesis 阶段仍慢于 240/480s timeout。
* Ch9 retest 完全无变化，根因不是 prompt 体量而是 model 偏好选 deepagents 虚拟 FS 工具，需要从 audit agent 移除 ls/glob 工具暴露才能根治。

---

### ISSUE-OLLAMA-V1-CANNOT-DISABLE-THINKING — `/v1/chat/completions` 不接受 think field (P2, 🟡 Wontfix-upstream 2026-05-01)

**状态**: 🟡 Wontfix at OLAV layer · **优先级**: P2（性能 / 速度）
**发现日期**: 2026-05-01

**症状**:
Ollama 的 OpenAI-compatible `/v1/chat/completions` endpoint 不解析 `think: false` / `chat_template_kwargs.enable_thinking` / `/no_think` user message suffix。所有 4 种关 thinking 方法实测失败：

| 方法 | 结果 |
|---|---|
| `think: false` body field | ❌ thinking 仍跑 |
| `chat_template_kwargs.enable_thinking: false` | ❌ |
| `/no_think` user message suffix | ❌ |
| `reasoning_effort: minimal` | ❌ |

**唯一 work 路径**: native `/api/chat` + `think: false` —— 实测 `2+2=4` 624ms vs 默认 1461ms 还没出 content（thinking 占满 budget）。

**OLAV 用 langchain-ollama provider 接 native API 的尝试**: `reasoning=False` 单独 bind_tools 测试 work（emit tool_call 干净）。但走 deepagents `create_deep_agent` + 重 prompt 后 27B-dense 模型不发 tool_call（R100 实证："thinking is load-bearing for ReAct adherence on heavy prompt"）。

**结论**: 两个独立 trade-off：
* 关 thinking → 快但 27B 复杂 agent 不 reliable 调工具
* 开 thinking → 慢但 tool calls 稳定

**当前生产 config**: openai provider + Ollama /v1 + thinking auto-on（无法关）+ summarization armed via `profile.max_input_tokens=64000`。

**长期 P2 候选**: 用更大模型（70B+）让 thinking off 也能 ReAct，或写 OLAV-side OpenAI→Ollama-native HTTP proxy 转换 `think` field（但 langchain-ollama provider 已是这条路，问题是 deepagents 集成）。

---

### ISSUE-GEMMA4-SUBAGENT-DELEGATION-WITH-THINKING-OFF — gemma4 + thinking off 不调 sub-agent task() (P2, 🔴 Open 2026-05-01)

**状态**: 🔴 Open · **优先级**: P2
**发现日期**: 2026-05-01

**症状**:
gemma4:31b + ollama provider + `OLAV_DISABLE_THINKING=1`:
* ✅ 简单 query (`"How many devices?"`) → execute_sql 直接调用，1 SQL → "6 devices"
* ✅ 复杂 ops query (`"Analyze BGP convergence"`) → describe_table + execute_sql 多步 → 正确答案
* ❌ **R102 conversational ingestion**: agent 把 propose_memory_draft 内容**作为文本 inline 输出**，不调 `olav_delegate(memory_curator)` → 0 tool call

**实测 prompt**:
```
"请 propose 一条新规则到记忆库：BGP 路由收敛时间正常应小于 60 秒...
intent=bgp_convergence_time_baseline, agent=ops, scope=global"
```

**Agent 输出**: 文本说"我计划提交...拟提交内容...确认？[y/N]" + dump 召回的 memory entries。`audit_tool_calls` 显示 0 entries during 3 min。

**根因假设**:
gemma4 + thinking off 在重 prompt（orchestrator + 7 sub-agents desc + AutoRecall）时，对 `olav_delegate` 这种"间接"工具的语义理解力不足。直接的 `execute_sql`（明确动词 + clear input）能调，但 `olav_delegate(subagent_name=..., task_description=...)` 需要更多推理才能正确路由。

**变通**: gemma4 + thinking ON（`unset OLAV_DISABLE_THINKING`）应该能正确触发子代理。需要后续实测验证。

**对 V2 demo runsheet 影响**:
* Ch3-Ch4 (NL queries / KB declarative) — 跑 gemma + thinking off 没问题
* Ch5 (R102 conversational ingest) — 必须 thinking on，或 fall back to stdin pipe 直调 commit_to_memory.py
* Ch7 (layered fault diagnosis) — 需 ops sub-agent 多步，需 thinking on
* Ch8 (CAB) — 需 task("ops-lab")，需 thinking on

---

### ISSUE-SCHEMA-PUSH-VS-PULL — schema_knowledge push 模式应改为 describe_table pull 模式 (P1, 🔴 Open)

**状态**: 🔴 Open · **优先级**: P1（架构层 — 影响小模型可扩展性）
**发现日期**: 2026-04-30 (V2 Ch3 in-vivo 测试)

**症状**:
* `/netops_init` 给每个 v_show_*_auto 自动写一条 `schema_knowledge` 记忆 —— demo7 实测 52 条 / 27.9 KB on disk
* 加上 34 条 `value_distribution` —— 共 86 行预灌输的 schema 派生数据
* AutoRecall 每次注入 quota=2 schema_knowledge + 2 value_distribution 到 `<relevant-memories>` —— 但**召回不一定准**（cross-vendor view 通常排在 top-2 之外）
* 实证 Q2 "Show BGP neighbors on R1"：agent 没有用 cached schema_knowledge，而是自己 introspect（`information_schema.views WHERE LIKE '%bgp%'`）后正确推理 R1 是 Juniper

**根因 / 设计反思**:
* Push 模式（pre-prime schema 到 memory）原意是替小模型代劳 introspection
* 实证 27B-dense 在缺数据时**会自己 introspect**（Q2 展示）
* 现有 `describe_table` 工具（`core/db_query/tools/describe_table.py`）就是为 pull 模式设计的（ARCH-18 progressive disclosure），但**没暴露到 core orchestrator 工具列表**
* 净效果：push 投入产出比是负的——花了存储 + AutoRecall 槽位，agent 却 ignore 了

**修复路径**（~6 步，1-2h 实施）:
1. 提升 `describe_table` 到 core orchestrator 工具列表（软链 + SKILL.md）
2. 删 `prime_memory_at_ingest` 里 schema_knowledge + value_distribution 写入路径（~80 LOC 删除）
3. 加 `core/guides/schema_introspection_via_describe_table.guide.yaml` —— 1 个 routing guide：用 5-10 个 keyword（schema / columns / view / introspect / 字段）+ 一段"if SQL fails with column error → call describe_table('netops.<x>')"
4. 删 `_CATEGORY_QUOTAS["schema_knowledge"]` + `_CATEGORY_FETCH["schema_knowledge"]` （middleware.py）
5. 同样处理 `value_distribution`
6. 加 `OLAV_LEGACY_SCHEMA_PRIME=1` env 让需要的人能保留旧行为（escape hatch）

**净效果**:
* DB 减 86 行；AutoRecall 槽位释放 2+2 给 usage_guide / expert_knowledge
* Prompt 减 ~4-6 KB
* 架构对齐"introspection-on-demand"——小模型该走的路

**风险**:
* 27B-dense 边角案例可能不会 introspect（直接报"找不到字段"）—— prompt 工程问题，可加更强 hint
* `query_pattern` memory（捕获过的成功 SQL）依然保留，是 pull 模式的天然伴侣

---

### ISSUE-R102-MULTITURN-HITL-BROKEN — task() 子代理 stateless 让 R102 多 turn HITL 永远不 commit (P1, ✅ Closed 2026-04-30)

**关闭 (2026-04-30, commit `6c44091`)**: filesystem draft persistence 解决跨 turn ctx 缺失。新增 `propose_memory_draft` 工具（Turn 1 写 draft 到 `<workspace>/.curator_drafts/<intent>.draft.json`），扩展 `commit_to_memory` 加 `from_draft=True` 模式（Turn 2 读 latest draft + commit + 归档到 `committed/`）。memory_curator system.md 教学两 turn 流程：propose → confirm → commit-from-draft。

**In-vivo 验证（Playwright 浏览器多 turn）**: BFD Down ACL UDP 规则 — Turn 1 (306s) 渲染 YAML 预览；Turn 2 (288s) 用户回 "可以，入库" → agent 调 `commit_to_memory(from_draft=True)` → `guide_ops_bfd_down_acl_udp_check` 进 LanceDB + 写 `ops/guides/.../bfd_down_acl_udp_check.guide.yaml`。**第一次端到端 NL conversational ingestion 完成**。

**保留下来的 Open issues**:
* ISSUE-CTX-PROMPT-INFLATION-AT-EVERY-TURN — orchestrator 仍重复加载 sub-agent 描述（已部分修复，commit 9d51d7f / 44f7cec slim 了 -55%）

---

### ISSUE-R102-MULTITURN-HITL-BROKEN-HISTORICAL — task() 子代理 stateless 让 R102 多 turn HITL 永远不 commit (P1, ✅ Closed)

**状态**: 🔴 Open · **优先级**: P1（R102 核心 demo flow 实际无法完成）
**发现日期**: 2026-04-30 (V2 Ch5 浏览器多 turn 测试)

**症状**:
* User Turn 1: "记住一条 BGP Connect firewall 规则"
* Orchestrator → `task("memory_curator", "Add a new rule ...")`
* memory_curator 渲染 YAML 预览 + 询问 "Confirm? (Reply 可以/OK/yes/入库)"
* User Turn 2: "可以，commit 入库"
* Orchestrator → `task("memory_curator", "Add a new rule ...")`  *(同样的 task_description)*
* memory_curator **又重新渲染 YAML 预览**，不调 commit_to_memory
* LanceDB 永远没新 row

**根因（架构性）**:
deepagents `task()` 是 **stateless per call** —— 每次进入子代理是新 ctx，看不到前 turn 的 YAML 预览。memory_curator 在 Turn 2 拿到 "user said 可以" 但没有上下文判断 "这是对哪个 draft 的确认"，按 HITL 设计回到 "请先看预览" 状态。

**这不是 prompt 工程问题** —— 是 task() 隔离 + HITL 跨 turn 的根本不兼容。R102 设计文档（dev_docs/70）假设 memory_curator 跨 turn 有状态，**实际不成立**。

**修复路径**（按工程量）:

| 选项 | 实施 | 缺点 |
|---|---|---|
| A. memory_curator 用 filesystem 持久化 draft | Turn 1 写 `/tmp/curator_drafts/<hash>.yaml`；Turn 2 看到确认信号 + fs draft → commit | ~50 LOC，最可靠 |
| B. orchestrator 在 Turn 2 重新拼 task_description（带 Turn 1 YAML 上下文）| `core/prompts/system.md` 加规则："confirming a memory_curator proposal" | 依赖 27B-dense 跨 turn 推理一致性，不稳 |
| C. memory_curator 不用 task() —— orchestrator 直调 commit_to_memory | 取消 sub-agent 隔离，HITL 在 orchestrator 层 | 大重构 |
| D. 文档化 single-turn 用法（"rule + 审阅完毕请直接入库" 同 turn 给齐）| 0 LOC | 违反 HITL 安全设计 |

**实证 in-vivo（demo7, 2026-04-30）**:
* Playwright 真实浏览器多 turn 测试（绕开 60s tab kill 用 `playwright launch chromium`）
* Turn 1: 183.6s, agent 正确渲染 YAML 预览 ✅
* Turn 2: 185.8s, agent **olav_delegate 但 NO commit_to_memory** ❌
* `audit_tool_calls` 显示 Turn 2 触发了 `recall_memory` + `olav_delegate(memory_curator)` × 2，**但 commit_to_memory 0 次**

**当前所有 R102 测试通过路径都是 stdin-pipe 直调 `commit_to_memory.py`**，绕过 agent ReAct + sub-agent 隔离。即"工具层 work，端到端 NL 不 work"。

**优先级**: P1（核心 R102 价值无法兑现 NL 流程）。建议先做 A（filesystem 持久化）。

---

### ISSUE-CTX-PROMPT-INFLATION-AT-EVERY-TURN — orchestrator base prompt 接近 64K 地板 (P1, ✅ Closed 2026-04-30 commit 05a9f4b)

**关闭 (2026-04-30 commit `05a9f4b`)**: 真根因不是 prompt 太大 —— 是 **summarization 永不触发**。`compute_summarization_defaults` (deepagents/middleware/summarization.py:170) fallback 到 `trigger=("tokens", 170000)` 当 `model.profile.max_input_tokens` 未设。本地 llama.cpp / vLLM 模型都没这个 profile 字段 → summarization trigger 永远是 170K → 64K ctx 早就 overflow。

**附加 reclaim (2026-05-01 Step 1+2+rename)**: 命名标准化伴随 agent topology 调整 —— ops orchestrator 从 6 sub-agents 减到 3 (devops/infra → top-level + lab/analyze/collect 保留), core 从 5 sub-agents 减到 4 (admin 折入 services)。两改累积让 ops orchestrator base prompt 进一步降低，V2 Ch7 layered diagnosis 终于跨过 gemma4:31b 触发 tool_call 的能力门槛（pre: 0 tool / 8min stuck → post: 12 tool / 8min actively diagnosing）。这是一次 prompt-side reclaim 的 quantitative 收尾验证，但仍是渐近优化，根因修复仍是 priming + 运维扩 ctx。

**修复**: OLAVAgent.__init__ 在 `create_deep_agent` 之前 prime `self.llm.profile["max_input_tokens"]` 来自 `llm.context_budget` (api.json) 或 `OLAV_LLM_CONTEXT_BUDGET` env。deepagents 自己的 `compute_summarization_defaults` 然后 picks fraction-based path (0.85 × 64000 = 54400 tok trigger) 自动。

设置 `llm.context_budget=64000` in api.json 后，实测 priming 生效（`agent.llm.profile = {'max_input_tokens': 64000}`），summarization trigger ≈ 54400 tok。多 turn ctx 累积现在会在 ~85% 时 auto-summarize 而非撞 64K 墙。

**4 轮 slim 仍然有价值**：把 base prompt 从 ~25K 砍到 ~12K，让 summarization 有更多 headroom 去保留对话连续性。但**核心修复是 priming**。



**状态**: 🟡 Mitigated（设计层穷尽，余下需运维扩 ctx）· **优先级**: P1

**4 轮 slim 累积量化**（commit 9d51d7f / 2e60fdd / 44f7cec / 76ae6ca / + ABD round 4）:
* core/prompts/system.md           7,225 →  3,540  (-51%)
* 5 core/guides/*.guide.yaml      27,052 →  9,827  (-64%)
* ops/prompts/orchestrator.md      8,638 →  3,683  (-57%)
* ops/analyze/SKILL.md             7,522 →  3,696  (-51%)
* ops/lab/SKILL.md                 6,313 →  3,784  (-40%)
* audit/auditor/SKILL.md           4,218 →  3,068  (-27%)
* ops/AGENT.md                     3,427 →  2,102  (-39%)
* ops/SKILL.md                     2,742 →  1,933  (-29%)
* 7 sub-agent description fields  ~1,500 →    700  (-53% × 2 occurrences = -1.6K)
* ops orchestrator tools 13 → 11 (removed register_service / record_network_event / search_commands / diff_configs Pydantic schemas) → -2.4 KB
* ops/analyze ROUTING_EXPERT_GUIDE 自动加载关闭 → -3.2 KB on BGP keyword match
* `prime_memory_at_ingest` schema_knowledge / value_distribution push 默认关 (commit 9d51d7f) → AutoRecall slot 释放 4 个

**累计 OLAV-side 节省**: ~50% workspace prompt budget, 但实测 LLM 仍报 66136 tok > 65536 ctx。

**根因**: deepagents 框架内置占地板：
* `BASE_AGENT_PROMPT` ~500 tok（不可触）
* 5 个 deepagents 中间件各自的 system prompt 段（TodoListMiddleware / FilesystemMiddleware / SubAgentMiddleware / SkillsMiddleware / SummarizationMiddleware）~1,500-2,500 tok 集合
* `task` / `write_todo` / `read_todo` / `list_files` / `read_file` / `write_file` / `edit_file` / `get_skill` / `list_skills` / `get_static_context` 等 framework tools 的 Pydantic JSON schema ~3-5 KB
* 我们注册的 13 个域工具的 Pydantic schema ~3-5 KB
* AutoRecall middleware 注入 ~1.5-2.5 KB
* 总 base 约 30-40K tok —— 与 OLAV 工作区无关，纯框架地板

**净结论**: 我们做了所有 OLAV-side 合理的事；剩下要解只剩运维扩 ctx。

**运维侧解（推荐）**: llama-server 配置 `--parallel 1 --ctx-size 131072` 单 slot 占全 128K（牺牲并发；demo / 小团队无关）。或 parallel=2 ctx=262144 总 256K。

**已闭环的子修复**:
* schema push → pull (ISSUE-SCHEMA-PUSH-VS-PULL ✅)
* multi-turn HITL via fs draft (ISSUE-R102-MULTITURN-HITL-BROKEN ✅)
* orchestrator + sub-agent SKILLs slim (4 rounds)
* auto-prime at install
* dim safety fail-fast

**长期 P2 候选**（如果觉得运维扩 ctx 不够好）:
* 改写 OLAV 工具的 Pydantic schema 为最小化 (`description=""` 给非关键字段)，节 ~2-3K tok
* 在 OLAVAgent 层覆盖 deepagents 的某些 middleware（去掉用不上的）—— 风险高
* 引入 plan-and-execute middleware 让 orchestrator base ctx 不重复加载（需自研）

---

### ISSUE-CTX-PROMPT-INFLATION-AT-EVERY-TURN-HISTORICAL — 历史描述 (P1, 🟡 Mitigated)

**状态**: 🔴 Open · **优先级**: P1（架构 —— 阻塞 V2 Ch3 Q3 测试）
**发现日期**: 2026-04-30 (V2 Ch3 Q3 ctx overflow 67570 > 65536)

**症状**:
* 单次 `olav --agent ops "Which interfaces on R3 are down?"` → ctx overflow 67570 tok
* AutoRecall + per-category quota 都设得很合理（schema_knowledge=2, usage_guide=3, ...）—— memory 注入 ~3-5 KB 不是元凶
* 真凶是**prompt 树本身**：每次 model 调用吃下：
  * core orchestrator system + cheatsheet ~2K
  * 6 sub-agent 描述 ~10K
  * ops 层 + 4 子代理 SKILL.md ~8-10K
  * on_intent references（routing expert / netbox api / lab / ...）~10-15K
  * 工具签名 ~3-5K
  * AutoRecall <relevant-memories> ~5-8K
  * + 累积的 tool 结果 ~10-30K
  * **总和 60-80K**

**架构问题**:
* 现状 = "全树 always-on" + "AutoRecall progressive" 的混合
* 真正应该 = **plan-and-execute**：orchestrator 只做路由（thin prompt），每个 step 进入 sub-agent 时是**隔离 ctx**，加载该步所需的最小集
* deepagents 原生支持 `task()` 子代理 delegation —— 子代理在**自己的 ctx 窗口**跑（ctx 隔离），返回时只把结果摘要带回 orchestrator
* 但 OLAV 当前 orchestrator 仍把所有子代理 SKILL.md 放在自己的 system prompt 里"备查"——重复 deepagents 已经做的事

**deepagents 已有 / OLAV 已用**:
* ✅ `task("ops_analyze", "...")` 子代理 delegation —— ctx 隔离
* ✅ SkillsMiddleware on_intent 加载（ADR-0008 / R92）
* ✅ `read_todo` / `write_todo` 跨步骤状态
* ✅ filesystem 工具用于跨 step 状态传递
* ❌ orchestrator 自己的 system prompt 仍然臃肿（包含全部 sub-agent 描述）

**修复路径**:
1. **Orchestrator 瘦身**: `core/prompts/system.md` 只保留路由表 + 基本工具（execute_sql / recall_memory / olav_delegate）—— 删所有 sub-agent 描述（让 `olav_delegate` 工具的 description 列表替代）
2. **Sub-agent 描述移到 task tool 的 schema**: `olav_delegate` 工具的 args schema 里枚举可用 sub-agent 的"intent + when to use"（不超 ~50 字/个）
3. **Sub-agent 内部走 AutoRecall**: 每个 sub-agent 进入时是新 ctx，触发 AutoRecall 召回**该 sub-agent scope** 的 memory（services / ops / audit / ...）
4. **量化目标**: orchestrator base prompt < 5K tokens, sub-agent base prompt < 8K tokens

**deepagents 是否原生这样做**:
* ✅ ctx 隔离（`task` tool）原生支持
* ❌ orchestrator 瘦身需要 OLAV 自己改 prompts —— deepagents 不强制
* ❌ "plan first then execute" 没有原生 planner middleware —— 需自己加（参考 LangGraph plan-and-execute pattern）

**短期 vs 长期**:
* **短期 unblock Ch3 Q3**: 把 llama.cpp ctx 从 65K 提到 128K（运维操作，5 min）
* **长期 P1**: orchestrator 瘦身 + plan-and-execute middleware

**相关 issue**: ISSUE-LLM-CTX-OVERFLOW-COMPLEX-AGENT-FLOW（先前已"运维侧关闭"——但本质未修，改了 ctx 大小不动架构）

---

### ISSUE-EMBEDDING-FALLBACK-DIM-MISMATCH-DESTROYS-DATA — local fallback (768) 触发 LanceDB drop-and-recreate 清空用户记忆 (P0, ✅ Closed 2026-04-30)

**关闭 (2026-04-30)**: `LanceDBStore` 现在 fail-fast — dim mismatch 时抛 `EmbeddingDimMismatchError`，**绝不**静默 drop 表。`add_memory` 也加了 vector dim 守卫，拒绝 wrong-dim 写入。Escape hatch：`OLAV_ALLOW_DESTRUCTIVE_DIM_MIGRATION=1`（debug-only）。`__init__` 改为 eager connect 让 mismatch 在构造时就暴露，而不是等第一次 query 才发现。

**修改的文件**:
* `src/olav/core/memory/__init__.py`:
  * 新加 `EmbeddingDimMismatchError` 异常类，错误信息含 fix 指引（包括 fallback 配置 + escape hatch env var）
  * `_check_and_migrate_vector_dim` 默认 raise 而非 drop；env var 控制 destructive opt-in
  * `add_memory` 在 injection scan 之前加 vector dim 检查，wrong-dim → `status="error"`，**不写入 table**
  * `__init__` 末尾 eager `self.connect()` 让 dim mismatch 在构造时立即抛出
* `tests/unit/test_embedding_dim_safety.py` (新增, 5 tests):
  * `add_memory` 拒绝 wrong-dim vector + 接受 correct-dim
  * 重新打开 store 时 dim mismatch 抛错且**保留原表数据**
  * `OLAV_ALLOW_DESTRUCTIVE_DIM_MIGRATION=1` opt-in 路径
  * 错误信息可操作（含 env var + fallback 提示）
* `tests/unit/test_memory_dim_migration.py` (重写, 6 tests): 把原本钉住"auto-drop"的测试改写为钉住"raise + 保留数据"

**验证**: 142 memory-related tests 全绿（含 11 个新 dim 测试）。

**余下可选改进**:
1. fallback embedder 启动时探测 dim，dim mismatch 直接拒绝启用 fallback（防御层 +1）
2. dim-mismatch 时把事件写 `audit.duckdb` —— 谁在什么时候导致 schema/dim 漂移可查
3. 为现有用户提供"显式迁移工具"（re-embed every row at new dim）

**Demo7 已临时禁用 fallback**：`api.json` `embedding.fallback.enabled=false`，注释指向本 issue。修复落地后可重新启用。

---

### ISSUE-EMBEDDING-FALLBACK-DIM-MISMATCH-DESTROYS-DATA-HISTORICAL — 历史描述 (P0, ✅ Closed)

**状态**: 🔴 Open · **优先级**: P0（数据安全 — 用户写入的记忆静默丢失）
**发现日期**: 2026-04-30 (R102 in-vivo 验证时)
**触发设备**: demo7（qwen3.6-27b-dense + qwen3-vl-embedding-2b 2048-dim API + BGE-768 local fallback）

**症状**:
1. 用户通过 `memory_curator` 子代理写入 NetBox tenant rule 到 LanceDB（2048-dim）
2. 后续 agent 调 `recall_memory(query=..., scope='"services"')` 时
3. 看到 stderr：`Embedding dim mismatch on table 'memory' (stored=2048, embedder=768). Dropping and recreating — existing rows will be lost.`
4. 整张 `memory` 表被 drop 重建：12 个 usage_guide + 用户刚 commit 的 NetBox rule 全部清空

**根因**:
* `api.json` 配 `embedding.fallback.enabled=true`
* API embedder = `qwen3-vl-embedding-2b` (2048-dim)
* Local fallback embedder = `BAAI/bge-base-en-v1.5` (768-dim)
* 中间某次 API 调用瞬时失败 → fallback 触发 → 返回 768-dim
* `LanceDBStore` 检测 dim mismatch → **drop-and-recreate** 而不是 raise/abort

**问题严重性**:
* 数据是用户级 ground truth（团队规则、runbook 摘要）
* drop 是**静默的**——只在 stderr 一行 warn，不阻断 agent 流程
* R102 memory_curator 把这条放大成了 P0：用户语义入库越方便，丢失越频繁

**临时缓解（demo7 已落地）**:
* `.olav/config/api.json` `embedding.fallback.enabled` → `false`
* 注释里写明"local fallback 与 api dim 不匹配，重启需先修平台 bug"

**根本修复（待实施）**:
1. **拒绝写入**：`add_memory(vector)` 检查 `len(vector) != self.embedding_dim` → raise，不写入
2. **永不 drop 用户数据**：dim mismatch 时报错抛 RuntimeError，让上层决定（迁移、重建、放弃），绝不静默 drop
3. **fallback 必须 dim-compatible**：fallback embedder 的 dim 必须匹配主 embedder 才能开启
4. **审计 trail**：dim 切换 / 表 schema 变化必须记入 `audit.duckdb`

**验证方式**: 写一个 `tests/integration/test_embedder_fallback_dim_safety.py`：
* 主 embedder 返回 2048
* fallback 返回 768
* `add_memory(...)` → 期待 RuntimeError，**期待 LanceDB 表完整保留**
* 现状会失败 → red bar

**相关日志**: 见 demo7 终端输出（embedding dim mismatch 报错紧跟在 `🔧 recall_memory` 之后）

---

### ISSUE-MULTIMODAL-MEMORY-VL-EMBEDDER-CAPABILITY-WASTED — VL embedding model 加载了但 OLAV schema 不支持图像 (P2, 🟡 Tier 1 planning)

**状态**: 🟡 Tier 1 设计完成 (dev_docs/68 = R100)，未实施 · **优先级**: P2
**发现日期**: 2026-04-29
**相关设计**: `dev_docs/68. R100_MULTIMODAL_MEMORY_AND_TOPOLOGY_GRAPHICS.md`

**症状**:
用户切到 `qwen3-vl-embedding-2b-q5_k_m` 是为了支持拓扑图形的 embedding 检索。但 OLAV `LanceDBStore.memory` 表只有 `text VARCHAR` 一列存内容 —— 即使 VL 模型能 embed 图，"被嵌入的图本身"没地方放：

```python
def add_memory(id, text, vector, ...):  # ← 只接受 text
```

净效果：VL embedder 在做纯 text embedding 工作，图像能力被 schema 埋掉。

**修复路径** (R100 三层设计):
* **Tier 1（推荐先做，~110 LOC，~1d）**: 利用 OLAV `format_and_export` 输出 mermaid 文本的现状 —— `capture_topology_artifact()` 把 mermaid source 当作 embed text 写入 `category="topology"`，metadata 存渲染图路径。零 schema 变更、零 LLM 变更、覆盖 ~95% topology 用例。
* **Tier 2（待用户故事触发）**: schema 加 `content_kind` + `media_path`，blob 存外面，`image_describer` 工具生成 caption。~290 LOC + schema migration。
* **Tier 3（更远）**: 换多模态 LLM，端到端 image-as-token。500-800 LOC，触及多核心层。

**触发 Tier 2 的具体用户故事**: 用户上传截图、第三方文档里的位图、要"找过去那张报警截图"等纯位图检索需求。Tier 1 不覆盖。

---

### ISSUE-DEEPAGENTS-WRITE-FILE-VIRTUAL-FS-DECEIVES-AGENT — write_file 假成功欺骗 agent (P1, ✅ Closed)

**关闭 (2026-04-29, R100/S4, commit `42ff7db`)**: 注入 `FilesystemPermission(operations=["write"], paths=["/**"], mode="deny")` 到 `create_deep_agent(permissions=[...])`。模型试 `write_file` → PermissionError → 自动 fallback 到 `format_and_export`（写真磁盘）。Env 关闭：`OLAV_ALLOW_VIRTUAL_FS_WRITES=1`。依赖：deepagents >= 0.5.3。

**v9 验证（complex prompt）**: 3979 字节 / 104 行生产级 bash 落到 `exports/scripts/backup_running_config.sh` —— 本地 27B-dense 第一次端到端通过 Ch8 canonical 复杂 prompt。

---

**状态（历史）**: 🔴 Open · **优先级**: P1 (阻塞 demo Ch8 复杂版)
**发现日期**: 2026-04-29 (Ch8 v8 — canonical complex prompt)

**症状**:
v8 跑 Ch8 原版（"Write a bash script to backup running-config..."），ctx=65536 充足，agent 行为：

1. ✅ execute_sql 取得 R1-R4 设备信息
2. ❌ **agent 选 deepagents 默认的 `write_file`** 而不是 OLAV 的 `format_and_export`
3. write_file 把 bash script "保存"到 LangGraph state["files"] 虚拟 FS（不是真磁盘）
4. agent 收到 success result，以为已落盘
5. agent 调 run_python_simulation 想 `os.chmod(path)` 那个 "文件"
6. chmod 找不到（或撞到昨天遗留的旧文件）
7. agent 循环重试 chmod ~20 分钟到 wrapper timeout
8. 最终 mtime 不变（昨天 4月28日 20:40），没新文件

**根因**:
deepagents `FilesystemMiddleware` 默认注入 `write_file` / `read_file` / `edit_file` 操作 LangGraph state["files"] 虚拟 FS，**不写真磁盘**。但 tool 描述/返回结果看起来像是写盘成功。OLAV 自己的 `format_and_export` 才写真磁盘。两者并存时，模型在开放式 prompt 下可能选错。

直接证据：v6（明确 prompt "save markdown table，SQL exactly..."）走 format_and_export 成功。v7/v8（开放 prompt "Write a bash script... Save to exports/"）走 write_file 失败。

**修复路径（按 ROI）**:

1. **关闭 deepagents write_file（推荐）** —— 用 `permissions=[FilesystemPermission(operations=["write"], paths=["/**"], mode="deny")]` 注入到 `create_deep_agent()`。模型 emit write_file 时收到 PermissionError，自动 fallback 到 format_and_export。约 ~30 LOC + 测试。OLAV agent.py:400 加 permissions kwarg。
2. **替换 write_file 实现** —— monkey-patch deepagents 让 write_file 走真磁盘 (与 format_and_export 同等)。比 1 复杂，行为变化大，可能影响其他 sub-agent 的 cross-turn 数据传递。
3. **更强 SKILL.md / directive** —— 已经做过，effective 在 v6（约束 prompt）下，对 v7/v8（开放 prompt）失效。Memory-tier 修不动。

**v6 vs v7/v8 总结**:
| version | prompt 形式 | tool 选择 | 落盘 |
|---|---|---|---|
| v6 | 强约束（"SQL exactly: ..."）| format_and_export | ✅ |
| v7/v8 | 开放（"Write a bash script..."）| write_file (虚拟 FS) | ❌ |

---

### ISSUE-LLM-CTX-OVERFLOW-COMPLEX-AGENT-FLOW — 32K ctx 不够 OLAV 多 turn agent 累积 (P1, ✅ Closed 运维侧)

**关闭 (2026-04-29 运维侧)**: docker-compose `qwen-llm` 的 `--ctx-size 32768` → `65536` (parallel=1) 后 v8/v9 不再撞 ctx 限制。q8_0 KV 占用 ~2KB/token × 65536 = 128MB 增量。
v9 实测：6+ turn 复杂 agent flow 在 65K ctx 下顺畅完成。
**OLAV 侧未来增强（可选 deferred）**: conversation trimming 自动管理超长 turn，防止用户 ctx 配低时的退化。当前 65K 对 demo 充足。

---

**状态（历史）**: 🔴 Open · **优先级**: P1 (阻塞 demo Ch8 复杂版)
**发现日期**: 2026-04-29 (Ch8 v7 — canonical "Write a bash backup script" prompt)

**症状**:
v7 跑 Ch8 原版 prompt（"Write a bash script to backup running-config from all routers"，开放式），agent 行为正确：
1. ✅ 1 次 execute_sql 取 routers (R1-R4)
2. ✅ write_file 写 backup_running_config.sh (但未完成)
3. ✅ run_python_simulation chmod +x 该文件
4. ❌ 下一轮 LLM 调用：`request (35039 tokens) exceeds the available context size (32768 tokens)`

总体 prompt 累积超 32K 是因为：
- system prompt + tools schema ≈ 12K
- AutoRecall memory inject ≈ 1K
- thinking_content (qwen3.6 reasoning per turn) ≈ 500-1500/turn
- tool result blocks (SQL data dump, simulation output) ≈ 2-5K/turn
- 5+ turn 后 ≈ 35K

**修复方向（按 ROI）**:
1. **运维侧**: docker-compose 给 qwen-llm 容器加 `--ctx-size 65536` (或更高)。q8_0 KV 占用 ~2KB/token × 65536 = 128MB 增量，对 24GB 卡完全不是问题。
2. **代码侧**: OLAV 加 conversation trimming —— LangGraph state 里保留最后 N turn + 摘要前面的，再喂模型。R85 的 inline-save 路径已经做过类似裁剪，可以推广。
3. **混合**: 65K ctx 给生产 + OLAV 在 80% 阈值时自动 trim 较老 turn。

**v6 vs v7 对比**:
| version | prompt 复杂度 | turn 数 | 总 token 累积 | 结果 |
|---|---|---|---|---|
| v6 (devices_v6) | 显式 SQL，单一表 | 2 | ~16K | ✅ 写文件成功 |
| v7 (canonical) | 开放，需推断 SQL + script logic + chmod | 5+ | 35K+ | ❌ ctx 溢出 |

---

### ISSUE-VL-EMBED-BATCH-SIZE-TOO-SMALL — qwen-embed 容器默认 batch=512 阻塞 OLAV 1.5K+ token prompt (P1, ✅ Closed)

**关闭 (2026-04-29, two-sided fix)**:
- **运维侧**: docker-compose `qwen-embed` 加 `--batch-size 4096 --ubatch-size 1024 --pooling mean --ctx-size 8192`。
- **OLAV 侧 (commit `f9b37bf`)**: `embed_text` 默认 input cap 6000 chars (≈1500-2000 tokens)，超长 input 静默截断 + debug log。Env override：`OLAV_EMBED_MAX_CHARS=N` (0=disable)。5 unit tests 覆盖。
组合保护：服务器侧 + 客户端侧都有边界。

---

**状态（历史）**: 🔴 Open · **优先级**: P1
**发现日期**: 2026-04-29 (Ch8 rerun on 35B-A3B + VL embed)

**症状**:
切到 llama.cpp full-stack（11433 router）后跑 OLAV agent，AutoRecall 调 embed 接口 500 报错：
```
embed_text failed: Error code: 500 - {'error': {'code': 500,
  'message': 'input (1442 tokens) is too large to process. increase
  the physical batch size (current batch size: 512)',
  'type': 'server_error'}}
```

OLAV 的 embedding 输入是 (system_prompt + user_message + tool_outputs) 串接，1.5-3K tokens 是常态。VL embed 容器启动用的默认 `--batch-size 512`，处理不下。

**修复**:
docker-compose 的 `qwen-embed` 块加：

```yaml
qwen-embed:
  command:
    - "-m"
    - "/models/qwen3-vl-embedding-2b-q5_k_m.gguf"
    - "--embedding"
    - "--pooling"
    - "mean"
    - "--ctx-size"
    - "8192"            # 原 2048 → 8192（embedding 模型自身 max-seq 通常 8K）
    - "--batch-size"
    - "8192"            # 关键：embedding 一次性吞整个序列
    - "--ubatch-size"
    - "8192"            # 同上，避免 micro-batch 切分降速
    - "-ngl"
    - "99"
```

**为什么不只是改 batch-size**:
- `--batch-size` 控制 prompt eval 一次喂多少 token（chat 用）
- `--ubatch-size` 控制单次 GPU forward 计算的 token 数（embedding 用）
- embedding 模型对单个输入做 1 次 forward，所以 ubatch 必须 ≥ 输入长度
- 同时 ctx-size 至少要 ≥ 输入长度

**当前症状下游**:
AutoRecall 静默失败 → directive guides 召不回 → 27B/35B agent 进入 "no memory" baseline → tool-call adherence 退化（见 ISSUE-QWEN-CLASS-IGNORES-DIRECTIVE-RECALL）

---

### ISSUE-QWEN-CLASS-TOOL-CALL-ADHERENCE-GAP — qwen3 系模型本地推理"知规则不能执行" (P1, ✅ Closed via code-layer fixes)

**关闭 (2026-04-29, 多 commit 组合修复)**:
本来认为是 model adherence 问题，prompt + memory 治不动。最终发现是 **schema permissive + 工具混淆 + 输入清洗缺失** 三个 code-layer 问题叠加，code 修复完之后本地 qwen3.6-27b-dense 端到端跑通：

| 修复 | commit | 作用 |
|---|---|---|
| `format_and_export(data: Any → str)` 严格 schema | `c2ce4f2` | LLM 看到清晰 string 契约，不再被 Any 引导嵌套 dict |
| `_strip_quote_leak()` 防御性输入清洗 | `c2ce4f2` | 抹掉模型在短字段 (filename/format/subdir) 烧入的字面引号 |
| 删 lenient dict-extraction logic | `c2ce4f2` | 让 ValidationError 浮上来，不再静默吞错产假成功 |
| `embed_text` input cap | `f9b37bf` | AutoCapture 长会话不再 batch 撑爆 |
| `FilesystemPermission deny write` | `42ff7db` | 阻断 deepagents 虚拟 FS write_file，强制 format_and_export |
| docker-compose ctx 32K → 65K | (运维) | 多 turn agent flow ctx 充足 |

**v6 (简单 prompt) 通过证据**: `exports/reports/devices_27dense_v6.md` 332 字节，干净 markdown 表，路径正确。
**v9 (canonical 复杂 prompt) 通过证据**: `exports/scripts/backup_running_config.sh` 3979 字节，104 行生产级 bash（vendor-aware Juniper SCP / Cisco IOS SSH 分支，timestamp 备份目录）。

**核心收获**:
prompt + memory 治不了 args malformation，schema strict + defensive coercion 能。本地小模型 adherence 不是对话工程问题，是 code 工程问题。

---

**历史状态**: 🔴 Open · **优先级**: P1 · **多轮诊断后定型**

**状态**: 🔴 Open · **优先级**: P1 · **多轮诊断后定型**
**发现日期**: 2026-04-29
**最终诊断 (v1-v4 series, 2026-04-29)**:
4 轮在同一硬件 / 同一模型 / 同一任务下的对照实验，确认 **27B-dense 不是"看不懂规则"，是"生成不出正确 args"**。

**测试基线**: demo7 Ch8 — `Save a markdown table to exports/devices_X.md.
The SQL is exactly: SELECT hostname AS name, ip_address, platform FROM netops.devices ORDER BY hostname.`

| run | 配置 | format_and_export 调用 | 结果 |
|---|---|---|---|
| v1 | qwen3.6-27b-dense, 无 calling-convention guide, thinking on | ✓ but `data={'format':'md','subdir':'reports','filename':'X'}` (nested dict 错配) | ❌ tool 失败 → fallback `write_file` → 最终也无文件 |
| v2 | + calling-convention guide primed (`guide_core_format_and_export_calling_convention`) | ✓ but **字符串值都带字面引号** (`"reports"` 真的是带引号的目录名)，data 内容是上次 write_file 的 stringified return | ⚠️ 文件存在但 path 全错，内容是 dict 字符串 |
| v3 | + thinking OFF (`OLAV_DISABLE_THINKING=1`, chat_template_kwargs.enable_thinking=False) | ❌ 完全不调 format_and_export | ❌❌ 输出 fake JSON memory dump (regression vs v1) |
| v4 | thinking back on, guide primed | ✓×3 全部 `data={'path':'...','size':2...}` (拿 write_file return 作 content) | ❌❌❌ 模型自己说 "That wrote incorrect content due to the malformed `data` argument. Let me fix..."，下一次 tool_call **一字不差再错一遍** |

**v4 决定性证据**:
```
🔧 format_and_export({'data': {'path': '...', 'size': 2...}})         ← 错一遍
"That wrote incorrect content due to the malformed `data` argument."  ← 模型自我诊断
🔧 format_and_export({'data': {'path': '...', 'size': 2...}})         ← 完全相同的错
🔧 format_and_export({'data': {'path': '...', 'size': 2...}})         ← 又错一遍
```

**关键结论**:
- ✅ Memory 注入 + reranker (R99/S2) 工作 — directive guide + calling-convention guide 都流入了 context（监控可见）
- ✅ 模型 reasoning 能识别问题（"malformed data argument"）
- ❌ 模型生成 tool_call args 时**机械重复同一错误**，prompt-tier 修不动

**Prompt + memory 治不好的原因**:
- 模型不是"没看懂规则"，是"看懂了但生成不出"
- LangChain → OpenAI client → llama-server 这条路径上，模型每次生成 `arguments` 字段都把它当作"我对工具理解的概要"而非"严格的 keyword args dict"，把 path/size/format 等都嵌套进 `data` 字段
- 增加 prompt example、换措辞、加 directive 都不能改变模型的低层 generation 偏好

**真正能修的路径（按 ROI 排）**:
1. **GBNF / json_schema strict args**（高 ROI，建议下一轮做）—— llama.cpp 支持 `response_format=json_schema`，token-level mask 强制 `arguments` 字段必须满足 `{data: string, format: string, ...}` 严格 schema。模型物理上无法生成嵌套 dict。约 100-150 LOC + per-tool schema 维护。
2. **简化 format_and_export 签名**（中 ROI）—— 把 4-param 砍到 3 param flat strings (`content`, `format`, `filename`)，subdir 由 format 推断（已经是默认行为）。约 30 LOC。
3. **换更强本地模型**（高代价）—— qwen3-72b-dense / Llama-3.3-70b 等，VRAM 翻倍。
4. **demo 切 cloud LLM**（短期止血）—— deepseek-v4-flash 在 Ch8 早期通过。

**Thinking 模式的发现（v3）**:
disable thinking via `chat_template_kwargs.enable_thinking=False` 节省 ~95% token（"2+2?" 152 → 8 tokens），但 27B-dense 失去 ReAct adherence 的"思考骨架"，**adherence 严重退化**（从"会调工具但 args 错"退到"完全不调工具，输出 fake JSON memory dump"）。
**结论**: thinking 是 qwen3.6:27b ReAct adherence 的 load-bearing 机制。OLAV 默认保持 thinking ON，env var `OLAV_DISABLE_THINKING=1` 仅供非-agent 场景调试用。

---

### ISSUE-QWEN-CLASS-IGNORES-DIRECTIVE-RECALL — qwen 系模型不可靠 honor write-class directive (P1, 🔴 Open, **历史描述**)

**状态**: 🔴 Open · **优先级**: P1 · **范围已 super-seded by ISSUE-QWEN-CLASS-TOOL-CALL-ADHERENCE-GAP**
**发现日期**: 2026-04-29

**历史观察（保留备查）**:
- Ollama qwen3.6:27b @ 11434 — directive 注入流入 context，最终输出仿造 JSON
- llama.cpp Qwen3.6-35B-A3B-Uncensored @ 11433 — 1 次 SQL 后退出，没有 format_and_export，没有最终文本
- 唯一未撞到此问题的是 cloud deepseek-v4-flash（demo 早期 Ch8 #6 通过）

A3B (35B-A3B-Aggressive / -unsloth) 都呈现"复制 TUI 渲染日志"的更严重失败，确认 MoE small-active 架构对 ReAct adherence 不足；27B-dense 是更轻症（"知规则不能执行"），见上一条 issue 的完整诊断。

**原 ISSUE-QWEN3.6-27B-IGNORES-DIRECTIVE-RECALL 内容继续记录在下**：

**症状**:
用户请求 "Save a markdown table to exports/devices_summary_v3.md..."（明确 save + exports/ 路径）。`guide_ops_save_artifact` 已经通过 `olav kb import-guides` 入库 (`category=usage_guide`)，AutoRecall 监控显示该 guide 的 text "ops-analyze saves reports directly via format_and_export; no delegation..." **确实流入 agent context**。但 qwen3.6:27b 跑了 1 次 SQL 之后，**最终输出是一段假装是 memory dump 的 JSON 文本**（包含 text/category/importance/tags 字段，但完全是模型自己捏的，不是任何工具的真实输出），然后退出 —— 没有调用 `format_and_export`，没有创建 v3.md 文件。

```
🔧 execute_sql({'query': 'list devices with hostname...'})    # ← 用了 query 不是 sql
[
  {"text": "Delegate snapshot diff/drift...", "category": "decision", ...},
  {"text": "Delegate what-if simulations...", ...},
  {"text": "ops-analyze saves reports directly via format_and_export; no
   delegation to writer is needed for inline-save.", "category": "fact", ...}
]
# (finish, exit 0, no format_and_export ever called)
```

**等同于 Ch8 baseline 失败模式** (#1-#4) 的复现 —— S1 (corpus 修复) + LlamaCppReranker (cross-encoder) 都没能阻止 27B 在面对这个 prompt 时"描述而非执行"。

**分层诊断**:
1. ✅ Memory 写入正常: `usage_guide` 4 行
2. ✅ AutoRecall 召回正常: directive 文本流入 context (监控可见)
3. ✅ Reranker 工作: cross-encoder 信号干净 (0.987 vs 0.0001 早测过)
4. ❌ **27B 模型 compliance 层失败**: 看到 directive 但不执行 → 输出仿造的 JSON

**模型问题，不是 OLAV 问题**:
对比 demo 早期跑过 deepseek-v4-flash 时同类 prompt 都能正常 emit tool_call。qwen3.6:27b 在这台 Ollama 部署上对 write-class prompt 的 tool_call adherence 显著低于 cloud-API 模型。

**复现方向 / 后续**:
1. 试跑 cloud LLM (deepseek-v4-flash) 对同样 prompt + 同样 memory state，验证是不是 27B 特定问题
2. 试 qwen3.6:27b 加更强 system prompt 条款（"MUST emit tool_call NOT JSON"），看是 prompt-tier 还是 model-tier 问题
3. 评估是否 ops agent 的 SKILL.md 需要 hardening（早前 R98 已加过一次）

**临时缓解**:
demo runsheet 的剩余 chapters 优先选不依赖 LLM compliance 的测试 —— Ch9 (write security gates 是 pre-LLM 硬门) 应该不受此影响；Ch10 (syslog ingest) 也是工具直调而非 agent loop 决策。

---

### ISSUE-CH9-AGENT-NEVER-REACHES-WRITE-GATE — Ch9 layers 1/2/4 via agent CLI 不触发 gate (P3, ✅ Closed via R100 cumulative fixes)

**关闭 (2026-04-29, R100 系列 cumulative effect)**:
不是直接修这条 issue，而是 R100/S2-S4 修复全部 ship 之后，agent 整体 adherence 改善，自然会撞 gate 而不是过度谨慎不撞。

**v2 验证（同样 prompt "Write test value to InfluxDB"）**:
- ✅ agent emit GET (recon)
- ✅ agent emit `api_request(service='influxdb_netops', method='POST', path='/api/v2/write...')`
- ✅ POST gate 触发 `requires_approval`
- ✅ agent 完整呈现写入计划（service / method / path / body / Line Protocol 解读）然后问用户 "Confirm?"

L4 (SSH write hard block) 验证: agent 识别 R1 是 Juniper → search_commands keyword=configuration → 找不到 SSH-write 工具 → 优雅停止（这是 L4 的预期"工具缺失 = gate"行为）。

5 layer gate 现在全部端到端可演示（之前需要 direct python 测才能证明 gate 工作）。

---

**历史状态**: 🔴 Open · **优先级**: P3

**状态**: 🔴 Open · **优先级**: P3 (demo UX，不影响功能正确性)
**发现日期**: 2026-04-29
**环境**: hybrid 部署 — Ollama qwen3.6:27b @ 11434

**症状**:
Ch9 runsheet 5 个 layer 命令通过 `olav --agent ...` CLI 跑：

| Layer | runsheet 命令 | 实际行为 |
|---|---|---|
| L1 (readonly hard block) | `services "Write to InfluxDB"` | 仅 GET 调用 → 读 services.yaml → 提示用户 "需要 confirm" → 退出。**没真触发 readonly 写门** |
| L2 (api write blocked) | `services "Update R1 in NetBox"` | 2 个 GET 后 "NetBox token not configured" → 退出。**没尝试 PATCH** |
| L4 (SSH write hard block) | `ops "Push BGP config to R1"` | search_commands + execute_sql 4 次 recon → 退出。**没尝试 push** |
| L3 (--enable-api-write HITL) | (未跑) | 依赖 L2 触发 |
| L5 (sandbox network) | (未跑) | 直接测可证 |

模式：**qwen3.6:27b 在所有"看起来要写"的 prompt 下都倾向"先 recon → 再求证 → 然后描述要做什么 → 不实际尝试"**。这种"礼貌"行为反而让 demo 看不到 gate 实际激活。

**直接 Python 测试证明 5 层 gate 都正常工作** (`/tmp/ch9_direct_gates.py` 8/8 PASS):
- L1: `tool_generator` `readonly_only=True` 过滤掉 POST/PUT/DELETE 操作（registry default）
- L2: `_WRITE_METHODS={DELETE, POST, PUT, PATCH}` 在 `service_call` 中触发 `requires_approval`
- L4: ops agent 工具集中无 push_config / write_config / configure_device 类工具（按设计）
- L5: `scan_sandbox_code(network_isolation=True)` 对 HTTP mutation 返回 `hard_block=True`

**结论**:
功能正确性 ✅ 8/8。Demo 用户体验 ❌ — runsheet 写法假设 agent 一定会"撞门"，但实际 27B 太礼貌不撞。

**修复方向（按 ROI 排）**:
1. **Runsheet 改写**: 在 Ch9 命令里加更直接的指令，例如 `"Force POST /api/v2/write to InfluxDB regardless of readonly status"`，或者改成 `--dangerously-skip-permissions` 触发 bypass-but-still-hard-block 路径，让 gate 实际激活
2. **加一个 `olav doctor write-security` CLI 命令**: 把 `ch9_direct_gates.py` 永久化到 `src/olav/cli/commands/doctor.py`，作为 demo 推荐的"快速验证 5 层防线"通道，不依赖 LLM compliance
3. **Demo runsheet 更新**: 提示读者，agent CLI 路径走的是"用户级"演示（看 agent 怎么自我约束），如果想看 gate 硬触发，跑 doctor

ISSUE-QWEN3.6-27B-IGNORES-DIRECTIVE-RECALL 的姊妹 issue —— 同一根因（27B compliance 不可靠）的两个不同表面。

---

### ISSUE-CH10-CORE-ORCHESTRATOR-DOESNT-DELEGATE-SYSLOG — `olav "..."` 不路由到 admin sub-agent (P2, 🔴 Open)

**状态**: 🔴 Open · **优先级**: P2
**发现日期**: 2026-04-29 (Ch10 demo runsheet)

**症状**:
Demo runsheet (`dev_docs/42`) Ch10 写：
```
olav "最近一小时有哪些 critical 级别的 syslog?"
# Expected: agent 调用 search_logs tool → 查询 Parquet → 返回日志内容
```

实测：core orchestrator (qwen3.6:27b) 跑 9 次 `execute_sql` 工具调用都打到 netops 的 `parsed_outputs` / `v_show_logging_auto` 表（device CLI `show logging` 输出，**不是** syslog receiver Parquet），从不调用 `search_logs`。最终输出"No syslog data found"，但其实 Parquet 里就有 12 条记录。

**直接调用证明 search_logs 工具本身是好的**:
```python
from search_logs import search_logs
search_logs.invoke({'query': 'BGP', 'hours_ago': 24})
# → 返回 3 条 CRITICAL "BGP neighbor down" 记录，schema 正确
```

**根因**:
`search_logs` 是 `core/admin` sub-agent 的 tool（`.olav/workspace/core/admin/SKILL.md` 里 list 了它）。core 顶层 orchestrator 必须通过 `task` 工具显式 delegate 到 `admin` 才能用上。但 core orchestrator 的 system prompt + AGENT.md 都**没提"syslog/log 查询应路由到 admin"**:

```
$ grep -i "syslog\|search_logs\|admin.*log" .olav/workspace/core/{prompts/*.md,AGENT.md,SKILL.md}
prompts/system.md:olav service start --all   # Start web + syslog + daemon
# (only mention is the start command — nothing about routing reads)
```

qwen3.6:27b 在没有路由提示时默认走 `execute_sql`，看不到 search_logs 这条路。

**修复方向**:
1. **轻**: 在 `core/prompts/system.md` 的 cheatsheet 里加一行 "syslog / log queries → delegate to admin sub-agent (search_logs)"
2. **中**: 在 `core/AGENT.md` 路由表里显式列出 admin 负责什么（log search, workspace health, service deploy）
3. **重**: 把 `search_logs` 也 export 到 core orchestrator 自己的 toolset（破坏 admin 边界，不推荐）

**直接相关**: Ch10 数据管道 ✅（receiver → Parquet → 12 records ingested 正常），admin sub-agent tool ✅（直接调用 OK）—— 仅 orchestrator routing prompt gap。

---

### ISSUE-CH10-SYSLOG-CLOCK-SKEW-1H-WINDOW — host vs ingest 时钟差导致 1h 窗口查空 (P3, 🔴 Open)

**状态**: 🔴 Open · **优先级**: P3 (cosmetic)
**发现日期**: 2026-04-29

**症状**:
12 条 syslog 在 `2026-04-29T01:40:05` 入库（host clock = `2026-04-28T...`，差 ~24h）。`search_logs(hours_ago=1)` → "No log records found"，实际 Parquet 有数据。`hours_ago=24` 才能命中。

**根因**:
host 时钟（demo7 运行机器）显示 2026-04-28，syslog Parquet timestamp 显示 2026-04-29 —— **跨日时差 ~24h**。多半是 demo7 host 的系统时钟偏。`search_logs` 内部 `WHERE timestamp >= NOW() - INTERVAL N HOUR` 时 NOW() 用的是 host 时钟（早 1 天）→ 任何 N<24 都查不到刚入的记录。

**修复方向（按 ROI 排）**:
1. **运维侧**: 修主机时钟 (NTP)，根本解决
2. **代码侧**: `search_logs` 加默认 fallback —— 当 `hours_ago=N` 返回 0 行时，自动放宽到 24h 并在结果里 hint "extended window due to clock skew detection"
3. **demo runsheet 侧**: Ch10 的 talking point 加一句"如果 host 时钟有偏，用 hours_ago=24"

---

### ISSUE-ADMIN-STATUS-TOOLS-COUNT-ZERO — `olav admin status` 永远报 tools_count=0 (P3, 🔴 Open)

**状态**: 🔴 Open · **优先级**: P3 (telemetry 准确性)
**发现日期**: 2026-04-29

**症状**:
```bash
$ olav admin status
{
    'data': {
        'agents_count': 6,
        'tools_count': 0          # ← 实际工作目录下 69 个 *.py 工具文件
    }
}
```

`agents_count` 正确，`tools_count` 永远是 0。后台 `find .olav/workspace -name "*.py" -path "*/tools/*" | wc -l` 返回 69。

**修复方向**:
`src/olav/cli/commands/admin.py` 的 status handler 大概率没实际数 tools。要么递归扫 `.olav/workspace/*/tools/`，要么从 OLAVAgent 注册的 toolsets 里 sum。

---

### ISSUE-CH8-DEMO-DIRECTIVE-VANISHES-ON-DIM-MIGRATION — embedding backend 切换会清空 memory，破坏 demo 体验 (P2, 🔴 Open)

**状态**: 🔴 Open · **优先级**: P2
**发现日期**: 2026-04-29 (demo runsheet Ch9 准备阶段)

**症状**:
切换 embedding backend (e.g. proxy 2048d → Ollama nomic 768d) 触发 `LanceDBStore.connect()` 自动 `Embedding dim mismatch on table 'memory' (stored=X, embedder=Y). Dropping and recreating — existing rows will be lost.` —— **所有现存 memory 行（包括 directive guides、L1 operational events、query patterns）一次清零**。Demo 用户重启后 agent compliance 直接退化到 baseline，因为 directive 没了。

**根因**:
`src/olav/core/memory/__init__.py` 的 dim auto-migration 是 destructive 的（设计为容错切换 backend），但 demo flow 没有 "embedding 变了 → 自动重新 import-guides + 重新 prime schema" 的钩子。

**修复方向**:
1. **轻**: dim mismatch 时不只 drop，而是先把 metadata 中保留的 source-of-truth 行（directive guides 来源 = `*.guide.yaml`，schema = `/netops_init`）记录下来，迁移完自动 re-import
2. **中**: dim mismatch 时打印 prominent warning + 推荐用户运行的恢复命令 (`olav kb import-guides .olav/workspace`)
3. **重**: 把 directive guides 从 LanceDB memory 表里移出，改为 system-prompt-time 注入（不依赖 embedding backend）—— 这条也对 ISSUE-QWEN3.6-27B-IGNORES-DIRECTIVE-RECALL 有 partial mitigation

---

### ISSUE-SERVICES-AGENT-NOT-PACKAGED — services 平台 agent 不在 olav wheel 里 (P1, ✅ Closed)

**修复 (2026-04-28)**:
1. `cp -r .olav/workspace/services` → `src/olav/data/workspace/services/`
   (6 files: AGENT.md / MANIFEST.yaml / SKILL.md / prompts / references /
   tools) —— hatchling 现自动打包
2. `init.py:_deploy_core_workspace` → **`_deploy_platform_workspaces`** —
   遍历 `data/workspace/*` 自动部署所有平台 agent；保留 back-compat
   alias 以防外部调用
3. 4 个新 governance test (`tests/unit/test_platform_workspaces_packaging.py`):
   - `services` 必须在 packaging source
   - `core` 必须在 packaging source
   - 所有 src 树 agent 必须出现在 wheel
   - 每个 agent 必须有 AGENT.md
4. **Demo7 fresh install 验证**: 删 services workspace + reinstall wheel
   → `olav init` 现在报 `✓ platform workspaces deployed: core, services`，
   `olav list` 6 个 agents 含 services。Ch7 不再需要手动 cp。

**Wheel 内容验证**:
```
workspace agents in wheel:
  core: 46 files
  services: 9 files     ← 之前 0
```

**遗留**（不阻塞）：`.olav/workspace/core/` (95 files) 与
`src/olav/data/workspace/core/` (48 files) 在 dev tree 已偏离 — 单独
issue 跟踪 dev workspace 漂移问题（dev 编辑了 runtime 副本未同步回
packaging 源）。本次修复仅解决 services 这一个 packaging gap；core
drift 是独立的 dev hygiene 问题。

---

### ISSUE-SERVICES-AGENT-NOT-PACKAGED — services 平台 agent 不在 olav wheel 里 (P1, Open) (历史描述)

**状态**: 🔴 Open — 2026-04-28 demo7 Ch7 测试时发现
**优先级**: P1（demo runsheet Ch7 在 fresh-wheel install 上完全跑不起来）
**发现日期**: 2026-04-28

**症状**:

`.olav/workspace/services/` 是 platform 拥有的 agent（CLAUDE.md
workspace 表已声明），但只存在于 dev tree。`pyproject.toml` 仅声明
`packages = ["src/olav"]`，wheel 只打 `src/olav/data/workspace/core/`
（46 个文件），其它平台 agent 全缺：

```
workspace/<top> dirs in wheel:
  core: 46 files
  (services / audit / topology / ops 全部 0 个文件)
```

`olav init` 的 `_deploy_core_workspace` 也写死只部署 core
(`src/olav/cli/commands/init.py:362-391`)。

Demo7 fresh install: `olav list` 只看到 audit / command_learner / core /
ops / topology（这些都是 olav-netops 装的），**没有 services**。Ch7
所有命令 `olav --agent services "..."` 都报"unknown agent"。

**根因**:

`.olav/workspace/services/` 不在 `src/olav/data/workspace/`，所以
`hatchling.build` 不会把它打进 wheel。`init.py` 也只 hard-code 部署
`core`，没有"扫描 data/workspace/* 全部部署"的通用循环。

**修复方向**（三层）:

1. **Workspace 源代码迁移**: 把 `.olav/workspace/services/` 从 dev
   tree 的"运行时位置"移到 `src/olav/data/workspace/services/`（打包
   源），让 wheel 自动收。dev tree 的 `.olav/workspace/services/`
   通过 `olav init` 自动生成（同 core 的处理）。
2. **`init.py` 通用化**: `_deploy_core_workspace` → `_deploy_workspaces`，
   遍历 `olav.data.workspace.*` 自动部署所有平台 agent。
3. **Governance test**: 加 CI 检查 `dist/*.whl` 必须包含全部
   `_register_all` / 平台 agent 列表里的 workspace。

**临时 workaround**（Ch7 测试用）:

```bash
cp -r /home/yhvh/Olav/.olav/workspace/services /path/to/demo/.olav/workspace/
olav refresh   # rebuild registry
```

**额外发现**: 同样的打包路径检查 audit / topology / command_learner
也都不在 wheel 里，但它们走 `olav-netops` skill install 路径，所以
demo7 通过 `olav skill install netops` 装上了。如果一个 fresh install
没装 netops（例如纯 olav 平台用），audit/topology 也会缺 — 这是更
广义的 ARCH 问题，不仅 services。

---

### ISSUE-DEAD-SCHEMA-R83-LEFTOVER — 5 个 R83 zero-ETL 后被废弃的表仍 BASE TABLE 挂在 DB 中 (P1, ✅ Closed)

**修复 (2026-04-28)**:
* `olav-netops/src/olav_netops/core/tables.py` 加 `_LEGACY_DEAD_TABLES`
  常量 + `drop_legacy_tables(con, schemas=("netops","main"))` 函数
  （demo7 现场实测发现死表实际在 `main` schema，不只是 `netops`，函数
  默认两 schema 都扫）
* `netops_init/run.py` Stage 3 在 `ensure_all_schemas` 后调
  `drop_legacy_tables` —— 幂等、安全（IF EXISTS）、自动报告 dropped
  列表
* 5 个新单测 (`olav-netops/tests/test_dead_table_migration.py`):
  netops schema、main schema、idempotency、不误删 live tables、空 DB
  情况

**Demo7 实测验证**:
* DROP 前: bgp_neighbors / bgp_routes / interfaces / ospf_neighbors /
  routes 5 表全在 main schema (0 行 dead)
* DROP 后: 5 表 gone；live views 仍在 (`v_bgp_neighbors_auto` 236 行,
  `v_ospf_neighbors_auto` 217 行, `v_routes_auto` 10520 行)
* Profile Author 重跑 → SQL 现在用 `FROM v_bgp_neighbors_auto` (live
  view)，不再选死表

---

**状态**: 🔴 Open — 2026-04-28 由用户在 demo7 Ch6 测试时刨根问底
**优先级**: P1（根因 — false-green audit 的结构源头）
**发现日期**: 2026-04-28

**实证清单**（对 demo7 + dev tree 双确认 grep 全 codebase）：

| 表 | INSERT writers (代码里) | DDL CREATE 出处 | 数据流向 |
|---|---|---|---|
| `interfaces` | 0 | 0 | `v_interfaces_auto` 取代 |
| `bgp_neighbors` | 0 | 0 | `v_bgp_neighbors_auto` 取代 |
| `bgp_routes` | 0 | 0 | JSON-extract from `parsed_outputs` |
| `ospf_neighbors` | 0 | 0 | `v_ospf_neighbors_auto` 取代 |
| `routes` | 0 | 0 | `v_routes_auto` 取代 |

5 个表 **没有任何代码 INSERT 到它们，也没有任何 CREATE TABLE 创建它
们**。`SCHEMA_REFERENCE.md` 已记录"R70 deleted bgp_sessions"等迁移；
但本批 5 表的 DROP 在 R70/R83 schema 演化里被遗漏。它们以
`BASE TABLE` 类型挂在每个 demo / dev / CI 的 main.duckdb 中，从不
被填，从不更新，永远 0 行。

**症状放大点**:

LLM 通过 `database_introspection`（列出 schema 表名 + DDL）按"看到
就选"逻辑选了 `bgp_neighbors` 写 audit SQL；6 个 BGP job 全部返回
0 行；render_report 渲染 "Network Health: ✅ Healthy" — **结构性
假绿**。

**修复**:

1. 写一次性 migration `dev_docs/00 § ISSUE-DEAD-SCHEMA-R83-LEFTOVER`
   对应的 DROP 脚本：`DROP TABLE IF EXISTS interfaces, bgp_neighbors,
   bgp_routes, ospf_neighbors, routes`。在 `netops_init` Stage 0 (DB
   bootstrap) 加一个守卫：版本号检查 + 自动 DROP（幂等）。
2. 加 governance test：grep 全 codebase 确认每个 BASE TABLE 都至少有
   一处 `INSERT` writer 或 `CREATE TABLE` source；缺一报错。
3. `database_introspection` 工具的 schema 输出加 `last_modified` /
   `row_count` 字段；LLM 看到 "row_count=0 + last_modified=NULL"
   立刻知道是死表（即使 1 没做完）。

---

### ISSUE-AUDIT-FALSE-GREEN-EMPTY-RESULTSET — Audit 把"SQL 0 行"误判为"网络健康" (P1, ✅ Closed for map_engine layer; render-layer prompt tuning ⏸️ deferred)

**修复 (2026-04-28, map_engine 层)**:
* `olav-netops/.olav/workspace/audit/auditor/tools/map_engine.py` 加
  `_extract_first_table()` + `_check_data_sufficiency()` helpers
* SQL job 路径在 findings 为空（且 raw_fallback 也无）时调
  sufficiency check：
  - 源表 0 行 → inject Critical 'empty_source' 合成 finding
  - 源表 >0 行但 window 内 0 行 → inject Warning 'empty_window' 合成
    finding，提示 "extend window or re-collect"
* 自动 promote job-level severity（empty_source → Critical；info →
  Warning）让 render 不能默认绿
* 8 个新单测 (`tests/unit/test_audit_empty_resultset_safeguard.py`):
  table extraction、empty source、stale window、missing table、
  finding shape pin

**Demo7 实测验证**:
* 测试场景：v_bgp_neighbors_auto 有 236 行历史数据，但全 > 24h
* 修复前: audit 报 "✅ Healthy — No anomalies detected"（假绿）
* 修复后: 3 个相关 job 全产 Insufficient Data 警告，含具体提示：
  "v_bgp_neighbors_auto has 236 rows total but 0 matched the audit
  window. Extend time window or re-collect"。报告 body 现含完整
  warning 表格 + action items

**遗留 (deferred render-layer fix)**: render_report 的 LLM 仍在
Executive Summary 写 "✅ Healthy" 即使有 3 个 Warning findings —
内部矛盾。需 render prompt 加规则: "若任一 job 有 severity ≥ Warning
的 finding，总判决禁用 ✅ Healthy，必须用 ⚠️ At Risk 或更差"。

---

**状态**: 🔴 Open — 2026-04-28 demo7 Chapter 6 测试时由用户审计发现
**优先级**: P1（生产级安全缺陷 —— 网络可能彻底失采集，audit 仍报绿）
**发现日期**: 2026-04-28

**症状**:

demo7 上 `bgp_neighbors` 表有 **0 行**（数据全在 `v_bgp_neighbors_auto`
view 层 236 行，R83 zero-ETL 架构），但 `bgp_health` profile 由 LLM
通过 `database_introspection` 选了 `bgp_neighbors` 表写 SQL，导致
所有 6 个 BGP job 返回 0 行；render_report 把"0 行"渲染为：

> Network Health: **✅ Healthy** — All BGP peering, prefix, and route
> metrics show no anomalies or deviations from baseline.

**两层缺陷**:

1. **Profile Author 选了空表**（缺陷 1）。`bgp_neighbors` 是 schema
   遗留的占位表，R83 决定走 zero-ETL 后实际数据落在 view 层
   (`v_bgp_neighbors_auto` 用 `unnest(from_json(parsed_outputs.parsed_data))`
   实时构建)。`database_introspection` 工具只列了表名 + DDL，没标
   "view is the authoritative source for this concept"。LLM 默认选了
   表，没察觉它是空的。

2. **Auditor 不分"健康"vs"无数据"**（缺陷 2，更严重）。`map_engine`
   把 0-行结果传给 `render_report`，后者渲染 "No anomalies detected"。
   生产级 audit **必须**区分：
   - `query result count == 0` 且 `WHERE 包含时间窗口` → "Insufficient
     data in window — collect data or extend window"（**WARN** 级）
   - `query result count == 0` 且**无**时间窗口 → "Network truly empty —
     verify discovery完成性"（**CRITICAL** 级）
   - `query result count > 0 且无 anomaly_field` → "Healthy" ✓

**修复方向**:

* 缺陷 1：`database_introspection` 工具增 `authoritative_view_for(table)`
  字段 — 当某 ETL 表为空但有对应 view 时，标注 view 是真源；prompt
  里加 "prefer v_*_auto views over base tables for query"。或者更激进：
  Profile Author 创建 profile 时**必须**先 `test_map_query` 看每条
  SQL 返回非 0 行才允许 save_profile，否则要求改写。
* 缺陷 2：`map_engine.run_map_engine` 在每个 job 后检查 row count；如
  return 0 rows，强制 inject finding `severity=Warning`,
  `metric_name="Insufficient Data"` 进 segmented JSON；render_report
  按这个 severity 在 Executive Summary 标 "⚠️ Insufficient data —
  cannot certify health"。
* 复测：写 unit test 喂 0-row job → assert 输出含 "insufficient data"
  且 health verdict 不为 "Healthy"。

**为什么没在更早抓到**: 之前 in-vivo / 测试都跑在 dev tree，
`/home/yhvh/Olav/.olav/databases/main.duckdb` 的 `bgp_neighbors` 表
也可能是空的，但因为没人对照过 view，假绿一直被当成真绿。Demo7
fresh-install 的"copy DB 而非真采集"路径放大了这个问题。

---

### ISSUE-AUDIT-PROFILE-PATH-LLM-HALLUCINATION — `_sanitize_path` 不接 LLM 常见的"丢 `.olav/` 前缀"路径 + render_report 完全未 sanitize (P2, ✅ Closed)

**状态**: ✅ **Closed 2026-04-28** — Chapter 6 demo7 fresh-wheel 测试时
现场抓到。LLM (grok-4.1-fast) 把 profile_path 写成
`/workspace/audit/profiles/bgp_health.md`（少 `.olav/` 前缀且多个 leading
slash），map_engine `_sanitize_path` 只 strip 了 leading slash，没尝试
prepend `.olav/`，导致 FileNotFoundError；render_report 路径上根本没调
sanitize。

**修复**:
* `olav-netops/.olav/workspace/audit/auditor/tools/map_engine.py` —
  `_sanitize_path` 加第 4 类 case：`.olav/` 或 `.olav/workspace/` 前缀
  尝试。7 路径变体全 round-trip 到 canonical
  `.olav/workspace/audit/profiles/bgp_health.md`
* `olav-netops/.olav/workspace/audit/auditor/tools/render_report.py` —
  在 `_parse_profile_md` 调用前先过 `_sanitize_path`（relative-import
  可能失败时 fallback 用 importlib.spec_from_file_location 直接加载 map_engine）

**验证**:
* `_sanitize_path` 7 case 单元演练全通过
* Chapter 6 step 2 demo7 重跑成功 — `run_map_engine` + `render_report` 两步全跑通
* 工件: `exports/audit_reports/bgp_health_20260428T054449Z.md` (1.4 KB,
  Executive Summary "Network Health: ✅ Healthy" + 6 BGP 指标段) +
  `bgp_health_20260428T054400Z.json` (segmented job results)

---

### ISSUE-LAB-PARENTS4-WHEEL-PATH-BUG — `Path(__file__).parents[4]` breaks lab module on wheel install (P1, ✅ Closed)

**状态**: ✅ **Closed 2026-04-28** — 修复落地：新建
`src/olav/core/lab/_paths.py` 导出 `olav_root()` /
`lab_config_path()` / `lab_workspace_tools_dir()` 三个 helper，
4 级解析（OLAV_HOME env → cwd 向上搜索 marker → __file__ 向上
搜索 marker → cwd fallback）。6 处 callsite (deploy_lab.py 2 处,
deploy_and_push.py 2 处, push_node_config.py 1 处, deploy_io.py
1 处) 全部改用 helper。9 个新 unit test (`test_lab_paths.py`)
覆盖 4 个解析分支 + 3 个 lab 模块 import 验证。

**Demo7 fresh-wheel 验证 (重做)**: 删 symlink workaround、unset 全部
env var，纯 fresh-install 跑 e2e — 12/12 stages PASS ~95s，含 4 次
真 LLM 调用全部返有效 JSON。说明修复**根治**了 wheel 部署路径问题。

**发现日期**: 2026-04-28 demo7 fresh-wheel install 时实测发现
**优先级**: P1（阻塞 wheel 部署的 lab 流程；workaround 简单，但 demo
体验差且会让用户误以为 R94 流程坏了）
**关联**: 与 R94 无关，是 pre-existing 路径解析约定与 wheel 部署不
兼容的 systemic 问题；R94 e2e 在 fresh demo 跑时把它暴露出来

**症状**:

`olav.core.lab` 共 6 处用 `Path(__file__).resolve().parents[4]` 推导
项目根，期望解析到 `<repo>/.olav/workspace/ops/lab/config/config.json`：

```
src/olav/core/lab/deploy_io.py:101
src/olav/core/lab/deploy_lab.py:31    (_CONFIG_PATH)
src/olav/core/lab/deploy_lab.py:260
src/olav/core/lab/deploy_and_push.py:48
src/olav/core/lab/deploy_and_push.py:52
src/olav/core/lab/push_node_config.py:41
```

**Dev 树**（editable）下 `__file__` =
`/home/yhvh/Olav/src/olav/core/lab/deploy_lab.py`，
`parents[4]` = `/home/yhvh/Olav` ✓

**Wheel install** 下 `__file__` =
`<venv>/lib/python3.14/site-packages/olav/core/lab/deploy_lab.py`，
`parents[4]` = `<venv>/lib/python3.14`，再加
`.olav/workspace/...` 显然不存在 → FileNotFoundError。

实测错误：
```
FileNotFoundError: [Errno 2] No such file or directory:
  '/home/yhvh/olav-demo7/.venv/lib/python3.14/.olav/workspace/ops/lab/config/config.json'
```

**Workaround（demo 当下能用）**:

```bash
# 在 demo 目录下，把 .olav 目录链接到 wheel 期望的 parents[4] 位置
ln -sfn $PWD/.olav .venv/lib/python3.14/.olav
```

**正确修复方向**（优先级排序）:

1. 改 `_CONFIG_PATH` 解析为**向上搜索**：从 `Path.cwd()` 往上找含
   `.olav/workspace/ops/lab/config/config.json` 的目录；找不到再回退
   到 cwd-relative
2. 加 `OLAV_HOME` env var override，让用户能显式指定
3. 长期：把 lab config 注册进 ServiceRegistry / config layer，统一
   寻址（不再 file-relative）

**额外发现的 demo7 prereqs**（应进 runsheet ch5b 前置说明）:

* `.olav/databases/main.duckdb` — `generate_clab_topology` 读 L2 链路
* `.olav/config/api.json` — LLM key（stage 11c 需）
* `.olav/config/services.yaml` — containerlab service 注册（`_get_clab_ssh_host` 走 `ServiceRegistry`）
* `.olav/workspace/ops/lab/config/config.json` — CLAB PAM 凭据（path bug 触发点）

文档化已在 `dev_docs/42. DEMO_RUNSHEET.md § Chapter 5b § Pre-flight`。

**修复 LOC 估**: ~30 LOC（新建一个 `_resolve_olav_root()` helper +
6 处 callsite 改用），加 5 个单测验证 wheel-vs-dev-tree 双场景。

---

### ISSUE-TCF-SIM-LAB-REACT-MISSING — sim/lab 都在跳过推理，TCF 跨验证只是 plumbing 通过 (P0, ✅ Closed)

**状态**: ✅ **Closed 2026-04-28** — R94.1/R94.2/R94.3 三波修复落地：
* R94.1 sim 端 deterministic prod-CLI 生成器 (`prod_cli.py`)：sim 写真
  Junos+IOS CLI，零省略号，CAB 拿到的 spec 可直接部署
* R94.2 verdict 诚实化：deterministic 层只产结构性 verdict
  (missing / extra / tvt-pattern / post_check-form)，不再为 device 名
  配对伪造 ``approved``
* R94.3 agent 语义层 (`semantic_review.py`)：LLM (grok-4.1-fast) 读
  TCF 双侧 paired blocks，输出带字段对照的 ``approved`` /
  ``rejected`` / ``info`` 真 verdict；in-vivo 实测 LLM 抓到 rollback
  policy 命名反转 + lab rollback 扫描范围超越 prod 计划等真审计发现

**未做** (deferred)：lab 端 prod CLI 真翻译（R94.2-B）—— 改由 agent
读判断路径替代；sim ReAct 修订循环 —— deterministic generator 已够
ebgp_direct，新 intent 加 generator 即可，sim 端 LLM 修订留作未来增量。
**优先级**: **P0**（直接影响 TCF 作为生产级 CAB change record 的可信度）
**发现日期**: 2026-04-28
**关联**:
* `dev_docs/65 § R94` — schema 已 ship，但 sim/lab 推理路径缺位
* `dev_docs/00 § ISSUE-OPS-LAB-CANT-TRANSLATE-PROD-TO-SRL`（已 closed）— 当时认为 R89 deterministic generator 解决了
  问题；R94 in-vivo 暴露 R89 实质是**绕开 sim 的 prod CLI 自合成**，
  原 issue 的核心矛盾未真正解决

**根因审计 — 4 项关键观察（对应 in-vivo 工件
`exports/cab/tcf_e2e_r1r4/spec.tcf.yaml`）**：

1. **`implementation[]` 没有真命令 — sim 没推理**

   `tests/integration/e2e_tcf_in_vivo.py:88-91` 把 `implementation_json`
   写死成占位符夹具：
   ```python
   {"device": "R1", "phase": 1, "cli": ["set protocols bgp group ebgp-r4 ..."]},
   {"device": "R4", "phase": 1, "cli": ["router bgp 65001 ..."]},
   ```
   `tcf_emit_from_sim()` 是**纯发射器**（校验 + 落盘），不思考。
   "sim 推理出 prod 命令"——**从未发生**。

2. **CAB approver / prod ops 拿到的 spec 不可执行**

   省略号 `...` 直接落盘。没 peer-as / 没 neighbor IP / 没 export-policy /
   没 update-source loopback。`rollback[]` 完全空。**操作员拿这份 spec 上
   R1/R4 真机部署不了**。这是 production blocker。

3. **lab 用 R89 自合成 SRL，旁路了 sim 的 prod CLI**

   `tcf_to_r89_args(tcf)` 只抽 intent.type + devices.prod_loopback +
   devices.prod_asn + intent.lab_subnet —— **完全没读
   `spec.implementation[]`**。R89 干的是"根据意图重新合成一份 SRL 配置"，
   **不是**"把 sim 写的 Junos / IOS 翻译成 SRL"。

   后果：即使 sim.implementation 全填错的 Junos 语法（peer-as 写成
   peeras），R89 仍合成正确的 SRL，lab BGP 仍 Established，step_verdict
   仍 "approved"。**整个跨验证层在当前实现下抓不到 sim 端 prod CLI 的
   任何错误**。

4. **e2e 跑通的是 plumbing，不是 reasoning**

   验证矩阵：

   | 流程节点 | 状态 |
   |---|---|
   | Schema / Pydantic / FK | ✅ |
   | `tcf_emit` 原子 I/O | ✅ |
   | R88-A topology 生成 | ✅ deterministic |
   | R89 SRL 生成 | ✅ 但**只为 ebgp_direct + 不读 prod CLI** |
   | R90 rollback 生成 | ✅ 同上 |
   | CLAB deploy + BGP 收敛 | ✅ ~85s |
   | TVT 行级模式匹配 | ✅（抓到 T_rollback_clean expect 错配）|
   | step_verdict device-name 匹配 | ✅ 但**语义无意义** |
   | **Sim 推理 prod CLI** | ❌ **从未发生** |
   | **Lab 验证 prod CLI 正确性** | ❌ **从未发生** |
   | **Sim ↔ lab 语义跨验证** | ❌ device 名匹配 ≠ 语义等价 |

**正确架构 — 两端都需 ReAct loop**：

* **Sim ReAct**（ops-analyze agent）—— 输入 intent + topology snapshot；
  调 expert_knowledge / vendor reference 推理出**完整 Junos R1 + IOS R4
  CLI**；自检 (AS 闭环 / IP 邻居互指 / 接口名匹配 prod_intf /
  policy-statement 存在)；写 rollback 对偶；写 TVT；emit。
  当前 `tcf_emit_from_sim` 是这个 loop 的**工具，不是 loop 本身**。

* **Lab ReAct**（ops-lab agent）—— 输入 spec.tcf.yaml（含 sim 写好的
  真 Junos+IOS）；**翻译 prod CLI → SRL CLI**（不是从 intent 自合成）；
  push CLAB；verify；为每条 prod CLI 行写 verdict（含 reason 引证具体
  spec/lab 行号 + 关键字段对照）；rollback 同套翻译 + 比对。
  R89/R90 退化为兜底（sim 没写 implementation 时的 fallback）。

* **step_verdict 必须做语义对照**，不只是 device 名匹配 ——
  比对粒度（peer-as / neighbor IP / interface / policy 名等关键字段）
  写进 reason，verdict.reason 必须引证具体 spec/lab 行号。

**重命名**（同步执行 — sim 写真命令、lab 写 SRL 提供数孪证据）：

* `lab.implementation_actual` → **`lab.implementation_lab`**
* `lab.rollback_actual` → **`lab.rollback_lab`**
* `lab.post_check_actual` → **`lab.post_check_lab`**
* `step_verdicts` 字段名不变；`lab_ref` 字符串内容同步改名

字段名表达**职责**：sim 写的是真命令（顶层 `implementation[]`），
lab 写的是 SRL 数孪证据（`implementation_lab`）；命名应明示这一点而
不是模糊的 "actual"。

**待用户拍板的 5 个设计决策**（动手前必须明确）：

* **Q1**: ops-analyze 当前是否具备产 Junos+IOS CLI 能力？
  expert_knowledge / vendor reference 是否就位？还是要先建库？
* **Q2**: lab 端翻译走哪条路 ——
  (a) LLM 做 Junos→SRL 语义翻译（强大但有幻觉风险），还是
  (b) 建结构化映射 / 解析器（输入是 prod CLI 不是 intent 的确定性
  generator），LLM 仅做 fallback + step_verdict reasoning？
* **Q3**: step_verdict 语义匹配粒度 —— (粗) 每行 spec.cli 对应至少
  一条 lab cli；(中) 关键字段对照；(细) 解析两端 CLI 成 OpenConfig-like
  AST 逐字段 diff？
* **Q4**: rollback 在 sim 端 "verify" 怎么做？sim ReAct 是否需要先调 lab
  模拟才能验证 rollback 可逆性？这就把 sim ↔ lab 变成两轮 ReAct 互调，
  能接受还是太重？
* **Q5**: 用户提的"先写和验证 implementation，rollback 让 TVT" —— 是否
  理解为：rollback 设计也下放到 TVT 矩阵里去验证（每个 implementation
  步骤都有对应 rollback TVT 和 post-rollback TVT），rollback 不再是孤立
  list 而是 TVT 的一部分？

**修复路线（Q1-Q5 拍板后）**：

| # | 工作 | 阻塞 | 状态 |
|---|---|---|---|
| A | sim 端 prod CLI 生成（ops-analyze 推理或 deterministic generator 写入 implementation/rollback）| Q1, Q5 | ✅ **Ship R94.1** — 走 deterministic generator 路径（与 R88-A/R89/R90 同模式），先选这条以避免 LLM 幻觉；ops-analyze 后续可在此基础上加 ReAct 修订。`olav.core.cab.prod_cli` 提供 `generate_junos_ebgp_config` / `generate_ios_ebgp_config` + rollback + `derive_prod_cli_from_tcf` 一键 dispatch；13 个新 unit test；in-vivo 仍 12/12 PASS。 |
| B | lab ReAct 翻译层（读 spec.implementation 翻译为 SRL；R89 退化为兜底）| Q2 | 🔴 待 |
| C | step_verdict 语义匹配升级 | Q3 | ✅ **Ship R94.2** — 设计决策：**不在 Python 里写 extractor / parser**。用户拍板："直接让 LLM 用 sandbox 中的文件工具自己阅读判断就行"。落地：deterministic 层只 emit 结构性 verdict (missing / extra / tvt-pattern / post_check-form-diff)，不再为 device-name 配对冒称 ``approved``；语义判断 (``approved`` / ``rejected`` / ``info``) 由 agent 读 TCF 自行产出，via ``tcf_record_lab_run(step_verdicts=...)``。in-vivo 现在产 3 honest verdicts（之前 7 个含 4 个伪 approved）。 |
| D | rollback / TVT 同套翻译 + 比对 | Q5 | 🟡 部分 — TVT expected_pattern 这次 in-vivo 修对了 (`"no bgp neighbor"` substring 匹配)；rollback 翻译归 B |
| E | e2e 改造为驱动真 agent（不是直调 helpers fixture）| A, B | ✅ **Ship R94.3** — `olav.core.cab.semantic_review.review_paired_blocks(tcf)`；e2e stage 11c 真 LLM 调用 grok-4.1-fast；4/4 paired blocks 返回有效 JSON；抓到 rollback policy 名反转 + lab 范围超 prod 计划等真审计发现 |

**当前遗留**：
* R94 schema + 跨验证层 ✅ ship + 重命名 (`_actual` → `_lab`) 已落地
* R94.1 sim 端 deterministic prod-CLI 生成器 ✅ ship —
  `olav.core.cab.prod_cli` 模块；in-vivo 工件
  `exports/cab/tcf_e2e_r1r4/spec.tcf.yaml` 现含 R1 12 行 Junos +
  R4 13 行 IOS + 零省略号；CAB approver 拿到的 spec **可直接部署**。
* R94.2 verdict 诚实化 ✅ ship — `_diff_cli_blocks` 不再为
  device-name 配对伪造 ``approved``；deterministic 层只产结构性 verdict
  (missing / extra / tvt-pattern / post_check-form)；语义判断由 agent
  读 TCF + ``tcf_record_lab_run`` 写回（"路径 B" 的轻量版）。in-vivo
  从 7 verdicts (4 伪 approved + 1 info + 2 tvt) → 3 honest (1 info
  + 2 tvt)。
* R94.2-B (lab 真翻译 prod CLI → SRL) ⏸️ **deferred** — 决策放弃 Python
  parser/extractor 路线；改由 agent 阅读判断
* R94.3 agent 语义层 ✅ ship — `olav.core.cab.semantic_review` 模块；
  10 个 mock-LLM 单测 + e2e stage 11c 真 LLM in-vivo 验证；agent 输出
  4 条带具体字段对照的 verdict（含 1 条 info 抓到 rollback 策略名反转
  + 范围超出 prod 计划这种真审计发现）；总单测 109/109 PASS；in-vivo
  12/12 stages PASS ~95s

---

### R94 Closure note (2026-04-28) — TCF sim/lab parallel + step verdicts

**Status**: ✅ Closed (shipped + in-vivo verified)

Schema 扩展 `ExecutionRecord` 加 4 个 R94 字段
(`implementation_lab` / `rollback_lab` / `post_check_lab` /
`step_verdicts`) + 新 `StepVerdict` 模型；
`tcf_record_lab_run` 接受 4 个新 kwargs（list-of-dicts 或 JSON
字符串均可）；`tcf_diff_spec_vs_lab` 自动产 step-level
`StepVerdict[]` 覆盖 implementation / rollback / post_check / tvt
(`approved` / `rejected` / `missing` / `extra` / `info`)；
markdown render 加 "Step Verdicts" 区块；e2e in-vivo 脚本
`tests/integration/e2e_tcf_in_vivo.py` stage 11a/b 双轮录入
(round 1 lab actuals → round 2 step verdicts)。

**Verification**:

* 65 TCF unit tests PASS（含 8 个 R94 新测试 — `tests/unit/test_tcf_lab.py` + `test_tcf_diff.py`）
* in-vivo on real CLAB（192.168.100.12）— 12 stages PASS in ~85s；
  TCF artifact 现含 22-line impl_lab × 2 + 7-line rb_lab × 2 +
  1 post_check_lab + 7 step_verdicts。
* 跨验证当场抓到 2 个真实 CAB-relevant 发现：
  (a) `spec.rollback[]` 未填 → R90 lab rollback 全标 "extra"
  (sim 应填 prod-form rollback)；
  (b) TVT `T_rollback_clean` expected `"no bgp config"` vs lab
  observed `"no bgp neighbor in output"` → 被标 `rejected`，证明
  step-level diff 比 lab.verdict 更严，捕到 sim 写错的 expect 模式。

**Files**: `src/olav/core/cab/tcf_schema.py` · `tcf_lab.py` ·
`tcf_diff.py` · `__init__.py` · `tests/unit/test_tcf_lab.py` ·
`test_tcf_diff.py` · `tests/integration/e2e_tcf_in_vivo.py` ·
`dev_docs/65. R90_TCF_STRUCTURED_CHANGE_CONTRACT.md`。

---

### ISSUE-AGGRESSIVE-SKILL-FOLD: ADR-0008 条件 #1 太严,大部分 privileged @tool 也能折成 skill scripts(对小模型重大优化)

**状态**: ✅ Closed (R92.6 ship — ADR-0008 rev1 + 5 privileged 工具折叠;ops/lab @tool 表 6→1;实测 prompt 节省 -3,103 tokens / 调用)
**优先级**: P0 → 已 close
**发现日期**: 2026-04-27 R92.3 close-out 时由用户提出
**关闭日期**: 2026-04-27 R92.6 (commit `f86492b`)
**关联**: ISSUE-DEEPAGENTS-SKILLS-MIDDLEWARE-NOT-USED 的延伸 —— 该 issue
仅折非特权工具,**还有更激进的空间**

**症状**:

ADR-0008 §"Decision" 写了 4 条 escalation 保留条件。条件 #1 写得太严:

> "Sandbox-external privilege. CLAB REST API auth, prod SSH (scrapli),
> privileged docker. Subprocess scripts can't do these without exposing
> credentials."

**这条说错了**。子进程**能**完成所有这些操作:

* CLAB REST: 子进程 ``requests.post(host, headers=..., json=...)`` 即可,
  ``host`` / ``token`` 走 env / config 文件——和 @tool 调用的方式没区别。
* scrapli SSH: 子进程读同样的 nornir hosts.yaml,创建一次性 driver,
  执行命令后关闭——OLAV 现状 scrapli session 本来就是一次性的。
* privileged docker: 子进程一样能 ``docker exec`` / ``nsenter``,
  权限取决于运行用户而不是进程类型。

**真正的内核**应该是:

> **需要 agent Python heap 内持有跨调用对象状态的工具**。

OLAV 当前**几乎不触发**这条 —— scrapli session 不持久,CLAB REST
是无状态调用,save_lab_config 写文件就完事。

**测量**(post-R92.3 ops/lab agent):

| 来源 | tokens |
|---|---|
| ops/lab 6 privileged @tool wrappers | **3,718** |
| core 6 inherited (含 execute_skill_script) | 4,809 |
| SKILL.md (`static_context` 全文) | 1,549 |
| prompts/system.md | 2,419 |
| **基线总开销** | **12,495** |

激进折叠后理论值:

| 来源 | tokens |
|---|---|
| ops/lab @tool surface(只剩 exec_on_node 因 streaming 需求) | ~585 |
| core (不变) | 4,809 |
| SKILL.md(R92.6 SkillsMiddleware metadata-only) | ~50 |
| prompts/system.md(不变) | 2,419 |
| **激进基线** | **~7,863** |

**节省: ~4,632 tokens / 调用 ≈ 37% 进一步削减**

加上 R92.2 已省的 ~2,500 → R91+R92 总共 ~7,100 tokens / 调用节省。
对 grok-4.1-fast 这类小模型 prompt 预算极大释放。

**ADR-0008 条件 #1 修正**:

```
旧: "Sandbox-external privilege. CLAB REST API auth, prod SSH..."
新: "Cross-call shared in-process state. A Python-heap object that
    must persist across multiple invocations within a single agent
    turn. Subprocess scripts can't share heap. (E.g., a long-lived
    SSH session reused over many show commands; OLAV currently
    has none.)"
```

**剩余真正需 @tool 的工具(post-激进折叠)**:

| Tool | 理由 |
|---|---|
| ``execute_skill_script`` | 替代所有 skill 调用 |
| ``execute_sql`` | 通用,跨 agent 共用 |
| ``read_file`` / ``write_file`` | 工作流原语 |
| ``recall_memory`` | 通用 |
| ``format_and_export`` | 通用 |
| ``web_search`` | 通用 |
| ``exec_on_node`` (debatable) | 实时 streaming(交互式 show 命令) |
| ``olav_delegate`` (orchestrator) | 子代理调度 |
| ``record_network_event`` / ``register_service`` | 强 audit row 要求 |

**激进折叠候选**(ops/lab 6 个 privileged 中):

| Tool | 当前 tokens | 折成 skill script 难度 |
|---|---|---|
| ``save_lab_config`` | 341 | 易,纯文件写 |
| ``destroy_lab`` | 377 | 易,REST DELETE 调用 |
| ``deploy_lab`` | 413 | 中,REST POST + 等待 |
| ``deploy_and_push_lab`` | 946 | 中,deploy_lab + 多 push 串联 |
| ``push_node_config`` | 1,056 | 中,scrapli SSH |
| ``exec_on_node`` | 585 | 难,需扩 ``execute_skill_script`` 加 streaming(留作下个 round) |

**实施门控**:

1. R92.6.0:更新 ADR-0008 条件 #1 措辞(写 ADR-0008-rev1 follow-up 或直接 amend)
2. R92.6.1:折 ``save_lab_config`` + ``destroy_lab`` 作为 warmup(共 718 tokens)
3. R92.6.2:折 ``deploy_lab`` + ``deploy_and_push_lab`` + ``push_node_config``(共 2,415 tokens)
4. R92.6.3:接通 ``SkillsMiddleware``,SKILL.md 走 metadata-only 注入
5. R92.6.4:扩 ``execute_skill_script`` 加 ``stream=True`` 模式(可选 — 只有 ``exec_on_node`` 需要)
6. R92.6.5:in-vivo 验证 ch4→ch5 全程通(task #220)

**风险**:

* CLAB REST credential 跨进程传递正确(env vars 子进程默认继承,核对一遍)
* scrapli SSH 在子进程里建立时 host_keys / known_hosts 路径正确
* Real-time progress 缺失对 deploy_and_push_lab 的 UX 影响——长操作没流式反馈
  agent 可能误判为 hang。Mitigation: stream 扩展或子进程定期 checkpoint。

**关闭条件**:

ops/lab tool 表 ≤ 2 (execute_skill_script + 可选 exec_on_node) +
SkillsMiddleware metadata-only injection 落地 + in-vivo 验证 ch4→ch5
全程通过。

---

### ISSUE-SKILL-MEMORY-DUPLICATE-CONTRACT: SKILL.md 是契约,memory 是智慧——R91/R92 写的 expert.yaml 大量抄 SKILL.md,造成漂移风险

**状态**: ✅ Closed (R92.7 ship — 删 4 个 paraphrase-only expert.yaml + 规则永久落档进 `src/olav/core/memory/expert_kb.py` docstring;判定 5 问句给以后所有 expert.yaml 写作)
**优先级**: P1 → 已 close
**发现日期**: 2026-04-27 R92.2 close-out 时由用户问 "skill 模式下 reference 还是 memory 好"引出
**关闭日期**: 2026-04-27 R92.7 (commit `fbd8bc5`)

**症状**:

R92.2 把 ops/lab 改成 SkillsMiddleware-first 后,SKILL.md 已经清晰承
载了"call-site 契约":

* 6 个 skill 脚本的存在 + 强制调用顺序
* ``execute_skill_script(skill_name="lab", script_name=..., args={...})`` 示例
* 安全护栏("NEVER hand-write yaml_content"等)
* PASS/FAIL 判定规则

但 R91/R92 治理过程中,我顺手在每个 fold 步骤写了 expert.yaml 进
LanceDB memory:

* ``ops/lab/expertise/cab_validation_workflow.expert.yaml``
* ``ops/analyze/expertise/sandbox_python_helpers.expert.yaml``
* ``audit/auditor/expertise/profile_authoring_python_api.expert.yaml``
* ``audit/curator/expertise/curation_python_helpers.expert.yaml``

事后审视,这 4 个 YAML 几乎都是**把 SKILL.md 的内容抄了一份**——不是
真正的"智慧",而是契约的副本。

**问题**:

1. **两份"权威"会漂移**。维护时漏掉一个就出 bug。
2. **召回命中时双重注入**。AutoRecallMiddleware 召回 expert.yaml +
   `static_context` 加载 SKILL.md → agent 看到同样内容两次 → 浪费 token。
3. **违反 dev_docs/200 业务模型**。商业模型说"用户注入自己的经验"
   是 memory 的地盘 —— SKILL.md 是作者契约。我把契约抄进了用户层。

**正确的分层**:

| 来源 | 性质 | 谁负责维护 | 进 LLM 上下文时机 |
|---|---|---|---|
| **SKILL.md / references/** | 静态**契约** | 技能作者 | static_context 触发(R92.6 后渐进披露) |
| **expert.yaml / usage_guide(memory)** | 动态**智慧** | 用户注入(dev_docs/200) | AutoRecallMiddleware 语义召回命中时 |
| **脚本自身 docstring + argparse 错误** | 调用契约 source-of-truth | 脚本作者 | 不进 LLM —— 给人读 / 错误反馈 |

**判定哪个 expert.yaml 该留**:

| YAML | 真有"智慧"? | 还是抄 SKILL.md? | 该不该留 |
|---|---|---|---|
| ``cab_validation_workflow.expert.yaml`` (R91) | ❌ 几乎全是工作流 | ✅ | **删除** |
| ``profile_authoring_python_api.expert.yaml`` (R91) | ❌ | ✅ | 删除 |
| ``sandbox_python_helpers.expert.yaml`` (R91) | ❌ | ✅ | 删除 |
| ``curation_python_helpers.expert.yaml`` (R91) | ❌ | ✅ | 删除 |
| ``bgp_validation_srl.expert.yaml`` (历史) | ✅ "小模型常见踩坑 + 如何识别" | — | **保留** |
| ``srl_config_push_gotchas.expert.yaml`` (历史) | ✅ "discard now / base64 / 2>&1" 经验性踩坑 | — | 保留 |
| ``junos_vs_ios_bgp_translation.expert.yaml`` (历史) | ✅ 跨厂商语义对照 | — | 保留 |
| ``srl_spec_generation_rules.expert.yaml`` (历史) | ✅ Cisco-shaped 训练数据下的 SRL 必填字段 | — | 保留 |

**判定规则**(给以后所有 expert.yaml 写作):

写一份 expert.yaml 之前问自己:

1. **它是踩坑教训么?** ("v1 出了 X 错误,识别特征是 Y,补救是 Z")→ 留 memory
2. **它是跨 skill 的 pattern 么?** ("当 ops-lab FAIL,可以让 ops-analyze 跑 diff 看上次 PASS") → 留 memory
3. **它是用户/项目偏好么?** ("此用户输出 PR 用 conventional commits") → 留 memory
4. **它是 trace_learner 提取的 constraint 么?** → 留 memory
5. **它只是把 SKILL.md / references/ 的工作流和示例 paraphrase 了一遍?** → **不要写,写在 SKILL.md 里**

**洞察(R92.6 后更明显)**:

ADR-0008 推到 R92.6 上 ``SkillsMiddleware`` 渐进披露后,SKILL.md 比
memory 还要"按需":

* SKILL.md: metadata(name + description)总在 base prompt(~10-50 tokens),
  full content 只在 agent 决定走这个 skill 时才读
* expert.yaml memory: embedding 召回命中时才进上下文(每次都要做语义搜索)

两条路径都收敛到"按需注入",区别变成**纯粹归属**:契约还是智慧。

**实施(R92.7)**:

* 删掉 4 个抄 SKILL.md 的 expert.yaml(R91/R92 沉积)
* 删掉 LanceDB 里对应的 memory entries(``prime_guides_from_dir`` 重跑会自动清)
* 写 ADR 备忘或 dev_docs 章节捕获"SKILL.md 拥有契约,memory 拥有智慧"原则
* 检查现有所有 expert.yaml,凡是抄 SKILL.md 的都删

**和 ISSUE-DEEPAGENTS-SKILLS-MIDDLEWARE-NOT-USED 的关系**:

* 后者说 SKILL.md 应该是 SkillsMiddleware 的载体
* 此 issue 说 SKILL.md 是契约,memory 是智慧 —— 两个不同的层
* 只有这两条 issue 都解决,SKILL.md/memory 的角色才算划清

---

### ISSUE-DEEPAGENTS-SKILLS-MIDDLEWARE-NOT-USED: ADR-0007 没走到位 —— 应该用 deepagents 原生 SkillsMiddleware,而不是只折成 sandbox Python

**状态**: ✅ Closed (R92.6 close-out 实测后纠正:SkillsMiddleware 在 OLAV 当前架构里**不会再省 token**;所有真正可省的开销已通过 R91+R92.2/3/6 的 tool-list 折叠 + skill scripts 拿到)
**优先级**: P0 → 已 close
**发现日期**: 2026-04-27 R91 治理过程中由用户提出
**关闭日期**: 2026-04-27 R92 close-out 实测验证
**关联**: 这是 ISSUE-MCP-OVER-TOOLING 的"二阶"问题 —— 但实测发现并没有"二阶"

**实测纠正记录**(2026-04-27):

最初登记此 issue 时假设 OLAV 通过 ``static_context`` 全文注入 SKILL.md body 进入 base prompt(因此 SkillsMiddleware progressive-disclosure 能省 ~1,500 tokens)。实测发现**这是误解**:

* OLAV 的 ``agent.py`` 只读 SKILL.md **frontmatter**(name / description / tools list / static_context refs)
* SKILL.md **body 从不直接注入 prompt** —— 它对人类是文档,对 agent 是 metadata 容器
* ``static_context`` 字段列出**单独的 references/ 文件**(不是 SKILL.md 自身),且 ``mode=on_intent`` 的不进 base prompt

也就是说,SkillsMiddleware 提供的"metadata-only 注入 + 按需读 SKILL.md"的优化路径,OLAV **早就在做**了(没有真正的 "SKILL.md 全文注入" 现状可省)。

**实测对比(ops/lab agent base prompt)**:

| 阶段 | tokens | 节省 |
|---|---|---|
| Pre-R91(估算) | ~13,045 | baseline |
| Post-R91 | ~10,500 | -2,545(20 工具折叠) |
| Post-R92.2 | ~10,225 | (SKILL.md 改写为 skill-script 模式,~+275) |
| **Post-R92.6** | **7,806** | **-5,239 总累计 (-40%)** |

* core/tools (6 inherited): 4,803
* ops/lab/tools (1 — exec_on_node): 584
* prompts/system.md: 2,419
* SKILL.md body: 0 (验证不在 prompt)
* LAB_REFERENCE.md (mode=on_intent): 0 base + ~4,400 on intent match

**SkillsMiddleware 在 OLAV 的实际价值评估**:

* **对 sub-agent**: 0 价值 —— 每个 sub-agent 自身是 skill,无 inner-skill 发现需求;SKILL.md body 本来就不在 prompt
* **对 orchestrator**: 边际价值 —— 已有 SubAgentMiddleware 注入 sub-agent metadata,SkillsMiddleware 会重复
* **跨 agent skill 浏览**: 理论收益,但 OLAV 当前的 olav_delegate 模型已覆盖

**结论**: R91+R92 的 tool-list 折叠 + skill scripts 是这条 issue 的**实质修复**。SkillsMiddleware 单独 wiring 不带来可测量的 prompt 节省,因此**不再是 actionable 的 follow-up 工作**。R92 关闭即是终态。

如未来需要"orchestrator 直接 read sub-agent SKILL.md"那种用法(目前 olav_delegate 不支持),才需要重新评估 SkillsMiddleware。

**Mitigation 进度**(R92.0 → R92.1 → R92.2):

* **R92.0** (`147a849`): ADR-0008 写入 `docs/adr/0008-skills-middleware-first-architecture.md`。明确"controlled ``execute_skill_script`` 替代 ``FilesystemMiddleware``"决定(OLAV agent.py:348 显式 bypass FilesystemMiddleware 的安全策略保留)。
* **R92.1** (`147a849`): ``olav.core.skill_runner.execute_skill_script`` 受控子进程运行器(11 unit tests:path-traversal / symlink-escape / non-py-extension / timeout 等都拦了)。
* **R92.2** (`8bc1c82`): ops/lab POC 完成 — 6 skill scripts 在 ``ops/lab/scripts/``,``execute_skill_script`` 加入 ``core/tools/`` 全 agent 共享,SKILL.md + prompts/system.md + expert YAML 重写,``run_python_simulation`` 退出 ops/lab 工具表。

**剩余工作**:
* R92.3 — audit/auditor、ops/analyze、curator 也按 R92.2 模式转 skill scripts(预计每个 agent 1-2 小时)
* R92.6 — 真正接通 deepagents 原生 ``SkillsMiddleware``(opt-in via agent metadata),让 SKILL.md 走渐进披露(metadata 总在 prompt,full content 按需加载)而不是 ``static_context`` 全量注入。当前 R92.2 的省 token 只来自工具数量减少;SKILL.md 渐进披露还能再省。
* R92.7 (task #228) — 删 4 个 R91/R92 沉积的 paraphrase-only expert.yaml(关联 ISSUE-SKILL-MEMORY-DUPLICATE-CONTRACT)
* in-vivo 验证 ch4→ch5 (task #220)

**症状**:

R91 治理(folder ADR-0007)的方向是把 @tool 包装的工具折成
``olav.core.<domain>`` Python 函数,再让 agent 在 ``run_python_simulation``
里 import + call。这相比每个工具一个 @tool 包装确实省 prompt(~150-300
tokens / 工具 → 共用一个 ``run_python_simulation`` ~200 tokens)。

但是,**deepagents 本身有原生的 ``SkillsMiddleware``**,走的是完全不同
的路径:

* 加载 SKILL.md 作为系统 prompt 上下文(渐进披露:只显示 name +
  description,完整内容按需读取)
* 脚本通过**绝对路径**列在 SKILL.md 里
* Agent 用通用工具(Bash / read_file)调用这些脚本
* **零 per-tool prompt 开销** —— 工具描述根本不进 LLM tool schema

OLAV 当前**没启用 ``SkillsMiddleware``**。``src/olav/agents/agent.py:400``
直接 ``create_deep_agent(tools=orchestrator_tools)`` —— 全部依靠自己的
``discover_tools()`` 扫 ``@tool`` / ``StructuredTool``。

**三种模式对比**:

| 模式 | 每工具 prompt 开销 | 执行位置 | OLAV 状态 |
|---|---|---|---|
| ``@tool`` 包装(原始) | ~150-300 tokens(docstring + Pydantic schema 进 tool schema) | langchain in-process | ✅ 历史路径 |
| sandbox Python(ADR-0007) | 0(共用一个 ``run_python_simulation``) | 同进程 sandbox 子进程 | ✅ R91 折叠目标 |
| **deepagents SkillsMiddleware** | **0(Bash 已经是通用 surface 一部分)** | **subprocess via Bash** | ❌ **没用** |

**ADR-0007 的盲点**:

ADR-0007 提了 4 条件测试(privilege / sandbox-write / audit / DB-join),把
非特权工具应该折成 sandbox Python。但**完全没考虑 SkillsMiddleware
这条**。所以 R91 把 20 个工具折下来后,sandbox 工具自己还要 ~200 tokens
描述,而 SkillsMiddleware 路径的开销是 0。

折算到本会话 R91 治理结果:

* Step 1-4 折叠了 20 个工具,节省 ~3600 tokens
* 但所有这些工具的**最终归宿**应该是 SKILL.md 里的脚本路径
* sandbox Python 是中间态,不是终态

**应该的 3 阶段**:

| 阶段 | 状态 | 描述 |
|---|---|---|
| 1. 全 @tool 包装 | 历史 | 每工具 ~150-300 tokens |
| 2. 非特权工具 → sandbox Python | ADR-0007 / R91(已做) | sandbox 工具 ~200 tokens 共用 |
| 3. 非特权工具 → SkillsMiddleware + 脚本 | ❌ 应该是终点 | 0 token,只剩特权工具 @tool |

**真正需要保留 @tool 的 4 类**:

1. **共享 stateful sandbox handle**(跨调用持有 Python heap state)—— OLAV 没这种
2. **Pydantic args validation 在 LLM-tool 边界拦截**—— 强类型需求
3. **langchain 原生 retry/streaming 集成**—— OLAV 没用
4. **特权 in-process operation**(CLAB REST / scrapli SSH,依赖 agent 的 process env)—— **真有**

只有(4)是硬需求。其他都是历史选择。

**ADR-0007 是对的方向但不够远**。真正彻底的治理:

* 切换到 ``SkillsMiddleware``(opt-in per agent,降低风险)
* 给需要的 agent 加 Bash / read_file 工具(deepagents 自带的
  FilesystemMiddleware 已被 OLAV 显式禁用,需要重新评估)
* R91 折下来的 20 个工具迁回 ``.olav/workspace/<skill>/scripts/*.py`` 形式
* SKILL.md 里描述 + 绝对路径
* 把 ``run_python_simulation`` 保留给真正需要 sandbox 隔离的工作

**实施计划**(R92):

* R92.0: 写 ADR-0008 SkillsMiddleware-First 工具架构(supersedes
  ADR-0007 的中间态)
* R92.1: ops/lab 作为概念验证 —— 启 SkillsMiddleware,把 R91 fold
  下来的 6 个 olav.core.lab/cab Python helper 转成
  ``ops/lab/scripts/*.py`` skill scripts;in-vivo 验证 ch4→ch5
* R92.2: 评估其他 agent 一个个迁(audit/auditor / ops/analyze / curator)
* R92.3: 度量真实 prompt token 节省(估算 ~3600-4500 tokens 之外又
  省 ~200 × N agents)

**实施门控**(同 ADR-0007):每个 agent 迁完先 in-vivo 验证,再上下一个。

**和 ISSUE-MCP-OVER-TOOLING 的关系**:

* ISSUE-MCP-OVER-TOOLING:每工具一个 @tool 是反模式(R91 折叠中)
* ISSUE-DEEPAGENTS-SKILLS-MIDDLEWARE-NOT-USED:**折叠到 sandbox
  Python 也不是终态**,SkillsMiddleware 才是 deepagents 原生路径
* 后者 supersedes 前者的"sandbox Python 终态"主张

**R91 的工作不浪费**:

* ``olav.core.<domain>``(cab / lab / auditor / curator / netops.diff)
  作为**业务逻辑库**保留 —— 这是 Python API,不是 MCP 工具
* SkillsMiddleware 路径下,scripts 仍然 import 这些 helper
* 改的只是"agent 怎么调用" —— 从 ``run_python_simulation`` import 变成
  Bash 调用脚本

---

### ISSUE-MCP-OVER-TOOLING: MCP 包装本质是 Python 函数的工具,导致 prompt 上下文爆炸

**状态**: ✅ Closed (R91+R92.2/3/6 累计 — 25 个工具折叠为 skill scripts 或 olav.core Python helper;ops/lab agent base prompt 实测从 ~13,045 → 7,806 tokens (-40%);其他 agent 同 pattern 折叠)
**优先级**: P0 → 已 close
**关闭日期**: 2026-04-27 R92 close-out (实测验证)
**发现日期**: 2026-04-27(R90 落地后用户审计)
**关联 commits 引入此模式**: `efe381e`, `37b4bb7`, `a507006`, `cde0103`,
`0ce414f`, `4487832`, `cacae40`, `8eaad54`(R88-A / R89 / R90 / TCF / F3 全套)
**关联 commits 修复此模式**:
* R91 (`d59ebe5`): 4 步折叠 — ops/lab 6 + audit/auditor authoring 4 + ops/analyze 5 + audit/curator 5 = 20 工具进 ``olav.core.{cab,lab,auditor,curator}``,~3600 token 节省 / agent 调用
* R92.0 (`147a849`): ADR-0008 + ``execute_skill_script``(controlled subprocess runner,11 unit tests)
* R92.2 (`8bc1c82`): ops/lab 概念验证 — 6 skill scripts via ``execute_skill_script``,run_python_simulation 退出 ops/lab 工具表

**剩余工作**:
* R92.3 — audit/auditor / ops/analyze / curator 转 skill scripts(类似 R92.2 路径)
* CUT 2 (task #222) — audit/auditor map_engine + render_report 主流程折叠
* Step 5 — topology + core 边际(~5-8 个候选)

(以下 "症状" / "为什么不该有" 等节为原始登记内容,作为历史记录保留)

**症状**:

R88-A → R89 → R90 → TCF 整条线我建了 7 个新 MCP 工具:
``generate_clab_topology``, ``generate_srl_lab_config``,
``generate_srl_rollback_config``, ``tcf_emit_from_sim``,
``tcf_load_for_lab``, ``tcf_record_lab_run``,
``append_validation_footer``. **每一个本质都是 Python 函数**,逻辑就是
``olav.core.cab.*`` / ``olav.core.lab.tools.*`` 模块里的纯 Python
代码。MCP wrapper 只是把 LangChain tool args 转成 Python 调用,
中间多了一道 schema 转换。

ops-lab 工具列表现在 **16 个 MCP 工具**,SKILL.md 加 prompts/system.md
里每次会话注入 ~3000-3500 tokens 工具描述。对 grok-4.1-fast 这种小模型
context 是真实成本:

* 每个工具 docstring + Pydantic args schema ≈ 150-300 tokens
* 16 工具 × 200 tokens 平均 ≈ 3200 tokens **永久** prompt 开销
* 工具越多,小模型选错的几率越高(R89 v1 empty-dict bug 就是这类)
* 每次 demo run 都付这个开销

**根因 — 架构方向选错了一档**:

R88-B v1 暴露的根本问题是"小模型在 free-form markdown 合成上不稳",
不是"小模型不能调 sandbox"。我把修复做成 MCP 工具(因为最直观),
却没意识到 ``run_python_simulation`` 已经是 deterministic sandbox,
有完整 Python(含 olav.core.cab Python API)。

正确的修法是:

```
expert_knowledge YAML(scope: shared:ops 或 ops-lab):
  → 教 agent 在 sandbox 里 import 哪些 Python API
  → 给 worked example,显式 R88/R89/R90/TCF 的 Python pattern

run_python_simulation:
  → agent 写 Python: from olav.core.cab import ...; tcf = CabTcf(...);
    tcf_emit(tcf, path)
  → sandbox 拿到结果,通过 _result 返回结构化 dict

少量 MCP 工具(只必要的几个):
  → 接外部系统的(CLAB API / SSH / 写 lab tmpdir / DB writes)
  → 不接外部、纯 Python 的一律不做 MCP
```

**架构规则(写下来不再犯)**:

> **MCP 工具 ⇔ 纯 Python 函数**:如果一个 "工具" 本质是 Python
> 函数 + 某些 args,**默认放进 ``olav.core.*`` 作为 importable Python
> API**,通过 ``run_python_simulation`` + memory guidance 调用。
>
> 只有以下情况才升级为 MCP 工具:
>
> 1. **需要 sandbox 外的特权**(CLAB REST API + auth、prod SSH、
>    privileged docker、网络服务)
> 2. **需要写到 sandbox 沙盒外的特殊位置**(deploy-lab 用的 tmpdir、
>    系统级配置)
> 3. **审计非常关键**(每次调用必须在 audit DB 里有独立 row,
>    不能藏在 Python 调用栈里)
> 4. **Args schema 验证 critical**(LangChain Pydantic 在 tool boundary 拦截
>    比 Python TypeError 更好的场景 —— 实际很少)
>
> 这四个条件**任一不满足都用 Python**。

**应折回 sandbox + memory 的 7 个工具**:

| 工具 | 当前位置 | 应改为 |
|---|---|---|
| `generate_clab_topology` | `ops/lab/tools/generate_clab_topology.py` (MCP) | `olav.core.lab.tools.generate_clab_topology` (Python API) + expert YAML 教法 |
| `generate_srl_lab_config` | `ops/lab/tools/generate_srl_lab_config.py` (MCP) | `olav.core.lab.tools.render_srl_ebgp_direct` (Python) + expert YAML |
| `generate_srl_rollback_config` | `ops/lab/tools/generate_srl_rollback_config.py` (MCP) | 同上,反向 |
| `tcf_emit_from_sim` | `ops/analyze/tools/tcf_emit_from_sim.py` (MCP) | `olav.core.cab.CabTcf(...)` + `tcf_emit(...)` —— 已经是 Python API,MCP wrapper 是多余 |
| `tcf_load_for_lab` | `ops/lab/tools/tcf_load_for_lab.py` (MCP) | `olav.core.cab.tcf_load(...)` + `tcf_to_r88/r89_args(...)` —— 同上 |
| `tcf_record_lab_run` | `ops/lab/tools/tcf_record_lab_run.py` (MCP) | sandbox 3 行: `tcf = tcf_load(p); tcf.lab.verdict=...; tcf_emit(tcf, p)` |
| `append_validation_footer` | `ops/lab/tools/append_validation_footer.py` (MCP) | deepagents `write_file` mode=append,或 sandbox `Path(p).open('a').write(...)` |

**仍必须保留 MCP 的工具**(满足规则中的 1-3 条):

* `save_lab_config`(写 lab tmpdir,deploy_and_push_lab 读这位置)
* `deploy_and_push_lab` / `deploy_lab` / `destroy_lab`(CLAB REST API + auth)
* `push_node_config` / `exec_on_node`(CLAB SSH + auth)
* `create_srl_links` / `fix_srl_topology`(privileged docker host SSH)
* `take_snapshot` / `execute_cli` / `execute_cli_parallel`(prod SSH)
* `search_commands` / `register_service` / `record_network_event`(DB writes 需 audit)
* `diff_*`(4 个 cross-snapshot DB joins)

**估算**:折叠 7 个工具 → 净省 ~1500-2000 tokens 永久 prompt 开销
(每次小模型对话);删除 7 个 .py + 7 个 test 文件;新增 1 个 lab_render.py
+ 1 个 expert YAML(workflow guide);ops-lab SKILL.md / system.md 简化。

**修复路线**(R91 候选):

1. ``src/olav/core/lab/tools/`` 新建模块,把 7 个工具的核心逻辑从 MCP 文件搬入(纯 Python 函数,无 @tool 装饰器)
2. 现有 unit tests 改为对 Python 函数测(测试覆盖不变)
3. 写 ``cab_validation_workflow.expert.yaml`` (scope: shared:ops):
   * 触发关键词: ``validate``, ``cab spec``, ``digital twin``, ``tcf``
   * body: 完整 8 步 sandbox Python pattern,含 worked example
4. 删 7 个 ops/lab/tools/*.py + ops/analyze/tools/*.py 的 MCP wrapper
5. 简化 SKILL.md tools list + prompts/system.md mandatory sequence,
   引导 agent 用 ``run_python_simulation`` + memory recall
6. in-vivo 跑一次 ch4→ch5,确认折叠后小模型仍能正确编排

**预计成本**:5-7 小时实施 + 1 小时 in-vivo 验证。

**长期规则**:在 ``CLAUDE.md`` / 设计 doc 里加一条:**默认 Python API,
不默认 MCP**。这条规则的好处:

* 工具表保持紧凑 → 小模型 prompt 预算友好
* 边界清晰:MCP = 必须的特权操作;Python = 一切其他
* 教学/扩展更便宜:新 intent 类型 = 加 Python 函数 + memory entry,不再
  需要造新 MCP 工具
* 跟 R87/R88/R89/R90 的 Type B 模式完全一致 —— 那些 deterministic
  generator 的 **核心** 都是 Python,MCP 包装只是历史包袱

---

### 全 agent MCP 治理审计(2026-04-27)

按上面规则全 workspace 扫了 14 个 agent / sub-agent,统计每个工具的归属:

✅ = MCP-essential(满足规则中至少 1 条:特权 / 外部系统 / 写沙盒外位置 / 关键 audit row)
❌ = 候选折叠(纯 Python,可走 sandbox + memory)
⚠️ = 需要看实现才能定

| Agent | Total | Tools (✅ essential / ❌ fold-candidate / ⚠️ check) |
|---|---|---|
| **core**(orchestrator)| 3 | ✅ execute_sql · ✅ recall_memory · ✅ web_search |
| **core/admin** | 7 | ✅ bulk_ingest · ✅ manage_cron · ✅ deploy_service · ✅ stop_service · ⚠️ workspace_health · ⚠️ analyze_logs · ❌ write_workspace_file(deepagents `write_file`)|
| **core/api_query** | 4 | ✅ api_request · ⚠️ service_health · ✅ web_search · ⚠️ format_and_export(目前提供 audit;sandbox 也能写)|
| **core/db_query** | 2 | ✅ execute_sql · ❌ describe_table(`db.execute('DESCRIBE x').fetchall()`)|
| **core/remote** | 2 | ✅ remote_execute · ✅ run_shell |
| **core/writer** | 0 | (LLM 渲染,不需要工具) |
| **audit/auditor** | 11 | **❌×9** map_engine / render_report / anomaly_engine / baseline_engine / incident_engine / database_introspection / test_map_query / analyze_thresholds / read_profile · ⚠️ save_profile · ⚠️ append_jobs |
| **audit/curator** | 5 | ❌ discover_view_schemas · ❌ fuzzy_map_schema · ❌ list_recipes(if mirror) · ⚠️ sync_schema_reference · ⚠️ scaffold_domain_agent · ❌ trace_learner |
| **command_learner** | 2 | ✅ learn_commands(子代理调用) · ✅ cmd_learn(slash 入口) |
| **services** | 4 | ✅ register_service · ✅ deploy_service · ✅ stop_service · ✅ api_request |
| **ops**(orchestrator)| 6 | ✅ execute_cli · ⚠️ search_commands · ✅ take_snapshot · ⚠️ diff_configs · ✅ record_network_event · ✅ register_service |
| **ops/analyze** | 6 | ✅ run_python_simulation(sandbox 本身)· **❌ tcf_emit_from_sim** · **❌×4** diff_sql_state / diff_topology_drift / diff_routing_drift / diff_configs |
| **ops/collect** | 1 | ✅ execute_cli_parallel |
| **ops/devops** | 0 | (仍未 ship)|
| **ops/infra** | 0 | (仍未 ship)|
| **ops/lab** | 14 | ✅×9 save_lab_config / deploy_and_push_lab / deploy_lab / push_node_config / exec_on_node / create_srl_links / fix_srl_topology / destroy_lab / run_python_simulation · **❌×5** tcf_load_for_lab / generate_clab_topology / generate_srl_lab_config / generate_srl_rollback_config / tcf_record_lab_run |
| **topology** | 5 | ❌ query_topology(纯 SQL+格式化)· ⚠️ discover_recipe · ⚠️ save_recipe · ✅ rebuild_views(DDL)· ❌ list_recipes |

**总计:71 工具 → 大约 30+ 折叠候选(40%+)**

按"治理回报 / 工作量"优先级排:

| 优先级 | Agent | 折叠 | 主要影响 |
|---|---|---|---|
| 🔥 P1 | **ops/lab** | 5 个(本会话引入)| 14 → 9 工具,清干净 R88-A/R89/R90/TCF 的 MCP 包装 |
| 🔥 P1 | **audit/auditor** | 9 个 | 11 → 2-3 工具,**最大单 agent 收益**(prompt 节省 ~1500-2000 tokens) |
| 🟡 P2 | **ops/analyze** | 5 个 | 6 → 1-2 工具(sandbox + 必要 audit) |
| 🟡 P2 | **audit/curator** | 3-4 个 | 5 → 1-2 工具 |
| 🟢 P3 | **topology** | 2-4 个 | 5 → 1-3 工具 |
| 🟢 P3 | **core/admin / api_query / db_query** | 1-2 个 each | 边际收益,可批量带 |
| ⏸️ skip | **core / core/remote / services / ops/collect / command_learner** | 0-1 | 已经精简 |

**估算总治理工作量**(Python API 化 + memory guidance + SKILL.md 简化):

* P1(ops/lab + audit/auditor):1.5-2 天
* P2(ops/analyze + audit/curator):0.5-1 天
* P3(topology + core 边际):0.5 天
* **合计 2.5-3.5 天**

**估算 prompt 收益**:

* P1 alone:省 ~3000-3500 tokens 永久 prompt 开销(每次小模型对话)
* P1+P2:省 ~5000+ tokens
* 全治理:净节约 ~6000-7000 tokens 工具描述 + audit 补丁 ~ 后续每次小模型会话都得益

**audit/auditor 的特殊位置**:

11 个工具几乎都是"DB 查询 + Python 分析 + 格式化 + 写文件"的组合。
这 agent 的设计本身就违反"少工具 + sandbox + memory"原则 —— 它把每个
分析维度做成一个工具。**应该重新审视 audit/auditor 的整体架构** —— 
不只是工具数量,可能是 **agent 设计本身需要 R91 治理之前先重构成 sandbox-first**。

**实施门控**(避免一次性大改风险):

每个 agent 的折叠都遵循:

1. 把 MCP 工具的核心 Python 函数搬进 ``src/olav/core/<domain>/tools/``
2. 现有 unit test 改测 Python 函数(覆盖不丢)
3. 写 expert_knowledge 或 usage_guide YAML 教 agent 怎么 import + 调
4. 简化 SKILL.md tools list + prompts/system.md
5. **in-vivo 跑一次** 该 agent 的核心 demo,确认工作不退化
6. 上线后再删掉 MCP wrapper(留一个 sprint 缓冲期)

**推荐启动顺序**:

P1.1 → ops/lab 折叠(本会话造的 5 个,语境最熟,最容易清)
P1.2 → audit/auditor 治理(收益最大,但需要 deeper agent 重构)
P2 → ops/analyze + audit/curator(批量带)
P3 → 收尾

---

### ISSUE-OPS-TOPOLOGY-ROUTING: ops orchestrator 误将 topology 可视化分派到 data-discovery skill (R83.4 / Chapter 4)

**状态**: 🟢 Mostly Mitigated (alias auto-route 落地; 仍有 prompt-fidelity 残余) · **优先级**: P1
**发现日期**: 2026-04-26 (Chapter 4 demo runsheet 验证)
**修复日期**: 2026-04-26 (`olav.agents.delegate_tool._SEMANTIC_ALIASES`)

**修复摘要**:

`src/olav/agents/delegate_tool.py` 加入跨工作区语义别名表 + best-match hint：

```python
_SEMANTIC_ALIASES = {
    "topology":     ("ops-analyze", "analyze", "writer"),
    "topology-viz": ("ops-analyze", "analyze"),
    "diagram":      ("writer", "ops-analyze"),
    "mermaid":      ("writer", "ops-analyze"),
    "simulation":   ("ops-analyze", "analyze"),
    "drift":        ("ops-analyze", "analyze"),
    "diff":         ("ops-analyze", "analyze"),
    ...
}
```

`olav_delegate(subagent_name='topology', ...)` 从 ops 调用时：
1. `topology` 不在 ops 的 `_compiled_runnables` → 触发 `_resolve_alias`；
2. 候选 `("ops-analyze", "analyze", "writer")` 中第一个命中的（`ops-analyze`）成为路由目标；
3. 调用透明转发到 `ops-analyze`，无需 LLM 重试。

未命中时返回 "Did you mean 'X'?" 建议（基于 substring + token-overlap 的 `_best_match`），代替之前的扁平 list 错误，让 LLM 二次纠正更可靠。

**验证**：单测 `tests/unit/test_delegate_alias.py` 10 用例（alias 路由 / case-insensitive / 缺失 fallback / "did you mean" 建议）全过；demo5 实测 `olav_delegate('topology', 'ping')` 现返回 `"Pong! Ready for network analysis"`（来自 ops-analyze），不再 "not found"。

**架构补丁**（同轮一并修复）:
* `ops/AGENT.md` 加入 `- path: ../core/writer/SKILL.md` 跨工作区 subagent——**writer 是 core 的子 agent，但 ops/audit 之前都看不到它，根本没法委派保存**。这是真架构 gap：`writer` 拥有唯一的 `format_and_export` 工具，所有 orchestrator 都该能委派；
* `ops/prompts/orchestrator.md` 加 **HARD RULE #0** 在最顶："任何 save / export 请求 → `olav_delegate('writer', ...)`，不调用即虚构"。

**Prompt-fidelity 残余 → SaveAssertionMiddleware 兜底**（同轮已落地）:

写了 `src/olav/plugins/middleware/save_assertion.py` 作为 deepagents 后置 hook（`aafter_agent`）：

1. **检测**：扫 final assistant message，看有无保存声明短语（`saved to` / `saved as` / `wrote to` / `exported to` / `report saved` / `保存到` / `已保存` / `已导出`）；
2. **验证**：扫 message 历史看有无 `format_and_export` ToolMessage 或 `olav_delegate(subagent_name='writer', ...)` 调用；交叉验证文件是否真在 disk 上；
3. **救援**：缺失证据时从 assistant content 提取 Mermaid 块（` ```mermaid ``` ` 围栏 / 裸 `graph TD/LR/...` 语法），写入 `exports/diagrams/recovered_<timestamp>.mmd`；
4. **降级**：内容无 Mermaid 时附 warning supplement 提示用户路径不存在。

实测：grok-4.1-fast 漏调 writer 时，`recovered_<ts>.mmd` 文件实际生成（直接打开可看到完整 graph TD subgraph）；single-step 单测 10 用例全过（claim 检测 / 信任路径 / 救援 / 边缘 case）。这把"声称保存但漏调工具"这一类幻觉**从工具层面彻底封堵**——不依赖 LLM 行为改善。

**全 agent 文件输出覆盖矩阵**（同轮审计后扩展）：

| Agent/Subagent | 文件类型 | 写入工具 | 漏调风险 | 兜底机制 |
|---|---|---|---|---|
| `core/writer` | 全部 | `format_and_export` | tool 自身 — 不涉及幻觉 | N/A |
| `audit/auditor` | `.md` 健康报告 | `render_report` (本地) | 声称"Report saved"未调用 | ✅ SaveAssertion (markdown 救援 + render_report 加入 _SAVE_TOOLS) |
| `ops/devops` | `.sh`/`.py` 脚本 | format_and_export via writer | 声称脚本路径未调用 | ✅ OutputFormatter `_auto_export_script` (SaveAssertion 让步) |
| `ops/infra` | `.json` 变更集 | format_and_export via writer | 声称变更集保存未调用 | 🟡 SaveAssertion warning (JSON 不可从 prose 救援) |
| `ops/lab` | `.clab.yaml` + 配置 | `save_lab_config` (本地) | 声称 lab 保存未调用 | ✅ SaveAssertion (save_lab_config 加入 _SAVE_TOOLS;不强行救援防破坏配置) |
| `ops/analyze` | Mermaid + `.md` 报告 | format_and_export via writer | 漏调 writer | ✅ SaveAssertion (Mermaid + Markdown 两路救援) |

**SaveAssertion 现状**（commit `<TBD>` 扩展）：

* `_SAVE_TOOLS` = `{format_and_export, render_report, save_lab_config, save_profile}` —— 一致涵盖所有 first-class save 工具；
* `_SAVE_DELEGATIONS` = `{writer, audit-auditor, ops-lab}` —— orchestrator 看到这些 subagent 委派即视为有保存意图；
* `_DEFERRED_TO_OUTPUT_FORMATTER` = `{sh, py}` —— 显式让 OutputFormatter 处理脚本场景，避免双警告；
* **救援 dispatch**: Mermaid (.mmd) → `_extract_mermaid` → `exports/diagrams/` ; Markdown 报告 (>200 chars + 多结构元素) → 写整 chat → `exports/reports/` ; 其他扩展（.csv/.json/.yaml）→ 警告路径（结构化数据从 prose 提取风险大于收益）。

**未来扩展模板**（每新增一类工具漏调）：
1. 加 tool 名到 `_SAVE_TOOLS` (信任路径)
2. 若有 subagent 包装，加到 `_SAVE_DELEGATIONS`
3. 若有 chat-extractable 内容，加到 `_attempt_recovery` dispatch
4. 否则依赖 warning path

测试: `tests/unit/test_save_assertion.py` 18 用例覆盖各 tool 信任 / 各类型救援 / 警告 / extension dispatch。

---

**症状**: `olav --agent ops "Show the network topology as a Mermaid diagram and save to exports/"` 触发 ops orchestrator 将请求 `olav_delegate("topology", ...)`——分派到全局 `topology` skill（一个**数据发现**层，专门管 BGP/OSPF/CDP-LLDP 协议关系 recipe），而不是 `task("ops-analyze")`。后果：
1. 调用爆炸（16-17 工具调用 vs core agent 路径 3 调用）
2. **声称已保存到 `exports/network_topology.mmd` 但 `format_and_export` 从未被调用**——纯虚构（ops orchestrator 工具列表里 `format_and_export` 在场，但 LLM 漏调）
3. ops AGENT.md OVERVIEW 还残留 `probe`/`diff` 等已合并的 subagent 名

**根因**:
1. 全局 `topology` skill 名（位于 `.olav/workspace/topology/`，`MANIFEST.yaml.kind=Agent`）被 LLM 关键字匹配抢先选中——单词命中比读 prompt 委派表权重高；
2. orchestrator prompt 委派表**未列出** topology 可视化路由（已修：commit `<TBD>` 加入"Topology diagram → ops-analyze"）；prompt 修改单独不够，因为还有上面 #1；
3. 小模型（grok-4.1-fast）对 prompt 中"⚠️ Do NOT olav_delegate('topology')"警告响应不可靠。

**修复方向**（三选一或组合）:
1. **重命名全局 topology skill** → `protocol_topology` / `topology_recipes`，避免名字单词冲突；
2. **作用域限制** `olav_delegate`：从 ops orchestrator 调用时只暴露其 AGENT.md 显式声明的 subagents（不含 sibling 顶层 agents）；
3. **后置 file 校验**：`format_and_export` 调用后，orchestrator 在最终回复前 read-back 检查 file 存在，缺则重试；

**短期 workaround**: 用 core agent + writer 路径（`olav "Show topology..."` 不带 `--agent ops`）—— core agent 委派 writer 子 agent，writer 模板里有强制 `format_and_export` 步骤。但 writer 的 data 参数序列化也有问题（见下条 issue）。

**关联**: `olav-netops/.olav/workspace/ops/AGENT.md` 第 31 行 OVERVIEW 残留 `probe`/`diff` 已合并的子 agent 名——单独清洗 doc。

---

### ISSUE-OPS-LAB-CANT-TRANSLATE-PROD-TO-SRL: ops-lab 不能可靠地把 prod CLI 翻译成 SRL CLI(根因:三个 infra 失败叠加)

**状态**: ✅ Closed (2026-04-28 in-vivo e2e PASS — 真实 CLAB 部署 R1/R4 → BGP Established 收敛 → rollback push → BGP 清除 → TCF 写回 verdict=PASS)
**优先级**: P0 → 已 close
**关闭日期**: 2026-04-28 task #220 in-vivo 验证通过 (12/12 stages green)
**E2E 跑通的链路**:

```
sim: tcf_emit_from_sim
  ↓
lab: tcf_load_for_lab → r88_args + r89_args
  ↓
lab: generate_clab_topology(**r88_args) → CLAB YAML w/ links:
  ↓
lab: generate_srl_lab_config(**r89_args) → 22 lines × {r1, r4}
  ↓
lab: save_lab_config(per node) → exports/lab/<lab>/{r1,r4}.json dropbox
  ↓
lab: deploy_and_push_lab → REAL CLAB deploy via REST API + scrapli SSH
  ↓
lab: exec_on_node bash -c 'sr_cli show ...' → "Established" detected
  ↓
lab: generate_srl_rollback_config(**r89_args) → 7-line delete CLI × {r1, r4}
  ↓
lab: push_node_config(rollback per node) → committed=True
  ↓
lab: exec_on_node → no Established (BGP gone)
  ↓
lab: tcf_record_lab_run → TCF.lab.verdict=PASS, TVT 全 PASS
  ↓
lab: destroy_lab → "Lab destroyed successfully"
```

**总耗时**: ~85s wall-clock(deploy 40s + BGP 收敛等待 20s + 其他)。
**凭据**: `olav/olav123` 写入 `.olav/workspace/ops/lab/config/config.json`(per olav-doc lab.en.md prerequisites)。
**发现日期**: 2026-04-27(R87 Phase 1.5 in-vivo + Chapter 4 → Chapter 5 closed loop)
**关联 task**: #220(in-vivo ch4→ch5 复跑验证)
**关联 dev_docs**: `dev_docs/63`, `dev_docs/64 § Type B 分类`, `dev_docs/42 § Chapter 5`

**Mitigation 链(R89 → R91 → R92.2)**:

* **R89**: ``generate_srl_lab_config`` deterministic Type B 翻译器 — 从 3 parallel
  arrays(nodes/loopbacks/asns)+ intent_type 渲染 22 行 SRL CLI 骨架。
  消除 LLM 自由翻译的 YANG 拒绝模式(``connectivity-endpoint`` /
  ``group type external`` / 缺 ``peer-group`` / ``afi-safi`` 错位)。
* **R91**: 翻译器作为 Python 函数定居 ``olav.core.lab.srl_render``。
* **R92.2**: ops/lab agent 通过 ``execute_skill_script(skill_name="lab",
  script_name="generate_srl_lab_config.py", args={...})`` 调用 — 系统提示 +
  expert YAML 双重禁止 hand-translation,ops-lab 工具表里没有 free-form
  CLI 写入路径。

留待关闭的步骤:in-vivo 跑一次 demo6 ch4→ch5,确认 BGP Established + 路由
交换正常(task #220)。

**架构纠正**(rev 2,2026-04-27):

之前这条 issue 错误地把责任放在 ops-analyze 上("sim 输出缺 SRL section")。**那个判断是错的**。

正确架构:
* **ops-analyze 的契约**: 产出 prod-aligned CAB spec — R1=Junos CLI、R4=IOS CLI,真实生产工程师跑的命令。**不应**写 SRL section。
* **ops-lab 的契约**: 拿 prod spec,内部把 prod CLI 翻译成 SRL CLI(因为 CLAB digital twin 只支持 SRL 容器),部署 + 验证。

之前看到 v3 hand-authored spec 同时含 prod section + SRL section,误以为 SRL section 是 sim 的责任 —— 实际上 v3 是为绕开 lab 翻译能力不足的临时手工拼接,不是契约。

**真实失败的是 ops-lab 的翻译流程**。

**症状**:

ops-lab 读到 ch4 prod spec(Junos R1 + IOS R4),试图翻译成 SRL CLI 部署到 r1/r4 容器,SRL v24.10.1 YANG 拒绝 4 处:

* `connectivity-endpoint` path 在 SRL 中无效
* `bgp group type external` 不支持(SRL 用 peer-as 不同推断 type)
* `neighbor` 必须显式 `peer-group`
* `afi-safi ipv4-unicast` 必须 enable(默认拒绝)

**根因 — 三个独立 infra 失败叠加,导致 agent 在 0 知识支持下硬写 SRL**:

ch5 run 实测 trace:

```
1. agent recall_memory("junos_vs_ios_bgp_translation")  → ?
2. agent recall_memory("SR Linux BGP translation...")    → ?
3. agent web_search ×4                                   → ❌ ddgs not installed
4. agent 编 SRL config → SRL YANG reject ×4              → CAB FAIL
```

3 个独立故障:

| 失败点 | 根因 | 后果 |
|---|---|---|
| **AutoRecall 中间件 crash** | R87 Phase 1.5 commit 15ea302 引入 metadata 处理 bug——`memory.get('metadata')` 返回 JSON string 而非 dict,但 `_precedence_rank` 在它上面调 `.get()` → `'str' object has no attribute 'get'` → 整个 enrich() 抛异常,`recall_memory` 调用即使能跑也会因为 AutoRecall 不工作而拿不到 expert_knowledge | agent prompt 里**完全没有 recall block** —— shared:ops 知识完全不可见 |
| **recall_memory 工具失败** | embedder 出 credit(OpenRouter 402)|tool 调用返回失败 |
| **web_search 工具失败** | `ddgs` python 包未安装 | 6 次 fallback 全 fail |

**P0 修复 1(已落地,commit `b6f3af1`)**: middleware.py JSON-decode metadata before `.get()`。回归测试 2 个,43/43 expert_kb tests 全绿。这恢复了 AutoRecall 的工作。

**P1 修复 2(待办)**: recall_memory 工具的 embedder fallback —— 当云端 embedder 不可用时降级到 BM25 / FTS keyword search,确保工具不会因 credit 问题完全失效。

**P1 修复 3(待办)**: `pip install ddgs` 加进 dependency,或者 web_search 失败给一个清晰错误而不是 silent。或者干脆从 ops-lab 工具 list 移除 web_search(prompt 早就禁了)。

**根本性修复 — 同 R88-A 模式的 prod→SRL deterministic 翻译工具(候选 R89)**:

即使三个 infra 失败修好,LLM-driven 翻译 prod CLI → SRL CLI 仍然脆弱(小模型 tool-arg adherence gap,R88-B v1 已证明)。架构正确的修法:**ops-lab 内部的翻译 deterministic 化**。

新工具 `ops/lab/tools/generate_srl_lab_config.py`:

```python
generate_srl_lab_config(
    devices: list[dict],     # [{"name": "R1", "platform": "junos",
                             #   "prod_intf": "ge-0/0/2", "loopback": "1.1.1.1",
                             #   "asn": 65000, "peers": [{"name":"R4","asn":65001}]}, ...]
    change_intent: dict,     # 从 prod spec 解析出的 intent
                             # {"type": "ebgp_direct", "lab_subnet": "172.16.99.0/30"}
) -> dict[str, str]          # {"r1": "set / interface ...", "r4": "..."}
                             # 每节点 23 行 SRL CLI,IPs/AS/group 自动替换
```

ops-lab 工作流变成:

```
1. read_file(prod_spec)
2. parse: 抽 change intent + devices
3. execute_sql netops DB → 拿真实 platform/asn/loopback/intf
4. generate_clab_topology(devices, lab_name)         ← R88-A 已有
5. generate_srl_lab_config(devices, intent)          ← R89 新工具
6. save_lab_config(r1, srl_config["r1"])
7. save_lab_config(r4, srl_config["r4"])
8. deploy_and_push_lab
9. exec_on_node verify
10. CAB report
11. destroy_lab
```

step 5 的 SRL CLI 由确定性渲染产出,不再走 LLM synthesis。

**Type A vs Type B 二次审查**:

| YAML | 当前 type | 重新评估 |
|---|---|---|
| `bgp_validation_srl` | Type A | ✅ 真 Type A(state decoder 是判断/解读) |
| `ospf_validation_srl` | Type A | ✅ 真 Type A |
| `srl_config_push_gotchas` | Type A | ⚠️ 一半 Type B(`discard now / commit now` 模板可下沉) |
| `srl_spec_generation_rules` | Type A | ❌ **是 Type B** — 配置渲染规则,R89 工具直接用作模板源 |
| `junos_vs_ios_bgp_translation` | Type A | ⚠️ 半 Type B(eBGP 配置翻译表是 Type B,iBGP RR 语义判断是 Type A) |

**修复路线**:

1. ✅ P0 修复 #1:JSON-decode metadata(commit `b6f3af1`)
2. **R89 实施**: 新建 `generate_srl_lab_config` MCP tool 在 `ops/lab/tools/`(同 R88-A 模式)
3. **接入 ops-lab**: SKILL.md tool list + prompts/system.md mandatory sequence
4. **退役** `srl_spec_generation_rules.expert.yaml`(被 R89 完全替代后)
5. **拆解** `junos_vs_ios_bgp_translation.expert.yaml` Type B 部分到 R89
6. **修复 #2**: recall_memory embedder fallback
7. **修复 #3**: ddgs 依赖或 web_search 移除
8. **闭环验证**: Chapter 4 → Chapter 5 demo runsheet 跑一次

**演示影响**:

P0 修复 #1 已落地(AutoRecall 恢复)。Chapter 4 → 5 chain 仍依赖 R89 落地后才稳定可演。当前可靠路径:hand-authored spec → Chapter 5 单独演(R88-B v5 PASS)。

**与 F3(ISSUE-LAB-NO-SPEC-FEEDBACK)的关系**:

F3 让 lab 把验证结果写回 spec 文件(footer)。这是反馈环修复,与 R89(翻译能力修复)正交。两者都做才完整:

* 本 issue(R89) — 让 lab 一开始就翻译对
* F3 — lab 的 PASS/FAIL 判决回到 spec 文件,做未来参考

**变更日志**:

* rev 1(2026-04-27 早上): 错误地把责任放在 ops-analyze 上,提议 ops-analyze 写 SRL section
* **rev 2(2026-04-27 下午,本版本)**: 用户纠正:ops-analyze 应该 prod-aligned,翻译是 lab 的责任。三个 infra 失败叠加才是 ch5 run 实际崩塌的原因。R89 工具归 ops-lab。CAB_SPEC_FORMAT.md 的 SRL section 强制段已 revert。

---

### ISSUE-LAB-NO-SPEC-FEEDBACK: ops-lab 验证后不写回原 spec 文件,FAIL 诊断只对当前 session 有用

**状态**: ✅ Closed (R90 TCF + ``tcf_record_lab_run`` structured write-back;R91 Python helper;R92.2 skill script)
**优先级**: P1 (closed)
**发现日期**: 2026-04-27(R87 Phase 1.5 in-vivo, Chapter 4 → Chapter 5 closed loop)
**关闭日期**: 2026-04-27 (R92.2 close-out;链:R90→R91→R92.2)
**关联 dev_docs**: `dev_docs/63 § F3`, `dev_docs/65 § R90 TCF`

**Resolution 链**:

* **R90 TCF**: 以结构化 ``CabTcf`` Pydantic schema 替代 markdown spec,内
  置 ``lab`` section(verdict / lab_name / snapshot_id / ran_at /
  journal / diagnosis / recommendation)+ 每个 ``TvtRow`` 自带
  ``actual_lab`` / ``status`` 字段。
* **F3 ``append_validation_footer``**(legacy): 给纯 markdown spec 追加
  footer。仍保留作为 ``olav.core.lab.append_validation_footer`` /
  ``ops/lab/scripts/append_validation_footer.py``,但 R90 流程下不优先用。
* **R91**: ``tcf_record_lab_run`` 作为 Python 函数定居
  ``olav.core.cab.tcf_lab``,atomic write-back via temp + rename。
* **R92.2**: ops/lab agent step 7 调用
  ``execute_skill_script(skill_name="lab", script_name="tcf_record_lab_run.py",
  args={spec_path, verdict, lab_name, tvt_test_ids, tvt_actual_lab,
  tvt_status, journal, diagnosis, recommendation})`` —— SKILL.md +
  prompts/system.md 把这一步标为 mandatory,destroy_lab 之前必须执行。

**结果**: 任何人重新打开 spec.tcf.yaml 都能 deterministic 看到 lab 的
verdict + 完整 journal + per-test actuals,与下一步 sim 修订有 FK 引用链。
markdown 路径下走 ``append_validation_footer`` 也能保证 footer 持久化。

**症状**:

ops-lab 在验证一个 CAB spec 后,产生独立的 `cab_<lab_name>_report.md` 报告文件,**与原 spec 文件没有任何引用链**。例:

* sim 写的 spec: `exports/reports/cab_r1_r4_ebgp_ch4.md`
* lab 验证后的报告: `exports/reports/cab_r1r4_ch4_ch5_report.md`

任何人(或下次 agent run)重新打开 `cab_r1_r4_ebgp_ch4.md`,**没法知道**:
* 这个 spec 已经被 lab 验过
* 验证结果是 FAIL
* 4 条具体的 SRL YANG 拒绝原因是什么
* 推荐的下次 sim 修订方向

ops-lab 高质量的诊断只对当下这一回合有用,**session 结束后就丢了**。

**根因**:

ops-lab 流程当前的 step 5 是 `format_and_export(独立 lab report)` + step 6 `destroy_lab`。没有任何步骤往原 spec 文件追加备注。

**修复方向**(F3 — Lab Validation Footer):

ops-lab 在 `destroy_lab` 之前增加一步 **5b**: 如果原 spec 路径已知且文件存在,往文件**追加** `## 🤖 Lab Validation Footer` block,包含:

* Decision(PASS / FAIL / BLOCKED)
* Lab name + snapshot 用的
* 失败原因列表(从 lab report 的 issues 字段拉)
* 给 sim 下次修订的建议
* 完整 lab report 文件路径

原 spec 内容保留不动,footer 是**纯追加**。

**FAIL 例**(本来应该写到 `cab_r1_r4_ebgp_ch4.md` 的内容):

```markdown
... (sim 写的原 spec 内容)

---

## 🤖 Lab Validation Footer — appended by ops-lab on 2026-04-27 13:52

**Decision**: ❌ FAIL  **Lab**: `cab_r1r4_ch4_ch5`  **Snapshot**: `2026-03-30_2210`

**Why this spec failed lab validation**:

* Spec contains only Junos / IOS CLI — ops-lab needs SRL CLI to push to digital-twin containers.
* On-the-fly translation tripped 4 SRL YANG rejections:
  - `connectivity-endpoint` path is invalid in SRL v24.10.1
  - `bgp group type external` not supported (group type inferred from peer-as mismatch)
  - `neighbor` requires explicit `peer-group` reference
  - `afi-safi ipv4-unicast` must be enabled (default is rejected)

**Recommendation for sim revision**:

* Add `## SRL Lab Substitution Table` mapping prod R1/R4 → lab r1/r4 (lowercase, `ethernet-1/1`)
* Add `## Config Lines — node r1 (SRL CLI)` + same for r4 using the 23-line SRL skeleton
* Use a NEW /30 for lab link IPs (e.g. `172.16.99.0/30`), not prod IPs

**Full lab report**: `exports/reports/cab_r1r4_ch4_ch5_report.md`
```

**实施**:

* 改 `ops/lab/prompts/system.md` mandatory tool sequence,加 step 5b
* 加一个 file-append helper(20 LOC,或扩展 `format_and_export` 支持 `mode=append`)
* PASS / FAIL 都写 footer(PASS footer 标记"已验证可用",FAIL footer 标记"需修订")
* 多次验证累积(append 模式),形成 audit trail

**与 Phase 4(企业版 curator)边界**:

| F3(本 issue) | Phase 4(企业版,deferred) |
|---|---|
| Lab 在文件 append footer | Curator agent 评审 captures,提升/降级 confidence |
| Markdown append,无 schema | Pending pool + confidence model + audit log |
| 给下个 reader(人或 agent re-read 文件)看 | 给 future recall 看(boosted/demoted memory) |
| 零新基础设施 | 新 subagent + cron + reward propagation |
| 开源 | 企业版 |

F3 是 Phase 4 的"零成本前菜",可以提前到开源版,与 Phase 4 互不冲突。

**与 ISSUE-OPS-ANALYZE-NO-SRL-SECTION 的关系**:

互补,两者都做才完整(见前一条 issue 末尾的对照表)。

---

### ISSUE-WRITER-MMD-SERIALIZATION: writer 调用 format_and_export 时把字符串包成 dict (R83.4 / Chapter 4)

**状态**: ✅ Closed (`format_and_export` 多层兜底落地) · **优先级**: P2
**发现日期**: 2026-04-26 (Chapter 4 verification)
**关闭日期**: 2026-04-26 (`src/olav/data/workspace/core/writer/tools/format_and_export.py`)

**修复摘要**: 在 tool 入口处添加 4 层 LLM-quirk 兜底，自动展开各种错误包装：

1. **JSON 解析**（已有）: `data='{"mermaid": "..."}'` → JSON loads → dict；
2. **AST literal_eval**（新增）: `data="{'mermaid': '...'}"` (单引号 Python repr，JSON 失败) → ast.literal_eval → dict；
3. **正则提取**（新增）: dict-shaped 字符串内含**真实换行**（不是 `\n` 转义），ast.literal_eval 抛 "unterminated string literal" → `_extract_known_wrapper` 正则提取 `(key, value)` 元组，仅对已知 content key（mermaid / diagram / mmd / content / report / text / markdown）触发，bail-out 安全；
4. **list-of-strings 拼接**（新增）: `format='mmd'` 时若 `data=['line1', 'line2', ...]` → `'\n'.join(data)`；
5. **字典 key 扩展**（新增）: 单键 dict 内键 `mermaid` / `diagram` / `mmd` 现在也走 unwrap 路径，附带 auto-set `format='mmd'`；
6. **Mermaid 语法识别**（新增）: 单键 dict 的**值**以 `graph TD/LR/...` 等开头 → 识别为 Mermaid 救援；
7. **Code-fence 剥离**（新增）: `.mmd` 写入前去掉 ` ```mermaid ... ``` ` 围栏（rules 要求裸 Mermaid）。

**验证**：单测 `tests/unit/test_format_and_export_mmd.py` 11 用例全过——dict 包装 / Python repr / 真换行 / list 行 / 围栏 / format auto-detect / 旧 content key 回归。

---

**症状**: `olav "Show network topology and save mermaid to exports/"` (core agent 路径) 成功调用 `format_and_export`，但传入 `data={'mermaid': '<mermaid_text>'}`（dict 包装）而不是 `data=<mermaid_text>`（裸字符串）。结果文件内容是 dict 的 `repr`：

```
{'mermaid': 'graph TD\n    R4["R4<br/>core"] -->...'}
```

**预期**:
```
graph TD
    R4["R4<br/>core"] -->...
```

**根因**: writer subagent prompt 或 `topology_diagram.md` 模板对 `data=` 参数的类型期望不明确。模板里写的是 `data=<mermaid_text>` 但 LLM 习惯性按"结构化 payload"包了 dict。`format_and_export` 接收 dict 时按 JSON repr 写入，结果 .mmd 文件内容不可被 mermaid 渲染器识别。

**修复方向**:
1. **`format_and_export` tool 加 type assertion**：`format='mmd'` 时强制 `isinstance(data, str)`，否则返回错误让 LLM 重试；
2. **`topology_diagram.md` 加显式样例**：`format_and_export(data="graph TD\\n  ...", format='mmd', filename='topology')` 字面字符串；
3. **writer prompt 显式约束**：".mmd 格式必须 data=str，禁止 data=dict"；

**关联**: 这同时影响其他文本类导出（.sh / .py / .md 脚本），凡 LLM 习惯包 dict 的格式都受此问题。可能需要 tool 端统一兜底 (`if isinstance(data, dict): data = data.get('content') or json.dumps(data)`).

---

### ISSUE-FIELD-SCOPED-NORMALIZATION: 同字符串多语义需 field-name 级 scoping (R83.4)

**状态**: 🟡 Open · **优先级**: P3
**发现日期**: 2026-04-25 (R83.4 follow-up bug-hunt)

**症状**: junos `v_show_interfaces_auto.admin_state='Enabled'`(30) 是 RFC 2863 ifAdminStatus=`up` 的 vendor 表达，但同字符串 `Enabled` / `Disabled` 也用在：

* `v_show_ip_http_server_status_auto.server_status` — HTTP 服务开关
* `v_show_interfaces_switchport_auto.switchport` — 端口能力开关
* `v_show_vtp_status_auto.pruning` / `traps` — VTP 特性开关

**为什么 R83.4 没修**: 加全局 `("enabled", "up")` / `("disabled", "down")` 规则会污染上面这些非 ifAdminStatus 字段（HTTP server `up` 语义飘移）。Memory primer 已捕获 `Enabled`(30) 变体，靠 LLM 跨厂商推理够用。

**修复方向（待 cross-vendor admin_state 查询真实失败再做）**:

引入 field-name-scoped 规则表：
```python
_FIELD_SCOPED_RULES = {
    # 仅 admin_state / oper_state / *_admin_status 触发
    "admin_state": (
        ("enabled", "up"), ("disabled", "down"),
        ("active", "up"),  # cisco show running-config style
    ),
}
```

`_normalize_value` 中加一层：先按 field 名查 scoped 规则，再走全局 `_STATE_RULES`。

**触发条件**: 出现 cross-vendor admin_state 查询返回不一致结果 → 回来加 scoped 规则。否则保持现状（memory + LLM）。

---

### ISSUE-TEXTFSM-COLUMN-MERGE: BGP summary 列合并 state 与 prefix-count (upstream)

**状态**: 🟡 Open · **优先级**: P3
**发现日期**: 2026-04-25 (R83.4 follow-up bug-hunt)

**症状**: `v_show_ip_bgp_summary_auto.state_or_prefixes_received='0'(4)` —— ntc-templates 的 `cisco_ios_show_ip_bgp_summary.textfsm` 把两个语义压在一列：BGP session 未 Established 时存 state 名（`Idle`/`Active`/...），Established 时存 prefix count（`0`/`5`/...）。Agent 写 `WHERE state IN (...)` 时无法区分 `'0'` 是"prefix=0"还是别的状态。

**为什么不在 normalizer 修**: 这是 upstream textfsm 模板设计问题，normalizer 处理单字段，不能拆列。

**修复方向（择一）**:
1. 自定义 textfsm 模板覆盖 `cisco_ios_show_ip_bgp_summary.textfsm`，拆为两个独立列 `state` + `prefixes_received`，置入 `seed_templates/`；
2. View-level CASE 拆分：用 `state_or_prefixes_received ~ '^[A-Za-z]'` 区分，但保留 raw 列；
3. 上游 ntc-templates 提 PR；

`v_show_ip_bgp_neighbors_auto.bgp_state` 已经是干净的 `Established`(4) — 跨表 join 规避 summary 这列即可短期回避。

**关联**: 这同时阻塞 BGP 跨厂商统一视图（`v_bgp_neighbors_auto`）的精度。

---

### ISSUE-INTERFACE-STATE-NOT-NORMALIZED: 接口状态没规范化到 RFC ifOperStatus (R83.2)

**状态**: ✅ Closed (R83.4 follow-up — 数据层规范化扩展) · **优先级**: P0
**发现日期**: 2026-04-25 (R83.2 双轮 demo 验证)
**关闭日期**: 2026-04-25 (R83.4 follow-up — 同时修复 prefix-match bug + 扩 RFC/IEEE 规则集)

**修复摘要**:

`olav-netops/src/olav_netops/tools/field_normalizer.py` 一处统一修复，**未新建机制**——直接在已有 `_STATE_RULES` 元组追加 + 修复 prefix-match 潜在 bug：

1. **`_canonical_state` 改双 pass**：Pass 1 exact-match 优先，Pass 2 prefix-match 仅对 `/` 或 `-` 后缀（OSPF `Full/DR` 风格）触发——根除"`connected` 被 BGP `connect` 规则吞掉成 `Connect`"的潜在 bug；
2. **扩 `_STATE_RULES` 4 大类规则**（共 ~30 行）：
   - **ifOperStatus** (RFC 2863)：`connected→up`、`notconnect→down`、`disabled→admin-down`、`administratively down→admin-down`、`Up→up`（lowercase 一致）；
   - **STP port role** (IEEE 802.1D)：`Desg→designated`、`Altn→alternate`、`Back→backup`；
   - **STP port state** (IEEE 802.1D)：`FWD→forwarding`、`BLK→blocking`、`LIS→listening`、`LRN→learning`；
   - **OSPF interface state** (RFC 2328 §9)：`LOOP→loopback`、`DR→dr`、`BDR→bdr`；
3. **`_STATE_HINT` 扩展**：新增 `role|admin|oper` 触发词（覆盖 `admin_state`、`oper_status`、`role` 字段名）；
4. **State check 提前**：在 `_normalize_value` 中，state 分支移到 iface 分支之前——修 `link_state` 字段名同时匹配 iface（"link"）和 state 模式时被 iface 误处理；
5. **普适清洗**：未匹配规则的字符串值返回 `value.strip()`——修 STP `'P2p '`、`'P2p Edge '` 的 parser trailing-whitespace bug。

**验证**：
* DB 数据：`Connect(16) → up(16)`、`Desg/Root → designated/root`、`FWD → forwarding`、`'P2p '/'P2p Edge ' → P2p/P2p Edge`、`Up → up` 跨 view 一致；
* Q1-Q4 全过：Q1=3、Q2=5、Q3=3 跨平台（含 R1 em2/VLAN + R2 Gi3，admin-down 字面）、Q4=11（之前 38）；
* 直击 bug 的 NL 查询 "which ports are down (cisco show interfaces status)" 返回 0 行——是正确答案（all 16 are up after canonicalization）；
* 单测：`tests/test_field_normalizer.py` 31 → **63 用例全过**（新增 32 用例覆盖 BGP/OSPF/ifOperStatus/STP/prefix-bug/trim 各场景）。

**架构原则确立**——后续遇到状态归一化按此判定：
* **三个条件全满足 → 进 Layer 1（数据层 `_STATE_RULES`）**：(a) 有 IETF/IEEE 标准定义；(b) 是闭集；(c) 跨厂商一致语义；
* **不满足 → 进 Layer 2（memory + LLM 推理）或 Layer 3（per-concept unified view）**。

---

**原 issue 详情（保留以备 reference）**:

**症状**: NL "Which interfaces are down" 第二次调用走 memory recall 命中前次 SQL `SELECT * FROM netops.v_show_interfaces_status_auto WHERE status NOT LIKE '%up%' AND status NOT LIKE '%Up%' LIMIT 20`，返回 **16 行 `Connect` 状态**当作 down，**全部错误**——cisco_ios `show interfaces status` 的 `Connect` 是 RFC 2863 ifOperStatus = `up`（已连接、数据通）。

**根因**: R83.2 P1 把 BGP/OSPF state 从 view-time SQL CASE 移到 ingest-time `field_normalizer._STATE_RULES` 时，**漏了接口状态枚举**:
* cisco_ios `show interfaces status` 用 `connected` / `notconnect` / `disabled` / `err-disabled`
* cisco_ios `show ip interface brief` 用 `administratively down`
* junos `show interfaces terse` 已是 `up`/`down` 小写——已合规

字符串 `Connect` 不含子串 `up`，所以 LLM 写的 `WHERE status NOT LIKE '%up%'` 把它误判 down。**数据没说谎，LLM 写的过滤条件错；要修的是数据层规范化**。

**修复方向**: 扩展 `field_normalizer._STATE_RULES`:

```python
("connected",       "up"),
("notconnect",      "down"),
("notconnected",    "down"),
("disabled",        "admin-down"),
("err-disabled",    "err-disabled"),
("administratively", "admin-down"),  # 处理 "administratively down" 文本前缀
```

理论依据: RFC 2863 ifOperStatus / ifAdminStatus 是 IETF 标准枚举（up/down/testing/dormant/notPresent/lowerLayerDown），对应 vendor 输出字符串 → IETF 名是 RFC compliance，不是 vendor mapping 硬编码。和现有 `Estab → Established` 同性质。

**重要分界**:
* ✅ 不算硬编码: vendor 输出字符串 → RFC ifOperStatus 枚举（有限闭集，不随设备增加）
* ❌ 算硬编码: vendor → 命令列表 / 命令 → 概念映射（per-fleet 易膨胀）

修完后 agent 写 `WHERE status='down'` 一次正确，跨平台统一。

---

### ISSUE-MEMORY-POISONING: query_pattern_capture 把瑕疵 SQL 写入 memory (R83.2)

**状态**: 🟡 Mitigated (R83.4 read-time fix) · **优先级**: P1
**发现日期**: 2026-04-25 (R83.2 双轮 demo 验证)
**缓解日期**: 2026-04-25 (R83.4 follow-up — AutoRecall diversifier + curated gather)

**R83.4 mitigation**:

读侧治理替代写侧：保留所有 query_patterns 入 memory，但 `AutoRecallMiddleware` 现在
- **curated per-category fetch**：先按 category 各发一个 vector search（`schema_knowledge` / `value_distribution` 各取 6，`query_pattern` 取 3），再补一次 long-tail hybrid——保证 `memory_primer` 写入的 schema/value 入选不再被 RRF 偏置挤掉；
- **diversifier cap**：最终 top-K 中 `query_pattern` 只保留 1 条（`_CATEGORY_CAPS`），同问被多次 capture 时不会 4 条同模板挤满前排。

效果：Q3 "Which interfaces are down" 即使在已被 4 次写入 cisco-only `query_pattern` 的情况下，agent 仍然在 `<relevant-memories>` 里看到 **junos `v_show_interfaces_terse_auto` schema**（diversifier 把 terse 推到 #2），写出 UNION ALL SQL 一次返回 R1 em2 + R1 VLAN + R2 Gi3 全部 3 个 down 接口。Q3 工具调用 6 → **3**，跨平台正确性达成。

读侧治理留给后续的写侧改造空间：
- 写侧 dedup（同 question 只保留最佳 1 条）和 time-decay 仍可独立做，但读侧 cap 已经把"放大错误"的链路切断。
- agent 自标 successful SQL（option 2）若做，可让 confidence 评分更可靠；但前提是写侧改 capture 时机。
- 真值 result-validation 不在路线图（option 4 复杂度太高）。

代码位置: `src/olav/core/memory/middleware.py:_gather_candidates` + `_diversify_by_category` + `_CATEGORY_CAPS` + `_CATEGORY_FETCH`，单测 `tests/unit/test_autorecall_diversifier.py` 4 用例 pin。

---

**原 issue 详情（保留以备 reference）**:

**症状**: P0 中间件按 `len(data)` 评分挑 "winner" SQL 写入 memory。Q3 第一次 17 步探索中，`SELECT * FROM v_show_interfaces_status_auto WHERE status NOT LIKE '%up%'` 返回 16 行（实际是 16 个 `Connect` 状态——上面 ISSUE-INTERFACE-STATE-NOT-NORMALIZED 的副作用），获得 winner 评分。第二次同问，AutoRecall 注入这条 cached SQL 作为 `<relevant-memories>`，agent **直接复用**，2 步内返回 16 行错误答案。Memory 不是修复，**反而是放大错误的载体**。

**根因**: `query_pattern_capture._score()` 仅看 row_count，没办法区分:
* "返回 16 行因为找到了 16 个真实 down 接口" (好 SQL)
* "返回 16 行因为 WHERE 条件错把 up 也匹配上" (坏 SQL)

只有 agent 的 final assistant text 能判断答案是否对，但 capture 时机在 `aafter_agent`——final text 已生成，但模型评分自己输出对错不可靠。

**修复方向（择一或组合）**:

1. **多模板存储 + AutoRecall 决断**: 不挑 winner，存所有 row_count > 0 的非 introspection SQL。AutoRecall 用 hybrid search（vector + tags）从多个候选挑相关，错的 candidate 自然失宠。
2. **agent 自标 successful SQL**: 让 writer subagent 在 `<recorded-sql>` 标签里显式 echo 用了哪个 SQL；capture 只存被显式标记的。强假设 agent 会标。
3. **Time-decay 加速**: 默认 60 天 half-life 太长。`query_pattern` 缩到 7 天，让坏模板快速衰减；连续 N 次 hit 才上升 confidence。
4. **后置 result-validation hook**: capture 后 agent 自检 final answer 是否反映真值（极复杂）。

**关联 ISSUE**: ISSUE-INTERFACE-STATE-NOT-NORMALIZED 修了之后，Q3 这个具体场景的瑕疵 SQL 不会再产生（status='down' 一次写对）。但 memory poisoning 类问题会以其他形式重现，所以这是普适治理项。

---

### ISSUE-OPS-MEMORY-NO-EFFECT: 子代理 memory recall scope 隔离 (R83.2)

**状态**: 🔴 Open · **优先级**: P3
**发现日期**: 2026-04-25 (R83.2 双轮 demo 验证)

**症状**: Q4 ("Simulate what happens if R2 loses all links") via `--agent ops`，第一次 25 工具调用、第二次 27——**没改善**。Q1/Q2/Q3（core agent）都通过 memory 加速了，ops 没沾光。

**根因猜测**: `query_pattern_capture` plugin 用 `scope="global"` 写入；但 `MemoryCapturePlugin` (auto-fact-extract) 也用 `global`。`AutoRecallMiddleware` 在 ops agent 调用时可能用 `scope="ops"` 查询，跨 scope 不返回。

或者 ops agent 的 system prompt（含 `references/SIMULATION_WORKFLOW.md`）token 已饱和，AutoRecall 的 budget guard 跳过注入（"non-first-turn budget threshold"）。

**修复方向**:
1. 验证 plugin scope 配置一致（capture 和 recall 用同一 scope 默认 `global`）
2. 验证 ops agent budget headroom 是否触发 recall 跳过
3. 跨子代理共享 query_pattern（intent-similarity 强于 scope-isolation）

**优先级 P3**: ops 简单查询 25 step 已经能完成；core 受益更明显。

---

### ISSUE-AGENT-PARSED-JSON-NAV: Agent 无法 JSON-extract parsed_data 找接口状态 (R82)

**状态**: ✅ Closed (R83 改架构方向 — `fe37b95`)
**发现日期**: 2026-04-25 (R82 Chapter 3 NL 验证)
**关闭日期**: 2026-04-25 (R83 DuckDB-native zero-ETL per-command auto-views)

**原症状**: NL "Which interfaces are down across all devices?" → 0 条记录误报（R2 Gi3 admin-down 找不到）。

**Fix path adopted**: 不走 R82 提的 cookbook 方向，而是 R83 升级数据架构——DuckDB-native zero-ETL **`build_per_command_views()`**：对 `parsed_outputs` 中每个 distinct command，用 `json_structure(parsed_data)` 推断 schema，`CREATE OR REPLACE VIEW v_<safe_command>_auto AS SELECT ..., unnest(from_json(parsed_data, '<inferred>'), recursive := true)` 自动建 typed view。Agent 用 `DESCRIBE` + `SELECT * FROM v_show_<cmd>_auto WHERE col=...`，不需要 JSON-extract 知识。52 个 per-command view 在每次 `/netops_init` 自动生成。verified Q3 后续运行 17 步 → 2 步（参见 ISSUE-MEMORY-POISONING 同时发现的副作用）。

---

### ISSUE-VIEW-SNAPSHOT-ACCUMULATION: Auto views 跨 snapshot 行倍增 (R82)

**状态**: ✅ Closed (R82 当轮修复) · **优先级**: P1
**发现日期**: 2026-04-25 (R82 Chapter 3 NL 验证)
**Commit**: 见 R82 view_builder fix

**症状**: `/netops_init` 跑 3 次后，`netops.v_bgp_neighbors_auto` 返回 18 行（每个 BGP session 重复 3 次），`v_ospf_neighbors_auto` 返回 12 行（4×3）。NL 查询 "Are all BGP neighbors established?" 给出 18 条 vs 真实 6 条 → 误导用户 fleet 规模。

**根因**: `view_builder.py` 的 `_bgp_branch` / `_ospf_branch` / `_custom_branch` 在 `parsed_outputs` 上 `WHERE p.command = '...'` 但**无 snapshot 过滤**，多个 snapshot 累积导致 N×M 行。`v_l2_links_auto` 走 `topology_links` 表（INSERT OR IGNORE 在 link_id 维度去重）所以不受影响。

**Fix**: 新增 `_LATEST_SNAPSHOT_FILTER` 子查询常量，per-(device, command) 取 MAX snapshot：

```sql
AND p.snapshot_id = (SELECT MAX(snapshot_id)
                     FROM netops.parsed_outputs s
                     WHERE s.device_name = p.device_name
                       AND s.command = p.command)
```

应用到所有 3 个 branch builder。语义：每个 (device, command) 对取最新一次解析结果。这避免了"全局 MAX"会把 `/learn_cmd` 的 fresh snapshot 当作所有命令最新的问题。

**验证**: 跑 2 次 `/netops_init` (parsed_outputs 有 2 个 distinct snapshot)，view 仍返回 6/4 行（不再 12/8）。NL "BGP neighbors" → 6 条 Established（数据准确）。

---

### ISSUE-EXPORTS-DOUBLE-PREFIX: 嵌套 `exports/exports/` 目录 (R82)

**状态**: ✅ Closed (R82 当轮修复) · **优先级**: P2
**发现日期**: 2026-04-25 (R82 Chapter 3 验证)

**症状**: 多次 NL 查询后 `~/olav-demo/exports/` 出现 `exports/exports/`、`exports/queries/`、`exports/query_results/` 等子目录混乱。`exports/exports/` 是 agent 把 `subdir="exports"`（冗余前缀）当字符串传，被 `format_and_export` 直接拼到 `EXPORTS_DIR / "exports"` 形成嵌套。

**Fix**: 在 `format_and_export.py` 的 subdir 解析层加 collapsing 逻辑：
- 剥 leading `/`（防止 absolute path 逃逸）
- `subdir == "exports"` → 视作 None
- `subdir.startswith("exports/")` → 剥前缀

R81 R82 联合 ISSUE-WRITER-WRONG-PATH (`b625e62`) + 这次修补 = subdir 容错完整。

---

### ISSUE-PORT-NAME-CANONICALIZATION: 拓扑端口名格式重复入库 (R82) ✅

**状态**: ✅ Closed (R82 commit `219e712`) · **原优先级**: P1
**发现日期**: 2026-04-25 (R82 fresh-demo 完整 chapter 1+2 验证)

**症状**: `netops.topology_links` 16 行中 5 对是同一物理链路、不同端口名格式的重复:

| Source | Dest | 重复因子 |
|---|---|---|
| R3 → R1 | `ge-0/0/2` 和 `512` | LLDP TLV 一会返回 ifName 一会返回 ifIndex |
| SW1 → R3 | `Uni Eth 0/1` 和 `Ethernet0/1` | `show cdp neighbors`（brief）parser 列对齐 bug |
| R2 → WAN | `Ethernet0/1` 和 `Uni Eth 0/1` | 同上 |
| SW2 → Switch | `Ethernet0/0` 和 `Uni Eth 0/0` | 同上 |
| R4 → SW2 | `Uni Eth 0/0` 和 `Ethernet0/0` | 同上 |

逻辑唯一链路 ≈11，DB 16 行。R81 `hostname_registry` 解决了 hostname 维度（`R4.local` → `R4`），端口名维度没做规范化。

**根因 (双层)**:

1. **格式差异（解决方案：netutils canonicalize）**: `Gi1`/`GigabitEthernet1`、`Eth0/1`/`Ethernet0/1` 等同义形式入库未规范化 → 不同 link_id → INSERT OR IGNORE 不生效。netutils `canonical_interface_name()` 已是依赖。
2. **解析器 bug（`show cdp neighbors` brief 列对齐错位）**: ntc-templates `cisco_ios_show_cdp_neighbors.textfsm` 在 brief 输出 (`Linux Uni  Eth 0/1`) 抓取 Port ID 时把 `Uni`+`Eth 0/1` 黏成 `Uni Eth 0/1`。Detail 命令 (`Port ID (outgoing port): Ethernet0/1`) 解析正确。
3. **数字 port-id（`512`）**: Junos LLDP TLV port-id-subtype=7（locally assigned）落库为 SNMP ifIndex，ntc-templates 将其作为字符串保留。

**R82 修复**:

修改 `topology_engine._insert_link()`:
1. **canonicalize** 两侧 interface 名（netutils）
2. **reject** dst_intf 形如 `^\d+$`（数字-only）— LLDP TLV ifIndex fallback
3. **reject** dst_intf 含**空格**（`Uni Eth 0/1` 类 parser 错位）

详细方案 + 实施在 commit 中。

---

### ISSUE-LLDP-NUMERIC-PORTID: Junos LLDP 数字 port-id 落入拓扑 (R82) ✅

**状态**: ✅ Closed (R82 commit `219e712`) · **原优先级**: P2
**发现日期**: 2026-04-25 (同上)

**症状**: `R3 Eth0/0 → R1 "512"` — `512` 是 R1 (Junos) `fxp0.0` 的 SNMP ifIndex，LLDP TLV port-id-subtype=7（locally assigned）下被设备当数字字符串发送，ntc-templates `cisco_ios_show_lldp_neighbors.textfsm` 直接落库。

**根因**: LLDP 标准允许 port-id-subtype 为多种值（ifName / ifAlias / locally assigned / MAC / ifIndex），Junos 在某些 LLDP 配置下选择 ifIndex（数字）。ntc-templates 不做反向映射。

**R82 修复**: 同 ISSUE-PORT-NAME-CANONICALIZATION 的 reject 规则——`_insert_link` 拒绝 `^\d+$` 形式 dst_intf。其他源命令（detail）仍能给出 `ge-0/0/2` 形式。

**长远方案（不在 R82 范围）**: ARCH-25 PaC parser overlay 学习 Junos LLDP raw 输出，提取 ifAlias/`Description` 字段反向解析。当前无足够样本（需要等 fleet 多个 Junos 版本），暂不做。

---

### ISSUE-NL-CLI-SILENT-FINAL: Agent 工具调用后无最终文本回复到 stdout (R81) ✅

**状态**: ✅ Closed (R82 commit `b625e62`) · **原优先级**: P0 (阻碍 demo)
**发现日期**: 2026-04-25 (Round 81 fresh-demo 验证)
**关闭日期**: 2026-04-25 (R82)
**Fix**: `cli/main.py` 在 `astream_events` 循环后，若 `_chunks` 空但 `_tool_results` 非空，从 `format_and_export` / `render_report` / `take_snapshot` 等 path-bearing tool 输出提取路径，作为 fallback 文本打印。grok-4.1-fast 通常自然输出 markdown 表格使 fallback 不触发；fallback 兜底仅在小模型完全静默时启用。

**症状**:
```
$ olav "列出所有设备及其IP地址"
  🔧 execute_sql({...})
  🔧 execute_sql({...})       # schema 错误重试
  🔧 olav_delegate({...})
  🔧 format_and_export({...})
  ← 期望: 表格 / "已导出到 exports/..." 提示
  ← 实际: 无任何 assistant text，用户不知结果在哪
```

工具运行成功 + 写入 `~/olav-demo/devices.csv` 都正常，但 CLI 不打印 agent 最终消息文本。Demo UX 重伤。

**位置可能**:
- `src/olav/cli/main.py` — agent run loop 是否捕获并打印 final AI message
- `src/olav/cli/streaming.py` (如有) — SSE 流式输出 chunk handling
- agent middleware 是否在最后一个 turn 把消息吞了

**修复方向**:
1. 定位 agent.invoke / .stream 后 message list 中的 final AIMessage
2. 确认 CLI 是否调用 print 输出该 message.content
3. 如 stream 模式：检查 `done` 事件后是否消费完所有 message chunks

---

### ISSUE-WRITER-WRONG-PATH: writer subagent 写错路径 (R81) ✅

**状态**: ✅ Closed (R82 commit `b625e62` + `cf583e9`) · **原优先级**: P1
**发现日期**: 2026-04-25 (Round 81 fresh-demo 验证)
**关闭日期**: 2026-04-25 (R82)
**Fix**: `format_and_export.py` 错把 `EXPORTS_DIR.parent`（项目根）当作 csv/json 输出目录——改成 `EXPORTS_DIR`（`exports/`）；md/txt/mmd 改为 `EXPORTS_DIR / "reports"`。新增 subdir 字符串容错：`"null"` / `"None"` / `""` / `"exports"` / `"exports/foo"` 前缀全部归一。

**症状**:
```bash
$ ls /home/yhvh/olav-demo/devices.csv     # ← 错路径（项目根）
hostname,ip_address,platform,role,model
R1,192.168.100.101,juniper_junos,border,vsrx
...
```

按 `references/OUTPUT_EXPORT_RULES.md` 应入 `exports/reports/devices_YYYYMMDD.md`（Markdown），实际写到根目录的 `devices.csv`（CSV，而且文件名/扩展名错）。

**位置**:
- writer subagent 的 system prompt（`src/olav/data/workspace/core/writer/prompts/system.md` 或 `agents/writer.md`）
- `format_and_export` 工具的默认路径解析

**修复方向**:
1. 确认 writer subagent 是否加载了 `OUTPUT_EXPORT_RULES.md`
2. `format_and_export` 工具签名: 默认 `output_dir` 是否解析到 `exports/reports/`?
3. 如果工具默认对，则是 LLM 判断错——加 prompt 强约束 `path` 参数必须 `exports/reports/...`

---

### ISSUE-AGENT-SCHEMA-CACHE: agent 不利用 DB_SCHEMA.md，第一次 SQL 字段错 (R81) ✅

**状态**: ✅ Closed (R82 commit `e520789` + R83 introspection prompt) · **原优先级**: P2
**发现日期**: 2026-04-25 (Round 81 fresh-demo 验证)
**关闭日期**: 2026-04-25 (R82-R83)
**Fix 路径采用方向 (4) 结构化签名**: `core/SKILL.md` `static_context` 加 `SCHEMA_REFERENCE.md`；`db_query/SKILL.md` 内联 "Known columns" 章节列每张表关键字段 + 常见错误（`mgmt_ip` → `ip_address`、`device_type` → `platform` 等）；`core/prompts/system.md` 加 5 行 cheatsheet 让 orchestrator 每轮都见到。R83 又加 `Cross-platform questions — introspect first` workflow 段（`SELECT DISTINCT platform → information_schema.views → DESCRIBE → UNION ALL`）。验证：grok-4.1-fast `SELECT hostname, ip_address FROM netops.devices ORDER BY hostname` 一次正确。

**症状**: deepseek-v4-flash 第一次 `execute_sql` 用了不存在的 `device_type` 字段，重试改成 `role` 后才成功。说明小模型没有有效利用 `references/DB_SCHEMA.md`。

**根因猜测**:
- `references/DB_SCHEMA.md` 是 on-demand 加载（progressive disclosure），但 orchestrator system prompt 没在调 `execute_sql` 前先 load reference
- 或 reference 已加载但 LLM 没好好读
- 模型尺寸越小，对 system prompt 中 schema 表的注意力越差

**改进方向（用户提示——cache + long memory）**:
1. **Schema cache 方案**: 第一次 `execute_sql` 前自动调 `DESCRIBE netops.devices` 等并缓存到 conversation context；之后所有 SQL 走前不再读 schema 文件，直接看缓存
2. **Long-term memory 方案**: 把"上次哪些 query 成功 / 哪些字段不存在"以 pattern 形式写入 memory store；同样 intent 第二次进来时，从 memory 检索成功 query 模板做 few-shot
3. **混合方案**: 启动时一次性 inject 当前 DB schema snapshot 到 system prompt（小代价）+ memory 记录 successful query templates per user intent
4. **结构化签名**: 把 `netops.devices` columns 列在 `db_query` subagent 的 SKILL.md frontmatter 里（不靠 reference 文件加载，每次都见）

trade-off：
- Cache 命中 → 0 LLM 调用、0 推理失败；DB schema 漂移时 cache 自动 rebuild
- Memory → 跨 session 持久；但 stale memory 风险（schema 变了）需 TTL / 版本签名

---

### ISSUE-EMPTY-CMD-LEARNING: 空数据命令每次重跑 + 上游 parser bug 噪声 (R81)

**状态**: ⏸️ **Wontfix —— 过度设计 (R81 自评收回)** · **优先级**: 无
**发现日期**: 2026-04-25 (Round 81 fresh-demo)
**关闭日期**: 2026-04-25 (用户挑战"是否过度设计"后自评收回)

**原观察**:
1. `show vrf` / `show vrf detail` / `show vlans` / `show vrrp all` 等在当前 fleet 每次返回 0 records，但 discovery list 仍每次跑
2. `cisco_ios_show_users.textfsm` 第 16 行 State Error（ntc-templates **上游 parser bug**）每次 collect 打日志噪声

**为什么不修——成本核算**:

| 项 | 量 | 真实开销 |
|---|---|---|
| 空数据命令次数 | ~6 cmd × 6 设备 = 36 次 SSH | nornir 并行 <3s |
| 当前 `/netops_init` 总耗时 | — | 33.8s |
| 空命令占比 | — | <10% |
| Snapshot 频率 | 小时/天级 | 完全可接受 |

**skip-cache 方案的代价远大于收益**：
- 新表 `command_skip_cache` + DDL 迁移
- 4 处状态机（check / bypass / update streak / invalidate）
- 新 failure mode：bug 让合法命令被静默跳过 → 调试困难
- 操作认知负担："我的 `show vrf` 输出怎么不见了？"
- 测试面：每条路径都需 coverage

设备替换正确性（device_hash + TTL + 触发器）是必须的——但仅当我们 **要** 建 skip-cache 时才有意义。不建就不存在这个问题。

**唯一保留的 actionable 改进**：
ntc-templates 解析失败的日志级别从 `WARN` 降到 `DEBUG`（1 行改动），消除采集时的 State Error 噪声。原始 bug 仍归 ntc-templates 上游。

**重启条件**：
- fleet 规模 → 100+ 设备 → SSH 开销变非平凡
- snapshot 频率 → 每分钟级别
- 任何一个达到，重新评估

---

### ISSUE-RAW-05: 5 个 P2 消费者缺少 raw fallback

**状态**: ✅ Closed (Round 37 reconciliation + discoverability pass)

**探测发现 vs issue 原描述**：底层 RAW-05 基础设施在 Rounds 18/35 前已 ship 完整——所差的只是 docstring/指引层面的 discoverability。Round 37 补 3 处 docstring/guide + 一条守护已有基础设施的 governance pin。

| Consumer | 原描述 | 实测结论 | Round 37 动作 |
|---|---|---|---|
| `discover_view_schemas.py` | 需加 UNION | ✅ Round 18 已加 UNION raw_output_store；`test_discover_view_schemas_raw_fallback.py` 3 条 pin | — 已钉 |
| `map_engine.py` | — | ✅ `_raw_fallback_probe()` (line 384) 在 SQL 分支 (178-179) + anomaly 分支 (198-201) 自动 kick in；emit `_warning: raw_only_data` 防 false-green | Round 37 加 governance pin 守护（5 条） |
| `ingest_manager.py` | — | ✅ `IngestManager.bulk_load()` 写 raw_output_store (line 124-135)；take_snapshot 通过 bulk_load 间接写 raw | — 无动作 |
| `health_full_drift.md` profile | 未用 COALESCE | ✅ CPU_Anomaly (line 28) + Memory_Anomaly (line 59) 均有 `raw_fallback: true` → 触发 map_engine probe | Round 37 pin raw_fallback flag 持续存在 |
| `take_snapshot.py` | 写入未写 raw | ❌ runtime 已写（bulk_load 经 ingest_manager），但 docstring + next_step 未宣告 → LLM/operator 不知有 raw fallback | Round 37 docstring "Results are written to" 加 raw_output_store 行；next_step 补 RAW-05 tag |
| `anomaly_engine.py` | 读 parsed 硬编码 | ❌ docstring 例子仅 parsed；未描述 `raw_fallback: true` flag 触发的 probe | Round 37 docstring 末尾加 "Raw fallback (RAW-05)" 段 |
| `ROUTING_EXPERT_GUIDE.md` | snapshot_id 查询不覆盖 raw | ❌ line 242/248 示例查 parsed_outputs，未提 raw fallback | Round 37 snapshot 查询后加 UNION ALL note + RAW-05 tag |

**Round 37 证据**：`tests/governance/test_raw_05_fallback_infrastructure.py`（11 条 pin）守护 `_raw_fallback_probe` helper 存在 + SQL/anomaly 两分支 wiring + profile raw_fallback: true flag + 3 处新 docstring/guide 不消失。

**发现日期**: 2026-04-17（Round 27 acknowledged；Round 37 close）

---

### ISSUE-CORE-01~06 + NETOPS-01~06（Round 18 core/netops platform audit）

**状态**: ✅ All Closed (Round 18 reconciliation; CORE-05 Option B Accepted Round 30)
**发现日期**: 2026-04-17（core + netops platform audit）

12 条 platform-audit 缺陷在 Round 18~30 全部 ship + pin。详情以压缩表形式保留；原完整描述见 git 历史 `git log --grep="CORE-0\|NETOPS-0"`。

| ID | 问题 | 修复位置 | Pin / 证据 |
|---|---|---|---|
| CORE-01 | ConfigLoader / get_store / get_router 三单例非线程安全 | `core/config.py:104` `_config_lock` · `router.py:477` `_router_lock` · `memory/__init__.py:659` `_store_lock`（均 `threading.Lock` + 双检锁）| `tests/governance/test_reported_issues_remain_fixed.py` |
| CORE-02 | ~20 处 `except Exception: pass` 静默吞异常 | 全部改 `except Exception as e: logger.debug(...)` 或加 `# noqa: BLE001` 注释 | `rg "except Exception:\s*pass\b" src/olav/core` 0 命中 |
| CORE-03 | `_backup_commands` SQL 字符串拼接（非参数化）| `core/ingest_manager.py:224` 改 `?` 占位符 + params | 源码注释标注 CORE-03 |
| CORE-04 | `tool_discovery.py` 全局 `sys.path` 污染 | `core/tool_discovery.py:62` 改 `importlib.util.spec_from_file_location` | 源码注释标注 CORE-04 |
| CORE-05 | `api_registry.py` 11 处 `print()` 绕过 logger | Option B Accepted（Round 30）：CLI handler 保留 print，non-CLI 禁止 | `test_core_05_print_discipline.py` 4 pin |
| CORE-06 | 配置文件编码异常静默 → 返回 `{}` | `core/config.py:130-135` 三独立 `except`（JSONDecodeError / UnicodeDecodeError / OSError）均 `logger.warning` | 源码注释 CORE-06 |
| NETOPS-01 | Nornir 连接池泄露（InitNornir 从不关闭）| `.olav/workspace/ops/netops_init/run.py:213,519` 显式 `nr.close_connections()` | 源码注释 NETOPS-01 |
| NETOPS-02 | `datetime.now()` vs `utcnow()` 混用 | 改 `datetime.now(UTC)`；残 1 处于 `recipe_seeds.py:102`（tz-aware 正确形式）| `datetime.utcnow()` 已 0 命中 |
| NETOPS-03 | `CommandRegistry` 单例 + hot-reload 无锁 | `olav-netops/.../command_registry.py:44` 加 `_lock = threading.RLock()` + 所有读写 `with cls._lock:` | 源码注释 NETOPS-03 |
| NETOPS-04 | netops_init 无断点续跑 | `run.py:713` 接 `--resume <SNAPSHOT_ID>`，checkpoint 走 `done.jsonl` | `test_netops_resume.py` 11 tests |
| NETOPS-05 | `safe_cmd` 仅替换空格/斜杠 → 路径遍历风险 | `run.py:679` 白名单化 `re.sub(r"[^a-zA-Z0-9_-]", "_", cmd)[:60]` | 源码注释 NETOPS-05 |
| NETOPS-06 | SSH stage 异常 → 设备全部标 failed（粗粒度）| 采集循环重构入 `_collect_cmd()` (`run.py:659-700`)，Nornir per-host 隔离保留 | 源码 docstring NETOPS-06 |

---


## 架构设计问题（Design Issues — OLAV 定位：只读诊断辅助）

> **OLAV 定位澄清（2026-04-17）**：
> OLAV 是 **辅助查询/诊断工具**，工作流到 Diagnose 为止。**不直接修复，不直接操作设备配置** ——
> 即便有 HITL 也过于危险。Remediate / Verify / Document 留给人类或 downstream 工单系统。
>
> 因此以下场景 **不在 OLAV 职责范围**，之前几条作废：
> - ❌ ~~ARCH-01: Workflow orchestrator 含 Remediate/Verify~~ → 越权，定位错误
> - ❌ ~~ARCH-02: Incident/Change 生命周期~~ → 工单系统职责，OLAV 至多输出诊断报告供上游引用
> - ❌ ~~ARCH-03: CLI session + commit confirmed~~ → OLAV 不做 config 写操作
> - ❌ ~~ARCH-04: Freshness SLA 强制~~ → 已有 `snapshot_id = (SELECT MAX(snapshot_id) FROM ...)` 切片规范（见 `core/references/SCHEMA_REFERENCE.md`），默认最新，对比场景显式指定 snapshot
> - ❌ ~~ARCH-05: Pluggable parser (gNMI/NETCONF)~~ → TextFSM 是有意选择，兼顾老设备 + 复杂度低；现代协议是远期增强，非当前缺陷
> - ❌ ~~ARCH-07: RBAC/角色矩阵~~ → 已由 `olav-ent` + LDAP 覆盖企业版职责
>
> 以下保留和新增的条目全部围绕 **只读诊断辅助** 定位。

---

### ISSUE-ARCH-06: 高层关系发现 —— 用 sandbox + recipe 取代硬编码 ETL

**状态**: 🟡 Partially Implemented (Round 67 re-opened) — P1 recipe seeds 确实 ship (view_recipes 15 rows 幂等 UPSERT)，但 P2 **runtime ETL 从未落地**：`topology_engine.py` 仍是 L2-only 硬编码；BGP/OSPF 虽已在 `network_model.py:927-1107` 按 query-time 重建，但无 SQL 持久化路径，`SELECT FROM netops.bgp_sessions` 不存在。ARCH-06 原方案的"sandbox + recipe 取代硬编码 ETL"在 ARCH-24 中重新认领并采用 LLM-frozen 方案（见下）。

| 原方案项 | 状态 | 证据 |
|---|---|---|
| `load_recipe_seeds()` 幂等 loader + UPSERT | ✅ | `olav-netops/src/olav_netops/core/recipe_seeds.py:75-134` |
| `view_recipes` table 结构 | ✅ | loader 创建 + UPSERT 幂等 |
| `view_recipes_seed.yaml` 初始 seeds | ✅ (Round 43) | 15 entries — 覆盖 6 个 core concepts × 3+ 厂商 |
| `netops_init/run.py` 启动 wire | ✅ | `load_recipe_seeds` tail 阶段调用 |
| Governance pin | ✅ | `tests/governance/test_recipe_seeds.py` 8 条全绿 |
| Pattern A examples (DuckDB + networkx sandbox) | ✅ (Round 60) | ROUTING_EXPERT_GUIDE.md 新 `## NetworkModel Recipes` 段含 Pattern A 片段（networkx 走 `model.physical.graph`）+ Pattern A* 跨层（OSPF adj ↔ L1 physical 对照） |
| Pattern B examples (LanceDB semantic recipes) | ✅ (Round 60) | 同段 Pattern B 片段明示"NoM + recall_memory 组合"边界：LanceDB 通过 `recall_memory` 走，不挂 `model.*` |
| Pattern C 确定性 policy 求值 | ✅ (Round 60) | Pattern C 片段调 `model.l4.policy(device, neighbor, direction, matches={...})` — Round 59 新端到端入口 |

**Round 60 后续**：seed library 扩容随每轮 diagnostic task 自然增长（当前 ≥15 floor 已由 `test_recipe_seeds.py` 钉）；`tests/governance/test_round60_arch06_pattern_ab_docs.py` 7 条 pin 守护 guide 不与 NetworkModel API drift。
**发现日期**: 2026-04-17（rev 140 修订；rev 141 重构设计：弃硬编码、走 sandbox + recipe）

**定位:** 拓扑自动发现（LLDP/CDP → `topology_links`）方向正确，是 **L1 ground truth**。问题在于 L2/L3/overlay/policy 等高层关系的呈现方式 —— 之前的提案（硬编码 `l3_adjacencies` / `vrf_membership` / `policy_chain` 视图）等于把 L1 ETL 跑道往 L2 复制一遍，重蹈 ISSUE-RAW-03 的覆辙：每加一种关系 = 新 parser + 新视图 + 新 schema 维护。

#### 重新分层：L1 硬编码 / L2 sandbox 派生 / L3 自动晋升

| 层 | 内容 | 实现 | 更新策略 |
|---|---|---|---|
| **L1 Ground Truth** | LLDP/CDP topology_links、devices 清单、原始采集（parsed_outputs JSON + raw_output_store） | 硬编码 ETL（已存在） | netops_init 周期性 |
| **L2 Recipe-derived** | L3 path、VRF membership、EVPN overlay、policy chain、跨设备一致性 | **Sandbox 中即时计算**：`duckdb` JSON UNNEST + `networkx` 图构建 + `lancedb` 语义检索 | 查询时按需 |
| **L3 Auto-promoted** | 高频被调用的 L2 recipe | 自动物化为 `v_<name>` 视图 + 写入 `view_recipes` 表 | 调用频次过阈值时自动 |

**核心洞察:** OLAV 已有所有原料 —— DuckDB JSON 函数、networkx、LanceDB embeddings、`schema_catalog`、`view_recipes`、sandbox。**缺的不是 parser，而是 recipe library + 默认调用模式。**

#### 三类 recipe 范式（sandbox 内可即时跑）

**范式 A：DuckDB JSON UNNEST + networkx 即时建图**
```python
# "R1 到 R5 路径？"
import duckdb, networkx as nx
con = duckdb.connect(MAIN_DB, read_only=True)
edges = con.execute("""
  SELECT device_name AS src,
         json_extract_string(t.value, '$.neighbor_id') AS dst
  FROM netops.parsed_outputs,
       UNNEST(json_extract(parsed_data, '$')) AS t
  WHERE command LIKE '%ospf neighbor%'
    AND snapshot_id = (SELECT MAX(snapshot_id) FROM netops.parsed_outputs)
""").fetchall()
g = nx.Graph(edges)
paths = list(nx.all_simple_paths(g, "R1", "R5", cutoff=10))
```
零新视图、零新 ETL —— 全靠现有 parsed_outputs JSON。

**范式 B：LanceDB 语义检索 + LLM 推理（避免硬编码 policy parser）**
```python
# "R1 为什么学不到 10.2/16？"
# 1. 把 R1 running-config 按 section 分块、embed、入 LanceDB（已有 KB pipeline 可复用）
# 2. 检索"prefix-list / route-map 含 10.2.x"
# 3. LLM 读匹配的 config 片段推理
hits = lancedb_store.search(f"route-map prefix-list 10.2 R1").limit(10)
# LLM 解释 hits → 不需要写 BGP policy AST parser
```

**范式 C：DuckDB pure SQL 派生（最简关系）**
```sql
-- "VRF_A 在哪几台 PE？"
SELECT DISTINCT device_name
FROM netops.parsed_outputs,
     UNNEST(json_extract(parsed_data, '$')) AS t
WHERE command LIKE '%show vrf%'
  AND json_extract_string(t.value, '$.vrf_name') = 'VRF_A'
  AND snapshot_id = (SELECT MAX(snapshot_id) FROM netops.parsed_outputs);
```

#### 设计建议（按工作量小 → 大 排序）

1. **写 recipe 种子库**（1-2 周）：在 `view_recipes` 表种 10-15 条高价值 recipe（L3 path、VRF members、ECMP 解析、邻居一致性、policy 检索…），每条含 SQL/python + 自然语言问句 + 示例输出
2. **`ops-analysis` system prompt 强化 recipe 优先**：指示 LLM 先查 `view_recipes` 找匹配模式，找到就照搬；找不到再 from scratch
3. **新增辅助工具 `derive_relationship(relation_type, scope)`**：包装范式 A/B/C，常见关系类型给固定模板（`l3_path`/`vrf_members`/`overlay_neighbors`/`policy_affecting`），减少 LLM 自由发挥的错误率
4. **观察 → 自动晋升**：sandbox 跑 recipe 时记录 `recipe_call_log`；某 recipe 调用次数 ≥10 + 平均耗时 >500ms → 触发自动 `CREATE VIEW v_<recipe>` 物化
5. **可选：networkx graph 缓存层** —— 一次构建多次查询同一 snapshot 的 graph

#### 该方案的优势

| 对比维度 | 硬编码 ETL（旧提案） | Recipe + Sandbox（新提案） |
|---|---|---|
| 新增一种关系 | 写 parser + 视图 + schema + 测试 | 写一条 recipe 入表 |
| 设备/厂商扩展 | 每厂商重写 parser | 复用 LanceDB 语义检索 |
| 与 ISSUE-RAW-03 关系 | 同样栽 TextFSM 坑 | 不依赖 vendor-specific parser |
| 与 ARCH-05 关系 | "添加更多 ETL"违背单一栈定位 | 与"TextFSM 即可"理念一致 |
| 与 ARCH-12 关系 | catalog 列表静态膨胀 | recipe 库自然成为发现入口 |
| LLM 角色 | 翻译需求到 SQL | 检索/复用 recipe，少自由发挥 |

**验证（端到端）:**
1. `olav "R1 到 R5 的 OSPF 路径？"` → sandbox 自动跑范式 A → 输出 ECMP 路径，**无需事先建 `l3_adjacencies` 视图**
2. `olav "VRF_CUSTOMER_A 在哪几台 PE？"` → 从 `parsed_outputs` JSON 派生
3. `olav "R1 为什么学不到 10.2/16？"` → LanceDB 检索 + LLM 推理
4. 跑 50 次 OSPF path 查询后 → `view_recipes` 计数到阈值 → 自动创建 `v_ospf_topology` 视图，下次查询直接命中

**关联:**
- ARCH-10（联合检索）：recipe 是检索结果之一
- ARCH-11（引用链）：每条 recipe 输出附带 `[recipe: <name>; src: snapshot_id=...]`
- ARCH-12（schema discovery）：`olav catalog` 自动列出所有 recipe 作为"能查的问题"
- ARCH-13（diff）：snapshot diff 也是一条特殊 recipe

#### 影响面分析（视图层 / SQL / Sim / Lab）—— rev 141 补充

> 关键澄清：`l3_adjacencies` / `vrf_membership` / `policy_chain` 等在代码库中**零引用**（旧 ARCH-06 提案，从未实现）。
> 因此采纳 recipe 方案是**纯增量改动 —— 不删视图、不改 audit profile、sim/lab runtime 不变**。

**视图分三档:**

| 档次 | 例子 | 处置 | 为什么 |
|---|---|---|---|
| **L1 基础表** | `netops.parsed_outputs` / `devices` / `topology_links` / `raw_output_store` | 保留（硬编码 ETL） | Ground truth |
| **L1.5 简单展平视图** | `v_bgp_neighbors_auto` / `v_ospf_neighbors_auto` / `v_interfaces_auto` / `v_topo_links_clean` / `v_routes_enriched` 等 | 保留 | 一对一展平 parsed JSON，是下游 audit profile 的稳定 schema 契约 |
| **L2 高层关系** | 跨设备一致性、L3 path、VRF 成员、policy 影响、EVPN overlay | 走 recipe，**不建新视图** | 高变动 + 多组合 + 用查询频次驱动是否物化 |

**逐层影响:**

| 组件 | 影响 | 改动 |
|---|---|---|
| **基础 ETL（netops_init / topology_engine）** | 无 | — |
| **现有视图（v_*_auto, v_*_clean）** | 无 | — |
| **Audit profiles（如 bgp_health.md）** | 无 | 全部仍查 L1/L1.5 |
| **`execute_sql` 工具** | 无 | 现有 schema_catalog 注入机制兼容 |
| **`run_python_simulation` 运行时** | 无 | sandbox 已有 `duckdb + networkx + netutils`，本就是范式 A 的执行环境 |
| **`ops-analysis` system prompt + ROUTING_EXPERT_GUIDE.md** | **调整** | 加范式 A/B/C 示例；示例代码示范"从 parsed_outputs 即时建图"而非"查 `l3_adjacencies`" |
| **`ops-lab` (containerlab)** | 无 | Lab 推 running-config 到容器，不读关系视图 |
| **SKILL.md `allowed_tables`** | 无 | 当前列表全是 L1/L1.5 + view_recipes + schema_catalog；未来新关系走 view_recipes 表查询，不需要加新名 |
| **未来高频 recipe** | 自动晋升 | recipe 调用阈值触发 → 自动 `CREATE VIEW v_<name>` → 加进 schema_catalog → 下次 LLM 写 SQL 时自动可见 |

**唯一需要主动改动的两处:**
1. `ops/analysis/SKILL.md` + `ROUTING_EXPERT_GUIDE.md`：加 recipe 范式 A/B/C 示例
2. `view_recipes` 表种 10–15 条种子 recipe（一次性数据迁移）

**升级路径完全平滑:** 旧 audit/SQL/sim/lab 代码正常运行 → 同时 recipe 库逐步丰富 → 高频 recipe 自动晋升为视图 → 系统自然进化，**无 big-bang 重构**。

---

### ISSUE-ARCH-08: Nornir inventory 无环境标签 —— 诊断查错设备风险

**状态**: ✅ Closed (Round 48) — Phase 1 (tag + dupe detect R25) + Phase 2 (tool signature R39 + devices.environment column R48) 全部 ship

| 原方案项 | 状态 | 证据 |
|---|---|---|
| `data.environment: [lab\|prod\|staging\|dev]` 字段 | ✅ | `.olav/workspace/ops/collect/config/nornir/hosts.yaml` 每台设备实测有 `environment: lab` |
| 重复 hostname 检测 | ✅ | `tests/governance/test_nornir_inventory_env_label.py` 3 条全绿 |
| 下游 `execute_cli` / `take_snapshot` 按 env 过滤 | ✅ (earlier) | `execute_cli.py:200` + `take_snapshot.py:347` 都接 `environment` kwarg；`tests/governance/test_environment_dispatch.py` 5 条 pin |
| `netops.devices.environment` 列 | ✅ (Round 48) | `olav_netops/core/tables.py` DevicesTable 加 nullable VARCHAR；`netops_init/run.py::_load_host_environments()` 解析 hosts.yaml data.environment；`_populate_devices` 加 `ALTER TABLE ADD COLUMN IF NOT EXISTS` 幂等迁移 + INSERT with `environment=COALESCE(EXCLUDED.environment, ...)` 保旧值；`tests/governance/test_round48_devices_environment_column.py` 7 条 pin |
| 视图 `v_*_auto` 也带 `environment` 字段列 | 🟢 自动 join 可行 | 视图是 auto-generated (`v_bgp_neighbors_auto` 等) 通过 view_recipes；消费端要带 env 可 `SELECT v.* FROM v_bgp_neighbors_auto v JOIN netops.devices d USING (device_name) WHERE d.environment = 'prod'` — 无需额外 ETL |
**发现日期**: 2026-04-17

**定位:** 即便 OLAV 只做读，读错设备（Lab R1 vs Prod R1 同名）也会导致 **诊断结论张冠李戴**。用户被误导去改生产，等于间接破坏。

**现状:**
- `ops/probe/config/nornir/` 无 `environment` 字段
- `execute_cli` / `take_snapshot` 按 hostname 查 inventory，重名以谁胜出不确定

**设计建议:**
1. Nornir inventory 强制 `groups: [lab|staging|prod]`
2. 所有采集/查询工具加 `environment` 参数；默认 prod；切换需显式
3. 视图/表中 device 加 `environment` 列，诊断报告必须渲染环境标签
4. Inventory 加载时检查同名跨环境 → 日志告警

**验证:** `olav "R1 BGP?"` 若 inventory 有 lab.R1 和 prod.R1，输出必须明确标示环境；若未指定则报错让用户选。

---

### ISSUE-ARCH-09: Syslog 驱动的只读自动诊断回路 —— Future Feature

**状态**: Future（未来功能，非当前缺陷）→ P3
**发现日期**: 2026-04-17（rev 140 修订；rev 141 标注 future）

**定位:** OLAV 不做自动修复，但可以做 **自动诊断**。已具备前置能力：
- ✅ `src/olav/services/syslog_receiver.py` 已集成，syslog 已入库
- ✅ 诊断 profile 框架（`audit/profiles/*.md`）已就绪
- ✅ 只读查询栈（execute_sql / retrieve_diagnostic_context 规划中）完整

**缺的是触发链:** 从 "syslog 入库" 到 "自动跑诊断 profile 并推送报告" 的胶水层。

**典型场景:** 凌晨 3 点 syslog 命中 `BGP-5-ADJCHANGE: Down` → OLAV 自动跑 `bgp_health` profile → 生成诊断 markdown → 推送到 Slack/企业微信/邮件 → **值班工程师早上直接看分析，决定是否处理**。纯只读，完全符合定位。

**设计建议（复用现有 syslog 基础设施）:**
1. 新建 `alerting/rules.yaml`：声明 syslog pattern → audit profile 映射（如 `pattern: "BGP-5-ADJCHANGE.*Down"` → `profile: bgp_health` + `window: 15min`）
2. syslog_receiver 写入时同步触发 rule 匹配（基于现有 IngestManager 回调或新增 hook）
3. 匹配命中 → 后台异步跑 audit profile → 产出 `exports/auto_reports/<rule>_<ts>.md`
4. 推送适配器：Slack webhook / 企业微信 bot / 邮件（MVP 阶段任选一）
5. **硬边界:** 只调用 `execute_sql` / `map_engine` / `render_report` 等只读工具；**禁止** execute_cli（即便只读命令）避免任何 SSH 负担

**验证:** 模拟 syslog `"%BGP-5-ADJCHANGE: neighbor 10.1.1.2 Down"` 入库 → 15s 内 `exports/auto_reports/bgp_flap_*.md` 生成 + webhook 通知，无任何 SSH 出站流量。

**优先级说明:** P3 且 Future —— 当前诊断仍可由工程师手动触发（`olav --agent audit "bgp health"`）完成同样流程；自动化是体验优化，非能力缺口。放在 ARCH-10 联合检索 + ARCH-11 引用链 完成之后再做。

---

### ISSUE-ARCH-10: 多源检索融合 —— Memory/KB 已统一，剩余四路待整合

**状态**: ✅ Closed (Round 46 reconciliation) — ARCH-16 tool-LIMITs + ARCH-18 subagent cap 即是 orchestration pattern；hybrid_search 按设计**不造**

| 原方案项 | 状态 | 证据 |
|---|---|---|
| KB + memory 统一（去 kb_chunks 割裂） | ✅ | `src/olav/core/memory/__init__.py` 统一 table；`recall_memory()` 合并 KB + history；rev 144 标注已完成 |
| 4 source tools 存在 (execute_sql / recall_memory / search_logs / search_commands) | ✅ | `.olav/workspace/core/tools/` 下 4 个 canonical 都在 |
| 多源 orchestration 模式（subagent delegation 避 fan-out） | ✅ (Round 46) | ARCH-16 tool-level LIMITs (R40/41) + ARCH-18 subagent return cap (R40) 已实现。Orchestrator prompt 按"迭代调用 + subagent 内部 fan-out 蒸馏"约束，而非由 hybrid_search tool 强合。Pattern 落在运行时，不在 API |
| `hybrid_search` unified API | ✅ 按设计不造 (Round 46) | 显式 non-goal — spec rev 144 / Round 27 均记"倾向不造 new tool，依赖 orchestrator prompt 约束 routing"。保留此列作 closed-by-design 标记，防止未来重新提议 |

**Round 43 两处 doc drift 修完**（web_search.py docstring + memory/__init__.py hybrid_search docstring），ARCH-10 完全收官。
**发现日期**: 2026-04-17（rev 144 修订：核对实际代码后大幅缩小问题范围）

**重大澄清:** 之前判断 "KB 与 memory 检索割裂" 已不成立 —— `dev_docs/43. UNIFIED_KNOWLEDGE_STORE_DESIGN.md` 设计已落地：
- `src/olav/core/knowledge/` 目录已删除（合并入 memory）
- 统一表 `MEMORY_TABLE = "memory"`，`recall_memory` 已是 KB + 历史事件 + agent 自动捕获 fact 的统一入口
- `.olav/workspace/core/tools/` 已无 `search_knowledge_lancedb.py`
- `migrate_kb_chunks()` 是从旧 `kb_chunks` 表的迁移工具（保留合理，旧用户需要）

**残留（小修即可）:**

| 残留 | 位置 | 处置 |
|---|---|---|
| `search_knowledge()` 注释引用 | `.olav/workspace/core/tools/web_search.py:67` | ✅ (Round 43) 改为 `recall_memory()` |
| `KB_TABLE` 在 docstring | `src/olav/core/memory/__init__.py:940` | ✅ (Round 43) 替换为 "unified memory store" 描述 |

**ARCH-10 关闭 (Round 43)**：两处残留注释 + docstring 修正 + `tests/governance/test_round43_arch10_arch06_close.py` 2 条 pin 守护不回归。多源融合 architecturally complete（ARCH-16 tool-level LIMITs / ARCH-18 subagent cap 已实现正确的 fan-out 约束方式）。

**实际待解决问题：剩余三/四路独立**

ARCH-10 真正剩下的检索源：

| 检索路径 | 范围 | 状态 |
|---|---|---|
| `execute_sql` | 结构化遥测（v_*_auto / parsed_outputs） | ✅ 独立 |
| `recall_memory` | 文档 + 历史事件 + agent fact（统一） | ✅ 独立（已合并 KB） |
| `search_logs` | syslog 全文 | ✅ 独立 |
| `search_commands` | 命令字典 / 平台能力 | ✅ 独立 |
| `raw_output_store` | 原始 CLI 文本 | ⚠️ 待 ISSUE-RAW-02/05 |

**与诊断需求的错配（缩小后）:** 用户问 "R1 BGP flap" 仍需 LLM 自己决定调三/四路并手动融合。例如：
- 当前 session state → `execute_sql` 查 `v_bgp_neighbors`
- 过去 24h 日志 → `search_logs`
- 设备型号已知问题 / 历史相似事件 → `recall_memory`（已统一，good）
- 相邻设备状态 → `execute_sql` join `topology_links`

但比之前简化很多 —— **memory 融合的核心已完成，剩下的是结构化数据 vs 文本数据 vs 时序日志 的融合**。

**设计建议（修订后，工作量小很多）:**

1. **小修残留**（< 1 小时）:
   - `web_search.py:67` 注释 `search_knowledge()` → `recall_memory()`
   - `memory/__init__.py:940` 移除 `KB_TABLE` 提及
2. **撤回原 fan-out 工具提议** —— 详见 ARCH-16，对小模型是反模式
3. **多源融合走 subagent delegation 而非 orchestrator 端 fan-out**：
   - Orchestrator 保持轻上下文 + 迭代调用
   - 复杂诊断任务 `olav_delegate("diagnostic-fusion", task)`，由 subagent 在自己的上下文里 fan-out + 蒸馏
   - Orchestrator 只看到 subagent 返回的 summary，不见原始 bundle
4. **`raw_output_store` 等 ISSUE-RAW-02/05 内部自动 fallback** —— 不需要 LLM 决定

**为什么降级 P1 → P2 (rev 145) → P3 (rev 146):**
- 最难的两路（memory + KB）已合并
- 剩余四路检索是不同形态（SQL/向量/全文），强行融合损失精度
- 小模型部署下 fan-out 会撑爆上下文（见 ARCH-16）
- 多源融合的正确机制（subagent delegation）已存在
- 残留只是注释问题

**与其他 issue 的关联:**
- ARCH-11（引用链）：每个工具独立返回时各自附带 evidence token，subagent 蒸馏时保留
- ARCH-16（上下文预算）：本条降级的根本原因
- ISSUE-RAW-02/05：解决后 raw_output_store 内部自动 fallback

---

### ISSUE-ARCH-11: 诊断结论缺引用链（Explainability） —— 人类复核成本高

**状态**: ✅ Closed (Round 45) — Phase 1 (source dicts + `[src:]` tags) + Phase 2 (linter) + `parse_src_token` (R44) + `olav explain` CLI (R45) 全部 ship

**Round 44 探测关键发现**：Phase 1 (`_source` dict + `[src: …]` tag 渲染) + Phase 2 (citation linter) 实际**早已 ship**，Round 25 matrix 是 stale doc。本轮同步 doc 状态 + 加 groundwork 为未来 `olav explain` CLI 做准备。

| 原方案项 | 状态 | 证据 |
|---|---|---|
| Report 渲染 pipeline（Phase 2+3） | ✅ | `.olav/workspace/audit/auditor/tools/render_report.py` |
| Findings 结构化 `_source` 字段 (`{table, snapshot_id, device, row_index}`) | ✅ (Phase 1) | `anomaly_engine.py` + `map_engine.py` 在 `emit_sources: true` 时附；`tests/governance/test_evidence_sources.py` 已 pin |
| Finding 后的 `[src:token]` 标记 | ✅ (Phase 1) | `_format_source_suffix()` + `_build_evidence_block()` 生成 "## Evidence" 段；pass to LLM 的 prompt 含 tag，LLM 可引用；同 pin |
| Evidence linter (report 里 non-empty section 必须有 citation) | ✅ (Phase 2) | `render_report_linter.py` (149 行) + `render_report.py` emit_sources gate 调用；`tests/governance/test_citation_linter.py` 已 pin |
| `parse_src_token()` 反解析 helper | ✅ (Round 44) | `render_report.parse_src_token(tag) -> dict` + `_format_source_suffix(parse_src_token(tag)) == tag` 往返不变量；`tests/governance/test_round44_arch11_reconcile.py` 13 条 pin |
| `olav explain <token>` CLI | ✅ (Round 45) | `src/olav/cli/commands/explain.py` 注册 + dispatch 在 `cli/main.py`；token 解析走 Round 44 的 `parse_src_token`，`_fetch_source_rows` 根据 token 字段（table/snapshot_id/device/row_index）构造 `SELECT ... WHERE ... ORDER BY created_at DESC LIMIT N` 查询；`read_only=True` pin + ban write-helper imports pin；`_format_row` field truncation。`tests/governance/test_round45_olav_explain_cli.py` 11 条 pin |

**结论 (Round 45)**：ARCH-11 完全收官。Phase 1 (source + tag 渲染) + Phase 2 (linter 守 citation 覆盖) + `parse_src_token` 反向解析 helper + `olav explain <token>` CLI 全链 ship。Operator 工作流：报告里看到 `[src: parsed_outputs#snap_20260418; device=R1]` → 复制 → `olav explain "..."` → 看原始 row → 验证 LLM finding。
**发现日期**: 2026-04-17

**定位:** 只读诊断工具最大价值是"把信息归并给人类"，那人类必须能**快速验证**。当前 LLM 输出的结论常包含"我推断 BGP 因为 MTU 不匹配 flap"之类的断言，但：
- 未链到具体 SQL / raw output 行
- 未说明 "我基于哪几个 snapshot_id、哪条 log"
- 用户要自己重跑 SQL 验证，成本高 + 易遗漏

**现状:** writer subagent 产出 markdown，但引用格式不结构化。

**设计建议:**
1. 规范诊断报告模板：每个结论尾随 `[src: v_bgp_neighbors#snap_xxx row=12; syslog#2026-04-16T02:13]`
2. `execute_sql` / `retrieve_diagnostic_context` 返回结果附带 **source token**，LLM 引用时必须带 token
3. CLI/TUI 支持 `olav explain <token>` → 回显原始数据
4. Audit 报告的每条 finding 已有 severity，但应追加 `evidence` 字段指向 raw 数据

**验证:** 诊断报告内每条断言都有 `[src: ...]`；点击/复制 token 能回溯到原始行；无引用的结论被 linter 拒绝。

---

### ISSUE-ARCH-12: 用户无入口知道"当前能查什么" —— Schema discovery UX 空白

**状态**: ✅ Closed (Round 25 reconciliation) → P2（rev 140 新增）

| 原方案项 | 状态 | 证据 |
|---|---|---|
| `olav catalog` 列出 topics | ✅ | `src/olav/cli/commands/catalog.py` `_TOPICS` 5 级分类（Device Inventory / Routing & BGP / Topology / Interfaces / Snapshot History） |
| `olav catalog <topic>` drill to tables | ✅ | `_render_topic()` 实现 |
| `olav catalog describe <table>` 输出 schema | ✅ | `_render_describe()` 实现 |
| Pin tests | ✅ | `tests/governance/test_catalog_command.py` 12 条全绿 |
| `olav suggest <device>` (Phase 2 enhancement) | ⏸️ Deferred P3 | 锦上添花，不阻塞核心 discovery UX |

**核心 Schema discovery UX 完整**。`olav suggest` 作为 Phase 2 P3 enhancement 延后。
**发现日期**: 2026-04-17

**定位:** 只读查询工具的"发现性"决定可用性。当前用户想知道"我能问什么"只能：
- 读 `core/references/SCHEMA_REFERENCE.md`（静态文档，易过时）
- 猜一个问题然后看 agent 是否答得上

**现状:**
- `schema_catalog` / `view_recipes` 表存在（`ops/analysis/SKILL.md` 列出）
- 但没有面向用户的 `olav schema` / `olav what-can-you-answer` 入口
- 用户自助发现能力弱

**设计建议（rev 148 修订：遵循渐进式披露）:**
1. 新 CLI 子命令 `olav catalog` —— **三级 drill-down**，不平铺：
   - 一级（默认）：5-7 个主题分类（设备清单 / 路由协议 / 拓扑 / 日志 / 配置历史 …）
   - 二级（`olav catalog <topic>`）：该主题下的表/视图 + 一句问句
   - 三级（`olav catalog describe <table>`）：完整 schema + 示例 SQL
2. 新诊断入口 `olav suggest <device>` —— 基于设备支持的命令、已采数据，列出"你现在可以问这些问题"
3. TUI 首屏加 "Ask me about:" + 5 条样例
4. `schema_catalog` 增加 `topic` + `example_questions` 列（中英文自然语言），catalog 命令展示

**验证:** 新手运行 `olav catalog` 一级输出 < 20 行 / < 500 tokens；drill-down 到具体表方可看到 schema 详情。

---

### ISSUE-ARCH-13: Snapshot 对比（diff）—— 诊断重要场景能力未完全贯通

**状态**: ✅ Closed (Round 47) — 4 个 diff_* tools + aggregator + CLI shortcut 全 ship

| 原方案项 | 状态 | 证据 |
|---|---|---|
| `diff_snapshots(device, snap_a, snap_b)` aggregator 工具 | ✅ | `.olav/workspace/ops/tools/diff_snapshots.py` + `_TABLES` registry 覆盖 4 个表（parsed_outputs / topology_links / raw_output_store / oc_outputs） |
| `diff_sql_state` / `diff_topology_drift` / `diff_routing_drift` / `diff_configs` | ✅ | 在 `.olav/workspace/ops/analyze/tools/` (R31 merge)；`diff_configs` symlink `core/tools/` → `ops/tools/` |
| Pin tests | ✅ | `tests/governance/test_diff_snapshots.py` 7 条全绿 |
| `olav diff <snap1> <snap2>` CLI shortcut | ✅ (Round 47) | `src/olav/cli/commands/diff.py` 包 `diff_snapshots()` LangChain tool，支持 `--table` / `--device` / `--max-rows` / `--json` 四个 flag；`cli/main.py` 注册 `build_diff_parser` + dispatch；`tests/governance/test_round47_*` 11 条 pin |
**发现日期**: 2026-04-17

**定位:** 只读诊断里最强大的手段是 "**昨天好今天坏，差异在哪？**"。OLAV 已有 snapshot_id 时间切片 + `ops/diff/`，但端到端用户旅程未验证。

**现状:**
- SCHEMA_REFERENCE 已规范 `snapshot_id = (SELECT MAX(snapshot_id) FROM ...)` 读最新
- `ops/diff/` 目录存在，但 SKILL 空（只有 prompts/）
- 没有工具 `diff_snapshots(device, snap_a, snap_b, dimension)` 一键生成差异表

**设计建议:**
1. 实现 `diff_snapshots` 工具：传入两个 snapshot_id + 设备范围 + 维度（bgp/ospf/interface/config），返回新增/消失/改变行
2. 诊断 agent 在 "state change" 类问题（flap/outage）默认调用此工具
3. 报告渲染 diff 为 side-by-side / unified 格式
4. `olav diff <device> <since>` CLI 快捷方式（since = "1h ago" / "yesterday"）

**验证:** 用户问 "R1 今天和昨天 BGP 状态差异？" → 自动定位最近两个 snapshot，跑 diff，输出新增/消失的邻居 + state 变化。

---

### ISSUE-ARCH-14: Sim/Lab 自动注入多层网络对象模型（NoM）—— 政策模拟必备

**状态**: ✅ Closed (Round 59) — P1 L1+L2+L3.ospf+L3.bgp + P2 L4 clause 提取 + P3 deterministic walker + BGP neighbor→route-map 绑定解析 + sandbox auto-inject，全链 ship；Junos policy-statement + prefix-list/community-list 内容解析留作 future vendor-expansion（不在核心 P1-P3 scope 内）

| 阶段 | 状态 | 证据 |
|---|---|---|
| **P1 L1** — physical layer (networkx over `netops.topology_links`) | ✅ (Round 52) | `olav-netops/src/olav_netops/sim/network_model.py` — `NetworkModel` lazy proxy + `PhysicalLayer` + `load_network_model()` 入口 + `scope` 过滤 + 读 `MAX(snapshot_id)` fallback；`tests/governance/test_round52_arch14_p1_l1_groundwork.py` 11 条 pin（lazy construction、graceful empty-DB、cache pin、L2/L3/L4 stub 均有 ARCH-14 指针、end-to-end networkx 建图、scope 过滤、默认 devices） |
| **P1 L2** — VLAN / L2 adjacency | ✅ (Round 55) | `L2Layer` 类 + `_build_l2_layer()` 读 `parsed_outputs` WHERE `command ILIKE '%vlan%'`；归一化 Cisco (`vlan_id`/`name`/`interfaces[]`) / Junos (`vlan_name`/`tag`/逗号分隔字符串) / 大写变体；`_normalise_interface_list()` helper 吞 list/str/None/int；graph 用 `VLAN:<id>` namespace 节点防与 hostname 冲突；`model.l2.by_device()` / `.vlan_ids(device=None)` / `.devices()` / `.vlans`；`tests/governance/test_round55_arch14_l2_layer.py` 16 条 pin |
| **P1 L3 OSPF** — 邻居建图 | ✅ (Round 53) | `OspfLayer` 类 + `_build_ospf_layer()` 读 `parsed_outputs` WHERE `command ILIKE '%ospf neighbor%'`；支持 vendor case 变体（`neighbor_id` vs `NEIGHBOR_ID`）；`model.l3.ospf.adjacencies` / `.graph` / `.neighbors(device)` / `.unhealthy()` / `.devices()`；`_L3Proxy` 持 parent back-ref + 缓存；`tests/governance/test_round53_arch14_ospf_layer.py` 13 条 pin |
| **P1 L3 BGP** — 邻居 + session state | ✅ (Round 54) | `BgpLayer` 类 + `_build_bgp_layer()` 读 `parsed_outputs` WHERE `command ILIKE '%bgp summary%'`；归一化 Cisco (`neighbor`/`as`/`state_pfxrcd`) / Junos (`peer`/`peer_as`/`state`) / Arista (case variants) 字段为 `{device, neighbor_ip, neighbor_as, state}`；`unhealthy()` 把 Cisco 数字 prefix count 视为 Established；`tests/governance/test_round54_arch14_bgp_layer.py` 15 条 pin |
| **P2 L4 policy** — route-map clause 提取 (read-only) | ✅ (Round 56) | `L4Layer` 类 + `_extract_route_maps_cisco_ios()` 行级 regex 解析 `route-map NAME permit\|deny SEQ` stanza + 缩进 match/set 行；从 `raw_output_store` WHERE `command ILIKE '%running-config%' OR 'show run%'` 读取；`.clauses` / `.by_device()` / `.by_policy()` / `.policies()` / `.devices()` / `.graph`（POLICY: namespace）；Junos policy-statement / prefix-list / community-list 等保留后续；`tests/governance/test_round56_arch14_l4_layer.py` 17 条 pin |
| **P3 policy evaluation** — walker + neighbor/direction 解析 | ✅ (Rounds 58+59) | R58 `L4Layer.walk(device, policy_name, matches)` deterministic 子句走法（dict/callable/None matcher，AND-evaluate，implicit_deny 默认），R59 `_extract_bgp_neighbor_policies_cisco_ios` 从 running-config 解 `neighbor X.X.X.X route-map NAME in\|out` → `L4Layer.neighbor_policies` dict；`L4Layer.policy(device, neighbor, direction, matches=)` 端到端：查 neighbor binding → 调 walk() → 返 `{action, sets, trace, missing, unbound, policy_name, device, neighbor, direction, reason}`；无 binding 返 Cisco permit-all 语义（`unbound=True`, `action="permit"`）。`tests/governance/test_round58_*` 16 条 + `test_round59_*` 16 条 pin |

**Round 52 groundwork 价值**：
- 公开 API 固定（`model.physical / l2 / l3.ospf / l3.bgp / l4.policy`），后续 rounds 只填 impl 不改签名
- Lazy construction 让 sim import 不碰 DB（sandbox / tool_help 路径可 cheaply 引用）
- Empty-DB 优雅降级让"部分 bootstrap"环境可用
- 为 `run_python_simulation` 自动注入预留 hook 点

**Round 57 sandbox auto-inject 落地**：
- `src/olav/platform/sandbox.py` `_NETWORK_MODEL_PROLOGUE` prepended 到每个 sandbox script
- `model = load_network_model()` binding auto-present；缺 olav_netops 时 degrade 到 `model = None`
- DuckDB 只读 monkey-patch 在前，NetworkModel 在后 → `model.physical` 查询继承只读约束
- `tests/governance/test_round57_sandbox_network_model_inject.py` 9 条 pin

实测：Design 完整（下文 P1/P2/P3 分阶段），但 0 LOC —

- 无 `NetworkModel` / `load_network_model` / `model.l3.ospf.*` 等 API
- 无 sandbox wiring
- 无 schema 定义文件
- `grep -r "NoM\|NetworkModel\|load_network_model" src/ olav-netops/ .olav/` → 0 hits

**下一步**：Design-first round，先写 P1 L1+L2+L3 schema + `load_network_model()` / `save_network_model()` API + 1-2 个 sim/lab 消费点 demo。XL 规模，多轮次分解。
**发现日期**: 2026-04-17

**为什么 ARCH-06 recipe 不够:** Recipe 是**单层、即时、无状态**的查询模式 —— 适合"VRF_A 在哪几台 PE"这种列表问题。但 sim/lab 需要 **L1→L4 联合视图 + 状态化 trace**，尤其 policy 需要 route-map 子句级求值。让 LLM 现场拼 networkx 错误率不可控。

#### 设计：sandbox 自动注入 `NetworkModel` 对象

`ops-analysis` 检测到 sim 类意图（`simulate` / `what-if` / `why doesn't` / `trace` / `impact`）时，自动调用 `load_network_model()` 注入 sandbox locals。**`model` 是 lazy proxy（rev 148 修订，遵循渐进式披露）—— 每层独立按需加载，不一次性构建全图**：

```python
from olav_sim import load_network_model
model = load_network_model(snapshot="latest", scope=["R1", "R2", "R3"])
# 此时仅加载元信息（device 列表 + snapshot_id），各层尚未实际构建

model.physical        # 首次访问才查 LLDP/CDP 建图，之后缓存
model.l2              # 首次访问才查 VLAN/EVPN，之后缓存
model.l3.ospf         # 同上
model.l3.bgp          # 同上
model.l4.policy(device="R1", neighbor="R2", direction="out")
                      # 首次调时才解析对应 route-map
model.trace_prefix("10.2.0.0/16", origin="R5", dest="R1")
                      # 高级 API 内部按需触达对应层 + 缓存复用
```

**按问题智能选层:**
| 问题类型 | 注入层 |
|---|---|
| 链路故障影响面 | L1 + L2 |
| 路由收敛 / 黑洞 | + L3 |
| 政策影响 / prefix 学习 | + L4 |

#### 三阶段实施（按 L4 难度递增）

| 阶段 | 范围 | L4 策略 | 工作量 |
|---|---|---|---|
| **P1** | L1+L2+L3 联合 model | 不模拟 | 中（解析现有 parsed_outputs + 组装） |
| **P2** | + L4 只读显示 | regex 提取 route-map / prefix-list 到 `policy_clauses` 表 + LanceDB 索引；trace 时**显示**经过哪些 policy 但不求值 | 中 |
| **P3** | + L4 政策求值 | 选 a/b/c | 大 |

**P3 两条技术路线（开源版）:**

| 路线 | 优势 | 劣势 | 适用 |
|---|---|---|---|
| **a. LLM-as-interpreter** | 贴 OLAV 现有栈、零外部依赖 | 不确定性 → 需 evidence 强制（ARCH-11） | 大多数 vendor、常规 route-map |
| **b. 手写 mini-interpreter** | 确定性 | 维护重，每厂商语法不同 | 长期高 ROI vendor |

**推荐:** P1 + P2 必做 → P3 用 **a + ARCH-11 强制引用链** 由人类复核。

> **企业版补强:** 确定性 policy 求值由企业版 `ent_batfish_analyzer` 提供（pybatfish 集成，复用 `IngestManager._backup_commands()` 的 raw config 数据管道），见 `dev_docs/34. ENTERPRISE_FEATURES.md`。开源版的 LLM-as-interpreter 作为企业版不可用时的 fallback。

#### Lab 联动：变更预检流程

Lab 从"盲推 + 测"升级为 **预测 → 推 → 验证**：

```
变更场景：R1 BGP route-map 调整
1. model_before = load_network_model(snapshot="latest")
2. model_after  = model_before.apply_change(device="R1", config_diff=...)
3. diff = model.diff_prefix_propagation(model_before, model_after)
   → 列出哪些 prefix 学习/广告会改变
4. 若变化超预期 → 警告用户、阻塞 push
5. 用户确认 → 推 containerlab 实跑
6. Lab 实测 → 与 model 预测对比 → 计入 model 校准数据
```

Lab 命中率显著提升；同时 lab 实测反过来校准 model（持续学习）。

#### 影响面（相对 ARCH-06）

| 组件 | ARCH-06 | + ARCH-14 NoM |
|---|---|---|
| 基础 ETL / 现有视图 | 不变 | 不变 |
| `run_python_simulation` runtime | 不变 | 自动注入 `model` 到 sandbox locals |
| `ops-analysis` system prompt | 加范式 A/B/C | 改为"sim 类问题优先 `model.*`，列表类用范式 C" |
| Lab pre-push 流程 | — | 新增 `model.apply_change` + diff |
| 新增数据 | — | `policy_clauses` 表（P2 起）；`model_predictions` vs lab 实测对比表（P3 起） |
| 新增依赖 | — | networkx（已有）；pybatfish（可选） |

**与其他 issue 的关联:**
- ARCH-06：recipe 是 L1.5 / L2 列表查询；NoM 是 L1→L4 状态化 sim，二者互补
- ARCH-10（联合检索）：NoM 是 trace 类问题的主要"检索"形态
- ARCH-11（引用链）：trace_prefix 输出每跳必须带 `[src: device.config#routemap_X clause_3]`
- ARCH-13（diff）：`model.diff_prefix_propagation` 是 snapshot diff 在 sim 维度的特例

**验证:**
1. P1: `olav "R1 到 R5 OSPF 路径？"` → sim 自动注入 model，调 `model.l3.ospf`，输出 ECMP
2. P2: `olav "R1 出向 R2 应用了什么 route-map？"` → `model.l4.policy(R1, R2, out)` 直出
3. P3: `olav "R1 为什么学不到 10.2/16？"` → `model.trace_prefix(...)` → 显示在 R3 被 prefix-list `BLOCK_RFC1918` 拒绝，附引用链
4. Lab: 准备一个 route-map 变更 → pre-push diff 显示"R1 将不再广告 10.1/16 给 R2" → 用户决定继续/取消

---

### ISSUE-ARCH-16: 小模型上下文预算管理 —— 反 fan-out 原则

**状态**: ✅ Closed (Round 47 reconciliation) — 核心 tool-LIMITs 3-of-3 (Rounds 40-41) + TIER_DEFAULTS 架构 landed；唯一残留"docstring typical-return 估算"属文档约定，非代码 gap

| 原方案项 | 状态 | 证据 |
|---|---|---|
| `TIER_DEFAULTS.context_budget` (small=8K, medium=32K, large=200K) | ✅ | `src/olav/core/config.py:39-64` |
| `tier_default(tier, key, fallback)` helper | ✅ | `config.py:67-69` |
| `ContextBudgetMonitor` 软警告 | ✅ | `tests/governance/test_context_budget.py` |
| Tool 签名级 LIMITs (`recall_memory` / `execute_sql` / `search_logs`) | ✅ (Rounds 40-41) | `recall_memory` (R40): `_resolve_recall_limit()` → `TIER_DEFAULTS.recall_top_k` (small=1/medium=2/large=3)。`execute_sql` (R41): `_resolve_context_rows()` → `TIER_DEFAULTS.execute_sql_context_rows` (small=10/medium=20/large=50)，替换硬编码 `MAX_ROWS_TO_CONTEXT=20`；CSV 导出仍 >50 行（data persistence 不受 tier 影响）。`search_logs` (R41): `_resolve_search_limit()` → `TIER_DEFAULTS.search_logs_default_limit` (small=20/medium=50/large=100) + hard ceiling 500。`tests/governance/test_round41_arch16_tool_limits.py` 15 条 pin（3 TIER_DEFAULTS + 5 execute_sql + 7 search_logs） |
| Per-tool token 估算 (docstring "typical return < N tokens") | ❌ | 未实现（docstring 约定未动） |

**ARCH-16 tool-level LIMITs 完结 ✅**：3 个 P2 目标 (recall_memory / execute_sql / search_logs) 全部落地 tier-aware 默认 + hard ceiling。TIER_DEFAULTS 扩充 5 个 key (`execute_sql_context_rows`、`search_logs_default_limit`) 以 5 个 key 总量（+ R40 的 `recall_top_k`、`return_compact_chars` 等）。
**发现日期**: 2026-04-17

**定位:** OLAV 主流部署是小模型（Qwen3 32B / Llama 3.1 8B / 本地推理），实际有效注意力窗口 **4-8K tokens**。任何诱使一次返回大上下文的工具/模式都是反模式。

**反模式表（要避免）:**

| 反模式 | 危害 |
|---|---|
| 单工具 fan-out 多源（如原 ARCH-10 提议的 `retrieve_diagnostic_context`） | 一次返回 5×N 段，10K+ tokens 灌入 orchestrator |
| 默认 `LIMIT 100` 的 SQL | 单查询轻易 5K tokens |
| `recall_memory(top=20)` | 向量检索 20 chunks 直撑爆 |
| Audit profile 一把把所有 finding 塞回 orchestrator | 应由 subagent 蒸馏 |
| `search_logs` 不带 time_window 或太宽 | 24h syslog 可达 1MB+ |
| Tool 返回 raw JSON 不 summary | LLM 自己 parse 代价高 + 易迷失 |

**正模式（已存在或应推广）:**

| 正模式 | 实现 |
|---|---|
| **迭代调用** | Orchestrator 一次一个工具，看结果决定下一步（OLAV 当前默认行为）|
| **Subagent delegation 边界** | 多源融合在 subagent 内部完成，只返回蒸馏后 summary（`olav_delegate` 已存在）|
| **每工具默认低 LIMIT** | `recall_memory` 默认 top-5；`execute_sql` > N rows 触发 auto-summary；`search_logs` 默认 top-20 + 1h 窗口 |
| **Tool 返回带 token 估算** | docstring 明确"典型返回 < 500 tokens"，超出在 result 里告警 |
| **Two-stage retrieval（按需）** | 重型场景：先 `*_index()` 返回 lightweight 摘要 + ids，LLM 选择性 `fetch_*(ids)` 拉详情 |

**设计建议（按工作量小→大）:**

1. **审计现有工具的默认行为**（半天）:
   - `recall_memory` / `execute_sql` / `search_logs` / `search_commands` 的默认 LIMIT 与 token 上限
   - 每个工具 docstring 加 "典型返回 < N tokens" 字样
2. **execute_sql 大结果 auto-summary**（已有？需验证）：行数 > 50 → 自动返回 `head(10) + tail(5) + count + columns`，引导 LLM 加 WHERE/LIMIT 重查
3. **System prompt 强化迭代原则**：
   - "每次只调一个检索工具，看结果决定下一步"
   - "需要多源融合时优先 `olav_delegate` 给诊断 subagent"
   - "禁止一次性请求所有可能相关的数据"
4. **拒绝 fan-out 类工具提案** —— 任何"一次返回多源 bundle"的工具设计需先证明对 8K 上下文友好
5. **可选：Two-stage retrieval helper**（重型场景）：
   - `context_index(symptom, device, time_window)` 返回 lightweight 概要 + 句柄列表，~500 tokens
   - LLM 决定 `context_fetch(handles=[...])` 选择性拉详情
   - 仅在 `audit-orchestrator` 这种重型 agent 启用

**模型档位与策略矩阵:**

| 模型档位 | 上下文 | 工具调用策略 |
|---|---|---|
| 小模型（< 32B 或本地）| 4-8K 有效 | 严格迭代、低 LIMIT、subagent fan-out 必经 |
| 中模型（70B / Sonnet 级）| 16-32K 有效 | 可批量小 fan-out（2-3 源） |
| 大模型（Opus / 200K+）| 100K+ 有效 | fan-out 上限放宽，但仍按需 |

实现：模型注册表加 `context_strategy` 字段；agent 初始化时按当前模型档位选择默认 LIMIT 与是否启用 fan-out 工具。

**验证:**
1. `recall_memory("BGP", top=999)` 应被强制截断到默认 top-5 + warning
2. `execute_sql("SELECT * FROM v_bgp_neighbors_auto")` 在 100+ 行时返回 summary 而非全表
3. 同一诊断任务在 8B 模型 vs 70B 模型上自动启用不同策略
4. CI 加测试：无任何工具单次返回 > 4K tokens（除非显式 `--full` flag）

**与其他 issue 的关联:**
- ARCH-10（多源检索）：本条是其降级的根本原因
- ARCH-11（引用链）：evidence token 必须紧凑，避免引用本身撑爆 context
- ARCH-14（NoM）：sandbox 注入的 model 是 Python 对象，零 LLM context 占用 —— 这是天然的"上下文友好"设计
- Writer subagent：完美范例 —— 在自己 context 里读多个 export，输出蒸馏 markdown 给 orchestrator

#### 底层设计原则：渐进式披露（Progressive Disclosure）

**ARCH-16/17/18 共同遵循的原则:** 任何上下文消费点（static_context、tool 返回、memory 注入、schema 暴露）都应**按需揭示**，而非"先把所有可能相关的都给出来"。

**判定标准（设计/代码 review 时用）:**
1. 默认返回是否摘要级？详情是否需要显式 opt-in（`full=True` / `mode=detailed`）？
2. 是否有"先索引后详情"的二阶段路径（`*_index()` → `fetch_*()`）？
3. 注入的内容是 push（自动塞）还是 pull（agent 主动取）？push 的必要性是否充分？
4. 是否可按 user/agent 信号过滤（`mode: on_intent` 关键词触发）？
5. 同一 API 是否提供"概览 / 详情"两层入口？

**当前已识别的 4 处半渐进 / 待收紧:**

| # | 位置 | 当前 | 收紧方向 |
|---|---|---|---|
| 1 | AutoRecallMiddleware | push-based 强制注入 top-3 | hybrid：首轮 push 1 条 hint，后续 pull-only via `recall_memory()` |
| 2 | `execute_sql` schema injection | 注入所有相关表 schema | 仅注入"问题中提到 + 已查过"的表；`describe_table()` 工具按需查 |
| 3 | ARCH-14 NoM `model` | `load_network_model` 一次构建多层 | `model.l3.bgp` 等 lazy property，访问才计算 |
| 4 | ARCH-12 `olav catalog` | 平铺所有表 | 三级：主题 → 表名 → schema，drill-down |

**Tool docstring 张力:** 完全瘦身会丢"何时用"语义。调和方案 = 一级 5 行核心 + 二级 `tool_help(name)` 完整决策树 + 三级 system prompt 一次介绍 tool family。

**反例（永远应避免）:**
- 单工具一次返回多源 bundle（违反 1+2+3）
- AGENT.md 注入整目录 reference（违反 1+4）
- 工具 docstring 含完整 schema dump（违反 1+5）

---

### ISSUE-ARCH-17: 静态上下文注入失控 —— 单 agent 启动即吃 16K tokens

**状态**: ✅ Closed (Round 47 reconciliation) — 三档 resolver (R24) + `OLAV_DEBUG_CONTEXT` (R36) + `load_reference` section slicing (R38) 全部 ship

| 原方案项 | 状态 | 证据 |
|---|---|---|
| 三模式 resolver (`always` / `on_intent` / `lazy`) | ✅ | `src/olav/agents/static_context_resolver.py:37-` |
| `TIER_DEFAULTS.static_context_mode` per tier | ✅ | `config.py:43/53/60` (small / medium=on_intent, large=always) |
| `OLAV_STATIC_CONTEXT_MODE` env 覆盖 | ✅ | resolver `_ENV_OVERRIDE` |
| Agent frontmatter 显式 `static_context_mode` 声明 | ✅ (Round 24) | `services/AGENT.md` (Round 16), `core/AGENT.md` (Round 24)；audit/ops 无顶层 static_context，不需声明 |
| `OLAV_DEBUG_CONTEXT=1` token audit 输出 ("static: 1234 / 8000 budget") | ✅ (Round 36) | `static_context_resolver.is_debug_enabled()` + `agent._debug_log_injection()`；钉在 `tests/governance/test_arch_17_debug_context.py`（24 条 pin） |
| `load_reference()` endpoint/section 切片加载 | ✅ (Round 38) | `load_reference(name, section=)` 按 `##` header 切片 + `section="?"` 列可用节；`tests/governance/test_round38_progressive_disclosure.py` 7 条 load_reference pin |

**结论**：ARCH-17 关闭完结（三档 resolver + debug env var + section slicing 全部 ship）。
**发现日期**: 2026-04-17

**核对实测：单 agent 启动（用户尚未输入）静态注入量已远超小模型预算。**

#### 实测数据

| Agent | static_context 注入 | 字节 | 估算 token | 占 8K 预算 |
|---|---|---|---|---|
| **devops** | `../infra/references/` 目录 + `./references/` 目录 | ~64,000 | **~16,000** | **200%** ❌ |
| **infra** | `netbox_dcim_api.md` (853 行) + `netbox_ipam_api.md` (326 行) + circuits + tenancy + virtualization | ~38,000 | **~9,500** | **119%** ❌ |
| **ops-lab** | `LAB_REFERENCE.md` | 14,311 | **~3,600** | 45% ⚠️ |
| **ops-analysis** | `ROUTING_EXPERT_GUIDE.md` | 9,095 | **~2,300** | 29% ⚠️ |
| ops-orchestrator | `prompts/orchestrator.md` (277 行) | ~12,000 | ~3,000 | 38% ⚠️ |

**用户尚未提问，devops/infra agent 已经吃光小模型预算。** 即便不算 tool docstring、AutoRecall 注入、subagent 调用，简单一句"列出所有交换机"都做不到。

#### 根因

| # | 根因 | 后果 |
|---|---|---|
| R1 | `static_context: [path: <dir>/]` 把整个目录无差别注入 | devops 拉了 9 个 NetBox/InfluxDB API 全文 |
| R2 | "知识就该全注入"心智模型 | 没有按查询意图过滤 |
| R3 | API reference 文档没分级（核心 schema vs 完整 endpoint 列表） | 90% 的 endpoints 在 90% 的查询里用不到 |
| R4 | 缺乏 token 预算测量 | 没有 "单 agent 启动 < N tokens" 的硬约束 |

#### 修复路径（按 ROI 排序）

**P1 — 立刻可做（半天）:**
1. **devops/SKILL.md 移除目录通配** —— `path: ../infra/references/` 改为按需 `load_reference("netbox.dcim.devices")`，腾出 ~16K
2. **infra/SKILL.md 拆分** —— 默认只注入 `netbox_quickref.md`（精简 schema 200 行），完整 API 走 `load_reference()` 按 endpoint 加载
3. **ops-analysis/SKILL.md 条件注入** —— 检测到 `simulate` / `analyze` / `root cause` 关键词才加载 ROUTING_EXPERT_GUIDE；普通查询跳过

**P2 — 1-2 周:**
4. **静态参考分级机制** —— AGENT.md 支持 `static_context` 加 `mode: always | on_intent | lazy`：
   ```yaml
   static_context:
     - path: ./references/netbox_quickref.md
       mode: always           # 200 行核心
     - path: ./references/netbox_dcim_full.md
       mode: on_intent
       intents: [device_lookup, rack_inventory]
     - path: ./references/netbox_circuits_api.md
       mode: lazy             # 只能 load_reference() 显式调
   ```
5. **`load_reference()` 工具升级** —— 支持按 endpoint / section 取片段，不取整文件
6. **每 agent 启动时打印 token 预算审计** —— `OLAV_DEBUG_CONTEXT=1` 日志输出 `static: 1234 / 8000 budget`

**P3 — 长期:**
7. **CI 增加上下文预算回归测试** —— 每个 agent 启动 + 注入后总 tokens 必须 < 阈值（小模型 4K，中 12K，大不限）
8. **模型档位驱动 mode 解析** —— 同一 `static_context` 在 8B 模型上只加载 `mode=always`，在 Opus 上 `always + on_intent` 全注入

#### 节省潜力

仅做 P1 三步：
- devops 启动节省 **~16K tokens**
- infra 启动节省 **~7K tokens**（保留 200 行 quickref）
- ops-analysis 启动节省 **~2.3K tokens**（条件注入命中率约 30%）

合计：**典型 session 节省 10K-25K tokens**，小模型从"启动即满"变成"启动后还有 50-70% 预算可用"。

#### 验证

1. `OLAV_DEBUG_CONTEXT=1 olav --agent devops "list devices"` → 日志显示 static < 4K
2. `infra` agent 收到 "device R1" 查询 → 自动加载 dcim/devices endpoint，不加载 ipam/circuits
3. CI 跑 `tests/context_budget.py`：每 agent baseline + 简单查询总 tokens 不超档位上限

#### 与其他 issue 的关联

- ARCH-16（小模型上下文预算总原则）：本条是 ARCH-16 落地的最大优化点
- ARCH-12（schema discovery）：`load_reference()` 升级后 catalog 自然成为入口
- 与 ISSUE-CORE-04（tool_discovery sys.path）无关，但同属"启动期" 优化家族

---

### ISSUE-ARCH-18: 中等上下文优化清单 —— Tool docstring + 返回截断 + AutoRecall 自适应

**状态**: ✅ Closed (Round 47 reconciliation) — 所有 5 个子项 (docstring budget / execute_cli compact / diff_configs / api_request / AutoRecall tier + budget / describe_table R39 / subagent cap R40) 全部 ship

| 原方案项 | 状态 | 证据 |
|---|---|---|
| Tool docstring ≤20 行 budget | ✅ | `tests/governance/test_docstring_budget.py` |
| `execute_cli` 返回截断 (`_COMPACT_OUTPUT_CHARS`) | ✅ | `test_compact_returns.py:36-46` |
| `diff_configs` compact 模式 (`full: bool = False`) | ✅ | 同上 |
| `api_request` 列表截断 (`_COMPACT_LIST_CAP`) | ✅ | 同上 |
| AutoRecall tier-aware `top_k` | ✅ | `test_autorecall_tier.py` 从 `TIER_DEFAULTS` 取 top_k (small=1, medium=2, large=3) |
| AutoRecall 预算感知跳过 (`recall_skip_headroom_pct`) | ✅ | `test_autorecall_budget_guard.py` |
| AutoRecall 首轮 push / 后续 pull 明确边界 | 🟡 | 注入实测，pull-via-tool fallback 未单独测 |
| Schema 选择性注入 + `describe_table()` 工具 | 🟢 describe_table ✅ (Round 39) · 选择性注入 ❌ | `.olav/workspace/core/tools/describe_table.py` canonical + `core/db_query/tools/describe_table.py` symlink + SKILL.md 声明；`tests/governance/test_round39_origin_tag_and_describe_table.py` 6 条 describe_table pin。选择性注入（去 execute_sql SchemaContext 预注入）留后续——需 SKILL-prompt 对账 |
| Subagent 返回默认 <1K tokens 限额 | ✅ (Round 40) | `src/olav/agents/delegate_tool.py` `_resolve_subagent_cap()` 读 `TIER_DEFAULTS[<tier>].return_compact_chars`（small=2000 / medium=5000 / large=10000），返回前经 `_truncate()` 附 `…[truncated, subagent produced N > M chars]` 后缀；config 不可用时 fallback 10000；`tests/governance/test_round40_subagent_cap_and_recall_default.py` 7 条 delegate pin |

**ARCH-18 全部收官**：tool docstring budget ✅ / execute_cli·diff_configs·api_request 返回压缩 ✅ / AutoRecall tier-aware ✅ / describe_table ✅ / subagent 返回限额 ✅。剩余"选择性注入"属 SKILL-prompt 层优化，归到 ARCH-17 cluster。
**发现日期**: 2026-04-17

> ARCH-17 解决的是"启动即满"，本条解决"运行中累积膨胀"。每条单独 token 节省不大，但都在 hot path 上，累计明显。

#### 优化清单（按 hot-path 频次排序）

**1. Tool docstring 瘦身（每次 tool 选择都加载）**

| 工具 | 当前 | 问题 | 修复 |
|---|---|---|---|
| `execute_cli` (`.olav/workspace/core/tools/execute_cli.py`) | 375 行（含 docstring） | 含 ⚠️ 警告段 + fallback 决策树 + 2 个 example | 5 行：name + 1-liner + arg list；"何时用"移到 system prompt 一次 |
| `execute_sql` | 含 schema_catalog 动态注入 + common queries | 已有 schema 注入是好事，但 example 太长 | example 减到 2 个最高频 |
| `api_request` | 含 NetBox / InfluxDB 通用调用约定 | 同 ARCH-17，约定可移到 infra/SKILL prompt | 留 1-liner |

**节省：每 turn ~1-2K tokens（取决于工具数）**

**2. 工具返回值默认压缩**

| 工具 | 当前返回 | 修复 |
|---|---|---|
| `api_request` | 自动 paginate 全量 | 默认 head-10 + total count；`full=True` 显式拉全 |
| `diff_configs` | 100 行截断 + 每变更 3 行 context | 默认 `summary_only=True`：变更行数 + 影响 sections 关键词；`unified=True` 才返回 patch |
| `take_snapshot` | 全量 result rows | 默认返回 per-device 状态计数，详情走 execute_sql |
| `execute_sql` 大结果 | 已有截断？需验证 | 行数 > 50 → 自动 head(10) + count + columns，提示加 WHERE/LIMIT |
| `search_logs` | 全文匹配 | 默认 head-20 + 1h 窗口；超出走 paging |

**3. AutoRecallMiddleware push → hybrid pull（rev 148 修订：更彻底渐进）**

`src/olav/core/memory/middleware.py:38` `RECALL_TOP_K = 3` 硬编码。每 turn 注入 3 段 ~200 字符 ≈ ~600-800 tokens；3 轮对话累积 ~2K tokens 是 memory 注入。**Push-based 本身违反渐进式披露 —— agent 没机会拒绝。**

**修复（hybrid 方案）:**
- **首轮 push**：注入 1 条最相关 memory 作为 hint（解决 cold start "agent 不知道自己应该想起什么"）
- **后续轮 pull-only**：依赖已有 `recall_memory(query, top_k)` 工具，agent 主动判断"我需要补充记忆"才调用
- 默认按模型档位：8B → 首轮 push 1 / 不再续注入；70B → push 2 + 后续 pull；Opus → 自由
- 配置项：`memory.recall.mode = "hybrid" | "push_only" | "pull_only"`，`top_k_by_model_tier`，`skip_if_budget_below`

**4. Subagent delegation 边界审计**

`olav_delegate` 是上下文边界（ARCH-16 正模式），但需确认：
- 传给 subagent 的 task description 是否包含 orchestrator 全部上下文？应只传必要任务描述
- subagent 返回是否限长？默认应 < 1K tokens；超出 subagent 内部应蒸馏
- 现状需检查 `src/olav/agents/delegate_tool.py` 的实现细节

**5. Schema injection 紧凑模式 + 提到-即注入（rev 148 修订：更彻底渐进）**

`execute_sql` 已注入 `schema_catalog`（好），但每次注入全部表列定义，违反渐进式披露。修复：
- **范围控制（按需）:** 仅注入 "用户问题中**字面提到**" 或 "**上一轮查询过**" 的表 schema；其它表仅出现在 `list_tables()` 列表里
- **提供 `describe_table(name)` 工具** —— agent 想看完整 schema 时主动调
- **格式紧凑（compact 模式）:** 默认只 `(table, column, type)` 三元组；`describe_table()` 才返回 description / sample_values
- 模型档位：小模型严格按需 + compact；大模型可放宽

**为什么不完全去掉 push:** 若 agent 第一次写 SQL 完全无 schema 提示，列名错误率高，多 1-2 轮调用反而更贵。"提到-即注入"是 push/pull 平衡点。

#### 累计节省

| 项 | 单次节省 | 频次 | 累计/session |
|---|---|---|---|
| Tool docstring 瘦身 | ~1.5K | 每 turn | ~5K (3 turns) |
| 返回值压缩 | ~0.5-2K | 每次工具调用 | ~3-8K |
| AutoRecall 自适应 | ~600 | 每 turn | ~2K |
| Schema compact | ~500 | 每次 SQL | ~1.5K (3 SQL) |
| **合计** | | | **~10-15K tokens / session** |

加上 ARCH-17 节省的 10-25K，**小模型实际可用 context 翻倍**。

#### 实施优先级

- **必做（半天）:** 1.execute_cli docstring；2.api_request 默认 head-10；3.RECALL_TOP_K 按档位
- **应做（1 周）:** diff_configs summary_only；execute_sql 大结果 auto-summary 验证
- **可做（1-2 周）:** Schema compact mode；delegate_tool 返回限长

#### 验证

1. `OLAV_DEBUG_CONTEXT=1` 跑 5 turn 对话 → 总 tokens 不应单调增长（recall 自适应起作用）
2. `api_request("/api/dcim/devices/")` 返回字符数 < 2000（除非 `full=True`）
3. CI tier-2 加 token budget 断言

**与其他 issue 的关联:**
- ARCH-16 / ARCH-17：同一原则的不同落点
- ARCH-11（引用链）：返回压缩时 evidence token 必须保留

---

### ISSUE-ARCH-19: 既有 token 节省机制评估 —— 配置层未按模型档位驱动

**状态**: ✅ Closed (Round 49) — SummarizationMiddleware tier (R42) + tool_help tier-aware (R38) + SKILL.md tools_docstring_mode (R49)；local LLM prefix cache 探测为部署环境功能（非代码 gap），保留为 Future

| 原方案项 | 状态 | 证据 |
|---|---|---|
| `ModelTier` enum (SMALL/MEDIUM/LARGE) | ✅ | `src/olav/core/config.py:22-33` |
| Regex 从 model name 推断 tier | ✅ | `config.py:76-77` (`_TIER_REGEX_SMALL/MEDIUM`) |
| `TIER_DEFAULTS` "宪法级" 配置 | ✅ | `config.py:39-64`，被 ARCH-16/17/18/19 共用 |
| `test_model_tier.py` 验证 enum + per-tier defaults + 解析优先级 | ✅ | 9 tests pass |
| **SummarizationMiddleware 按 tier 动态阈值** (8B → 50%, 70B → 65%, Opus → 80%) | ✅ (Round 42) | `src/olav/agents/_deepagents_bridge.py` `compute_summarization_trigger(tier)` 返回 `("tokens", context_budget × summarization_trigger_pct)`（small=4000 / medium=20800 / large=160000），`build_summarization_middleware(model, tier=)` 新 kwarg 直接实例化 `SummarizationMiddleware` with tier-specific `trigger`；`agent.py` 过 `get_llm_config().model_tier`；TIER_DEFAULTS 扩充 `summarization_trigger_pct`；`tests/governance/test_round42_summarization_tier_threshold.py` 14 条 pin |
| `--debug-summarization` flag | 🟢 env 变量已实现 (Round 42) | `OLAV_DEBUG_SUMMARIZATION=1` → bridge emit INFO 级 log 含 tier + resolved trigger + keep。CLI `--debug-summarization` flag 未加（env 足以覆盖运维需求） |
| Local LLM prefix cache 探测 (vLLM / llama.cpp / Ollama) | ❌ | 未实现 |
| `tools_docstring_mode: compact \| full` SKILL.md 字段 | ✅ (Round 49) | `tool_help(name, detail=None, agent_id=)` — 新 `agent_id` kwarg 让 resolver 读该 agent 的 `SKILL.md::tools_docstring_mode` frontmatter（接受 `compact`/`brief`/`full`/`long`/`verbose` aliases）。Precedence explicit > SKILL.md > tier default > fallback。`_SKILL_MODE_ALIASES` + `_read_agent_docstring_mode` 暴露公开可 pin。`tests/governance/test_round49_skill_md_docstring_mode.py` 11 条 pin |

**剩余 gap**：SummarizationMiddleware tier 阈值是真正的新代码（>100 LOC），需独立一轮设计。本轮不做。
**发现日期**: 2026-04-17

**核实结论:** OLAV 已有四块 token 节省机制（SKILL whitelist / auto-discovery / SemanticCache / SummarizationMiddleware），**架构方向都对**，但 **配置层未做模型档位驱动**，对小模型实际收益打折。

#### 现状评估表

| 机制 | 设计 | 小模型实际收益 |
|---|:---:|:---:|
| SKILL.md tool whitelist | ✅ | ✅ 显著（未声明工具 docstring 不入 context） |
| Auto-discovery（entry points + filesystem） | ✅ | ✅ 中性（启动期一次性） |
| SemanticCache（命中即 0 LLM 调用） | ✅ | ✅✅ 强适配 |
| Long-term Memory store | ✅ | ✅ 中性 |
| AutoCaptureMiddleware（后台抽 fact） | ✅ | ✅ 输入端 0 影响 |
| AutoRecallMiddleware (`RECALL_TOP_K=3` 硬编码) | ⚠️ | ❌ → ARCH-18 已收紧 |
| SummarizationMiddleware（deepagents 默认阈值） | ⚠️ | **❌ 本条目** |
| AnthropicPromptCachingMiddleware | ✅* | ⚠️ 仅 Anthropic 生效；本地小模型用不上 |

#### 三个待补的配置缺口

##### A. SummarizationMiddleware 无模型档位驱动阈值

**位置:** `src/olav/agents/agent.py:476` `_summ = build_summarization_middleware(self.llm)`

**问题:**
- 一律启用 deepagents 默认阈值（约 75% of model window）
- 8K 模型在 ~6K tokens 才触发 → 已经"lost-in-the-middle"
- 触发后调用 LLM 做 summary → 小模型 summarize 质量低 → 信息丢失 → 后续 re-ask 浪费更多
- 用户对触发时机/内容不可见

**修复:**
1. 按模型档位设阈值：8B → 50% 触发；70B → 65%；Opus → 80%
2. summary 用更大的 LLM（如本地 Qwen 8B 跑业务，Sonnet 跑 summary）—— 类似 "speculative summary" 模式
3. CLI `--debug-summarization` 显示触发时机 + 压缩前后 token 数 + 被压缩的 turn 范围
4. 配置项：`summarization.threshold_by_model_tier`、`summarization.summarizer_model`（可与主 LLM 不同）

##### B. 本地小模型无 prompt cache 等价机制

**问题:** AnthropicPromptCachingMiddleware 仅对 Anthropic 模型生效。OLAV 主流场景是本地 Qwen3 / Llama 推理，每轮重发整个 system prompt + tool docstrings，**没有等价的 prefix 复用机制**。

**实际可用的本地后端机制（OLAV 未对接）:**
| 后端 | 复用机制 | 配置点 |
|---|---|---|
| vLLM | automatic prefix caching | `--enable-prefix-caching` |
| llama.cpp | `cache_prompt: true` | request 参数 |
| Ollama | `keep_alive: <duration>` | request 参数 |
| LM Studio | session 级 cache | UI |

**修复:**
1. `LocalLLMBackend` 检测后端类型，自动加上对应 cache 参数
2. system prompt 排版严格"前置稳定，后置变化"（稳定段 = AGENT.md + tool docstrings；变化段 = 用户消息 + recall hint）—— 利于 prefix matching
3. 统计指标：cache hit ratio per request；CLI `olav stats --prompt-cache`

##### C. Tool whitelist 不约束 docstring 体积

**问题:** SKILL.md `tools: [a, b, c]` 只控制"用哪些"，不控制这些工具的 docstring 多大。即便只 5 个工具，每个 100 行 docstring = 500 行入 system prompt。

**修复（与 ARCH-18 协同）:**
1. CI 加 `tests/tool_docstring_budget.py`：每个工具 docstring 字符数 < N（小模型 1000 / 中 3000 / 大 不限）
2. 提供 `tool_help(name)` 二级详情查询
3. SKILL.md 增加 `tools_docstring_mode: compact | full`（按 agent 设置）

#### 验证

1. `OLAV_DEBUG_CONTEXT=1 olav --model qwen3-8b "..."`：日志输出每段 token：static / tools / recall / history / user → 看 summary 触发时机
2. `olav stats --prompt-cache`：本地推理后端的 cache hit ratio
3. CI: `tests/tool_docstring_budget.py` 在 small-tier 配置下断言每工具 < 1000 字符

#### 与其他 issue 的关联

- ARCH-16/17/18：本条是配置层补强，与设计层互补
- ISSUE-CORE-04（tool_discovery sys.path 污染）：discovery 启动期问题，本条解决运行期 token 损耗

---

### ISSUE-ARCH-20: 边界与组件设计审计 —— v0.18 迁移未完成 + 工具大规模重复

**状态**: 🟢 Phase 1+2+3 Closed (Round 26 reconciliation) · Phase 1 在 ARCH-20 首次投入时就清了 → P1（rev 150 新增）

| Phase | 描述 | 状态 | 证据 |
|---|---|---|---|
| **Phase 1** 删 v0.17 残留 7 顶层 agent | (原 Sprint 1 #1) | ✅ Closed | `tests/governance/test_v018_compliance.py::test_removed_v017_agents_absent` 确认 audit-auditor/audit-designer/config/devops/gitea/infra 全删 |
| **Phase 2** 工具去重 (diff_configs 等 6→2) | Sprint 1 #2 | ✅ Closed (Round 26) | `tests/governance/test_tool_dedup_phase2.py` 7 条全绿；17 canonical files + 23 symlinks 无 divergence；cross-domain tools (`execute_cli`/`diff_configs`/`take_snapshot`/`search_commands`) 按 core→ops symlink 模式 |
| **Phase 3** Platform/Netops 边界修复 | Sprint 1 #3 | ✅ Closed (Round 26) | `tests/governance/test_arch20_phase3_boundary.py` 5 条全绿；`topology_engine.py` + `auto_learn.py` 已迁至 `olav-netops/src/olav_netops/core/`；legacy path `from olav.core.topology_engine` 被 `test_legacy_imports_are_broken` pin 守住（正确报 ImportError） |

**所有 Phase 完成**。
**发现日期**: 2026-04-17

**总评:** OLAV 的**架构方向合理**（platform/netops/ent 三包分离 + workspace agent 自动发现 + subagent delegation 边界），但**实施严重滞后**：v0.17 → v0.18 迁移仅完成一半，留下大量边界违规和工具副本。

#### 问题 1：v0.18 spec 与现状的鸿沟（P1）

`dev_docs/46. AGENT_BOUNDARY_v0.18.md` 明确：**3 个顶层 agent**（`core` / `ops` / `audit`），其它都应是 subagent。

**实测 `ls .olav/workspace/`（10 个顶层）:**

| 当前 | v0.18 应在 | 处置 |
|---|---|---|
| `core` | core ✅ | 保留 |
| `ops` | ops ✅ | 保留 |
| `audit` | audit ✅ | 保留 |
| `audit-auditor/` | `audit/auditor/` | **删除顶层** |
| `audit-designer/` | `audit/designer/` | **删除顶层** |
| `config/` | 不应存在（功能合并入 core） | **整体删除** |
| `devops/` | `ops/devops/` | **删除顶层**（subagent 已存在）|
| `infra/` | `ops/infra/` | **删除顶层**（subagent 已存在）|
| `ops-lab/` | `ops/lab/` | **删除顶层**（subagent 已存在）|
| `gitea/` | 不在 v0.18 spec 中 | **裁定**：保留为顶层 / 移至 ops/gitea / 删除 |

**用户层影响:** `olav --agent audit-auditor` 与 `olav --agent audit` 同时存在 → 用户困惑、路由混乱、token 浪费（同时维护两套 SKILL.md）。

#### 问题 2：工具大规模重复（P1）

实测 find 结果（仅主仓库，不含 worktree）:

| 工具 | 副本数 | 位置 |
|---|---|---|
| `diff_configs.py` | **6** | `olav-netops/.olav/workspace/ops/diff/tools/`、`olav-netops/.olav/workspace/ops/tools/`、`.olav/workspace/ops/diff/tools/`、`.olav/workspace/ops/tools/`、`.olav/workspace/core/tools/`、`.olav/workspace/config/tools/` |
| `execute_cli.py` | **4** | `olav-netops/.olav/workspace/ops/tools/`、`.olav/workspace/ops/tools/`、`.olav/workspace/core/tools/` ⚠️、`.olav/workspace/config/tools/` ⚠️ |
| `execute_sql.py` | **4** | `src/olav/data/workspace/core/db_query/tools/`（vendored）、`src/olav/data/workspace/core/tools/`（vendored）、`.olav/workspace/core/db_query/tools/`、`.olav/workspace/core/tools/` |

**根因:**
1. v0.18 重组后未清理旧位置
2. `src/olav/data/workspace/` 是包内 vendored 模板，部署时 copy 到 `.olav/workspace/` —— 双轨并存正确，但应只在 `data/workspace/` 维护
3. 跨 agent 注入工具直接复制文件而非通过 entry point 引用
4. `execute_cli` 出现在 core 与 config —— 说明历史上多次"应急加进 core"未回收

**风险:** 不同副本逐渐偏离（其中一个先被修复，其它继续 bug），ARCH-19 提到的"docstring 体积"在副本之间不一致。

#### 问题 3：Platform/Netops 边界违规（P1）

**实测违规:**

| 文件 | 当前位置 | ownership doc 规定 | 违规类型 |
|---|---|---|---|
| `src/olav/core/topology_engine.py` | platform core | **netops**（doc 06 line 87 明文） | platform 含 netops 代码 |
| `src/olav/core/auto_learn.py` | platform core | 应在 netops（TextFSM 模板生成是网络域的） | 同上 |
| `src/olav/core/config.py` 含 `domains/netops/nornir/` 兜底路径 | platform core | netops 应通过插件契约注册 | 平台知道域细节 |
| `src/olav/core/ingest_manager.py` `_backup_commands` 读 `backup_only_commands.yaml` | platform core | 该 yaml 属 netops_init | 同上 |

**为什么需要严守:** 平台/域分离是企业版架构的前提（`olav-ent` 通过 entry point 装载）。平台核心混入域知识 → 第三方域包（如未来 `olav-secops`）无法干净接入。

#### 问题 4：跨 agent 静态注入耦合（P2）

`devops/SKILL.md` 用 `static_context: path: ../infra/references/` —— 在 SKILL 层跨 agent 边界引用。

**问题:**
- 隐式依赖（删 infra 会让 devops 静默失效）
- 违反 ARCH-17（静态注入失控的源头之一）
- 反例：subagent 之间共享 reference 应通过 platform 级 `references/` 统一目录或 `load_reference()` 机制

#### 问题 5：subagent 骨架空壳（P3）

`ops/diff/`、`ops/lab/` 等只有 `prompts/` 或 `references/`，没有 `SKILL.md` + `tools/`。运行时是否真的作为 subagent 调度？还是只是 prompt 片段？

**核查:** 与 `delegate_tool.py` 配合使用情况；如果 orchestrator 从不 delegate 到这些，就是死代码。

#### 修复路径（按依赖与 ROI）

**Phase 1 — 完成 v0.18 迁移（1-2 周，P1）:**
1. 删除 7 个顶层 agent 目录（`audit-auditor`、`audit-designer`、`config`、`devops`、`infra`、`ops-lab`、`gitea`），保留 subagent 副本
2. 更新 `PLATFORM.md` 自动生成器，确保只列 3 个顶层
3. 用户兼容：`--agent audit-auditor` 自动 redirect 到 `--agent audit + delegate(auditor)`
4. CI tier-0 加断言：`ls .olav/workspace/ | grep -v '\.md$' | wc -l == 3`

**Phase 2 — 工具去重（1 周，P1）:**
1. 单一权威源 = `src/olav/data/workspace/<agent>/tools/<tool>.py`（vendored）
2. `.olav/workspace/` 是部署时 copy，不在版本控制中维护
3. 跨 agent 引用通过 entry point / 符号链接，不复制文件
4. `execute_cli` / `take_snapshot` / `diff_configs` 等 netops 工具从 `core/tools/` 移除；core 通过 `olav_delegate("ops", ...)` 间接调用

**Phase 3 — Platform/Netops 边界修复（1-2 周，P1）:**
1. `src/olav/core/topology_engine.py` → `olav-netops/src/olav_netops/core/topology.py`，平台通过 plugin 契约注册
2. `src/olav/core/auto_learn.py` 同理迁移
3. `src/olav/core/config.py` 移除 `domains/netops/` 硬编码；netops 通过 `entry_points["olav.config_paths"]` 注册
4. `_backup_commands` 走 plugin contract 而非读固定 yaml

**Phase 4 — 跨 agent 注入清理（半周，P2）:**
1. 所有 `static_context` 必须指向**自己 agent 目录内** 或 platform 统一 `references/`
2. CI 断言：`grep -r "static_context.*\.\./" .olav/workspace/` 应为空

**Phase 5 — subagent 死代码清理（半周，P3）:**
1. 审计每个 subagent 目录，如无 `SKILL.md + tools/` → 要么补全要么删除
2. 文档化"subagent 必备文件清单"

#### 验证

1. `olav refresh` 后 `PLATFORM.md` 只列 3 agent
2. `find .olav/workspace -name "execute_cli.py" | wc -l` == 1
3. `grep -r "from olav.core.topology_engine" src/olav/` 无结果（已迁移）
4. CI tier-0 加 `tests/test_v018_compliance.py` 验证以上断言

#### 与其他 issue 的关联

- ARCH-17（static_context 失控）：跨 agent 注入是其根因之一
- ARCH-18 / 19（小模型 token 优化）：工具去重 + 边界清理后，docstring 维护成本降一半
- ISSUE-CORE-04（sys.path 污染）：tool_discovery 启动期问题，与本条的"重复加载"叠加
- 企业版 `ent_batfish_analyzer`：依赖 platform/netops 边界清晰才能干净接入

---

### ISSUE-ARCH-21: v0.18 spec 自身审视 —— 收紧建议 v0.18.1

**状态**: ✅ Closed (Round 46 reconciliation) — 矩阵 9/9 全绿（A / B1 / B1.5 / B2 / C / D + services / designer / ops-lab 三条 landed 早）

| # | 收紧建议 | 当前状态 | 映射/证据 |
|---|---|---|---|
| **A** | Core "god agent" 瘦到 ≤7 跨域 tools | ✅ Closed (Round 33) | ADR-0006 declares 7 capabilities (`run_python_code / execute_sql / recall_memory / search_knowledge_lancedb / web_search / format_and_export / manage_cron`)；core/SKILL.md 21→7；`test_core_agent_has_at_most_7_tools` xfail→PASS；8 条 `test_step_e_core_capabilities.py` pin |
| **B1** | `ops/analysis + diff → ops/analyze` | ✅ Closed (Round 31) | Sprint 3 Step C 完成 — 5 tools 合并到 `ops/analyze/`，双模式（Analysis / Drift），orchestrator `task("ops-analyze", ...)` routing 统一；13 条 `test_step_c_analyze_merge.py` pin；ADR-0003 pre-authorized |
| **B1.5** | Sprint 3 Step D-后半: probe→collect (lab stays) | ✅ Closed (Round 32) | ADR-0005 supersedes ADR-0003 "probe+lab→collect"（lab 10 tools CAB workflow 保留独立）；probe 重命名 collect（name: ops-collect，execute_cli_parallel 留下）；10 条 `test_step_d_probe_rename.py` pin；`test_ops_subagents_merged_to_collect_analyze` xfail→PASS |
| **B2** | `audit/learner → audit/curator` 重命名 | ✅ Closed (Round 34) | ADR-0003 §B.2 结清；audit/AGENT.md subagents 更新；SKILL.md name: curator + replaces: learner；core/tools/tool_help.py _TOOL_ROOTS 同步；tests/unit/test_scaffold_domain_agent 路径更新；8 条 `test_b2_learner_to_curator_rename.py` pin |
| **C** | Audit/ops parity ADR 文档化 | ✅ Closed (Round 29) | [`docs/adr/0003-audit-ops-sub-agent-parity.md`](../docs/adr/0003-audit-ops-sub-agent-parity.md) — "distinct workflow count" 原则 + audit:2 / ops:4 (Sprint 3 post) / services:0 理据 |
| **D** | 新顶层 agent default=reject 扩展政策 | ✅ Closed (Round 29) | [`docs/adr/0004-new-top-level-agent-extension-policy.md`](../docs/adr/0004-new-top-level-agent-extension-policy.md) — default=reject + 4 条批准 criteria + services 作为 approved precedent |
| — | `services/` 顶层 agent | ✅ (Round 16) | `tests/governance/test_services_agent.py` 14 条 |
| — | `audit/designer → audit/auditor` 合并 | ✅ (Round 17) | `tests/governance/test_step_b_designer_merge.py` 7 条 |
| — | `ops-lab/` 顶层 shell 删除 | ✅ (Round 18) | `tests/governance/test_step_d_lite_ops_lab_removal.py` 8 条；xfail → PASS: `test_top_level_is_exactly_four_agents` + `test_ops_lab_folded_into_ops` |

**本轮剩余 Deferred 的映射**：
- A → Sprint 3 Step E：core 19 tools 涉及用户流程 / escalation 决策（4 网络 tools + 2 service lifecycle 应否留 core），需独立一轮
- B1 → Sprint 3 Step C：L 级 orthogonal 合并
- B2（learner→curator）→ 独立 audit 重构
- C/D → 文档产物，后续 `docs/adr/0003-audit-ops-parity.md` / `docs/adr/0004-new-toplevel-extension-policy.md`
**发现日期**: 2026-04-17

> **范围:** 本条评价的是 v0.18 **设计 spec 本身**，不是实施差距（实施差距见 ARCH-20）。

#### v0.18 spec 的 4 处实际问题

##### 问题 A：`core` 已沦为"上帝 agent"，14 工具职责严重越界

实际属于 netops 域却塞进 core 的 7 个工具：

| 工具 | 应迁至 |
|---|---|
| `execute_cli` | **ops** only（SSH 是 netops） |
| `take_snapshot` | **ops/probe** |
| `diff_configs` | **ops/analyze**（合并后）|
| `search_logs` | **ops**（syslog 是 netops 域） |
| `search_commands` | **ops**（CLI 命令字典） |
| `deploy_service` | **services** 新顶层 |
| `stop_service` | **services** |

core 应只剩 7 个真跨域工具：`execute_sql`、`recall_memory`、`web_search`、`format_and_export`、`run_python_code`、`api_request`、`load_reference`（+ `olav_delegate`）。

##### 问题 B：9 个 subagent 对小模型路由是负担

`ops/analysis`、`ops/probe`、`ops/diff`、`ops/lab`、`ops/devops`、`ops/infra` + `audit/designer`、`audit/auditor`、`audit/learner` = 9 个。

LLM 在 9 段 description（约 450 tokens）中分辨 `analysis` vs `diff`、`designer` vs `learner` 路由错误率高。

**合并建议:**

| 合并 → | 来源 | 理由 |
|---|---|---|
| `ops/analyze` | analysis + diff | 都是只读分析（路径/差异都是查询） |
| `audit/curator` | designer + learner | 都是 profile 创建 + 演化 |
| `ops/lab` 独立 | — | 少数"动手"场景，安全隔离 |
| `ops/probe` 独立 | — | 唯一发起 SSH 的，安全边界清晰 |
| `ops/devops` / `ops/infra` | 评估实际调用频次 | 可能可降级为 ops 共享 reference |

合并后 6 个 subagent，路由清晰。

##### 问题 C：`audit` 与 `ops` 平级是否合理？

**保留平级的理由:** audit 产物（合规报告）面向**不同用户群**（合规官/管理层 vs 工程师）。

**风险:** 没有在 spec 里**显式说明**这个决策依据。建议补 ADR（Architecture Decision Record），未来回头才知道为何如此分。

##### 问题 D：spec 缺多域扩展路径声明

v0.18 没回答：`olav-secops` / `olav-cloudops` 怎么接入？

| 路径 | 影响 |
|---|---|
| **平铺顶层**（当前默认）| 第三方域包都是顶层 agent → 走向 v0.17 "10 个 agent" 老路 |
| **域作为子分类** | 顶层稳定（core/domain/audit），扩展靠 `domain/netops/` 子目录 |

**必须在 spec 里显式声明**采用哪种路径 + 新增顶层 agent 需 spec 评审，防失控。

#### 推荐：v0.18.1 收紧版（最小改动）

```
.olav/workspace/
├── core/                        ← 7 跨域工具
│   ├── tools/                   execute_sql, recall_memory, web_search,
│   │                            format_and_export, run_python_code,
│   │                            api_request, load_reference, olav_delegate
│   └── writer/                  subagent: 文档/报告
│
├── ops/                         ← netops 域（含原 search_logs/commands）
│   ├── tools/                   execute_cli, take_snapshot, search_logs,
│   │                            search_commands  (4 共享工具)
│   ├── analyze/                 ← 合并 analysis + diff
│   ├── lab/                     ← 保留（隔离场景）
│   └── probe/                   ← 保留（唯一 SSH 发起方）
│
├── audit/                       ← 合规域（保留平级，文档化原因）
│   ├── auditor/                 profile 执行
│   └── curator/                 ← 合并 designer + learner
│
└── services/                    ← 新增：服务生命周期
    └── tools/                   deploy_service, stop_service, register_service
```

#### 收益对比

| 指标 | v0.18 | v0.18.1（推荐） |
|---|---|---|
| Core 工具数 | 14（god agent） | 7（真跨域） |
| Subagent 数 | 9 | 6 |
| Core 启动 token | ~5K（含 netops 工具 docstring） | ~2.5K |
| 边界违规 | execute_cli 在 core 等 7 处 | 0 |
| 服务生命周期家 | 散落在 core | 独立 services agent |
| 多域扩展路径 | 未声明 | 显式声明（默认拒绝新顶层）|

#### 实施依赖

- 必须先完成 ARCH-20 Phase 1（删除 v0.17 残留 7 顶层）才能 talk about v0.18.1
- ARCH-20 Phase 2（工具去重）后单一权威源就绪
- 然后做 ARCH-21 重组：移工具 + 合并 subagent + 新增 services agent

#### 验证

1. 小模型 8B 跑 ops 路由测试：5 个典型问题路由准确率 v0.18 vs v0.18.1
2. core 启动 token 实测从 5K → 2.5K
3. 单工具职责唯一：每个工具只在一个 SKILL.md 的 `tools:` 出现

#### 与其他 issue 关联

- ARCH-17（static_context 失控）：core 瘦身后压力减半
- ARCH-18 / 19（tool docstring 预算）：subagent 合并后 docstring 体积自然降
- ARCH-20（v0.18 实施债）：本条是其完成后的下一步

---

### ISSUE-ARCH-22: 垃圾代码 / 历史债 / 硬编码 盘点

**状态**: ✅ Closed for platform scope (Round 66) — 9 Closed + 3 Fixed + 1 Warning-only (C5)。Round 66 sweep closed A2 (v0.19 cut + audit_logger.py delete) + A4 (reclassified — `_legacy_archived/` gitignored so never ships; local disk cleanup is operator concern) + C2 (DEDUP_STRATEGY module constant + OLAV_DEDUP_STRATEGY env override)。**0 Deferred, 0 Externally-blocked** — ARCH-22 fully closed for platform scope.
**发现日期**: 2026-04-17

> 本条只报 ARCH-20/21/CORE/NETOPS 未覆盖的新问题。已核实到文件级。

#### Round 19 核对后状态矩阵（rev 159）

| 子项 | 处置 | 证据 |
|---|---|---|
| **A1** `.disabled` 清理 | ✅ Closed | `tests/governance/test_disabled_cleanup.py` 钉 refresh sweep；`test_arch22_round19_batch::test_a1_no_disabled_files_under_workspace` 钉 deployment 无残留 |
| **A2** 删 `audit_logger.py` | ✅ Closed (Round 66) — pyproject 0.18.0→0.19.0；`src/olav/core/audit_logger.py` 删；`tests/unit/test_audit_logger_noop.py` 删；`test_a2_audit_logger_has_legacy_remove_marker` → `test_a2_audit_logger_deleted`（断言反转：文件必须不存在）；`test_a2_current_version_is_still_pre_019` → `test_a2_version_at_or_past_v019`（版本 floor 立于 0.19 防回退）|
| **A4** 移出 `_legacy_archived/`（180MB） | ✅ Closed (Round 66, reclassified) — `.gitignore:98` 早已 exclude 该目录，clone/wheel/release artefact 均不带；180MB 本地磁盘占用是 operator 清理 concern，非 platform governance 问题。`test_a4_legacy_archive_still_present` 改断言 `.gitignore` 包含该条目 |
| **B** `COMPATIBILITY_CUTOFF` → v0.15 | ✅ Closed | `src/olav/core/version.py` 已到 `0.15.0`；`v0_12_users.py` DDL 提升到 `core.auth.schema`，`v0_13_rbac.py` 删；`tests/governance/test_compat_cutoff.py` 钉 |
| **C1** `backup_only_commands.yaml` 进 config | ✅ Fixed (Round 46) | `src/olav/core/utils.py:_BACKUP_COMMANDS_PATH_ENV = "OLAV_BACKUP_COMMANDS_PATH"` env override 插在 `find_backup_commands_yaml` 查找优先级 0（三个默认候选之前），env 指向不存在的文件时 fall through 到默认（防 typo 静默禁用）；`tests/governance/test_round46_reconcile_and_backup_env.py` 6 条 pin |
| **C2** `DEDUP_STRATEGY` 常量 | ✅ Closed (Round 66) — `src/olav/enterprise/audit_dataset_export.py` 加 `DEDUP_STRATEGY = os.environ.get("OLAV_DEDUP_STRATEGY", "exact_v1")` 模块级常量；2 个 call site 由 literal 改为引用常量；`test_c2_dedup_strategy_still_in_enterprise_module` 加 `DEDUP_STRATEGY =` + `OLAV_DEDUP_STRATEGY` + 禁止 call-site literal 三条断言 |
| **C3** `DEFAULT_WEB_PORT` env override | ✅ Fixed (Round 19) | `src/olav/core/defaults.py:22` `_env_int` helper；`DEFAULT_WEB_PORT = _env_int("OLAV_WEB_PORT", 2280)`；5 条 pin tests |
| **C4** 示例 IP `192.168.1.10` → `server1.example.com` | ✅ Fixed (Round 19) | canonical `src/olav/data/workspace/core/remote/tools/remote_execute.py:36` + deployment `.olav/workspace/core/remote/tools/remote_execute.py:36` 同步；`test_c4_*` × 3 |
| **C5** memory `store` 参数 deprecation | 🟢 Warning landed (Round 50) — param 删除仍留 next major | `src/olav/core/memory/__init__.py::SemanticCache.__init__` 对 `store=non-None` 和 `table_name != "query_cache"` 发 `DeprecationWarning`（message 含 "next major release" 指引），runtime 行为不变（仍是 no-op）；`tests/governance/test_round50_semantic_cache_deprecation.py` 8 条 pin 守护默认构造静默 + 显式默认 table_name 静默 + store/table_name warning emit + 二者一起时 both emit + ARCH-22 C5 tag 在源码里；参数物理删除等 next major cut |
| **D** 27 处 legacy 逐条标签 | ✅ Closed (Round 20 reconciliation) — grep 实测 33 hits：14 已显式 `LEGACY-{KEEP,REMOVE-v0.19,UNCHECKED}` + 19 语义合法使用（Sphinx `.. deprecated::`、用户面向错误字串、CLI help、docstring 描述 legacy 行为）。0 条遗漏 tech debt。pin：`tests/governance/test_claude_md_consistency.py::test_arch22_d_no_untagged_legacy_tech_debt` |
| **E** `_deepagents_bridge.py` helper 重构 | ⏸️ Deferred — 代码重构 50→10 行，独立轮次 |
| **F** TODO/FIXME 审计 | ✅ Closed | grep 0 hit 已确认（rev 154 已写明），无需再查 |

**Round 19 统计**：12 个子项 → 4 Closed / 2 Fixed / 6 Deferred（均附延后原因）。

**Round 20 更新**：D 关 → 5 Closed / 2 Fixed / 5 Deferred（A2/A4/C1/C2/C5/E）。

**Round 30 更新**：E 关 → 6 Closed / 2 Fixed / 4 Deferred（A2/A4/C1/C2/C5）。

**Round 46 更新**：C1 关 → 6 Closed / 3 Fixed / 3 Deferred（A2/A4/C2/C5）。

**Round 50 更新**：C5 半关（DeprecationWarning landed，物理删除留 next major）→ 6 Closed / 3 Fixed / 2 Deferred（A2/A4/C2）+ 1 Warning-only（C5）。

**Round 66 更新**：A2 + C2 双关 + A4 重分类 → 9 Closed / 3 Fixed / 0 Deferred + 1 Warning-only（C5）。pyproject 0.18.0 → 0.19.0 的 v0.19 cut 完成 audit_logger.py 删除。ARCH-22 platform scope 完全清零。

#### A. 死代码（可直接删）

| 项 | 位置 | 处置 |
|---|---|---|
| `run_python_code.py.disabled` (148 行) | `.olav/workspace/core/run_python_code.py.disabled` | **部署副本仍在**；vendored 源已由 commit c6ac568 删除。修复：`olav refresh` 加上部署目录 `.disabled` 清理步骤 |
| `sync_tools.py.disabled` (61 行) | `.olav/workspace/core/sync_tools.py.disabled` | 同上 |
| `audit_logger.py` 全模块 | `src/olav/core/audit_logger.py` | **验证确实零 importer** (`grep -rn "from olav.core.audit_logger\|import audit_logger" src/olav/ \| grep -v __pycache__` → 0 结果)。 已被 `audit_recorder.AuditEventRecorder` 取代。可删 |
| `_legacy_archived/` 顶层目录 | `/home/yhvh/Olav/_legacy_archived/` | **180MB**，含 archive_v0_to_v0.11、hermes-agent、memory-lancedb-pro、session.py.legacy 等。**移出主仓库**（独立归档仓库 / git tag），不随克隆拉下来 |

**验证:** 删除上述后 `pytest` 全量通过 + 主仓库 clone 大小减 ~180MB。

**rev 155 核实更新:** commit c6ac568 已经清理了 `src/olav/data/workspace/` vendored 副本，但用户的 deployment 目录 `.olav/workspace/` 仍有残留。修复需加入 `olav refresh` 或安装时的清理步骤。

#### B. 版本兼容性债（cutoff 严重滞后）

**`src/olav/core/version.py:273`** —— `COMPATIBILITY_CUTOFF = "0.9.0"`
- 当前版本 `v0.18.0`（pyproject.toml）
- **cutoff 滞后 9 个小版本**
- 保留的影响：
  - `src/olav/core/migrations/v0_12_users.py`、`v0_13_rbac.py`、`v0_14_sessions.py` 三个 migration 必须永久维护
  - `compare_versions` / 兼容分支判断保留 0.9–0.11 死路径
  - CHANGELOG "breaking change" 列表积累

**修复:**
1. `COMPATIBILITY_CUTOFF` 滚动升到 `v0.15.0`（保留最近 3 个版本）
2. 删除 `migrations/v0_12_users.py`、`v0_13_rbac.py`（0.15 之前的 migration 不再支持从源版本升级）
3. `v0_14_sessions.py` 评估保留（视上线时间）
4. 加规则：每发布一个新 minor 版本，cutoff 跟进 —— CI 加断言 `current - cutoff <= 3 minor`

#### C. 硬编码 / 分散的配置常量

| 硬编码 | 位置 | 建议 |
|---|---|---|
| `backup_only_commands.yaml` 三级 fallback 路径 | `src/olav/core/ingest_manager.py:43-51` | 提取到 `config.py` `BACKUP_COMMANDS_PATH` + env override `OLAV_BACKUP_COMMANDS_PATH` |
| 去重策略字符串 `"exact_v1"` | `src/olav/enterprise/audit_dataset_export.py:1347, 1405` | 模块级常量 `DEDUP_STRATEGY = "exact_v1"` + 允许 config 覆盖 |
| 默认 web port `2280` | `src/olav/core/defaults.py:95` | 已有 default 定义是合理的（常量文件就是干这个的），但应支持 env `OLAV_WEB_PORT` 覆盖 |
| 示例中的 IP `192.168.1.10` | `src/olav/data/workspace/core/remote/tools/remote_execute.py:36`（docstring） | 改为 `server1.example.com` 避免误导 |
| `memory/__init__.py:744` `store` 参数注释 "kept for API compatibility, no longer used" | 同文件 | 下一个 major 移除；CHANGELOG 标 breaking |

#### D. Legacy references (27 处 "legacy/deprecated")

`grep -rn "legacy\|LEGACY\|deprecated\|DEPRECATED" src/olav/ | grep -v __pycache__ | grep -v legacy_archived` → 27 处。

**审计建议:** 逐条审视 —— 对每处打标签：
- `LEGACY-REMOVE-v0.19`: 下个版本删
- `LEGACY-KEEP`: 永久保留（如 `migrate_kb_chunks` 旧用户迁移工具）
- `LEGACY-UNCHECKED`: 待决策

grep + 手工标注，耗时 < 半天，收益：清晰退路。

#### E. `_deepagents_bridge.py` 重复 ImportError 处理块

**状态**: ✅ Closed (Round 30) — `src/olav/agents/_deepagents_bridge.py:82-` 已重构。

原 4 块 `if HAS_X: try/except: None; else: None` 用 `_safe_imports()` helper 统一替代：
- Helper 支持 `(module_path, attr, alias | None)` 三元组 + `enabled` gate
- "All or nothing" 原子语义保留（任一导入失败 → 所有名字 = None）
- Block D (AnthropicPromptCachingMiddleware) 的 `HAS_PROMPT_CACHING = False` demote 副作用保留
- 未来新增 deepagents feature 加 3 行而非 10 行

Smoke 验证：`HAS_SUMMARIZATION/PROMPT_CACHING/LOCAL_SHELL_BACKEND/ASYNC_SUBAGENTS` 全 True，4 个导出类正常。整套 governance suite 全绿（任何 import 回归都会级联 fail）。

#### F. TODO/FIXME 审计

`grep -rn "TODO\|FIXME\|XXX\|HACK" src/olav/ | grep -v __pycache__` → **0 结果**。

**评价:** ✅ 代码里没有遗留 TODO/FIXME —— 这是相当干净的信号。债务主要在结构层（上述 A-E），不在代码内注释里。

#### 修复优先级

**P1（立刻）:**
- A1: 删 2 个 `.disabled` 文件
- A2: 验证 `audit_logger.py` 零 importer 后删除
- A4: `_legacy_archived/` 移出主仓库（独立归档仓）

**P2（Sprint 2 内 / 配合其他修复）:**
- B: `COMPATIBILITY_CUTOFF` 升到 v0.15
- C1: `backup_only_commands.yaml` 路径进 config（配合 ISSUE-ARCH-20 Phase 3）
- E: `_deepagents_bridge.py` 重构（技术债清理）

**P3（可选 / 背景工作）:**
- C3/C4/C5: 小硬编码散件
- D: legacy 27 处逐条标注

#### 验证

1. `pytest` 全量在 A1-A2 删除后通过
2. `du -sh /home/yhvh/Olav | head -1` 在 A4 之后显著缩小
3. CI 加 `tests/test_no_disabled_files.py`：`find .olav -name "*.disabled" \| wc -l == 0`
4. CI 加 `tests/test_compat_cutoff.py`：`__version__ - COMPATIBILITY_CUTOFF <= 3 minor`

#### 关联

- ARCH-20 Phase 2（工具去重）：`execute_sql.py` 多副本在 ARCH-20 已覆盖
- ARCH-21 v0.18.1：core 瘦身时顺手删 audit_logger.py 类死代码
- ISSUE-CORE-02（bare except）：`_deepagents_bridge.py` E 重构时顺手加 log

---

### ISSUE-ARCH-23: Core 5-subagent MVC 重构 — T0 A18 FAIL

**状态**: ✅ Closed (Round 64, 2026-04-19)

**Round 64 收尾动作**（packaged installer source = `src/olav/data/workspace/core/`）:

| 工具 | 移出 `core/tools/` → | 原 Round 触发修改 |
|---|---|---|
| `describe_table.py` | `db_query/tools/` | R39 (ARCH-18) |
| `search_logs.py` | `admin/tools/` | R41 (ARCH-16) |
| `get_static_context.py` | `admin/tools/` | R24 (ARCH-17) |
| `load_reference.py` | `admin/tools/` | R38 |
| `tool_help.py` | `admin/tools/` | R38 + R49 |

Orchestrator 留 **3** 个真跨域 @tool：`execute_sql` / `recall_memory` / `web_search`。
SKILL.md `tools:` 白名单同步扩展 (`admin/SKILL.md` +4、`db_query/SKILL.md` +1)。
Installer-source governance pin (`test_installer_source_has_modified_tools.py`) 改为 per-tool 新路径映射；ARCH-22 D allowlist 随 `tool_help.py` 迁移更新 (`tests/governance/test_claude_md_consistency.py`)。

Deployment (`.olav/workspace/core/`) 同步：`AGENT.md` frontmatter `subagents:` 5 条，`writer/SKILL.md` + `writer/references/` 从 packaged 补齐（deployment writer/ 原只有 AGENT.md）。Deployment `core/tools/` 的旧 monolithic 副本保留未强拆，下一轮 hygiene 处理。

**证据（Round 64）**:
- `bash tests/ci/tier0_packaging.sh` → **32/32 PASS, 0 warnings** (prev 30/32；A18 + B11 双绿)
- `uv run pytest tests/governance/` → **677 passed, 3 skipped, 0 failed**
- `uv run python scripts/scan_ownership.py --summary` → 0 ERROR (24430 paths)

**关联 T0 B11 修复（顺手）**: `src/olav/core/config.py::_resolve_nornir_config_path()` 加优先级 0 `collect/config/nornir/config.yaml`（R32 重命名的新规范路径，此前只能 fallback 到 `probe/`）；`.olav/workspace/ops/collect/config/nornir/config.yaml` + `olav-netops/...` 两份 `config.yaml` 的 `host_file/group_file/defaults_file` 由 `probe/` → `collect/`；`netops_init/run.py:192` 错误提示同步。

---

### ISSUE-ARCH-23-HISTORY: （下为历史记录，保留溯源）

**状态**: 🟡 Partially Implemented (scaffold 完成，tool-migration + frontmatter 未 ship)

**发现轮次**: Round 63 (2026-04-19，T0 packaging 跑 FAIL 时暴露)

**背景**：`186db90 feat: writer subagent — MVC separation` 引入 T0 A18 断言：core orchestrator 工具必须恰好 3 个（`run_python_code` / `execute_sql` / `recall_memory`），其余迁到 5 个 subagents（`writer` / `admin` / `api_query` / `db_query` / `remote`），`core/AGENT.md` frontmatter 声明 `subagents: [...]`。commit 当时 T0 32/32 全绿。

**当前现状**（实测 rev 197 之后）:

| 检查点 | 期望 | 实测 |
|---|---|---|
| `.olav/workspace/core/tools/*.py` 数量 | 3 | **20** |
| `.olav/workspace/core/AGENT.md` frontmatter `subagents:` 键 | 长度 5 | **缺失** |
| `.olav/workspace/core/{writer,admin,api_query,db_query,remote}/` 目录 | 存在 + 每个带 SKILL.md + tools/ | ✅ 存在（但 4 个新目录全部 git untracked） |

**证据**：

```
git status --short | grep "^\?\? .olav/workspace/core/"
?? .olav/workspace/core/admin/
?? .olav/workspace/core/api_query/
?? .olav/workspace/core/db_query/
?? .olav/workspace/core/remote/
```

`src/olav/data/workspace/core/{admin,remote}/tools/*.py` 有 `M ` 修改未提交。

**完成清单（给下一轮）**：

1. 把 `.olav/workspace/core/tools/` 里的 17 个非-orchestrator 工具按语义搬家：
   - `format_and_export.py` → `core/writer/tools/`
   - `deploy_service.py`, `manage_cron.py`, `analyze_logs.py` → `core/admin/tools/`
   - `execute_sql.py` 保留在 `core/tools/`（orchestrator 用）
   - `api_request.py` → `core/api_query/tools/`
   - `describe_table.py`, `recall_memory.py`（保留在 `core/tools/`，orchestrator 用）→ `core/db_query/tools/` 或 `core/tools/`
   - `run_shell.py`, `run_python_code.py`（保留 orchestrator）→ `core/remote/tools/`
2. `core/AGENT.md` frontmatter 追加：
   ```yaml
   subagents:
     - path: ./writer/SKILL.md
     - path: ./admin/SKILL.md
     - path: ./api_query/SKILL.md
     - path: ./db_query/SKILL.md
     - path: ./remote/SKILL.md
   ```
3. `git add .olav/workspace/core/{admin,api_query,db_query,remote}/` 提交 scaffold + tools
4. 同步 `src/olav/data/workspace/core/` (installer source)
5. 跑 T0：A18 应 PASS

**为什么本轮没做**：需 ADR 批准工具迁移语义变化（capability scoping 改动），不在 auto 模式 cleanup scope 内。已在 T0 `tests/ci/tier0_packaging.sh` B11 修好 `probe/`→`collect/` 路径笔误（独立 trivial fix）。

**Pin**: T0 A18（`tests/ci/tier0_packaging.sh:178-199`）已存在 — FAIL = 明确信号。重构完成后自然转绿。

**影响范围**：T0 Phase A 1 FAIL。T1/T2/T3/T4 全不受影响（因为这些测试调用 `core` agent 时 tool discovery 是按目录 glob，不按 frontmatter subagents 列表）。

---

### ISSUE-ARCH-24: L3 Topology Self-Learning ETL（LLM-frozen，CDP/LLDP-exemplar-driven）

**状态**: ✅ Closed — Superseded + Deleted (Round 70, 2026-04-22) —— ARCH-28 views + ARCH-29 skill 完全替代；~1240 LOC + 5 frozen artifacts + 2 DDL 已删除。参见 `git log --grep=ARCH-24` 看历史
**原发现日期**: 2026-04-21
**发现日期**: 2026-04-21（Chapter 2 demo 暴露 BGP/OSPF 无 SQL 持久化）
**依赖**: ARCH-06 reopen（P2 runtime ETL）+ AUTO-LEARN-PERF（--skip-auto-learn 解耦 LLM 路径）

**设计**：采用两层 crystal-line：
- **硬编码层**：`protocol_keywords.py` 关键字注册表（BGP / OSPF / ISIS × {cisco_ios, juniper_junos, arista_eos}）+ `etl_contract.py` 函数签名契约 `extract(parsed_rows, l2_graph, vendor) -> list[EdgeRow]`
- **LLM-frozen 层**：每 `(protocol, vendor)` 对，首次运行时 sandbox 内由 LLM 以 `topology_engine.py` CDP/LLDP ETL 为 few-shot exemplar 生成 Python 提取器 → AST 白名单过滤 → sandbox 执行 → 对比 NetworkX oracle (±20%) → 通过则冻结到 `.olav/templates/etl/<protocol>_<vendor>.py`。后续 snapshot 直接 `importlib` 加载，无 LLM 调用

**新建模块** (`olav-netops/src/olav_netops/core/`):
| 文件 | 职责 |
|---|---|
| `protocol_keywords.py` | `L3_PROTOCOL_REGISTRY` + `iter_applicable(con, snapshot_id)` |
| `etl_contract.py` | `ETL_CONTRACT_VERSION`, `validate_rows`, `dry_run_gate`, header-stamp 格式 |
| `etl_registry.py` | `load_etl` / `save_etl` / `invalidate`；version + sha256 check |
| `etl_discovery.py` | LLM ReAct loop（mirrors `auto_learn.py:307-417`），AST allowlist，sandbox-validate |
| `l3_topology_etl.py` | Stage 3.6 runner；iterate keywords → load-or-generate → UPSERT + 打印 oracle-diff |

**新建 DDL** (`olav-netops/src/olav_netops/core/tables.py`):
- `netops.bgp_sessions` (device_name, neighbor_ip, neighbor_as, local_as, router_id, state, uptime, snapshot_id, last_seen) — PK (device_name, neighbor_ip, snapshot_id)
- `netops.ospf_adjacencies` (device_name, neighbor_id, neighbor_ip, interface, area, state, dead_time, snapshot_id, last_seen) — PK (device_name, neighbor_id, interface, snapshot_id)

**集成点**：`.olav/workspace/ops/netops_init/run.py` 第 536 行后（L2 topology ETL 之后）插入 Stage 3.6 调用。

**验收**：cold-start `--resume <snap> --skip-auto-learn` 应产出 bgp_sessions ≥5 / ospf_adjacencies ≥1；warm-run 二次跑 Stage 3.6 <2s 且 0 LLM call；`.olav/templates/etl/{bgp_cisco_ios.py, ospf_juniper_junos.py}` 冻结上盘。

**Out of scope**：并行化 auto_learn；ISIS/LDP/MP-BGP 扩展；`network_model.py` L3 builder 弃用 parsed_outputs 走新表（保留为下轮 migration）。

---

### ISSUE-R73-SCHEMA-DRIFT: execute_cli / search_commands 查旧 schema(Critical 回归)

**状态**: ✅ Closed (同轮修复,R73 审计发现)
**发现日期**: 2026-04-23(R74 后 audit 通过 Agent+Explore)

**症状**: R73 把 commands 表迁 `commands` → `netops.commands`,列 `command_name` → `command`,删 `allowed` 列。3 个 ops tools 仍查旧 schema → 每次调用 fail open `Catalog Error: Table with name commands does not exist`,**白名单完全失效**。

**影响**: `execute_cli` 对任何命令都允许(包括 `reload`、`ping`),黑名单机制形同虚设。`search_commands` 工具返回空列表或报错。实际 demo 环境由于 fail-open 行为没阻断,但安全边界破了。

**修复**:3 文件同步到新 schema:
- `.olav/workspace/ops/tools/execute_cli.py:53-121` — `_validate_command` 改查 `netops.commands`,列 `command` / `blacklisted` / `pipe_allowed`
- `.olav/workspace/ops/tools/execute_cli_parallel.py:38-76` — 同上
- `.olav/workspace/ops/tools/search_commands.py:82-113` — `_search_commands_in_db` 改查新表,`has_template` 从 `parser_type IS NOT NULL` 推导,`category` 字段废弃

**验证**:demo7 实测 `show version` → allowed,`nonsense command xyz` → warned(不在 registry),`show ip bgp summary` → allowed。search_commands 返回 5 个 ospf 命令正常。

**教训**: schema 变更应该同步 grep 所有 `FROM commands` / `command_name` 引用,R73 commit 时漏了这 3 个文件。加入 pre-merge check:新表名替换旧名时全仓 grep 验证。

---

### ISSUE-R73-DEAD-CODE: command_registry.py 孤儿 + backup_only_commands.yaml 冗余(Low)

**状态**: ✅ Closed(R75 实施完成)

**现象**:
- `olav_netops/command_registry.py`:`_load_whitelist` / `_load_blacklist` / `_load_platform_commands` 三个数据路径 R73 后无读者。R73 引入的 `sync_commands` 做了**全部**这些事
- 只剩 2 个 entry-point wrappers 还被调:
  - `olav.reload_hooks.netops = "olav_netops.command_registry:reload_hook"` → 调 `CommandRegistry.reload()`(重读孤儿数据)
  - `olav.config_commands.netops = "olav_netops.command_registry:get_config_commands"` → 读老 `backup_only_commands.yaml`
- `backup_only_commands.yaml`(ARCH-22 C era,schema `[{command, type, platforms}]`)和 R73 新 `user_commands.yaml`(schema `{commands: {platform: [cmd]}}`)内容重叠,两个文件管同一件事

**修复(R75 shipped)**:
1. ✅ `reload_hook()` 改调 `commands_sync.sync_commands(conn)`;entry-point 签名不变
2. ✅ `get_config_commands()` 改读 `netops.commands WHERE backup_only=true`
3. ✅ 删 `CommandRegistry` class 全部(`_load_whitelist` / `_load_blacklist` / `_load_platform_commands` / `reload` / `get_platform_commands` / `get_backup_only_commands` / `_scan_templates` / `_load_templates` / `_load_ntc_templates`)
4. 🟡 `backup_only_commands.yaml` 保留(被 `olav.core.ingest_manager.find_backup_commands_yaml` 用 — 属 platform 层,本轮不动 netops 外代码);迁移到 `netops.commands` 的全链路由 ARCH-22 C 后续轮次做
5. 🟡 `find_backup_commands_yaml()` 保留(同 4)

**实际量**:`command_registry.py` 420 LOC → **94 LOC**(-77%);删 `CommandRegistry` 单例 + 7 个方法 + 4 个缓存 dict 属性。

**测试**:
- `test_command_registry.py` 重写:从 7 个类(Singleton/ModuleSideEffects/TemplateLoadingPriority/Whitelist/Blacklist/Reload/GetConfigCommands)压缩到 3 个(ReloadHook / GetConfigCommands / ModuleSurface),10 个用例全绿
- `test_gate_additional_claims.py::commands_db` fixture 更新到 R73/R75 schema(`netops.commands` 9 列,不再是 bare `commands` 5 列)
- `test_cli_builtin.py::test_device_short_flag_accepted` 更新到 R71c 直调路径(stub `take_snapshot._run_one` + `learn_commands`)

**端到端验证**(demo7 fresh venv,本地 wheel):
```
olav.reload_hooks.netops → sync_commands → {ntc:962, user:17, total:979, seeded:0}
olav.config_commands.netops → 10 backup-only commands via DB query
netops test suite: 333 passed / 53 skipped / 0 failed
```

**跨仓 follow-up(R76 shipped)**:
- ✅ `src/olav/core/ingest_manager.py::_load_backup_commands` 改为优先读 `netops.commands WHERE backup_only=true`,YAML 仅 fallback(pre-R73 部署兼容)
- 🟡 `src/olav/core/utils.py::find_backup_commands_yaml` 保留(governance pins + fallback path)
- 🟡 `backup_only_commands.yaml` 保留一版(零部署破坏)。下一个 major release 可删

---

### ISSUE-LEARNER-BATCH-CUT: 砍掉 netops_init 自动学习;改为 coverage 报告 + 用户按需 /learn_cmd

**状态**: ✅ Closed (R72 commit ship — `auto_learn.py` 删除已验证;netops_init Stage 3.5 改为 parse coverage classifier;`/learn_cmd` interactive 入口保留)
**发现日期**: 2026-04-22(R71 实测后架构反思)
**依赖**: LEARNER-ARCH(skill unification 已完成);INGEST-NORMALIZATION(并行推进)

**背景**: R71 把 batch learner 统一到 `command_learner` skill,速度从 20min+ → 3.5min。但**用户指出真正的问题不在速度**:

> 不同型号设备同一命令输出不同 → frozen parser 模型本质脆弱 → auto-learn 对长尾命令是 false confidence
> LLM 本来就能查 DB + raw 直接回答 → 批量学 everything 是工程师的野心不是用户的需求

**核心洞察**:
1. **ntc-templates 已覆盖 ~70% 主流命令**;剩下 30% 大多是 long-tail,用户未必查
2. **学习长尾 parser 非常脆**:一个 OS 版本变了就挂,frozen 假稳定感有害
3. **用户最清楚自己要什么**:"我只要 BGP+OSPF+拓扑"是主流需求
4. **Agent 路径天然支持 raw fallback**:`SELECT raw_output FROM raw_output_store` + LLM 抽字段,rare query 性价比高
5. **sim/lab 的关键 4-5 个核心 parser** 用户可以**按需**跑 `/learn_cmd`,一次性成本 ~2min

**架构修正(R72)**:

```
Before (R71)                           After (R72)
─────────────────                     ─────────────────
Stage 3.5: command_learner (3.5min)    Stage 3.5: parse coverage report (<5s)
  强制对 31 命令学 parser                 分类 parsed / raw-only / unsupported
  经常失败 8/11 → 用户不知道怎么办       明确告知要不要 /learn_cmd
  LLM cost 入库阶段                       zero LLM in ingest pipeline
                                       
                                       用户按需:
                                         /learn_cmd "show bgp summary" -d R2
                                         (4-5 个核心命令即可,其余 raw+LLM)
```

**具体改动**:
- `netops_init/run.py` Stage 3.5 **删除** `learn_commands()` 调用
- 替换为**parse coverage classifier**:
  - parsed via ntc-templates / custom textfsm / frozen PaC → ✓
  - raw exists but no parser worked → ⚠ raw-only
  - raw matches `% Invalid input` / `Unknown command` / `Permission denied` → ✗ device-unsupported
- **持久化** `.olav/config/unsupported.json`:`{(device, command): reason}`
- 下次 `/netops_init` **自动排除** unsupported pairs(节省 SSH 时间 + 避免再报错)
- **用户提示**:Stage 4 summary 后打印建议 `/learn_cmd` 列表(仅对 sim/lab 核心的 BGP/OSPF)
- `command_learner` skill **保留**(`/learn_cmd` 直调入口仍有效,单元测试不变)

**Agent raw fallback 策略**(`db_query/SKILL.md` + core system.md 新增):
```
When queried about device data:
  1. First try: SELECT from netops.v_<concept>_auto view
  2. If empty: SELECT parsed_data FROM netops.parsed_outputs
  3. If parsed_data NULL: SELECT raw_output FROM netops.raw_output_store
     Read raw text, extract answer via reasoning
```

无需新 tool,靠 prompt 教 agent 兜底。

**对 sim/lab 的影响**:
- sim `NetworkModel` 需要 BGP/OSPF 结构化数据(纯 Python,不能 LLM-at-query)
- 若 ntc-templates 解不出用户设备的 BGP → 视图空 → sim 缺数据
- **解决**:`/netops_init` 末尾提示用户手动 `/learn_cmd` 这 4-5 个核心命令(首次 ~2min,之后 frozen 命中)
- lab 配置 deploy 不依赖 parser,不影响

**收益**:
- `/netops_init` 首装 3.5min(R71)→ **~60s**(R72)
- 用户控制 LLM 开销时机
- 诚实架构:框架不再假装"学会一切"
- 多型号设备场景:新 model 入网不会把现有 frozen parser 搞坏(因为它根本不在 pipeline 里自动学)
- rare query 走 raw+LLM 天然鲁棒

**保留(R71 成果不浪费)**:
- `command_learner` skill 全部 tools 和 API
- `/learn_cmd` 直调路径
- 失败缓存 7 天 TTL
- 44 个单元测试
- learn_commands() API 仍是 sim-critical 命令的学习渠道

**验证(R72e)**:fresh `~/olav-demo6` venv E2E:
- V1: `/netops_init` 完成时间 ≤ 90s
- V2: parse coverage 报告出现,列出 raw-only + unsupported
- V3: `v_l2_links_auto` ≥ 8(CDP/LLDP ntc 能解);`v_ospf_neighbors_auto` 取决于 ntc 能不能解该厂商
- V4: 用户跑 `/learn_cmd "show bgp summary" -d R2` 后,`v_bgp_neighbors_auto` 出现 ≥ 4 行
- V5: agent raw fallback 能回答 ad-hoc 问题(如"R1 的 running-config 里 BGP 配置")
- V6: unsupported.json 自动维护,下次 run 跳过

---

### ISSUE-INGEST-NORMALIZATION: 解析入库时就用 netutils 标准化,不推到视图层

**状态**: ✅ Closed (R72 ship — `olav_netops.tools.field_normalizer.normalize_fields` 集成进 `textfsm_parse.py:238`,parsed_outputs 入库前规范化;view_builder CASE 简化已合入 R83)
**发现日期**: 2026-04-22
**关联**: LEARNER-BATCH-CUT(同轮实施)

**问题**: 当前架构中字段归一化发生在**消费端**(topology 视图 SQL CASE、ARCH-26 observed_canonical 早年设计)。这有两个坏处:

1. **多次重复归一化**:同一字段被 Device ETL、topology ETL、view_builder 各归一一次
2. **归一逻辑散落**:SQL CASE / Python 代码 / prompt 混在多处,新增字段要改多处
3. **原始 JSON 在 parsed_outputs 里就不标准**:后续 agent 直查 parsed_data 还是看到 `Gi0/0` vs `GigabitEthernet0/0` 的不一致

**正确位置**:**parse 返回后、写入 `parsed_outputs` 之前**。

**入口**: `olav_netops/tools/textfsm_parse.py::parse_output` 的最后一步,所有 caller 自动获益。

**归一化范围**(netutils 能给的):

| 字段类型 | 归一化函数 | 前 → 后 |
|---|---|---|
| 接口名 | `netutils.interface.canonical_interface_name` | `Gi0/0` → `GigabitEthernet0/0` / `ge-0/0/0` → `ge-0/0/0` |
| IP 地址 | `ipaddress.ip_address()` str | `10.00.00.1` → `10.0.0.1` / `::ffff:10.0.0.1` → `10.0.0.1` |
| ASN | `netutils.asn.asn_to_int` | `1.1` → `65537` / `"65001"` → `65001` |
| MAC | `netutils.mac.mac_normalize` | `AA:BB:CC:DD:EE:FF` → `aabb.ccdd.eeff`(选 Cisco 格式) |

**探测策略**(不需要字段类型 schema):
- 字段名匹配:`/interface/i`, `/neighbor_ip|ip_address|address/i`, `/asn|as_number|_as$/i`, `/mac|hwaddr/i`
- 值匹配:IP regex、MAC regex,才 apply 对应 canonical
- 字段名 + 值双重验证,避免误归一(比如 `version` 字段恰好像 IP)

**改动位置**: `olav_netops/tools/textfsm_parse.py`:

```python
def parse_output(platform, command, raw):
    result = _try_parsers_chain(platform, command, raw)
    if result:
        return _normalize_fields(result)  # NEW
    return result

def _normalize_fields(rows: list[dict]) -> list[dict]:
    """Canonicalize interface names, IPs, ASNs, MACs in-place.

    Applies netutils.interface.canonical_interface_name etc. via
    field-name heuristics. Safe:  if value doesn't match expected
    format, pass through unchanged.
    """
```

**收益**:
- view_builder `_json_extract` / CASE 简化(不再管 `Gi0/0` vs `GigabitEthernet0/0`)
- Agent 直查 parsed_data 看到一致格式
- topology_engine 删 `_normalise` / interface 多字段 COALESCE
- 新接入设备自动享受规范化(只要字段名 match)

**风险**:
- 字段名 heuristic 误识(`version` 被当 IP)→ 加值格式校验双保险
- netutils 某些函数会改大小写(`Gi0/0` → `GigabitEthernet0/0`)→ 需确认下游代码兼容

**验证**:
- `parsed_outputs.parsed_data` 里 interface 字段值看到 `GigabitEthernet0/0` 全称
- view `v_l2_links_auto.source_interface` 一致(不再有 `Gi` 缩写)
- 端到端 `sim.NetworkModel.physical` 能链接起跨厂商 interface

---

### ISSUE-LEARNER-ARCH: 两个 learner 串联 + netops_init 持有学习逻辑(ADR-0002 违规)

**状态**: ✅ Closed (R72 + 后续 ship — 验证通过 2026-04-27 R92 close-out 审计):
* (1) **netops_init 持有学习逻辑 已解耦**:`auto_learn.py` 整文件删除(R72),`run.py:475` 注释化历史前情;Stage 3.5 改成 coverage classifier 不再调 LLM
* (2) **串联 learner 已消失**:auto_learn 删后只剩 `parser_learner.py` (PaC),Stage 3.5b 单条路径
* (3) **`command_learner` skill 已 ship**:`olav-netops/.olav/workspace/command_learner/` 完整目录(AGENT.md / MANIFEST.yaml / SKILL.md / tools / prompts / references);`/learn_cmd` 通过 `cli.builtin:cmd_learn` 直调 `learn_commands` API,无 agent round-trip
**优先级**: P1 → 已 close
**发现日期**: 2026-04-22(AUTO-LEARN-PERF 分析时浮出)
**关闭日期**: 2026-04-27 R92 close-out 审计
**依赖/关联**: AUTO-LEARN-PERF(D5 的真正根因)+ ARCH-25(PaC 独立引入时的重构缺失)

**架构问题陈述**(用户提出,本 issue 正式记录):

1. **`netops_init` 不应持有学习逻辑**: `run.py:502 from olav_netops.core.auto_learn import auto_learn_failed_parses` 和 `:541 from olav_netops.core.parser_learner import pac_learn_failed_parses` 把 LLM 调用代码**硬嵌进管道**。按 ADR-0002,管道层只做编排,学习应是**可替换的 skill/tool**。目前的耦合意味着:
   - 无法单独演进学习器(不重新发 `olav-netops` wheel 就改不了)
   - `/learn_cmd` 和 batch 路径**两套完全不同的代码路径**解决同一个目标(raw → parsed JSON)
   - 测试只能 E2E,无法把学习器换成 fake 做 unit test

2. **串联 learner 是历史叠加,不是设计**: `auto_learn`(Stage 3.5)和 `pac_learn`(Stage 3.5b)是两个时期各做了一半的痕迹。Round 67 引入 ARCH-25 PaC-Learner 时**没有重构 auto_learn**,而是叠在后面。结果对同一堆难命令付出 **2× LLM 成本**(Stage 3.5 失败 → Stage 3.5b 再试),demo4 805s 大头就是这个重复消耗。

3. **`/learn_cmd` slash command 的间接路径 —— 而且目前是死码**: `pyproject.toml:44` 注册 `learn_cmd = "olav_netops.cli.builtin:cmd_learn"`。该函数内部 `_get_or_create_agent(agent_id="ops")` → 自然语言任务委托给 ops agent → ops agent 用自己的 tools 完成。对**单命令交互**来说,这层间接是"agent-mediated 好处"(灵活);对**批量 31 命令**来说,每命令走一次 agent → **ReAct 多轮推理**,比直接调学习器慢几倍。**不能简单把 netops_init 的 batch 路径换成调 cmd_learn**,会更慢。

   **更严重的是**(Round 70 审计发现)**`/learn_cmd` 目前是 registered but non-functional 的死码**:
   - `ops/SKILL.md:4-10` 的 tools 白名单:`execute_cli / search_commands / take_snapshot / diff_configs / record_network_event / register_service` —— **没有 learner tool**
   - `ops/tools/learn_cmd.py` **文件不存在**(`find .olav/workspace -name 'learn_cmd.py'` = 0 hits)
   - E2E pin test `test_claims_e2e.py::TestNE17LearnCmdGeneratesTemplate::test_learn_cmd_creates_template_with_value_lines` 靠 `pytest.skip("learn_cmd.py not found in workspace tools")` **永远跳过**,实际从未验证过
   - 用户真敲 `/learn_cmd "show x" -d R1`:ops agent 收到 NL 任务,能 `execute_cli` 跑命令看到 raw,能 `search_commands` 找已有模板,但**无法起 LLM 生成新模板** —— 最终输出大概率是"这是命令的输出 [raw],要生成模板请手动..."

   **结论**: 现有 cmd_learn 对解决 AUTO-LEARN-PERF **零帮助**。它是被切断过的历史设计(ARCH-23 把 `learn_cmd` 从 core/tools 迁走时漏搬了 netops 这份),需要与新 `command_learner` skill 一起重建。

**正确架构**:抽一个真正的 learner skill,batch + interactive 两入口共用同一个**直接可调**的 `learn_commands(samples, budget, max_workers)` API。agent-mediated 只保留在用户**主动交互**时(`/learn_cmd "show x" -d R1` 单命令场景)。

```
           ┌─────────────────────────────────────┐
           │  command_learner skill              │
           │    (olav-netops/.olav/workspace/    │
           │     command_learner/)               │
           │                                     │
           │  直接可调入口:                       │
           │    learn_commands(samples, budget,  │
           │                   max_workers=5)    │
           │      → {(dev,cmd): parsed_data}     │
           │                                     │
           │  内部:                              │
           │    router(raw) → dsl kind           │
           │      table-like → TextFSM           │
           │      multi-line block → Python      │
           │      kv-pairs → Python              │
           │    draft_parser(kind, samples)      │  LLM 1 次
           │    validate(parser, samples)        │
           │    freeze(parser, plat, cmd)        │
           └──────────┬──────────────────┬───────┘
                      │                  │
           ┌──────────┘                  └──────────┐
           ▼                                        ▼
 netops_init Stage 3.5                      /learn_cmd(交互)
  (batch, ThreadPool × 5, budget=600s)       (agent-mediated,单命令)
  run.py 一行调用                            cli/builtin.py 调 learn_commands
```

**修复 diff**:

| 改动 | 内容 |
|---|---|
| **删除** | Stage 3.5b(run.py ~40 LOC);`auto_learn_failed_parses` / `pac_learn_failed_parses` 两个 pipeline 入口(内部 helper 并入新模块) |
| **新建** | `command_learner/` skill:`SKILL.md` / `MANIFEST.yaml` / `tools/analyze_structure.py` / `draft_parser.py` / `validate_parser.py` / `freeze_parser.py` / `learn_commands.py` |
| **改动 run.py** | Stage 3.5 变一行 `learn_commands(failures, budget=600)`;Stage 3.5b 删 |
| **改动 cli/builtin.py** | `cmd_learn` 直接调 `learn_commands([sample])`,不再 `agent.ainvoke(...)` 的 NL 委托 |
| **保留** | `.olav/templates/<plat>/*.textfsm` + `.olav/templates/parsers/<plat>/*.py` 两种 freezer 产物格式;`textfsm_parse.py` 3-级优先级链 |

**收益**:

| 维度 | 现状 | 新 |
|---|---|---|
| learner 模块数 | 2 + run.py 直接 import + cmd_learn 间接 | 1 skill,2 入口共用 |
| LLM 调用/难命令 | 2(auto + pac) | 1(router 路由) |
| 并发 | 无 | ThreadPool × 5(D1) |
| budget | 无 | `--auto-learn-budget SECONDS`(D3) |
| demo5 首装 | 23min 理论(20min timeout 砍) | ~4-5min(5× 并发 + 2× 调用去重) |
| 交互式入口体验 | 单命令也要起 ops agent + NL 解析(慢) | 直接调,秒回 |

**Out of scope 留到后续**:
- 新 DSL(YAML declarative template、jq、lua)扩展 —— router 设计好后加 DSL kind 即可
- 从 quarantine 自动 promote(第 2 台同平台设备入库时)
- 跨源 oracle(topology_links 反推 expected record count)

**建议执行计划**(Round 71):
- 71a:建 `command_learner/` skill 骨架 + `learn_commands()` 合并 API(先保留两 DSL 生成器,只统一入口 + 并发 + budget)—— 拿掉 D1 + D3
- 71b:实现 router,合并 LLM 调用(拿掉 D5)
- 71c:切换 `run.py` + `cli/builtin.py` 到新 API,删旧入口,pin 测试

验证标尺:demo5 fresh install 在 5-6min 完成首装(现在 20min+)。

---

### ISSUE-AUTO-LEARN-PERF: Stage 3.5 auto_learn 设计性能瓶颈

**状态**: ✅ Closed (R72 ship — `auto_learn.py` 整文件删除;Stage 3.5 改为 coverage classifier 不再调 LLM,串行性能瓶颈消失;LEARNER-ARCH 同期解决)
**发现日期**: 2026-04-21(Chapter 2 demo);2026-04-22(Round 70 fresh demo5 重现,见下方根因拆解)
**代码**: `olav-netops/src/olav_netops/core/auto_learn.py:307-417`

**现象**:fresh install 上首次 `/netops_init` 停留在 Stage 3.5 长达 20-30 分钟;demo5 实测 20min timeout 被 SIGTERM 终止时仍在 auto_learn 阶段,跟 Round 67 原貌一致。

**Round 67 已做(CLI escape valve)**:
- `--skip-auto-learn`(跳过整个 Stage 3.5)+ `--auto-learn-retries N`(默认 5,demo5 用 3)
- `run.py` argparse guard 分支打印 `🎓 Stage 3.5: skipped (flag)`

**Round 70 根因拆解(5 个设计性问题,D1-D5)**:

| 代号 | 问题 | 证据 | 影响 | 修复估 |
|---|---|---|---|---|
| **D1 串行** | `auto_learn.py:334` 主循环对每个 (platform, command) 顺序跑,无并发 | fresh demo5:31 unique 对 × 3 retry × ~15s LLM = **~23min wall** | 首装必卡 20-30min | 低(`ThreadPoolExecutor(max_workers=5)`,~30 LOC) |
| **D2 无失败缓存** | 失败的 `(juniper_junos, show bgp summary)` 每次 `/netops_init` 都重试 | 每次 fresh install 都 full cost | re-run 等于 first run | 中(写 `.olav/templates/_failed.json` + TTL 校验) |
| **D3 无 wall-clock 预算** | 只有 retry 计数,没有"整体 10 分钟封顶,剩下跳过"的硬上限 | CI/demo 失控(timeout 20min SIGTERM demo5 就是证据) | CI 不稳 | 低(`--auto-learn-budget SECONDS`) |
| **D4 retries=3/5 边际收益低** | ReAct 第 2、3 次大多重复产生同样格式错的 TextFSM(LLM 不真正从 `prev_error` 学新东西)| demo4/demo5 失败的 6 命令都是 retry=3 才放弃 | 3× 的成本换几乎没有成功率提升 | 低(默认降 2,硬命令 early-exit) |
| **D5 auto_learn + PaC 串联重复** | Stage 3.5(TextFSM)失败 → Stage 3.5b(Python parser,ARCH-25)再调 LLM;两个阶段的 LLM 调用目标相同,只是产物 DSL 不同 | demo4 805s 大头是这两个串联消耗 | 对同一堆难解析输入付出 2× LLM 成本 | 中(3.5 低概率直接跳到 3.5b 或并行;语义决定谁先谁后) |

**demo5 账本**:
```
31 unique (platform, command) after dedupe
× 3 retries (--auto-learn-retries 3)
× 15s avg (grok-4.1-fast + ReAct validate)
= 1395s ≈ 23min 理论上界  (实测 20min timeout 打断,符合预期)
```

**Fix 优先级**(ROI 排序):
1. **D1 并发**:首装 23min → 5min(5 workers),改动小,风险低
2. **D3 全局预算**:CI 可控性,改动小
3. **D2 失败缓存**:re-run 体验改善,中等改动
4. **D4 retries 默认 2**:一行配置,风险可控
5. **D5 auto_learn + PaC 合并**:需要语义设计决策(保留两者?合一?),改动大

**建议**:Round 71 做 D1+D3(快进胜利;demo 首装从 23min 降到 5-6min),D2/D4 次要,D5 单独起 design round。

**Out of scope**:`should_learn()` 黑名单扩展(`show running-config` / `show configuration` 这类永不可学)-- 另立小 issue 处理,与并发正交。

---

### ISSUE-NETOPS-COLLECT-OSPF-CMD: Cisco IOS OSPF 采集命令拼写错误

**状态**: ✅ Closed (R92.6 close-out, 2026-04-27) — ARCH-27 (Round 68) 通过命令列表从 ntc-templates 文件名反推已经间接修复;R92.6 final sweep 删 ``textfsm_parse._NTC_FILENAME_ALIASES`` 里残留的 ``"show ip ospf neighbors"`` 别名条目 + 更新 docstring 注释
**优先级**: 已 close
**发现日期**: 2026-04-21(ARCH-24 complete-validation 暴露)
**关闭日期**: 2026-04-27 R92.6
**影响**: `ospf × cisco_ios` 整条 pipeline 失效 —— Stage 3 采到 "% Invalid input"，Stage 3.5 auto_learn 正确识别为 `should_learn=False` 跳过，Stage 3.6 L3 ETL 因 parsed_outputs 为 NULL 无法触发学习

**症状**：
- R2/R3/R4/SW1/SW2 的 `raw_output_store` 里 `show ip ospf neighbors` 记录全部是 `% Invalid input detected at '^' marker.`（66 bytes）
- `netops.parsed_outputs` 里这条命令 0 行
- `netops.ospf_adjacencies` 缺 cisco_ios 端数据

**根因**：命令用了 **复数** `show ip ospf neighbors`，IOS 经典 / IOS-XE 实际正确形式是 **单数** `show ip ospf neighbor`。复数 `s` 只在少数 NX-OS / Arista EOS 风格下被接受。实验室 cisco_ios 镜像（IOSv / CSR1000v）拒绝复数形式。

**4 处用到错误命令的文件**：
| 文件 | 行 | 当前内容 | 正确内容 |
|---|---:|---|---|
| `olav-netops/.olav/workspace/ops/netops_init/run.py` | 61 | `"show ip ospf neighbors",` | `"show ip ospf neighbor",` |
| `olav-netops/.olav/workspace/ops/netops_init/config/discovery_commands.yaml` | 13 | `- "show ip ospf neighbors"` | `- "show ip ospf neighbor"` |
| `olav-netops/src/olav_netops/tools/textfsm_parse.py` | 21 | `"show ip ospf neighbors": "show_ip_ospf_neighbor",` | 删除此 alias 行（命令与模板名同形） |
| `olav-netops/.olav/workspace/ops/netops_init/run.py` | 125 | `"show ip ospf neighbors": "show_ip_ospf_neighbor",` | 同上，删除该 `_CMD_ALIASES` 条目 |
| `olav-netops/src/olav_netops/core/protocol_keywords.py` | 50 | `"cisco_ios": {"canonical_cmd": "show ip ospf neighbors"}` | `"cisco_ios": {"canonical_cmd": "show ip ospf neighbor"}` — R67 本次新文件沿袭了旧错误，一并改 |

**修复方案**：统一 5 处为单数形式，删除 2 处 plural→singular 别名映射（对称 workaround 已不需要）。

**验证步骤**：
1. 用新命令重跑 snapshot：`python3 run.py`（full collect，非 resume）
2. R2-R4 的 raw 应含 `Neighbor ID   Pri   State ...` 表头
3. ntc-templates 的 `cisco_ios_show_ip_ospf_neighbor.textfsm` 直接匹配（索引 `sh[[ow]] ip o[[spf]] n[[eighbor]]`）
4. Stage 3.6 L3 ETL 会触发 `ospf × cisco_ios` 冻结

**注意**：ntc-templates 模板文件名沿袭复数（`cisco_ios_show_ip_ospf_neighbor.textfsm` 事实上是单数 `neighbor`，文件名里也是单数；只有个别 show 命令历史遗留用复数如 `show_vlans` 才需要别名）。

---

### ISSUE-ARCH-25: Parser-as-Code (PaC) Learner — 多样本 Python parser 取代单样本 TextFSM

**状态**: ✅ Closed (R67 ship — `olav_netops.core.parser_contract` + `parser_registry` + `parser_learner` 三模块落地;netops_init Stage 3.5b 集成)
**发现日期**: 2026-04-21（ARCH-24 complete-validation 暴露 auto_learn 5 个结构缺陷）
**依赖**: ARCH-24 基础设施（`ast_safety_check` / `execute_in_sandbox` / header stamp pattern 全复用）

**问题**：现有 `auto_learn_failed_parses` 有 5 个结构性缺陷 —— 单样本、TextFSM DSL 难表达 multi-line record、反射信号太粗、无外部 oracle、TextFSM 是终端产物。Junos 18.4R3 `show bgp summary` 3+5 retry 都失败，换设备版本 / 模拟器又换新问题 —— 写模板不是出路，**改造 learner 本身**才是。

**设计**：

| 层 | 当前 | ARCH-25 新增 |
|---|---|---|
| 产物语言 | TextFSM 模板（DSL）| Python 函数 `def parse(raw, sample_hint) -> list[dict]` |
| 持久化位置 | `.olav/templates/<platform>/<cmd>.textfsm` | `.olav/templates/parsers/<platform>/<cmd>.py`；单样本 → `_quarantine/` 子目录 |
| 训练样本 | 单样本（第一个设备的 raw） | **多样本**（同 platform+command 的所有设备 raw 一次喂 LLM，逼迫变化字段 generalize 成 `\S+`） |
| Oracle | 无 | 轻量：`_estimate_data_rows` 启发 + `>0` 检查；单样本降级到 quarantine，P2 再接 topology-bound oracle |
| Reflection | "State Error at line 24" | 两阶段：**结构诊断**（blank-line 块 / indented continuation / table header 预扫描作为 prompt 前置）+ **字段诊断**（retry 时指名道姓哪个设备的哪个字段缺失） |
| 优先级 | ntc-templates → auto_learn TextFSM → NULL | ntc-templates → auto_learn TextFSM → **PaC-Learner → NULL** |

**新增 3 模块** (`olav-netops/src/olav_netops/core/`):
- `parser_contract.py` — `PARSER_CONTRACT_VERSION` + `validate_parser_output` + header stamp helpers（与 ETL contract 分版本，解耦）
- `parser_registry.py` — `load_parser` / `save_parser` / `invalidate`，含 quarantine 支持，镜像 `etl_registry.py`
- `parser_learner.py` — `pac_learn_failed_parses`，多样本累积、结构预扫描、ReAct 3 retry、全样本通过才 promote

**集成点**：`.olav/workspace/ops/netops_init/run.py` Stage 3.5 之后插 **Stage 3.5b**：从 `all_rows` 捞 `parsed_data` 仍为 NULL 的残余命令，交给 `pac_learn_failed_parses`。Sync 到 `olav-netops/.olav/workspace/ops/netops_init/run.py` 副本。

**首个 test case**：Junos BGP summary（对应 NETOPS-PARSE-JUNOS-BGP），单样本 → quarantine。

**Out of scope（P2 follow-ups）**：
- 跨源 oracle（topology_links BGP 计数推断 expected record count）
- LLM 并行化
- 从 quarantine 自动 promote（第 2 台同 platform 设备采入时）
- `textfsm_parse.py` lookup chain 扩展到 take_snapshot 运行时路径

**验证**：端到端测试 — 删 DB + 清所有 frozen artifacts → `python3 run.py --resume <snap> --auto-learn-retries 3` → `.olav/templates/parsers/_quarantine/juniper_junos/show_bgp_summary.py` 生成 → R1 parsed_data 非空（2 entry）→ ARCH-24 级联触发 `bgp × juniper_junos` ETL 学习 → `bgp_sessions` 新增 2 条 R1 行。

---

### ISSUE-ARCH-26: Canonical-form contract + LLM-driven normalization（别再硬编码映射表）

**状态**: ✅ Closed (R68-R83 ship — 实施路径与原方案略调:`observed_canonical.py` 没单独建,而是 `field_normalizer.normalize_fields` 直接做 ingest-time 规范化(BGP `Estab`→`Established`、OSPF `FULL/DR`→`Full/DR`),硬编码映射保持最小;view_builder CASE 表达式作为兜底)
**发现日期**: 2026-04-21（ARCH-25 E2E 成功后实测 bgp_sessions 发现 state/interface/ASN 跨设备不一致）
**依赖 / 同层**: ARCH-24 (L3 ETL) + ARCH-25 (PaC-Learner)

**核心主张（用户提出）**：既然我们走"LLM 自学 ETL/parser"路线，就**不应**再往系统里加 `BGP_STATE_MAP={"Establ":"Established","0":"Established",...}`、`_VENDOR_ALIASES` 之类硬编码映射。那是 pre-LLM 时代 ETL 的做法 —— 每遇到一个新 vendor / version 就得人肉扩表。LLM 自学的价值**就是**让 agent 自动发现这些 mapping，屏蔽底层差异，产出规范化字段。硬编码映射 = 否认 LLM 的自学能力。

**症状**（本轮 demo 实测）：

| 字段 | 不一致值 | 来源 |
|---|---|---|
| `bgp_sessions.state` | `"Establ"` (R1 Junos) vs `"Established"` (R2-R4 Cisco) vs `"0"` (Cisco 历史：数字表示 prefix count) | Junos 输出截断 + Cisco 惯例差异；LLM-generated ETL 不知道要归一 |
| `topology_links.*_interface` | `"Gi0/2"` vs `"GigabitEthernet0/2"` vs `"ge-0/0/2.0"` | ntc-templates 不做归一；`topology_engine.py` `_normalise()` 只 strip 空格 |
| `bgp_sessions.neighbor_as` / `local_as` | 都是字符串；4-byte asdot 记法（如 `"1.1"`）与 plain int（如 `"65537"`）会同时存在 | LLM 生成器直接照搬原值 |

**查询破坏**：`SELECT COUNT(*) FROM bgp_sessions WHERE state='Established'` 只统计 R2-R4，漏 R1 的 `"Establ"`；`SELECT * FROM topology_links WHERE source_interface='Gi0/2'` 在 Junos 设备上永远 0 行。

**两个反模式（都禁止）**：

```python
# ❌ 反模式 1：硬编码具体映射
BGP_STATE_MAP = {"Establ": "Established", "0": "Established", "UP": "Established", ...}
# 每来一个新 vendor / version 就扩表；Round 68 出现 "EST"、Round 69 出现 "Active(NoNeg)"，永远修不完

# ❌ 反模式 2：硬编码 canonical 允许集合
BGP_STATE_ALLOWED = frozenset({"Established", "Active", "Idle", "Connect", "OpenSent", ...})
# 仍然是人工拍脑袋决定的标准；现实中可能有 "Active (NoNeg)"、"Down/Admin"、"Established (stale)" 等合法变体被误拒
```

**正确模式（数据多数派当 de facto canonical）**：

> **用户表述**："不是用一个硬编码契约，而是用通俗法 —— 看看现在 ETL 中主要用的什么，那么就用什么"

核心：**canonical 不是人工定的，是从现有 `netops.parsed_outputs` / `bgp_sessions` 里**观察出来**的**。哪个值出现最多，哪个就是 canonical。

### 数据驱动归一算法

```python
# olav-netops/src/olav_netops/core/observed_canonical.py
def observe_canonical_for(con, table: str, field: str) -> dict[str, int]:
    """Return {observed_value: frequency} from the current table — caller picks
    the argmax as the de facto canonical. Empty dict on first run (no prior data)."""
    rows = con.execute(f"SELECT {field}, COUNT(*) FROM {table} GROUP BY {field}").fetchall()
    return {v: n for v, n in rows if v is not None}

def dominant_form(counts: dict[str, int]) -> str | None:
    """The value with the highest count. None on empty / tie at 1 sample each."""
    if not counts:
        return None
    items = sorted(counts.items(), key=lambda kv: (-kv[1], kv[0]))
    return items[0][0]
```

### Prompt 给 LLM 的输入（每次学习时动态生成）

```
### Observed canonical distribution from prior snapshots
Field `bgp_sessions.state`:
  - "Established"  (count: 4)    ← dominant form, use this as canonical
  - "Establ"       (count: 1)    ← outlier, normalize to dominant
  - "0"            (count: 0)    ← (not seen yet)

Your parser/ETL MUST emit `state` matching the dominant form above.
If this run's raw produces a new value not in this list, map it to the
closest dominant variant based on semantic similarity (Cisco numeric
prefix count = Established, Junos truncated "Establ" = Established, etc.).
```

### Validator

```python
def validate_against_observed(rows, con, field) -> tuple[bool, str]:
    """Warn (not fail) if a row's field value is NOT in the current
    observed distribution — prompts a reflection hint but doesn't block.
    First run has empty distribution → always passes."""
    observed = observe_canonical_for(con, <table>, field)
    dom = dominant_form(observed)
    if dom is None:
        return True, ""  # bootstrap: no prior data
    outliers = [r.get(field) for r in rows
                if r.get(field) and r.get(field) != dom and r.get(field) not in observed]
    if outliers:
        return False, f"field '{field}' has outliers {outliers} vs dominant '{dom}'"
    return True, ""
```

### Bootstrap（冷启动时没有先验数据）

第 1 轮 snapshot：`observed == {}` → validator pass-through，LLM emits raw values。第 2 轮开始：第 1 轮结果成为 dominant；第 2 轮的新样本必须向第 1 轮对齐。**自增强**：最先到的样本定下 canonical，后续都向其收敛。

**风险缓解**：若第 1 轮恰好采到 buggy 格式（如某 vendor bug 版本输出全部是 `"Establ"`），导致 canonical 被锁在错误值上 —— 靠 `PARSER_CONTRACT_VERSION` / `ETL_CONTRACT_VERSION` bump + manual `registry.invalidate()` 可擦除，重新 observe。实际部署中：首轮用生产典型环境（非异常 vendor）基本能避免。

### netutils 作为"协助工具"而非"标准来源"

netutils 不当 oracle（它没有 BGP/OSPF state）但依然可用：

- Prompt 告诉 LLM：`netutils.interface.canonical_interface_name` 可用 —— 如果 observed dominant form 与 netutils canonical form **恰好一致**（很常见，因为 `GigabitEthernet0/2` 是 Cisco + netutils 共同默认形式），LLM 直接调 netutils 就规范化了，不用自己写 map
- 如果 dominant form 与 netutils 不一致（罕见），以 observed dominant 为准，netutils 不调

**落地点**：

| 文件 | 改动 |
|---|---|
| 新建 `olav-netops/src/olav_netops/core/observed_canonical.py` | `observe_canonical_for` / `dominant_form` + 一个 `format_for_prompt(con, table, field) -> str` 把分布表渲染成 prompt 片段 |
| `olav-netops/src/olav_netops/core/etl_contract.py` | **不**新增 `*_ALLOWED` 常量；`validate_rows` 接收可选 `observed_dominants: dict[str,str]` 参数做软告警 |
| `olav-netops/src/olav_netops/core/parser_contract.py` | 同上 |
| `olav-netops/src/olav_netops/core/etl_discovery.py` | Prompt 动态查 `observed_canonical_for(con, target_table, 'state' / 'interface' / 'neighbor_as')`、渲染进提示；AST allowlist 加 `netutils` |
| `olav-netops/src/olav_netops/core/parser_learner.py` | 同上；注意 parser 输出的是 `parsed_data` JSON（键名由 LLM 决定），所以 observed_canonical 在 parser 层只看"常见键名"（e.g., 过去都叫 `neighbor_ip` 不叫 `peer`）|
| **禁止新增**：`state_normalizers.py` / `BGP_STATE_MAP` / `BGP_STATE_ALLOWED` / `_VENDOR_ALIASES` 类硬编码映射或枚举模块 |

**验证**：

1. **Bootstrap 干净**：第一次学习 bgp/cisco_ios 时 observed = {}，LLM 产出 `state="Established"` 入库（dominant 自动就是它）
2. **收敛**：后续学习 bgp/juniper_junos 时，prompt 显示 `state: Established (count=4)` → LLM 把 R1 的 `"Establ"` 映射到 `"Established"`
3. `SELECT DISTINCT state FROM netops.bgp_sessions` 只剩 **1 个值**（不管叫什么）
4. 合成样本注入 `state="NewValueNotSeenBefore"` → validator 返回 `(False, "outlier ...")` → PaC/ARCH-24 reflection 重试
5. **不应存在**：`grep -rn "BGP_STATE_MAP\|BGP_STATE_ALLOWED\|_VENDOR_ALIASES" src/` 返回空

**与 ARCH-25 的关系**：ARCH-25 的 `allow_llm` 模式不变；contract 一收紧，现有 frozen parsers/ETLs 通过 `PARSER_CONTRACT_VERSION` / `ETL_CONTRACT_VERSION` bump 自动失效，触发一轮 re-learn。这是 version-decoupled header 的用途。

---

### ISSUE-ARCH-27: Discovery 命令列表硬编码 —— 应从模板文件名反推

**状态**: ✅ Closed (R68 ship — `olav_netops.core.platform_canonical.canonicalize_platform` + `commands_sync.sync_commands` 落地;netops_init 不再硬编码 `DISCOVERY_COMMANDS_*` list)
**发现日期**: 2026-04-21（追问 "cisco OSPF 复数错误从哪里来" 时暴露）
**关联**: NETOPS-COLLECT-OSPF-CMD（具体症状之一）；ARCH-26（同原则：事实源是数据，不是硬编码）

**问题结构**：
- `run.py:54-67` 写死 `DISCOVERY_COMMANDS_IOS = [...]`、`DISCOVERY_COMMANDS_JUNOS = [...]`
- `run.py:125` `_CMD_ALIASES = {"show ip ospf neighbors": "show_ip_ospf_neighbor", ...}` 用 alias map 兜住命令名 vs 模板文件名的拼写差异
- `olav-netops/src/olav_netops/command_registry.py:161` **已有** `_load_platform_commands()` 从模板文件名（`cisco_ios_show_ip_ospf_neighbor.textfsm`）解出命令 —— 但 `netops_init/run.py` **从不调用**它
- 结果：两套并存，discovery 列表与 parse 模板**各写各的**，拼写漂移后靠 alias map 掩盖，IOS 直接拒绝（`% Invalid input detected`）静默失败

**为什么拼写错一直没人发现**：3 层掩盖

1. `run.py` 命令列表写 `"neighbors"`（复数，IOS 拒收）
2. `_CMD_ALIASES` 把 `"neighbors"` → `"show_ip_ospf_neighbor"`（单数模板），让 parser 以为"命令对了模板对了"
3. `auto_learn.should_learn()` 过滤掉 `% Invalid input` error 文本 → 既不 alert 也不 learn → 沉默

**修复方案**：删除 `DISCOVERY_COMMANDS_IOS` / `_JUNOS` / `_UNIVERSAL` 硬编码 list，改为：

```python
# run.py
from olav_netops.command_registry import CommandRegistry

_PLATFORM_COMMANDS = {}
for platform in ("cisco_ios", "juniper_junos", "arista_eos", "cisco_nxos"):
    _PLATFORM_COMMANDS[platform] = CommandRegistry.get_platform_commands(platform)
```

CommandRegistry 从这几个源收命令：
1. ntc-templates 自带模板文件名（事实源：社区维护）
2. `.olav/templates/<platform>/*.textfsm` 用户自定义 + auto_learn 学来的
3. `.olav/templates/parsers/<platform>/*.py` PaC-Learner 学来的 Python parser（ARCH-25）

添加一条新命令 = 放一个新模板文件 → 下次 netops_init 自动采到。
删一条命令 = 删模板 → 自动不再采。
**`_CMD_ALIASES` 整个删掉**（命令名=模板名，不再有裂缝）。

**落地点**：

| 文件 | 改动 |
|---|---|
| `olav-netops/.olav/workspace/ops/netops_init/run.py` | 删 `DISCOVERY_COMMANDS_*` + `_CMD_ALIASES` + `_PLATFORM_COMMANDS`；改用 `CommandRegistry.get_platform_commands()` |
| `olav-netops/src/olav_netops/command_registry.py` | `get_platform_commands` 扩展覆盖 PaC parsers 目录（ARCH-25 对称）|
| `olav-netops/.olav/workspace/ops/netops_init/config/discovery_commands.yaml` | 降级为"opt-in 覆盖"（用户想加不在模板里的命令才写这里），或直接删除 |

**收益**：
- 修 NETOPS-COLLECT-OSPF-CMD 根因（不仅改单复数，而是消除命令名与模板名漂移的可能）
- 删重复代码 + alias map
- 与 ARCH-25 / ARCH-26 自学路径一致：ntc-templates + auto_learn + PaC 任何一层新增 artifact 都自动进入 discovery

**验收**：
1. `grep -n "DISCOVERY_COMMANDS\|_CMD_ALIASES" olav-netops/.olav/workspace/ops/netops_init/run.py` 空
2. `olav --agent ops "/netops_init"` 首次跑 → 发 `show ip ospf neighbor`（单数）到 cisco_ios 设备
3. `netops.parsed_outputs WHERE device_name='R2' AND command LIKE '%ospf%'` 有行
4. Stage 3.6 触发 `ospf × cisco_ios` L3 ETL 学习
5. 加一个新模板 `.olav/templates/cisco_ios/show_arp_vrf_foo.textfsm` → 下次 netops_init 自动采到 `show arp vrf foo`

**Round 77 更新 (2026-04-24)**: Round 68 声称删掉了 `DISCOVERY_COMMANDS_*`，实际代码回退了（`run.py:48-94` 三份 list 还在，`_PLATFORM_COMMANDS` 也在）。本轮真正收尾：
  - `netops_init/run.py` 现在 Stage 1.5 调 `sync_commands(conn)`（先前从不调用 → `netops.commands` 表 0 行）
  - 添加数据驱动 `_discovery_commands_for(platform, conn)` helper：query `netops.commands WHERE platform=? AND NOT blacklisted` + 一个 **vendor-agnostic intent prefix list**（`show version` / `show interfaces` / `show bgp` / `show ospf` / …）过滤 143 条 cisco_ios 到 ~14 条 operational essentials
  - 删除 `DISCOVERY_COMMANDS_UNIVERSAL/_IOS/_JUNOS/_PLATFORM_COMMANDS/DISCOVERY_COMMANDS` 全部 5 份硬编码
  - backup 命令（`backup_only=true`）单独 append
  - **副作用**：新装 Arista EOS 设备 → 47 条 ntc-templates 全自动可见，无需改 run.py
  - **副作用**：OSPF 单复数 bug 根源消除 —— `netops.commands` 只有单数形式（ntc 文件名决定），复数命令永远不会被发 SSH

---

### ISSUE-NORNIR-INVENTORY-METADATA-LOSS: Device ETL 把 groups/role/site/aliases 全丢了

**状态**: ✅ Closed (Round 77 续, 2026-04-24 — 同轮登记 + 同轮修复)
**发现日期**: 2026-04-24 (DEMO_RUNSHEET Chapter 2 walk-through, 核对 "nornir 设备 + 分组入库" claim)
**关联**: ARCH-22 (设备元数据完整性)

**问题**：
```python
# netops_init/run.py:_populate_devices line 792-804
conn.execute("""
    INSERT INTO netops.devices (hostname, ip_address, platform, site, role, vendor, model, os_version, environment, ...)
    VALUES (?, ?, ?, NULL, NULL, ?, ?, ?, ?, ...)
""", [device_name, mgmt_ip, plat, vendor, model, os_ver, env_tag])
```

`site=NULL, role=NULL, metadata=NULL` 硬写。但 hosts.yaml 里每条 host 有：

```yaml
R3:
  hostname: 3.3.3.3
  platform: cisco_ios
  groups: [test]
  data:
    role: core              ← 丢失
    site: lab               ← 丢失
    environment: lab        ← 只这个保留了
    aliases:                ← 丢失
      - 核心路由器1
      - R3路由器
```

**后果**：
- 用户："列出所有 core 路由器" → agent 无法回答（`role` 全 NULL）
- 用户："核心路由器1 的 BGP 状态" → 自然语言别名完全匹配不上（aliases 没入库）
- 运营者在 hosts.yaml 里维护的 group 元数据（border/core/access、site、environment）在 DB 里彻底看不见

**修法候选**：
1. `_populate_devices` 访问 nornir inventory，读 `host.groups`、`host.data`，写入 `site`/`role`/`metadata` 列。`metadata` 列存 JSON 含 `groups: [...]` 和 `aliases: [...]`
2. 新表 `netops.device_groups(device_name, group_name)` 多对多关系
3. 新表 `netops.device_aliases(device_name, alias)` 支持自然语言查询 JOIN

最简方案：1。metadata JSON 里兜住 aliases + groups，role/site 走独立列。

**Not Started** —— 本轮先收尾 ARCH-27，分组 metadata 留下轮。

---

### ISSUE-NETOPS-GLUE-AUDIT: netops_init + take_snapshot 流程里的硬编码与粘合剂 —— 13 处

**状态**: ✅ Closed (R68 ship — 全 13 项与 ARCH-27 合并交付:`platform_canonical` 统一 4 处 vendor 别名、`universal_parse` 三级 priority chain、Junos PaC seed parser、bare except → logger.warning)
**发现日期**: 2026-04-21（ARCH-27 登记后系统性审计）
**关联**: ARCH-25 / ARCH-26 / ARCH-27 —— 都是自学习架构；本 issue 盘点剩余需要治理的硬编码债务

本审计覆盖 **`netops_init/run.py` (934 行) + `take_snapshot.py` (507 行)**，共 **13 项** 硬编码 / 粘合剂 / 静默失败。按严重度分级：

#### 🔴 P1 — 直接违反 ARCH-26 "禁硬编码映射" 原则（3 处**重复**！）

| # | 文件:行 | 症状 | 根因 |
|---|---|---|---|
| G1 | `l3_topology_etl.py:33` `_VENDOR_ALIASES` | 硬编码 `{"cisco_ios": {"cisco_ios","ios","cisco-ios","cisco"}, ...}` | ARCH-24 R67 引入 |
| G2 | `netops_init/run.py:93` `_JUNOS_PLATFORMS = {"juniper_junos", "juniper", "junos"}` | 硬编码集合，与 G1 意义重叠 | 旧代码 |
| G3 | `netops_init/run.py:96-105` `_normalise_platform()` 函数 | if/elif 链 cisco_ios/juniper_junos/nxos 写死 | 同上 |
| G4 | `take_snapshot.py:73-79` `SCRAPLI_PLATFORM_MAP = {"cisco_ios":"cisco_iosxe",...}` | Nornir→Scrapli 平台映射硬编码 | 第 3 份重复 |

**整治**：三份重复应合并为一个地方（最好是 `command_registry.get_platform_aliases()` 从模板文件名反推，或者干脆让 agent 用 observed canonical 学习）。ARCH-27 是起点；此 issue 追查剩余。

#### 🔴 P1 — Discovery 命令列表硬编码（ARCH-27 主题）

| # | 文件:行 | 症状 |
|---|---|---|
| G5 | `run.py:48-67,70-80,83-90` `DISCOVERY_COMMANDS_*` + `_PLATFORM_COMMANDS` + `# Legacy flat list used when --commands override is passed` | 三份硬编码列表，cisco OSPF 复数拼写错误就藏在这里 |
| G6 | `run.py:124-131` `_CMD_ALIASES` 命令名→模板名 alias map | 掩盖命令名与模板文件名的拼写漂移 |

**整治**：ARCH-27 删 G5/G6，改从模板文件名反推。

#### 🟡 P2 — LLM-自学路径覆盖缺口

| # | 文件:行 | 症状 |
|---|---|---|
| G7 | `take_snapshot.py:_try_parse_textfsm` | **未查 `.olav/templates/parsers/*.py`** (PaC-Learner 产物)；只试 ntc-templates + 自定义 textfsm。结果：ARCH-25 的 frozen Python parser 只在 netops_init Stage 3.5b 生效，take_snapshot 运行时（agent 实时跑命令）完全绕过。Junos BGP 的 quarantine parser 对 agent 交互式调用不可见 |
| G8 | `run.py:740-765` Junos `show interfaces terse` 内联 regex 解析 | 注释 `# ntc_templates has no template for this command; parse raw output directly` —— **这正是 ARCH-25 PaC 该解决的场景**，但 Device ETL 自己手写 regex 绕过，导致 PaC 学的 parser 不被复用 |
| G9 | `run.py:697-700` Device ETL 字段嗅探判 vendor | `if v.get("JUNOS_VERSION"): plat = "juniper_junos"; elif v.get("HARDWARE"): plat = "cisco_ios"` —— 两行 if 干着 ARCH-26 原则上应该让 LLM observe distribution 推断的事 |

**整治**：
- G7：扩展 `_try_parse_textfsm` 查找链，加第 0 优先级 `parser_registry.load_parser()`（Python 优先，TextFSM 次之）
- G8：Device ETL mgmt IP 提取应该依赖 parser_learner 学出来的 parse 结果，而非手写 regex；或至少把手写 regex 注册为一个 "seed parser" 给 PaC 作为起点
- G9：让 observed_canonical 覆盖 `netops.devices.platform` 字段

#### 🟡 P2 — Magic heuristics

| # | 文件:行 | 症状 |
|---|---|---|
| G10 | `run.py:724-728` mgmt IP 提取偏好 | 硬编码 `"loopback"` 关键字 + `"10."` 前缀 RFC1918 跳过启发。真实网络里 `10.x` 常是合法管理段 |
| G11 | `run.py:332,486,535` `if host_obj else "cisco_ios"` fallback | 未知设备默认假设 Cisco IOS → 给 Arista/Juniper 发 IOS 命令会被拒 |

**整治**：
- G10：mgmt IP 策略应从设备 inventory 显式 declare（`nornir hosts.yaml` 加 `data.mgmt_interface`），或从 observed distribution 推断（哪些接口模式最常被标注为 mgmt）
- G11：unknown platform → 抛 `ValueError` 或进入 "platform discovery" 子 pipeline（pingable + banner 匹配），不要沉默 default

#### 🟡 P2 — 静默失败（swallows exception）

| # | 文件:行 | 症状 |
|---|---|---|
| G12 | `run.py:148-149, 167-168, 266-267, 704-705, 736-737, 766, 770` `except Exception: pass` | 7 处沉默异常（6 处完全 bare，1 处含注释解释）|
| G13 | `take_snapshot.py:203-204` scrapli 失败 logger.debug | 失败静默降级到 netmiko，调用者看不到为什么 scrapli 失败，只知道路径 1 不工作 |

**整治**：全部改为 `except Exception as e: logger.warning(...)`；错误信息写进某 audit 结构化字段（如 `netops.ingest_errors` 新表）让上游可查。

#### 🟢 P3 — 配置不一致

| # | 文件:行 | 症状 |
|---|---|---|
| — | `run.py:_textfsm_parse` 只试 2 种模板路径 vs. `take_snapshot.py:_try_parse_textfsm` 试 3 种（含 flat + 嵌套）| 两个入口 parse 逻辑不统一，修一处不修另一处总会出问题；应合并为 `olav_netops.tools.textfsm_parse._parse()` 共享函数 |

---

**整治路线建议**：
1. **Round 68**：ARCH-27（G5/G6）+ NETUTILS-INTEGRATION + ARCH-26 当前 in-progress 线
2. **Round 69**：G1/G2/G3/G4 vendor alias 三合一（由 `command_registry` 从模板反推 + observed canonical 补全）
3. **Round 70**：G7/G8 让 take_snapshot + Device ETL 真正用 PaC parsers（收口"LLM 学了但没人用"的路径覆盖问题）
4. **独立 issue**：G10/G11/G12 magic heuristic + 静默异常清理 —— CORE-02 风格的 sweep

---

### ISSUE-ARCH-28: view_recipes-driven SQL views 替代 ARCH-24 物化 ETL

**状态**: ✅ Closed (Round 69) — views ship，demo3 E2E drift=0；Round 70 ARCH-29 skill 化时完成对 ARCH-24 真删除
**发现日期**: 2026-04-22（Round 68 完成后的架构 rethink）
**可能弃用**: ARCH-24 大部分机器（etl_discovery / etl_registry / etl_contract / observed_canonical / l3_topology_etl — ~800 LOC）

**核心洞察**（用户提出）：OLAV 的 DB 联合查询 + `view_recipes` (ARCH-06) 已经有能力直接从 `parsed_outputs` 抽出 BGP/OSPF 邻接，**不需要 ARCH-24 的 LLM-生成 ETL 物化层**。当初 Round 67 建 ARCH-24 是因为我没意识到 `view_recipes` (ARCH-06) 还差最后一步 runtime view builder —— 前面 95% 已经做完（YAML + 数据 seed 到 DB），就差一行 `CREATE VIEW FROM view_recipes`。

**现状**：
- `view_recipes` 表：每次 snapshot 都 seed，15 rows（BGP/OSPF × IOS/Junos/Arista 的 field_mappings 已就位）
- 下游消费者：**不存在**。`v_bgp_neighbors_auto` / `v_ospf_neighbors_auto` 在 SKILL.md `allowed_tables` 里被列为可查表，但实际**从未被 CREATE 过**
- ARCH-24 反过来建了平行机器：LLM 生成 Python ETL → 冻结 → 写 `bgp_sessions` 表

**Path B 设计（本 issue）**：

1. **view_builder.py（新）**：读 `view_recipes`，对每个 `concept` 生成：
   ```sql
   CREATE OR REPLACE VIEW netops.v_bgp_neighbors_auto AS
     SELECT device_name,
            json_extract_string(entry, '$.BGP_NEIGHBOR') AS neighbor_ip,
            json_extract_string(entry, '$.NEIGHBOR_AS') AS neighbor_as,
            CASE WHEN json_extract_string(entry, '$.STATE_OR_PREFIXES_RECEIVED') ~ '^\d+$'
                 THEN 'Established'
                 ELSE json_extract_string(entry, '$.STATE_OR_PREFIXES_RECEIVED')
            END AS state
     FROM netops.parsed_outputs,
          LATERAL (SELECT value AS entry FROM json_each(parsed_data))
     WHERE command ILIKE '%bgp summary%'
       AND device_name IN (SELECT hostname FROM netops.devices WHERE platform='cisco_ios')
   UNION ALL
     SELECT ... (Junos variant) ...
   ;
   ```

2. **每 snapshot 结束调用 `build_all_views(conn)`** —— 幂等，无 LLM 调用

3. **Pydantic 输出契约**（新）`olav_netops/schemas/topology.py`：
   ```python
   class BGPSession(BaseModel):
       device: str
       neighbor_ip: IPvAnyAddress
       neighbor_as: int
       state: Literal["Established","Active","Idle","Connect","OpenSent","Down"]

   class TopologySnapshot(BaseModel):
       snapshot_id: str
       bgp_sessions: list[BGPSession]
       ospf_adjacencies: list[OSPFAdj]
       l2_links: list[L2Link]
   ```
   Agent 工具 `query_topology()` 返 typed JSON —— langchain `with_structured_output(TopologySnapshot)` 强制 schema 验证

4. **NetworkX 保留**：仅用于真正的图算法（Dijkstra / centrality / what-if simulation），不是数据检索层

**Path B 解决 Round 68 遗留的 3 个 P0 语义 gap**：

| Gap | Path B 解法 |
|---|---|
| ARCH-26 canonical 跨 vendor 污染（Junos `Full` 被强行归一为 Cisco `FULL/BDR`）| view 里 UNION ALL 各 vendor 保留各自语义；跨 vendor 统一只发生在 enum field（state）的 CASE 表达式里，**声明式而非 LLM 推断** |
| BGP state='0' UX 反常识 | view CASE WHEN state ~ '^\d+$' THEN 'Established' ELSE state END —— 一行 SQL 替代 observed_canonical 全套 |
| first-snapshot-wins 脆弱 | view 定义不依赖 DB 先验数据，确定性 |

**可弃用的 ARCH-24 机器（待 Path B 验证后）**：

| 文件 | LOC | 状态 |
|---|---:|---|
| `olav-netops/src/olav_netops/core/etl_discovery.py` | ~460 | 可删 |
| `olav-netops/src/olav_netops/core/etl_registry.py` | ~170 | 可删 |
| `olav-netops/src/olav_netops/core/etl_contract.py` | ~170 | 可删 |
| `olav-netops/src/olav_netops/core/observed_canonical.py` | ~130 | 可删 |
| `olav-netops/src/olav_netops/core/l3_topology_etl.py` | ~310 | 可删 |
| 合计 | ~1240 | 约占 netops core 的 30% |

加上 `.olav/templates/etl/*.py` 冻结 artifacts 全部不再需要。

**ARCH-25 PaC parser 保留**：parse 层（raw CLI → JSON）的格式变体是真难题，LLM 自学有价值；ARCH-28 只替换 ETL 层（JSON → 规范化表），不碰 parse 层。

**小实验验证路径**：
1. 手写 `v_bgp_neighbors_auto` SQL（从 view_recipes 15 rows 推导）
2. 在 demo2 上查询，跟现有 `bgp_sessions` 表对比
3. Pydantic 包一层
4. 若结果一致 → Path B 可行 → 登记 ARCH-28 进 `/plan` 走完整实施

**验收**（Path B ship 完成时）：
1. `netops.v_bgp_neighbors_auto` 查询返回 ≥ 6 rows（与 bgp_sessions 一致），state 全部 canonical
2. 添加一个新 vendor（如 arista_eos）只需在 `view_recipes` YAML 加 3-4 行，无代码改动
3. `agent.query_topology()` 返 `TopologySnapshot` Pydantic 对象，downstream 类型安全
4. `grep -rn "bgp_sessions\|ospf_adjacencies" src/` 只剩 view 定义 + 弃用警告；物化表可选开启（`OLAV_MATERIALIZE_L3=1` env var）
5. 删除 ~1240 LOC ARCH-24 机器后，完整 E2E 仍通过

---

### ISSUE-ARCH-29: netops/topology sub-skill —— 意图驱动 + LLM 起草 recipe + pipeline 集成

**状态**: ✅ Closed (R70 ship — `olav-netops/.olav/workspace/topology/` sub-skill 完整落地:AGENT.md / MANIFEST.yaml / recipes/builtin/{8 protocol YAML} / topology_intent loader / view_builder declarative SQL CASE;ARCH-24 物化 ETL 已删 ~1240 LOC)
**归属**: **olav-netops domain**（不是 olav core）
**依赖**: ARCH-28（view_recipes + view_builder + Pydantic）已就位
**前置讨论**: 用户提出 "为什么走完全自动发现？是难度问题还是架构归属问题？"；结论 —— **canonical 语义归属是人决定的**（"dominant form" ≠ "correct form"），完全自动会回到 ARCH-24 跨 vendor 污染 / first-snapshot-wins 脆弱的老路。正确架构是**意图声明（user）+ recipe 起草（LLM，一次性）+ view 运行时（纯 SQL 无 LLM）**。

---

#### 一、设计主张

| 层 | 谁 own | 改动频率 | 自动化程度 |
|---|---|---|---|
| **意图** (`~/.olav/config/topology.yaml`) | 用户 | 偶尔（加一个协议）| 无 —— 人手写 |
| **Recipe** (`view_recipes` DB + `recipes/user/*.yaml`) | LLM 起草 + 人可编辑 | 首次发现 + 手动修正 | **LLM 起草，YAML 声明** |
| **View** (`v_*_auto`) | view_builder 自动生成 | 每 snapshot 幂等 | 纯 SQL 零 LLM |
| **Parse** (parsed_outputs) | ntc-templates → auto_learn → PaC | ARCH-25 不变 | LLM 自学（parse 层真难） |

**关键分界**（vs 被否决的 ARCH-24）：LLM 产 **YAML recipe（声明数据）** 而不是 **Python ETL（行为代码）**。前者可读 / 可编辑 / 无 AST 风险 / 语义归属可 override。

---

#### 二、Skill 结构（归 olav-netops domain）

```
olav-netops/.olav/workspace/topology/       # sub-skill，不是 platform core
├── SKILL.md                    # name, description, tools, references
├── MANIFEST.yaml               # route_keywords
├── AGENT.md
├── tools/
│   ├── query_topology.py       # ✅ 已有 (Round 69)
│   ├── discover_recipe.py      # ❗ NEW — ReAct 扫 parsed_outputs 起草
│   ├── save_recipe.py          # ❗ NEW — 验证 + 写 DB + 写 user YAML
│   ├── rebuild_views.py        # ❗ NEW — 触发 view_builder.build_all_views
│   └── list_recipes.py         # ❗ NEW — olav recipes list 等价
├── prompts/
│   └── system.md               # 发现流的 prompt + 例子
├── recipes/
│   ├── builtin/                # ship 随 olav-netops wheel
│   │   ├── bgp_{cisco_ios,juniper_junos,arista_eos}.yaml
│   │   ├── ospf_{cisco_ios,juniper_junos,arista_eos}.yaml
│   │   ├── cdp_lldp.yaml       # 现有 CDP/LLDP topology_l2
│   │   └── l2_topology.yaml    # @topology_links 特殊指令
│   └── user/                   # runtime 生成，gitignored in olav-netops
│       └── (empty by default, populated on discovery)
├── schemas/
│   └── topology.py             # ✅ 已有 (Round 69) — 内置 BGP/OSPF/L2 Pydantic
└── references/
    ├── INTENT_FORMAT.md        # 用户 topology.yaml 格式
    ├── RECIPE_FORMAT.md        # 内部 recipe YAML 格式（给 agent 看）
    ├── SUPPORTED_BUILTIN.md    # 目前 ship 的 protocols × vendors
    └── EXTENSION_COOKBOOK.md   # BFD/HSRP/VRRP/ISIS 扩展实例
```

---

#### 三、用户意图（唯一要写的配置）

```yaml
# ~/.olav/config/topology.yaml   ← 用户编辑的唯一文件
protocols:
  - bgp                 # 内置 builtin/bgp_*.yaml
  - ospf                # 内置
  - cdp_lldp            # 内置
  - bfd                 # agent 自动发现
  - hsrp                # agent 自动发现
```

就一个 `protocols:` list —— 无 vendor、无 field_mappings、无 command。

---

#### 四、ReAct 发现流（agent 执行，仅新 concept / 新 vendor 触发）

```
触发: user /query_topology bfd  OR  topology.yaml 新增 bfd
  │
  ├─ 1. 扫 inventory: SELECT DISTINCT platform FROM netops.devices
  │     → {cisco_ios, juniper_junos}
  │
  ├─ 2. 对每 vendor 找候选命令:
  │     SELECT DISTINCT command FROM netops.raw_output_store
  │     WHERE command ILIKE '%bfd%' AND device_name IN (WHERE platform=?)
  │     → ['show bfd neighbors']
  │
  ├─ 3. 取样: SELECT parsed_data FROM parsed_outputs WHERE command=? LIMIT 1
  │     → keys: {local_ip, peer_ip, state, interface, last_event}
  │
  ├─ 4. LLM 起草 recipe YAML (5-10 行):
  │     - command: show bfd neighbors
  │       concept: bfd_sessions
  │       vendor_hint: cisco_ios
  │       field_mappings:
  │         neighbor_ip: peer_ip
  │         state: state
  │         interface: interface
  │
  ├─ 5. save_recipe(): dry-run build_view → 检查 >0 非空行 → 写 view_recipes DB
  │     + recipes/user/bfd_cisco_ios.yaml
  │
  ├─ 6. 对每 vendor 重复 2-5 (Junos: 'show bfd session', 字段 'remote_ip' → peer_ip)
  │
  └─ 7. rebuild_views() → v_bfd_sessions_auto 生成
```

**首次 ~30 s**（2-3 LLM 调用）；**后续所有 snapshot 0 LLM 调用**（recipe 已在 `view_recipes` 表中）。

---

#### 五、双入口：Pipeline 模式 + Agent 模式

**关键需求（用户 Round 69 收尾强调）**：skill 不仅为 agent interactive，也必须嵌入 pipeline。recipe 保存后，snapshot/netops_init 调 view_builder 直接用，不再二次学习。

| 入口 | 触发时机 | 是否调 LLM | 路径 |
|---|---|---|---|
| **netops_init Stage 3.5 (NEW)** | 每次 netops_init 开始前 | 仅当 `topology.yaml` declare 了 recipes 缺失的 protocol | 加载 `topology.yaml` → 检查 `view_recipes` DB 是否有齐全的 (protocol × vendor) 组合 → 缺则 log WARN（不 block），或可选触发 auto-discover |
| **netops_init Stage 3.7 (existing)** | 每次 Stage 3.6 L3 ETL 之后 | ❌ 永不 | `build_all_views(con)` 纯 SQL |
| **take_snapshot 后处理 (NEW)** | `_run_one` 返回 success 后 | ❌ 永不（recipe 已存在时）| 检测 captured command 属于某 concept → 调 `rebuild_views_for(concept)` → 刷新对应 view |
| **query_topology 第一次命中未知 concept** | agent tool 调用 | ✅ 一次（首次发现）| discover_recipe → save_recipe → rebuild_views → 返查询结果 |
| **skill install 时** | `olav skill install olav-netops` | ❌ | builtin/*.yaml → `view_recipes` upsert |

Recipe 一旦写入 `view_recipes` + `recipes/user/` 就**永久存活**（snapshot 可以跨 session 复用），直到用户手动 `olav recipes invalidate` 或 `PARSER_CONTRACT_VERSION` bump。

---

#### 六、Pydantic schema 三层策略（应对用户自定义 concept）

| Concept | Schema 处理 |
|---|---|
| 内置 (bgp / ospf / l2) | `TopologySnapshot.bgp_sessions` 强类型 `list[BGPSession]`，`Literal` enum 校验 |
| 用户自定义（bfd / hsrp / vrrp / isis / ...） | `TopologySnapshot.custom_concepts: dict[str, list[dict]]`，动态 `pydantic.create_model()` 基于 recipe field_mappings（Literal 放宽为 `str`）|
| 用户声明 enum override | recipe YAML 里 `literal_values: {state: [Up, Down, AdminDown]}` → 动态 Literal |

---

#### 七、CLI 脚手架（可选，P2）

```bash
olav recipes list                                 # 当前 DB 内 + user/ 文件
olav recipes validate                             # dry-run 所有 recipe，报错行
olav recipes discover --protocol bfd              # 手动触发 agent 发现（不用聊天）
olav recipes add --from-snapshot <snap> \         # 扫 snapshot，起草 user recipe
                 --command "show bfd neighbors" \
                 --concept bfd_sessions
olav recipes invalidate --protocol bfd --vendor cisco_ios   # 删 recipe，下次 discover
```

---

#### 八、与现有 code 的关系（Round 70 实施动作）

| 现有文件 / 表 | Round 70 动作 |
|---|---|
| `olav-netops/src/olav_netops/core/view_builder.py` | 移到 `.olav/workspace/topology/tools/` 作为 skill library（或保留 import path） |
| `olav-netops/src/olav_netops/schemas/topology.py` | 同上，skill 内部；扩展 `custom_concepts` 容器 |
| `olav-netops/.olav/workspace/ops/tools/query_topology.py` | **移到** `.olav/workspace/topology/tools/query_topology.py` |
| `olav-netops/src/olav_netops/data/view_recipes_seed.yaml` | 拆成 per-recipe 文件 → `recipes/builtin/*.yaml` |
| `olav-netops/src/olav_netops/core/recipe_seeds.py` | 改为扫 `recipes/{builtin,user}/*.yaml` 而非单 YAML |
| `~/.olav/config/topology.yaml` | **新增** 用户配置文件 |
| `netops_init/run.py` Stage 3.7 | 保持 |
| `netops_init/run.py` Stage 3.5 (NEW) | 加 topology.yaml coverage check，缺口打 WARN |
| `take_snapshot._run_one` | 加 post-hook 调 rebuild_views_for(captured command's concept) |

---

#### 九、验收（Round 70 完成时）

1. `~/.olav/config/topology.yaml` 加 `- bfd`，agent 首次 `query_topology bfd` → 自动发现 + 冻结 recipe（仅 2-3 LLM 调用）
2. 第二次 `query_topology bfd`：0 LLM，<1 s 返回
3. `take_snapshot R1 "show bfd neighbors"` → post-hook 刷 `v_bfd_sessions_auto`，数据当场可查
4. `olav recipes list` 显示内置 6 + 用户 1（bfd）
5. 删除 `recipes/user/bfd_cisco_ios.yaml` + `view_recipes` 对应行 → `olav recipes list` 不含 bfd；下次 query 自动重新发现
6. `netops_init` Stage 3.5 检测到 topology.yaml 有 `- bfd` 但 `view_recipes` 里该 vendor 缺 → 打 `⚠ topology intent 'bfd' × 'cisco_ios' has no recipe yet` warn
7. Pydantic：内置 BGPSession.state 是 Literal；bfd custom concept 返 list[dict] 但 keys 一致

---

#### 十、不做 / 延后（P2）

- 自动 quarantine promotion（第 2 vendor 到 → promote）
- Recipe schema version 与 Pydantic Literal 自动同步
- Cross-snapshot recipe drift（recipe v1 → v2 更新时自动 re-learn old data）
- VRRP/ISIS/LDP/MP-BGP built-in（先框架、后逐个 ship）

---

### ISSUE-NETUTILS-INTEGRATION: netutils 声明为依赖但从未进入 netops_init 流程

**状态**: ✅ Closed (R68-R72 ship — netutils 进入 `field_normalizer` 做 interface/IP/ASN/MAC canonicalize;ingest-time 用,与 ARCH-26 同路径交付)
**发现日期**: 2026-04-21

**事实**：
- `olav-netops/pyproject.toml:16` 声明 `netutils>=1.17.1`
- `olav-netops/.olav/workspace/ops/tools/run_python_simulation.py:129` 注入到 analysis sandbox 全局
- `topology_engine.py`（CDP/LLDP ETL）自写 `_normalise()` 处理接口名，**没调** `netutils.interface.canonical_interface_name`
- ARCH-24 LLM-generated L3 ETL 没被告知 netutils 可用；AST allowlist 里也没 netutils
- ARCH-25 PaC-Learner 同上
- Device ETL / view_recipes seeds 层均无 netutils 调用

**netutils 能帮的**：
- `interface.canonical_interface_name` — 接口名归一
- `asn.asn_to_int` / `int_to_asdot` — ASN 格式归一
- `mac.mac_normalize` — MAC 归一（未来需要）
- `ip.*` — IP 校验 / CIDR 计算 / same-subnet 判定

**netutils 不能帮的**（见 ARCH-26）：
- BGP / OSPF state 归一（没这个 API）
- vendor platform 归一（部分能做 via `lib_mapper.NAPALM_LIB_MAPPER` 但不覆盖 olav 的 `cisco_ios` / `juniper_junos` 命名）

**修复方案**：和 ARCH-26 合并实施 —— 把 netutils 加入 LLM prompt 的"建议 import"列表 + AST 白名单，让 LLM 在生成 ETL 代码时主动调 `canonical_interface_name` / `asn_to_int`。不要在 core 代码里 wrap 它。

---

### ISSUE-NETOPS-PARSE-JUNOS-BGP: Junos `show bgp summary` ntc-templates 不匹配

**状态**: 🟡 Delegated to ARCH-25 PaC-Learner（方案 A 手写模板 deprecated —— 学习器层面解决更通用）
**发现日期**: 2026-04-21（ARCH-24 complete-validation 暴露；auto_learn 3 & 5 次重试均失败）
**影响**: `bgp × juniper_junos` L3 ETL 无法触发 —— R1 虽有 2 个真实 BGP peer（3.3.3.3 / AS65000 + 10.1.12.2 / AS65001）但 `parsed_outputs` 中该命令为 NULL

**根因分析（实测）**：

直接调用 `ntc_templates.parse.parse_output(platform='juniper_junos', command='show bgp summary', data=raw)` 抛：

```
TextFSMError: State Error raised. Rule Line: 24.
Input Line:                        1          0          0          0          0          0
```

ntc-templates 模板 (`juniper_junos_show_bgp_summary.textfsm`) 的 BGP state 规则：

```textfsm
^Peer\s+AS\s+InPkt\s+OutPkt\s+OutQ\s+Flaps\s+Last\s+Up\/Down\s+State\|#Active\/Received\/Accepted\/Damped...$$
^${PEER_IP}\s+${PEER_AS}\s+${IN_PKT}\s+${OUT_PKT}\s+${OUT_Q}\s+${FLAPS}\s+${LAST_FLAP}\s+${UP_DOWN}\s+${STATE}
^\s+${INTERFACE}:\s+${ACTIVE}\/${RECEIVED}\/${ACCEPTED}\/${DAMPED} -> Record
^\s+. -> Error
```

两处与实际 Junos 18.4R3-S3 输出不匹配：

1. **Header 字段缩写**：模板期望 `Last Up/Down State`，实际输出 `Last Up/Dwn State`（Junos 18.4 缩写 "Down" 为 "Dwn"）→ header 行不进入 BGP state
2. **inet.0 多行切分**：模板期望 `inet.0: 0/1/1/0` 单行，实际 R1 输出：
   ```
   Table          Tot Paths  Act Paths Suppressed    History Damp State    Pending
   inet.0
                          1          0          0          0          0          0
   ```
   即 `inet.0` 在表头行，数字在下一行 —— 模板里 `inet.0:...` 的 Record 规则永不触发，反而 `^\s+1          0 ...` 触发了 `^\s+. -> Error`

**auto_learn 为何也没学出来**：
- prompt 把整段 raw 丢给 LLM，LLM 生成的模板仍按"每行一个 peer"简化，无法同时处理 "多行合并" + "Up/Dwn 缩写"
- 3-5 次 retry 都在同一思维框架里打转，反射 feedback 只说 "parsed 0 rows"，没告诉 LLM 要考虑 multi-line record transition

**修复方案**（从简到繁）：

**方案 A（推荐）：在 `.olav/templates/juniper_junos/` 手写覆盖模板**
- 位置：`~/.olav/templates/juniper_junos/show_bgp_summary.textfsm`（`auto_learn.py:108-142` 的 load_custom_template 会优先读自定义模板）
- 工作量：1 人日；只需修两处（`Up\/(Down|Dwn)` 正则 + 多行合并的 BGP_INET state）
- 风险：仅影响 Junos BGP 解析，不触及平台代码
- 覆盖所有 Junos 版本（16/17/18/20），向后/向前兼容

**方案 B：PR 给上游 ntc-templates**
- 真正根治，但合入周期长，不可控
- 可作为 A 的 follow-up

**方案 C：在 auto_learn ReAct prompt 里加 "multi-line record" hint**
- `auto_learn.py:229-286` 的 prompt 里显式列 "注意 Junos / 某些 Cisco 输出可能每条记录跨 2-3 行，用 ``-> Record`` 标记累积记录"
- 间接帮，不保证成功；对 Junos BGP 这种特殊格式效果仍不确定

**方案 D（ARCH-24 副作用路径）：绕过 TextFSM，让 L3 ETL LLM 直接吃 raw_output**
- 当前 L3 ETL 只吃 `parsed_data`；改成可选接受 raw_output 作为 fallback
- LLM 从 raw 直接写 extractor，跳过 TextFSM 层
- 架构更激进，写 extractor 比写 TextFSM 容易（LLM 更擅长 Python 正则 + 状态机），但放弃了 ntc-templates 的规范化收益

**建议**：先落地方案 A（手写一个 Junos BGP 模板），把 R1 数据救回来；方案 C 作为 AUTO-LEARN-PERF 的子项单独跟进；方案 D 作为 ARCH-24 的 P2 延伸（"raw fallback 路径"），让 LLM ETL 对 ntc-templates 空白地带也有话可说。

**验证步骤（方案 A）**：
1. 写 `.olav/templates/juniper_junos/show_bgp_summary.textfsm`（从 ntc-templates 模板派生 + 修 `Up\/(Down|Dwn)` + 加 BGP_INET 辅助 state 累积数字）
2. 单元测试：`parse_with_custom_template(dir, 'juniper_junos', 'show bgp summary', r1_raw)` 应返回 2 条 entry
3. DROP SCHEMA + --resume 重跑：`parsed_outputs.show bgp summary` for R1 应 len=2
4. Stage 3.6 应看到 `🎓 L3 ETL: learning bgp/juniper_junos` + 冻结
5. `bgp_sessions` 新增 R1 的 2 条 session（R1 → 3.3.3.3 / AS65000 + R1 → 10.1.12.2 / AS65001）

---

### ISSUE-R67: Web 登录流 + demo 部署元数据对齐（2026-04-21 demo run）

**状态**: ✅ Closed (Round 67)
**发现日期**: 2026-04-21（Demo Runsheet Chapter 1 fresh-install 回归）

Chapter 1 执行过程暴露 3 个 Web auth 缺陷 + 2 个文档/元数据不一致：

| # | 问题 | 修复 | 文件 |
|---|---|---|---|
| 1 | 登录页 `<h2>&#9632; OLAV</h2>` 渲染为 ■ 方块（placeholder 未换 logo）| 改为 `⚡ OLAV` 与 `index.html:154` 品牌一致 | `src/olav/api/static/login.html:24` |
| 2 | `_require_auth()` 构造 **空 Request** (`Request({"type": "http", "headers": []})`) 来读 Bearer → Bearer / cookie / 无凭证一律 401。所有需要鉴权的 API (`POST /threads`, `GET /threads/search`, `POST /threads/{id}/runs/stream`, `POST /runs/stream`) 彻底不可用 | 重写为 FastAPI `Depends(_bearer_scheme)` + `Request`；先读 `Authorization: Bearer`，fallback 到 `olav_session` cookie；4 处 endpoint signature 切换到 `Depends(_require_auth)` | `src/olav/api/server.py:64-115` + 4 endpoints |
| 3 | `GET /` 只检查 cookie **存在**，不验证 **有效性**。用户使用 pre-wipe 实例的陈旧 cookie 访问 → SPA 正常渲染 → 每次 API 调用 401 → UI 看上去"发不出消息"但无任何登录提示 | 在 `/` 处理器中调用 auth provider 验证 cookie；无效则 302 `/login` + `delete_cookie` | `src/olav/api/server.py:279-303` |
| 4 | 运行手册 Step 4 提示 "`embedding.mode=api` + OpenRouter → semantic routing 401"，但实测 OpenRouter `/embeddings` 完全可用（model 加 `openai/` 前缀、`base_url=https://openrouter.ai/api/v1`）| 删除过时警告；说明改为 OpenRouter 正确使用方式 | `dev_docs/42. DEMO_RUNSHEET.md:122,127` |
| 5 | `olav-netops/workspace.yaml` `version: "0.17.2"` 陈旧（未被 git tracking），与 `pyproject.toml` (0.19.0) + `__init__.py` (0.19.0) 不一致 → `olav skill install` 报告 `netops v0.17.2` | 对齐到 `version: "0.19.0"` | `olav-netops/workspace.yaml:2` |

**Playwright 端到端回归**（`192.168.100.50:2280`）：
- `302 ?token= → set cookie → 200 GET /` ✅
- `POST /threads → 200` ✅
- `POST /threads/{id}/runs/stream → 200` (SSE) ✅
- 页面显示 AI 回复："Hello. How can I help with infrastructure data queries, topology analysis, or OLAV platform tasks?"
- 0 console errors / 0 page errors

**发现路径**：用户跑 Chapter 1 → `GET /` 看到 ■ 方块 → 尝试发消息 → 8+ 次 `POST /threads 401` 于 `web.log` 显露 → `_require_auth` fake-Request 代码路径定位 → 修复后 playwright 仍观察到用户的浏览器 401，推断陈旧 cookie → `/` 端缺 cookie 有效性校验 → 补齐。

**副作用**：
- `olav-netops/pyproject.toml` 已包含 `version = "0.19.0"` bump（modified but not committed — M in git status）；depends `olav>=0.19.0`
- 本地 wheel `/home/yhvh/Olav/dist/olav-0.19.0-py3-none-any.whl` 为 fix #2 之后、fix #3 之前打包 → 如重新 wipe+install 需重打包以固化 fix #3

**未 pin 的 follow-up**：
- 上游：`tests/governance/` 下缺 Web auth 正向测试（stale cookie → /login、bearer & cookie 双路径 → 200、ni no auth → 401）。本轮修复未添加 governance pin。

---

### ISSUE-R63: Round 63 — 6 运行时 + 清洁度修复（T2 run-5~10 暴露）

**状态**: ✅ Closed (Round 63)

**六项修复**（都在本轮 ship + pin）:

| # | 问题 | 文件 | Pin |
|---|---|---|---|
| 1 | `olav-netops/.olav/workspace/ops/netops_init/run.py` 陈旧 `from olav.core.topology_engine` import（ARCH-20 Phase 3 之后没同步）| 从 canonical 路径 `.olav/workspace/ops/netops_init/run.py` 同步 | `test_netops_init_standalone_load.py::test_packaged_run_py_matches_canonical_import_guards` |
| 2 | `run.py` `from .checkpoint import ...` 在 `spec_from_file_location` 加载下 `ImportError: attempted relative import with no known parent package` | try/except ImportError + sys.path fallback at lines 287 and 787 | `test_netops_init_standalone_load.py::test_canonical_run_py_loads_standalone` |
| 3 | `LLMFactory.get_chat_model()` 当 config `model_provider` 为空时，把 `x-ai/grok-4.1-fast` 直接传给 langchain `init_chat_model` → `ValueError: Unable to infer model provider` | `src/olav/core/llm.py` 加 base_url → provider 自动映射（openrouter / together / groq / deepseek / perplexity）| `test_llm_provider_autodetect.py` (3 tests) |
| 4 | `tests/ci/tier2_integration.sh` `OLAV_DEV_CONFIG=<dir>` 走 fallback 分支生成极简 api.json（缺 `model_provider`）| 脚本自动识别目录，auto-resolve 到 `<dir>/api.json`；T2-07 / T2-11 输出捕获到 test_dir | — |
| 5 | `olav-netops/.olav/workspace/ops/` 下 Sprint-3 前陈旧目录 `probe/` `analysis/` `diff/`（已被 R32 `collect/` + R17 `analyze/` 取代）| 删除 3 个陈旧目录 + 从 canonical 同步 `collect/` `analyze/` `services/` `audit/` 到 `olav-netops/.olav/workspace/` | — |
| 6 | `olav-doc/docs/netops/quick-agent/` v0.15 之后被 Core Agent 合并，但文件仍在磁盘；`claim-registry.{en,zh}.md` 10 处 + `quick-start/index.{en,zh}.md` 2 处指向该死目录 | 目录删 + 12 处坏链重写到 `core-agent/index.md` | — |

**Governance**：Round 63 新增 2 个测试文件，5 条 pin：
- `tests/governance/test_llm_provider_autodetect.py` (3 tests)
- `tests/governance/test_netops_init_standalone_load.py` (2 tests)
- Governance 总数 674 → **679 passed, 1 skipped, 0 failed**

**T0 B11** 独立 trivial 修复：`tests/ci/tier0_packaging.sh` 中 `ops/probe/config/nornir/` 路径过时（R32 已改名）→ sed rename `probe`→`collect`。

**验证**:
- T2 run-10 Group 1 (T2-01~06) + Group 2 (T2-07~10) + T2-11 全绿（run-5 的 5 FAIL 清零）
- T3 run-1 **13/13 PASS**
- T4 run-1 **29/29 PASS**
- Ownership scan 24422 paths / **0 ERROR**

**为什么不是 ARCH-XX**：都是 T2 执行过程暴露的 runtime regression + 部署 hygiene，不是架构设计级 issue。合并为 R63 条目便于追溯。

---

### ISSUE-WRITER-01: T2-14 工具调用计数包含 delegate 内部调用

**状态**: ✅ Closed (Round 39) — (a) origin tag + (b) threshold 双修齐活

**Round 39 修复 (a)**：`src/olav/cli/main.py` `on_tool_start` event 加 `_delegate_depth` 嵌套计数器：
- `_DELEGATE_TOOLS = {"olav_delegate", "task"}` — 两个 delegation 入口都计数
- 输出格式 `🔧[orch] <tool>(...)` / `🔧[sub] <tool>(...)` — 区分顶层 vs subagent 内部
- `tests/ci/tier2_integration.sh` T2-14 grep 改 `🔧\[orch\]`，threshold 从 ≤8 回归 ≤5（writer arch 前 baseline）
- `tests/governance/test_round39_origin_tag_and_describe_table.py` 5 条 WRITER-01 (a) pin + `test_writer_01_threshold.py` 更新为 Round 39 状态（5 条全绿）

**Round 28 修复 (b)**（已被 Round 39 的 (a) 方案 supersede）：曾将 threshold 从 3 上调到 8 作过渡方案；Round 39 origin tag 上线后回归 5。

**发现日期**: 2026-04-16

T2-14 统计 `🔧` 总数（包括 olav_delegate 内部的 writer/subagent 工具调用）。Writer delegation 导致计数从 5 → 16。

**修复:** T2-14 只统计 orchestrator 直接的工具调用，排除 delegate 内部。或调整阈值 ≤3 → ≤8。

---

## 已关闭（rev 155 新增 —— 核实完成）

### ISSUE-RAW-01: Topology ETL raw fallback ✅

**关闭日期:** 2026-04-17（rev 155 核实 —— 实际在 2026-04-17 commit bc68852 就已完成）
**证据:**
- commit bc68852 `fix: RAW-01/02 — topology ETL raw fallback + execute_sql raw hints`
- `src/olav/core/topology_engine.py:162-211` 加 raw fallback 逻辑 + Junos/IOS 正则提取
- `tests/unit/test_raw_fallback.py::TestTopologyRawFallback` 3 tests PASS

### ISSUE-RAW-02: execute_sql 引导 raw_output_store ✅

**关闭日期:** 2026-04-17（同 commit bc68852）
**证据:**
- `.olav/workspace/core/tools/execute_sql.py:288, 523` docstring 含 raw_output_store + fallback 示例
- `src/olav/data/workspace/core/tools/execute_sql.py:490-491` 同步更新
- `tests/unit/test_raw_fallback.py::TestQueryFallback` 3 tests PASS

### ISSUE-RAW-03: Junos TextFSM 中期方案 ✅（长期方案仍 open）

**关闭日期:** 2026-04-17（中期方案：auto_learn Stage 3.5）
**证据:**
- 5 commit: `1c5e613 feat: auto_learn module` → `a0d1544 integrate Stage 3.5` → `868f00a ReAct pattern` → `54c6b44 closed loop` → `56e5c5c ReAct reflection`
- `src/olav/core/auto_learn.py::auto_learn_failed_parses`
- `.olav/workspace/ops/netops_init/run.py:381-387` 整合调用
- **长期方案（ntc-templates PR / OpenConfig）仍 open，但已不在 OLAV 代码库责任域内** —— 作为上游 dependency 议题

### ISSUE-RAW-04: raw_output_store UPSERT 验证 ✅

**关闭日期:** 2026-04-17（同 commit bc68852）
**证据:**
- `tests/unit/test_raw_fallback.py::TestRawUpsert` 2 tests PASS:
  - `test_upsert_overwrites_same_device_command`
  - `test_upsert_does_not_grow`（二次 init 后行数稳定）

---

## 已缓解

### ISSUE-P1-DEV-ENV-MASKS-PRODUCTION-BUGS

**状态**: Mitigated — CI 5 层全量 0 FAIL，`scripts/release.sh` 强制 Tier 0 验证
**发现日期**: 2026-04-13

---

## 加速系统总览

| 机制 | 状态 | 说明 |
|---|:---:|---|
| Semantic Cache | ✅ | cache get/put + 数据变更自动 invalidate |
| AutoCapture (Memory) | ✅ | middleware await，每次查询自动提取 fact |
| AutoRecall | ✅ | middleware before_model 自动注入相关记忆 |
| Semantic Router | ✅ | LanceDB index，首次查询时创建 |
| Embedding 自动检测 | ✅ | `detect_embedding_dim()` 探针，进程级缓存 |
| Query Cache TTL | ✅ | 24h 过期 + IngestManager 变更失效 |
| FTS 全文搜索 | ✅ | BM25 + rebuild threshold |
| Long-term Memory | ✅ | LanceDB unified store |
| Writer Subagent | ✅ | MVC 输出引擎，7 种报告模板 |
| OutputFormatterPlugin | ✅ | 确定性后处理（safety net） |

---

## 已关闭 Issues 汇总

### v0.18.0-post（12 个修复 + writer 架构 + 依赖精简）

| 类别 | 数量 | 代表性问题 |
|---|:---:|---|
| 输出架构 | 3 | writer subagent (MVC)；OutputFormatterPlugin；report_type references (7 种) |
| safetensors | 1 | fd1+fd2 redirect (#2) |
| CLI/UX | 2 | trajectory Rich markup (#3)；T1-21b→SKIP |
| LLM 行为 | 2 | Output Rules (#4)；execute_sql docstring (#7) |
| 代码清理 | 2 | 808 行垃圾代码删除；disabled 文件清理 |
| 依赖精简 | 1 | 43→33 core deps；10 deps → optional groups |
| CI | 1 | T1-53 TUI echo pipe test |

### v0.18.0（19 个 bug 修复 + core subagent 架构）

| 类别 | 数量 | 代表性问题 |
|---|:---:|---|
| 架构重构 | 2 | core 15-tool → 5 subagents；run_python_code 移除 |
| execute_sql | 3 | syntax error；schema hints 动态化；self→context fix |
| 路由器 | 2 | index count 公式；LanceDB `.tables` API |
| 服务/平台 | 4 | service refs → core；remote_execute；service_health；registry output_dir |
| netops | 4 | CommandRegistry；dead code；silent failures；env_hint |
| 文档 | 4 | DEMO_RUNSHEET；SessionState；claim registry；olav-doc |

### v0.17.0（28 个 bug 修复 + 13 新功能）

*(详见 git log)*

### v0.15.0（14 个）、v0.14.x（55 个）、v0.13.0（28 个）、v0.10~0.12（92 个）

*(详见 git log)*
