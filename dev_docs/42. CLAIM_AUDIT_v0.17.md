# 42. Claim Registry Audit — v0.17.0 Fresh Venv Validation

**Date:** 2026-04-13
**Methodology:** Every claim below was tested in a **completely fresh venv** installed from wheel (not editable install). Claims that passed only in the development environment are marked as failed.

---

## Failed Claims (claimed ✅ but broken in fresh env)

### Platform Core

| ID | Claim | Failure | Fixed in |
|---|---|---|---|
| C-L2-04 | Install a Skill from a local directory | Single workspace only; multi-workspace skill (olav-netops) installed only first workspace | v0.15.2 |
| C-L2-13 | Initialize project directory structure | `system.md not found` (path hardcoded "olav" instead of "core"); LLM error message unfriendly | v0.15.1 |
| C-L2-14 | TUI interactive mode | `deepagents-cli` not in deps → ImportError; 0.0.37 API incompatible with code | v0.17.0 |
| C-L2-16 | Web service provides browser UI | `secure=True` cookie blocks HTTP login → infinite redirect loop; `auth.mode=ture` typo → 500 TypeError | v0.15.1 |
| C-L2-18 | Syslog receiver | `Address already in use` on restart (no SO_REUSEADDR) | v0.15.1 |
| C-L2-37 | Skills auto-create isolated venv | Skill install doesn't overwrite existing files on re-install | v0.15.7 |

### NetOps Extension

| ID | Claim | Failure | Fixed in |
|---|---|---|---|
| C-NE-01 | `/netops_init --dry-run` passes | `shell_runner.py` used system python (not venv) → `No module named 'nornir'` | v0.15.3 |
| C-NE-03 | `/netops_init` populates parsed_outputs | Works, but `take_snapshot` wrote wrong filename → data never ingested by IngestManager | v0.15.7 |
| C-NE-04 | `/netops_init` generates topology links | `topology_links` table never created (`_register_all()` only registers in memory, `ensure_schema()` never called) | v0.15.4 |
| C-NE-08 | `netops.devices` contains hostname, ip_address | **Table always empty** — no Device ETL existed anywhere in codebase | v0.16.2 |
| C-NE-09 | `v_bgp_neighbors_auto` auto-generated | **View never created** — `execute_sql` referenced 7 phantom views that don't exist | v0.16.4 (removed references) |
| C-NE-15 | Natural language query returns results | Agent queried non-existent views → multi-step fallback → 12 tool calls for simple query | v0.16.4 |

### Knowledge Store

| ID | Claim | Failure | Fixed in |
|---|---|---|---|
| C-KB-05 | capture writes `origin="agent"` | `MemoryCapturePlugin.aafter_agent` used `asyncio.create_task` → fire-and-forget → process exited before completion → 0 memories captured | v0.16.4 |

---

## Narrowed Claims (work but with caveats)

| ID | Original Claim | Actual Behavior | Recommended Narrowing |
|---|---|---|---|
| C-NE-06 | Custom TextFSM templates take priority | `command_registry.py` ntc path wrong → loaded 0 templates | Fixed path; claim valid after fix |
| C-NE-12 | `nornir/hosts.yaml` defines device inventory | `hosts.yaml.example` had wrong group names → KeyError; only 2 devices instead of 6 | Fixed example; claim valid |
| C-L2-24 | `/trace-review` analyzes failure patterns | Old TUI code removed in v0.17.0 migration; needs re-implementation as skill | ⚠️ Narrow to "available in TUI mode only" |
| C-L2-46 | `/model` switches LLM within session | Wired via OLAV `SLASH_COMMANDS` registry (separate from deepagents-cli). Verified in `dev_docs/24. CLI_VERIFICATION_LOG.md` 2026-04-03. | ✅ Verified v0.17.0 |
| C-L2-47 | `@file.txt` file content injection | Wired in `src/olav/cli/input_parser.py` → `run_single_query()` since v0.17.0. T4-29 PASS. | ✅ Verified v0.17.0 |
| C-L2-48 | `!command` shell passthrough | Wired in `src/olav/cli/input_parser.py` → `run_single_query()` since v0.17.0. T4-28 PASS. | ✅ Verified v0.17.0 |

---

## New Capabilities (not in original registry)

| ID | Capability | Added in |
|---|---|---|
| C-L2-52 | `olav init` auto-downloads embedding model | v0.16.2 |
| C-L2-53 | `olav skill install` supports multi-workspace (`workspaces:` list in workspace.yaml) | v0.15.2 |
| C-L2-54 | `olav skill install` auto-runs `pip install -e` when `pyproject.toml` present | v0.15.2 |
| C-L2-55 | `POST /reload` hot-reloads agent after skill install | v0.15.1 |
| C-L2-56 | `GET /agents` returns dynamic agent list from workspace registry | v0.15.1 |
| C-L2-57 | `GET /memory/graph` serves knowledge graph visualization | v0.15.1 |
| C-L2-58 | CIDR allowlist middleware for web API | v0.15.1 |
| C-L2-59 | Semantic cache with auto-invalidation on data ingest | v0.17.0 |
| C-L2-60 | Embedding dimension auto-detected from model (no hardcoding) | v0.16.1 |
| C-NE-45 | Platform-aware SSH collection (Junos + IOS command lists) | v0.15.4 |
| C-NE-46 | Device ETL extracts hostname/IP/platform/vendor/model from parsed_outputs | v0.16.2 |
| C-NE-47 | `netops_init --dry-run` validates full Nornir inventory (group references) | v0.15.3 |

---

## Verification Gap Analysis

**118 claims marked ✅ in the registry.** Of these:

- **12 claims** failed in fresh venv testing (10% failure rate)
- **6 claims** need narrowing due to TUI migration
- **12 new capabilities** added but not yet in registry

**Root cause of false ✅:** All 118 claims were verified in the **development environment** using `pip install -e .` with accumulated state. None were verified from a fresh wheel install.

**Recommended actions:**

1. Update claim registry: downgrade 12 failed claims to ⚠️ or re-verify after fix
2. Add 12 new claims
3. Narrow 6 TUI-dependent claims
4. Add "Verified From" column: `dev` (editable) vs `wheel` (fresh venv)
5. Require all future claims to pass fresh venv verification before marking ✅
