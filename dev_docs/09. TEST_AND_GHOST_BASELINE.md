# 09. Test and Ghost Baseline (Phase 0)

**Status:** Active baseline  
**Version:** v0.1 (2026-03-20)  
**Depends on:** `dev_docs/00. issues.md`, `dev_docs/06-12` chain

---

## 1. Purpose

This file is the Phase 0 authoritative baseline to prevent development planning from being misled by stale tests, stale docs, and ghost assumptions.

It records:

1. which artifacts are authoritative
2. which artifacts are legacy or historical
3. which tests are current gates versus regression/historical checks
4. what must be cleaned before Phase 1 can start

---

## 2. Current Authoritative Design Chain

The following docs are the current authoritative chain:

1. `dev_docs/10. OPENCONFIG_SCHEMA_DESIGN.md`
2. `dev_docs/08. LEGACY_CUT_LINE.md`
3. `dev_docs/11. NETWORKX_SANDBOX_DESIGN.md`
4. `dev_docs/17. CAB_CHANGE_VALIDATION_LOOP.md`
5. `dev_docs/12. CONTROL_PLANE_SEMANTIC_ENGINE.md`
6. `dev_docs/05. PLATFORM_NETOPS_OWNERSHIP_MAP.md`
7. `dev_docs/02. milestones.md`

---

## 3. Legacy / Historical Artifacts (Do Not Drive New Plan)

### 3.1 Legacy Docs

| Artifact | Classification | Action |
|---|---|---|
| `dev_docs/old_07. OPENCONFIG_SCHEMA_DESIGN.md` | historical | keep for archive reference only; do not use for planning |
| old CLAB_CAB_AGENT_DESIGN draft (archived) | historical | keep for archive reference only; do not use for planning |

### 3.2 Historical Archive

| Artifact | Classification | Action |
|---|---|---|
| `dev_doc_archive/**` | historical archive | not authoritative for current phase planning |
| `_legacy_archived/**` | historical archive | not authoritative for current phase planning |

---

## 4. Known Stale Narrative Risk

The following document contains legacy schema narrative (`schema_mappings`, `standard_name`) that can conflict with current `schema_catalog + semantic views` direction if used without context:

| Artifact | Risk | Action |
|---|---|---|
| `dev_docs/api_discovery.md` | legacy naming/mapping narrative may conflict with current 07/08 contracts | treat as topic design reference, not current runtime authority; align in later cleanup phase |

Note:

- `tests/e2e/test_phase4_nl_acceptance.py` references `schema_mappings` as a **negative assertion** (table must not exist). This is valid and should not be removed blindly.

---

## 5. Test Baseline Classification

### 5.1 Current E2E Regression Set (Valid but Historical Scope)

| Test | Classification | Current Role |
|---|---|---|
| `tests/e2e/test_phase1_collection.py` | valid regression | historical OpenConfig sprint gate |
| `tests/e2e/test_phase2_normalization.py` | valid regression | historical OpenConfig sprint gate |
| `tests/e2e/test_phase3_topology.py` | valid regression | historical OpenConfig sprint gate |
| `tests/e2e/test_phase4_nl_acceptance.py` | valid regression | historical OpenConfig sprint gate + ghost guard |

These tests remain useful, but they are not alone sufficient as the new 07-12 phase gates.

### 5.2 New-Phase Gate Principle

For each new phase in `dev_docs/02. milestones.md`, completion requires all of the following:

1. unit tests for new or changed logic
2. real-device E2E aligned to current phase scope
3. real-LLM-API E2E aligned to current phase scope
4. evidence verification records

---

## 6. Phase 0 Deliverables Checklist

- [x] Authoritative design chain identified
- [x] Legacy/historical artifacts classified
- [x] Stale narrative risk items identified
- [x] Current E2E regression set classified
- [x] Phase-specific gate test mapping finalized (Phase 1 input)
- [x] Cleanup ownership assigned for stale narrative docs

---

## 7. Exit Criteria for Phase 0

Phase 0 can be closed only when:

1. this baseline file is accepted as current reference
2. `dev_docs/00. issues.md` active blocker `PHASE0-BASELINE-1` is closed
3. phase-specific gate mapping for Phase 1 is written and reviewed
4. no unresolved high-risk stale artifact remains unclassified

---

## 8. Notes

This file is a living baseline for Phase 0 and early Phase 1.

Any newly discovered stale test, stale document, or ghost assumption must be appended here first before it can influence planning decisions.

---

## 9. Phase 1 Gate Test Mapping (Finalized)

This mapping is the authoritative Phase 1 gate input.

### 9.1 Unit Gate (Contract Logic)

| Scope | Test File | Gate Intent |
|---|---|---|
| Query contract execution | `tests/unit/test_query_runner.py` | Ensure query contract execution path is stable |
| Query agent contract understanding | `tests/unit/test_query_agent_oc.py` | Ensure query pipeline consumes normalized contract |
| Schema authority behavior | `tests/unit/test_schema_engine_phase1.py` | Ensure schema processing follows current phase constraints |
| Mapping compatibility behavior | `tests/unit/test_build_mapping_rules.py` | Ensure mapping compatibility remains compatibility-only behavior |

Baseline evidence (2026-03-20):

1. `uv run pytest -q tests/unit/test_query_runner.py tests/unit/test_query_agent_oc.py tests/unit/test_schema_engine_phase1.py tests/unit/test_build_mapping_rules.py`
2. Result: `45 passed in 15.76s`

Phase 1 gate execution evidence (2026-03-21):

1. `uv run pytest -q tests/unit/test_query_runner.py tests/unit/test_query_agent_oc.py tests/unit/test_schema_engine_phase1.py tests/unit/test_build_mapping_rules.py`
2. Result: `84 passed in 18.02s`

### 9.2 Real-Device E2E Gate (Data Reality)

| Scope | Test File | Gate Intent |
|---|---|---|
| Collection reality | `tests/e2e/test_phase1_collection.py` | Real-device collection path remains valid |
| Normalization reality | `tests/e2e/test_phase2_normalization.py` | Real-device parsed data matches normalization expectations |

Required evidence checks:

1. `ls exports/snapshots/{date}/raw/{device}/`
2. `ls exports/snapshots/json/*.json`
3. `duckdb .olav/databases/main.duckdb "SELECT COUNT(*) FROM parsed_outputs"`
4. `duckdb .olav/databases/main.duckdb "SELECT COUNT(*) FROM topology_links"`

Phase 1 gate execution evidence (2026-03-21):

1. `uv run pytest -q tests/e2e/test_phase1_collection.py tests/e2e/test_phase2_normalization.py`
2. Result: `28 passed in 2.95s`

### 9.3 Real-LLM-API E2E Gate (NL Contract)

| Scope | Test File | Gate Intent |
|---|---|---|
| NL query acceptance | `tests/e2e/test_phase4_nl_acceptance.py` | Real LLM query path must follow canonical contract |

Required constraints:

1. real API key configured (no mocked LLM response)
2. assertions must verify structured outputs, not only plain text fragments
3. no legacy-schema hallucination acceptance

Phase 1 gate execution evidence (2026-03-21):

1. `uv run pytest -q tests/e2e/test_phase4_nl_acceptance.py`
2. Result: `47 passed in 28.96s`

---

## 10. Stale Narrative Cleanup Ownership and Schedule

| Artifact | Risk | Owner Role | Planned Phase | Exit Action |
|---|---|---|---|---|
| `dev_docs/api_discovery.md` | legacy `schema_mappings/standard_name` narrative can mislead contract direction | Platform schema maintainer | Phase 1 | rewrite terminology to `schema_catalog + semantic views`, keep legacy terms in explicit compatibility note only (completed 2026-03-20) |
| `dev_docs/old_07. OPENCONFIG_SCHEMA_DESIGN.md` | historical doc may be confused as current authority | Documentation maintainer | Phase 1 | add clear historical banner and archive pointer (completed 2026-03-20) |
| old CLAB_CAB_AGENT_DESIGN draft (archived) | old draft may conflict with current CLAB design | Documentation maintainer | Phase 1 | add clear historical banner and archive pointer (completed 2026-03-20) |

Execution policy:

1. no Phase 1 completion without all three exit actions finished
2. any newly found stale narrative must be appended to this table first

---

## 11. Phase 1 Test Audit Report (2026-03-21)

**Auditor:** AI agent (OLAV Team orchestrator + 2× explore agents)  
**Baseline doc:** `dev_docs/10. OPENCONFIG_SCHEMA_DESIGN.md` v0.12.5-redraft  
**Method:** Every test function in all 7 Phase 1 gate files was read, and its assertions compared against doc 07 target contracts.

### 11.1 Classification Legend

| Tag | Meaning | Phase 1 Evidence? |
|-----|---------|-------------------|
| **VALID** | Assertions align with doc 07 target contracts | ✅ Yes |
| **NEEDS-REVIEW** | Not contradictory but tests current-state implementation, generic utility, or lab fixture — not doc 07 contract proof | ⚠️ Keep for regression, not as architecture evidence |
| **STALE** | Assertions directly contradict doc 07 target contracts | ❌ No — quarantine before Phase 2 |

### 11.2 Summary Counts

| File | VALID | NEEDS-REVIEW | STALE | Total (pytest) |
|------|-------|--------------|-------|----------------|
| `tests/unit/test_query_runner.py` | 3 | 42 | 0 | 45 |
| `tests/unit/test_query_agent_oc.py` | 2 | 7 | 1 | 10 |
| `tests/unit/test_schema_engine_phase1.py` | 0 | 11 | 2 | 13 |
| `tests/unit/test_build_mapping_rules.py` | 9 | 7 | 0 | 16 |
| `tests/e2e/test_phase1_collection.py` | 2 | 3 | 1 | 6 |
| `tests/e2e/test_phase2_normalization.py` | 8 | 12 | 2 | 22 |
| `tests/e2e/test_phase4_nl_acceptance.py` | 25 | 19 | 3 | 47 |
| **TOTALS** | **49** | **101** | **9** | **159** |

*Note: pytest collects 159 tests total. Classification counts are approximate for NEEDS-REVIEW/VALID split in E2E files where some tests were added after initial audit read. All named STALE tests (9) are exact.*

### 11.3 STALE Tests (Must Quarantine Before Phase 2)

These 9 tests encode assertions that directly contradict doc 07 target contracts.

| # | File | Test Function | Line | Contradiction |
|---|------|---------------|------|---------------|
| S1 | `test_query_agent_oc.py` | `test_reference_documents_oc_parsed_outputs_pattern` | — | Asserts `parsed_outputs` contains OpenConfig-structured JSON; doc 07 §3.2 says raw snapshot stores vendor TextFSM fields |
| S2 | `test_schema_engine_phase1.py` | `test_save_mapping_builds_upsert_request` | — | Stages `upsert_mapping` into `mapping_rules` as write target; doc 07 §6.4 says `schema_catalog` is authoritative, `mapping_rules` is derived |
| S3 | `test_schema_engine_phase1.py` | `test_build_mutation_request_importable` | — | Constructs mutation request targeting `mapping_rules` as active write destination |
| S4 | `test_phase1_collection.py` | `test_r2_has_netconf_domains` | — | Asserts R2 must have 4 `netconf_*` command rows; doc 07 is CLI-first, NETCONF domain presence is not a gate |
| S5 | `test_phase2_normalization.py` | `test_apply_oc_mapping_integration_remaps_real_data` | ~189 | Tests `apply_oc_mapping()` as active pipeline; doc 07 marks this as legacy |
| S6 | `test_phase2_normalization.py` | `test_parsed_outputs_latest_snapshot_has_oc_keys` | ~258 | Asserts `parsed_outputs.parsed_data` has `openconfig-*` top-level keys; contradicts raw snapshot SSOT |
| S7 | `test_phase4_nl_acceptance.py` | `test_p4_2_mapping_rules_is_ground_truth` | ~924 | Explicitly asserts "mapping_rules is ground truth"; doc 07 §6.4 says `schema_catalog` is authoritative |
| S8 | `test_phase4_nl_acceptance.py` | `test_s3_topology_protocol_to_link_type_consistency` | — | Asserts OSPF/BGP ⇒ `link_type='L3'`; doc 07 classifies BGP under logical peering, not L3 adjacency |
| S9 | `test_phase4_nl_acceptance.py` | `test_s4_r2_protocol_to_link_type_consistency` | — | Same BGP/L3 misclassification as S8 |

**Action:** These tests must NOT be used as Phase 1 gate evidence. They should be quarantined (marked `xfail` or moved) during Phase 2 cleanup. Per the principle "清理是阶段内义务", they belong to Phase 2/3 cleanup scope, not Phase 1.

### 11.4 VALID Tests — Phase 1 Evidence Set

These 45 tests have assertions aligned with doc 07 target contracts and can serve as Phase 1 gate evidence.

#### Unit: `test_query_runner.py` (3 VALID)

- `test_build_messages_prefers_summary_topology_views` — prompt prefers semantic topology views (doc 07 query contract)
- `test_build_messages_contains_primary_loopback_guidance` — prompt guides loopback queries toward semantic shape
- `test_build_messages_enforce_semantic_view_priority` — prompt enforces "semantic views first, `mapping_rules` not default target"

#### Unit: `test_query_agent_oc.py` (2 VALID)

- `test_reference_does_not_use_parsed_outputs_snapshot_for_views` — forbids cross-table `MAX(snapshot_id)` from `parsed_outputs` in views
- `test_reference_prefers_same_view_snapshot_examples` — snapshot examples use each view's own max

#### Unit: `test_build_mapping_rules.py` (9 VALID)

- `test_build_mapping_rules_admin_status_matches_interface_path` — correct YANG-first canonical mapping
- `test_build_mapping_rules_oc_paths_exist_in_yang_leaves` — every `oc_path` derives from `yang_leaves` (doc 07 §6.4 derivation direction)
- `test_lldp_command_fields_map_to_lldp_module` — domain-correct LLDP derivation
- `test_lldp_filtering_does_not_affect_bgp_commands` — BGP commands map to BGP paths
- `test_cdp_command_also_filtered_to_lldp_module` — CDP → LLDP/neighbor semantic mapping
- `test_lldp_field_alias_disambiguation` — exact neighbor/local LLDP leaf mapping
- `test_interface_ip_address_maps_to_openconfig_interface_ip` — YANG-first interface IP mapping
- `test_bgp_state_maps_to_session_state` — canonical BGP state mapping
- `test_bgp_peer_as_maps_to_peer_as` — canonical BGP peer-as mapping

#### E2E: `test_phase1_collection.py` (2 VALID)

- `test_all_devices_have_parsed_outputs` — raw snapshot SSOT existence
- `test_latest_snapshot_is_fresh_enough` — operational freshness

#### E2E: `test_phase2_normalization.py` (8 VALID)

- `test_yang_leaves_table_exists` — canonical YANG bootstrap asset
- `test_yang_leaves_has_rows` — YANG populated
- `test_yang_leaves_interface_paths_present` — interface YANG paths present
- `test_yang_leaves_has_required_columns` — canonical metadata schema
- `test_mapping_rules_reference_valid_yang_paths` — derivation integrity (`mapping_rules.oc_path` ⊆ `yang_leaves`)
- `test_parsed_outputs_table_exists` — raw snapshot SSOT
- `test_parsed_outputs_has_required_columns` — raw snapshot schema
- `test_mapping_rules_lldp_fields_map_to_lldp_module` — semantic correctness of derived compatibility mapping

#### E2E: `test_phase4_nl_acceptance.py` (21 VALID)

- `test_s1_all_lab_devices_have_interface_data` — raw snapshot input availability
- `test_s1_parsed_outputs_has_interface_commands` — raw source readiness
- `test_s1_loopback_data_reachable_via_parsed_outputs` — raw source readiness
- `test_s1_v_interfaces_supports_all_device_loopback_inventory` — semantic-view query contract
- `test_s1_latest_snapshot_loopback_query_shape_returns_all_devices` — semantic-view acceptance
- `test_s2_bgp_parsed_outputs_present` — raw source readiness
- `test_s2_bgp_neighbor_state_fields_queryable` — semantic-view canonical query
- `test_s3_topology_links_has_l2_entries` — topology as derived operational truth
- `test_s3_topology_no_self_loops` — topology contract
- `test_s3_l2_topology_links_have_complete_interfaces` — topology contract
- `test_s3_clean_topology_view_excludes_non_device_endpoints` — device-to-device topology contract
- `test_s3_latest_snapshot_lldp_edges_are_canonical_and_unique` — clean-view uniqueness
- `test_s3_l2_topology_summary_pairs_are_canonical_and_unique` — derived-view consistency
- `test_s3_l2_topology_summary_matches_clean_view_latest` — projection consistency
- `test_s4_r2_lldp_data_in_parsed_outputs` — raw source readiness
- `test_s4_r2_l2_links_have_complete_interfaces` — topology contract
- `test_s4_r2_clean_view_has_only_device_neighbors` — topology contract
- `test_s4_latest_snapshot_r2_neighbor_pairs_are_unique` — derived-view quality
- `test_s4_device_neighbors_summary_matches_clean_view_latest` — projection consistency
- `test_p4_2_yang_leaves_is_populated` — canonical YANG source
- `test_nl_loopback_ip_query` — NL semantic-view query acceptance
- ... (plus `test_nl_bgp_down_query`, `test_nl_l2_topology_query`, `test_nl_r2_neighbors_query` — 4 NL tests total)

*Note: 4 NL query tests are VALID but require real LLM API key to execute.*

### 11.5 NEEDS-REVIEW Tests (94 total)

These tests are not contradictory but serve as:
- **Generic utilities** (SQL parsing, formatting, error handling) — 42 in `test_query_runner.py`
- **Current-state implementation checks** (static schema reference, `mapping_rules` table shape) — ~20 across unit files
- **Lab fixture assertions** (exact row counts, specific device data) — ~15 in E2E files
- **Transitional compatibility checks** (`mapping_rules` existence/population) — ~17 across E2E files

**Recommendation:** Keep for regression value. Do NOT cite as doc 07 architecture evidence. Review individually during Phase 2/3 to decide: keep as-is, rewrite to test target contracts, or remove.

### 11.6 Key Audit Conclusions

1. **~49 of 159 tests (~31%) are safe Phase 1 evidence.** These directly validate doc 07 contracts: semantic-view-first querying, YANG-based canonical metadata, raw snapshot SSOT, and topology derivation.

2. **9 tests (6%) are STALE and must be quarantined.** The most dangerous are `test_p4_2_mapping_rules_is_ground_truth` (explicitly asserts the wrong authority) and `test_parsed_outputs_latest_snapshot_has_oc_keys` (asserts write-time OC rename on raw snapshot).

3. **~101 tests (~63%) are NEEDS-REVIEW.** The majority are generic utility tests in `test_query_runner.py` (42 tests) that validate SQL parsing and formatting — useful for regression but not architecture evidence.

4. **No test file is 100% clean.** Every file except `test_query_runner.py` contains at least one STALE or architecturally ambiguous test.

5. **The strongest Phase 1 evidence cluster** is the semantic-view query contract chain: `test_build_messages_enforce_semantic_view_priority` → `test_reference_prefers_same_view_snapshot_examples` → `test_s1_v_interfaces_supports_all_device_loopback_inventory` → `test_nl_loopback_ip_query`. This chain proves the doc 07 query contract from prompt construction through view access to NL acceptance.

6. **The YANG derivation chain is also strong:** `test_yang_leaves_*` → `test_build_mapping_rules_oc_paths_exist_in_yang_leaves` → `test_mapping_rules_reference_valid_yang_paths`. This proves `schema_catalog + yang_leaves → mapping_rules` derivation direction per doc 07 §6.4.

---

## 12. Phase 2 Audit Update (2026-03-21)

### 12.1 Test Quarantine Execution

12 tests quarantined with `@pytest.mark.xfail(reason="Phase 2 quarantine: contradicts doc 07/08 target contracts")`:

**Original 9 STALE tests (from §11.3):**

| # | File | Test | Contradiction |
|---|------|------|---------------|
| S1 | `test_query_agent_oc.py` | `test_reference_documents_oc_parsed_outputs_pattern` | Asserts OC `json_extract`/`->>` in SCHEMA_REFERENCE (raw ≠ OC) |
| S2 | `test_schema_engine_phase1.py` | `test_save_mapping_builds_upsert_request` | Assumes `mapping_rules` write path is authoritative |
| S3 | `test_schema_engine_phase1.py` | `test_build_mutation_request_importable` | Same as S2 |
| S4 | `test_phase1_collection.py` | `test_r2_has_netconf_domains` | Assumes OC-shaped parsed_outputs |
| S5 | `test_phase2_normalization.py` | `test_apply_oc_mapping_integration_remaps_real_data` | Tests OC rename on raw snapshot |
| S6 | `test_phase2_normalization.py` | `test_parsed_outputs_latest_snapshot_has_oc_keys` | Asserts OC keys in raw parsed_outputs |
| S7 | `test_phase4_nl_acceptance.py` | `test_p4_2_mapping_rules_is_ground_truth` | Explicitly asserts mapping_rules as ground truth |
| S8 | `test_phase4_nl_acceptance.py` | `test_s3_topology_protocol_to_link_type_consistency` | Assumes topology depends on mapping_rules |
| S9 | `test_phase4_nl_acceptance.py` | `test_s4_r2_protocol_to_link_type_consistency` | Same as S8 |

**3 additional tests exposed by SCHEMA_REFERENCE cleanup:**

| # | File | Test | Contradiction |
|---|------|------|---------------|
| S10 | `test_query_agent_oc.py` | `test_reference_contains_oper_status_example` | Asserts OC `oper-status` example in SCHEMA_REFERENCE (deleted) |
| S11 | `test_query_agent_oc.py` | `test_reference_contains_bgp_neighbor_path` | Asserts OC `neighbor-address` example (deleted) |
| S12 | `test_query_agent_oc.py` | `test_reference_contains_lldp_path` | Asserts OC `system-name` example (deleted) |

### 12.2 SCHEMA_REFERENCE Cleanup

Deleted stale OpenConfig section from `.olav/workspace/quick/references/SCHEMA_REFERENCE.md`:
- Removed "OpenConfig JSON Path Queries" header and all OC JSON path examples (lines 177-249)
- False claim removed: "Since v0.11.0, all data in parsed_outputs.parsed_data follows OpenConfig YANG structure"
- Preserved: all non-OC content including Common Mistakes, Schema Validation Rules, yang_leaves table reference

### 12.3 Doc 08 Code-Reality Annotations

Phase 2 code review found 3 §6 sections with stale descriptions vs actual code:

| Section | Doc 08 Claimed | Code Reality | Correction |
|---------|---------------|-------------|------------|
| §6.3 `schema_cache` | Needs migration to `schema_catalog` | Already reads `schema_catalog` (`schema_cache.py:77-79`) | Marked as already migrated |
| §6.5 `topology_engine` | Uses `parsed_outputs × mapping_rules` | No `mapping_rules` reference; reads `parsed_outputs` directly | Updated to reflect actual input chain |
| §6.6 `query_runner` | Must switch from mapping_rules | Prompt already says "never use mapping_rules by default" | Marked as partially switched |

All corrections recorded as Type A in doc 08 ADJ Log (#2-#4).

### 12.4 Phase 2 Gate Test Results

```
102 passed, 3 xfailed, 9 xpassed in 33.20s
```

- **0 failures** across 6 test files
- **3 xfailed**: S10-S12 (correctly fail after SCHEMA_REFERENCE OC content removal)
- **9 xpassed**: S1-S9 (contractually stale but still pass against current implementation)
- **102 passed**: All non-quarantined tests continue to pass

### 12.5 Remaining Test Gaps for Phase 3

From doc 08 §7.4 audit register:
- §7.2: 4/5 new test categories MISSING (raw vendor fields, raw fallback, topology ETL, render_oc)
- §7.3: 3/5 minimum acceptance tests MISSING, 2/5 partial
- 94 NEEDS-REVIEW tests from §11.5 still pending individual assessment

---

## 13. Phase 3 Gate Run Baseline (2026-03-21)

### 13.1 Scope

First execution of the new Phase 3 OpenConfig coverage gate, focused on
`ISSUE-P0-OPENCONFIG-COVERAGE` and `ISSUE-P0-PHASE-GATE-DRIFT`.

### 13.2 Tooling

New audit script:
- `scripts/audit_openconfig_coverage.py`

Purpose:
- measure mapping coverage from `schema_catalog.fields`
- enforce explicit gate thresholds
- emit machine-readable JSON evidence

### 13.3 Gate Command And Result

Command:

```bash
uv run python scripts/audit_openconfig_coverage.py --json-out tmp/openconfig_coverage_20260321.json
```

Result:

```text
OpenConfig Coverage Audit
DB: .olav/databases/main.duckdb
Coverage: fields 0/10482 (0.0%), platform-commands 0/938 (0.0%)
Thresholds: field>=90.0%, command>=95.0%
Gate: FAIL
```

### 13.4 Failure Attribution

Attribution: **code/data-contract gap**, not environment shortage.

Evidence:
1. `schema_catalog.fields` entries currently use `{name, type}` shape and do not carry
	`openconfig_path` mapping metadata
2. coverage audit returns 0 mapped fields and 0 mapped platform-commands
3. environment already has real-device and real-LLM capabilities; this failure is a
	runtime mapping-contract implementation gap

### 13.5 Evidence Files

1. `tmp/openconfig_coverage_20260321.json`

### 13.6 Recovery Implementation Update (2026-03-21)

Implemented first-batch metadata recovery for `schema_catalog`:

1. New script: `scripts/backfill_schema_catalog_from_mapping_rules.py`
2. Dry-run result:

```text
Rows: total=938, changed=850
Fields: total=10482, added=3859, already_mapped=0, missing_mapping=6623
```

3. Apply result (committed to DB):

```text
Rows: total=938, changed=850
Fields: total=10482, added=3859, already_mapped=0, missing_mapping=6623
```

4. Re-run coverage gate:

```text
OpenConfig Coverage Audit
Coverage: fields 3859/10482 (36.82%), platform-commands 850/938 (90.62%)
Thresholds: field>=90.0%, command>=95.0%
Gate: FAIL
```

Interpretation:
1. Recovery path is effective (coverage improved from 0.0% to 36.82%).
2. Gate remains blocked; additional mapping generation and contract cleanup are still required.

Additional evidence files:
1. `tmp/backfill_schema_catalog_20260321_dryrun.json`
2. `tmp/backfill_schema_catalog_20260321_apply.json`
3. `tmp/openconfig_coverage_20260321_after_backfill.json`

### 13.7 Gap Analysis and LLM Mapping Generation (2026-03-21)

Implemented gap analysis workflow:

1. New script: `scripts/report_openconfig_mapping_gaps.py`
2. Gap analysis result:

```text
Total gap fields: 6623
Commands with gaps: 879/938
Top gap commands:
  - cisco_nxos "show hardware internal bigsur all-ports detail": 163 missing fields
  - cisco_asa "show asp drop": 92 missing fields
  - brocade_netiron "show interfaces": 74 missing fields
  - brocade_fastiron "show interfaces": 74 missing fields
  - huawei_vrp "display lldp neighbor": 68 missing fields
```

LLM Mapping Generation:

1. New script: `scripts/generate_openconfig_mappings_with_llm.py`
2. First test execution (1 field):
   - Generated: 1 mapping
   - High confidence (>=0.7): 1
   - Target field: alcatel_aos "show chassis" → model_name
   - Generated path: `/openconfig-platform:components/component[name='*']/state/hardware-info/model`
   - Confidence: 0.9

Evidence files:
1. `tmp/openconfig_mapping_gaps_20260321.csv` (6623 rows)
2. `tmp/openconfig_mapping_gaps_20260321_summary.json`
3. `tmp/openconfig_mapping_generation_20260321_140048.json` (test run with 1 field)

### 13.8 Unit Test Baseline (2026-03-21)

Ran OpenConfig-related unit tests:

```text
tests/unit/test_schema_engine_oc.py: 9/9 PASSED
tests/unit/test_bootstrap_yang.py: 27/27 PASSED
tests/unit/test_build_mapping_rules.py: 9/9 PASSED
————————————————————————————————————————
Total: 45 PASSED
```

Results:
- All core OpenConfig schema transformation logic passes unit tests
- YANG leaf compilation, module parsing, mapping rule generation all validated
- No unit test failures detected

Evidence:
- Full test output in `pytest` run against `tests/unit/test_schema_engine_oc.py`, `tests/unit/test_bootstrap_yang.py`, `tests/unit/test_build_mapping_rules.py`

