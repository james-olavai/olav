{
  "name": "olav-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "OLAV Monorepo - Cloudflare Pages wrapper for olav-web",
  "scripts": {
    "build": "cd olav-web && npm install && npm run build",
    "dev": "cd olav-web && npm run dev",
    "preview": "cd olav-web && npm run preview"
  },
  "workspaces": [
    "olav-web"
  ]
}
