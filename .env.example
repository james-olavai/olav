# OLAV Environment Configuration
# This file contains ONLY sensitive credentials and Docker-specific variables
# Application configuration is in config/settings.py

# ============================================
# LLM API Keys (SENSITIVE)
# ============================================
LLM_PROVIDER=openai              # openai | ollama | azure
LLM_API_KEY=sk-your-key-here
LLM_MODEL_NAME=gpt-4-turbo       # Override default if needed

# ============================================
# Infrastructure Credentials (SENSITIVE)
# ============================================
# PostgreSQL
POSTGRES_USER=olav
POSTGRES_PASSWORD=OlavPG123!
POSTGRES_DB=olav

# OpenSearch (Docker environment variables)
DISABLE_SECURITY_PLUGIN=true
OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m
discovery.type=single-node

# ============================================
# NetBox Integration (SENSITIVE)
# ============================================
NETBOX_URL=https://netbox.company.com
NETBOX_TOKEN=your_netbox_token_here

# ============================================
# Device Credentials (SENSITIVE)
# ============================================
DEVICE_USERNAME=cisco
DEVICE_PASSWORD=cisco

# ============================================
# Runtime Environment Detection
# ============================================
# ENVIRONMENT=local              # local | docker (auto-detected)

# ============================================
# Manual URI Overrides (Optional)
# ============================================
# Only set these if you need custom endpoints
# POSTGRES_URI=postgresql://olav:OlavPG123!@custom-host:5432/olav
# OPENSEARCH_URL=http://custom-host:9200
# REDIS_URL=redis://custom-host:6379
