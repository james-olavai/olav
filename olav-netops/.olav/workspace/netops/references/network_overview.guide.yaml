schema_version: 1
intent: network_overview
agent: netops
keywords:
  - network overview
  - what devices
  - what is the topology
  - inventory
  - which routers
  - what AS
  - network topology
  - device inventory
  - which routers
  - environment
  - what does the network look like
  - lay of the land
body: |
  # Network Overview
  _Auto-generated 2026-05-10 00:30 from netops.devices + netops.v_bgp_neighbors_auto + netops.v_l2_links_auto._
  
  **Inventory**: 4 routed, 2 access, 0 other.
  
  ## Devices
  
  - `R1` (juniper_junos) role=border mgmt=192.168.100.101 loopback=1.1.1.1 AS=65000
  - `R2` (cisco_ios) role=border mgmt=192.168.100.102 loopback=2.2.2.2 AS=65001
  - `R3` (cisco_ios) role=core mgmt=192.168.100.103 loopback=3.3.3.3 AS=65000
  - `R4` (cisco_ios) role=core mgmt=192.168.100.104 loopback=4.4.4.4 AS=65001
  - `SW1` (cisco_ios) role=access mgmt=192.168.100.105 loopback=- AS=-
  - `SW2` (cisco_ios) role=access mgmt=192.168.100.106 loopback=- AS=-
  
  ## BGP topology
  
  **6 sessions** (6 Established, 4 of those receiving 0 prefixes).
  
  | Device | Peer | AS | State | Prefixes |
  |---|---|---|---|---|
  | R1 | 10.1.12.2 | 65001 | Established | - |
  | R1 | R3 (3.3.3.3) | 65000 | Established | - |
  | R2 | 10.1.12.1 | 65000 | Established | 0 |
  | R2 | R4 (4.4.4.4) | 65001 | Established | 0 |
  | R3 | R1 (1.1.1.1) | 65000 | Established | 0 |
  | R4 | R2 (2.2.2.2) | 65001 | Established | 0 |
  
  ## L2 topology (LLDP/CDP)
  
  **8 adjacencies** (showing first 80):
  
  - `R1:ge-0/0/2` ↔ `R3:to_R1_Gi2` (LLDP, up)
  - `R1:ge-0/0/0` ↔ `WAN:Ethernet0/0` (LLDP, up)
  - `R2:GigabitEthernet1` ↔ `WAN:Ethernet0/1` (CDP, up)
  - `R3:Ethernet0/0` ↔ `R1:ge-0/0/2` (LLDP, up)
  - `R4:Ethernet0/0` ↔ `R2:GigabitEthernet2` (CDP, up)
  - `R4:Ethernet0/1` ↔ `SW2:Ethernet0/0` (CDP, up)
  - `SW1:Ethernet0/0` ↔ `R3:Ethernet0/1` (CDP, up)
  - `SW2:Ethernet0/2` ↔ `Switch:Ethernet0/0` (CDP, up)
  
  ## Notes for the agent
  
  - Use this overview BEFORE calling inspect_* — it answers "who exists", "what AS", "who peers with whom" without any tool calls.
  - Cross-reference device hostnames + AS + loopback IP when the user mentions an IP — the BGP table above maps both ways.
  - Numbers reflect the most recent ingested snapshot at the time of generation.  For real-time state changes, still call inspect_routing or take a fresh snapshot.
