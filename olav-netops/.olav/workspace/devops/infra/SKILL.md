---
name: infra
description: "Query registered services (NetBox DCIM/IPAM, InfluxDB, etc.) + generate bulk change scripts. Sub-agent of devops orchestrator."
agent_type: api  # rev 266: skip TodoListMiddleware
tools:
  - format_and_export
  - api_request           # hit NetBox / InfluxDB / other registered services
  - service_health        # check connectivity to a service before querying it
  - execute_sql           # cross-reference with OLAV local cache when needed
  - recall_memory         # prior endpoint / payload decisions
static_context:
  - path: ./references/netbox_dcim_api.md
  - path: ./references/netbox_ipam_api.md
  - path: ./references/influxdb_netops_Query_api.md
  - path: ./references/influxdb_netops_Health_api.md
static_context_mode: on_intent
system: $ref:./prompts/system.md
metadata:
  version: 0.2.0
  type: agent
  agent_type: api
  category: infrastructure-management
  intents:
    - device_lookup
    - ip_query
    - metric_query
    - changeset_generation
---

# Infra Workspace

Operational agent for infrastructure query and changeset generation.

All tools are inherited from the `core` workspace (globally available).
This workspace provides references and prompt context for infrastructure-specific workflows.

| Tool | Used for |
|------|---------|
| `api_request` | Query registered services (GET) |
| `run_python_code` | Simulate and validate changesets |
| `format_and_export` | Export changesets to `exports/scripts/` |

## API References

Reference docs are auto-generated by `olav registry register`:
- `references/netbox_api.md` — NetBox endpoint reference
- `references/influxdb_netops_api.md` — InfluxDB endpoint reference

Run `olav registry register <service>` to regenerate after schema changes.
