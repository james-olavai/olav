1. 解决架构问题
- 迁移到deepagents和langchain的原生方案，使用原生的agents，subagents，agents之间任务调度，缓存等高级功能，以及原生的skills，tools调用
- 符合claude code skill框架，可以实现和底层agent框架无关的迁移，既原代码可以用deepagent，也可以用用claude code的框架
- skill为中心的框架，用户可以直接编辑skill来完成大部分的系统配置，添加新的功能，agent
- 良好的扩展能力

2. TDD开发完善
- 建立测试规范，整理测试脚本，编写测试文档，整理test目录，分为pytest的unit测试和e2e测试，删除冗余的测试项目
- 完善e2e功能测试：完善目前极大核心功能的真实e2e测试，模拟用户行为与场景，调用真实LLM和设备进行测试，而不是绕过模拟，进行作弊测试。1. CLI交互，CLI命令，CLI行为，guard，缓存，会话，CLI输出效果，人机交互等机制。2. 核心架构，多agnet系统，任务分配，编排，多agent数据收集，agnet fallback，工具调用，回答结果判断，markdown等 3. sql query agent功能测试，检查查询，复杂，联合查询，缓存快速命中，性能 4. CLI agent输入和输出，黑名单，缓存快速命中，性能 5. expert agent，复杂问题的推理结果准确性，多工具的调用能力，协调能力，langchain原生的多工具使用能力，snapshot，cli，diff，案例知识库，用户知识库，联网搜索，未来log等工具的集成能力 5. snapshot-inspection功能，数据收集的完整性，多厂商集成能力，map-reduce分析流程，厂商无关的巡检skill 6. txtfsm自学习 react工具，使用deepagents和langchain标准组件实现新命令的自学习，并导入数据库

3. 清理冗余代码，规范项目结构，使用pytest和ruff，提高代码质量，发现问题并修复，集成CI/CD，稳定版本，安全检查，检查gitignore，发布