# OLAV v0.9.8 开发指南

## ✅ 核心架构

### Orchestrator 统一协调器
**职责**: 路由 + 执行 + 评估 + 输出（单一 LLM 入口）

```python
class Orchestrator:
    async def orchestrate(query: str):
        # 1. 检查缓存 → FastPath (0.2秒)
        # 2. 路由决策 + 计划生成（LLM 1）
        # 3. 执行 Agent(s)
        # 4. 质量评估 + 升级决策（LLM 2）
        # 5. Markdown 输出（LLM 3）
        # 6. 缓存学习
```

### Expert Agent 增强
**能力**: Embedding 检索 + 网络搜索 + 文档编辑

```python
class ExpertAgent:
    async def diagnose(symptom: str):
        # 1. Embedding 检索历史案例（symptom_embedding）
        # 2. RAG 检索知识库（.olav/knowledge/）
        # 3. 网络搜索（可选，敏感信息过滤）
        # 4. ReAct 诊断
        # 5. 自动更新知识库 → 触发向量化
```

### 知识库白名单模式
**位置**: `.olav/knowledge/` （运行时数据，Git 排除）

```
.olav/knowledge/
├── protocols/          # 协议排错手册
├── solutions/          # 解决方案
└── cases/              # Agent 自动维护的案例
```

**流程**: Agent 直接编辑 → Git commit → 自动向量化 → 下次检索

---

## 🚫 禁止模式

### 1. 不创建冗余组件
```python
# ❌ 禁止
class QualityChecker: pass
class ResultMerger: pass
class PlanAgent: pass

# ✅ 统一到 Orchestrator
```

### 2. 不使用硬性时间窗口
```python
# ❌ 禁止
WHERE age_days <= 30

# ✅ 让 LLM 判断时效性
ORDER BY created_at DESC LIMIT 10
```

### 3. 不使用草稿审核流程
```python
# ❌ 禁止
.olav/drafts/ → 人工审核 → .olav/knowledge/

# ✅ 直接编辑 + Git 回滚
.olav/knowledge/ → Git commit → 向量化
```

---

## � 参考文档

详细设计请查看：
- `docs/01_db_design.md` - 数据库架构
- `docs/02_expert_design.md` - Expert Agent 设计
- `docs/03_router_design.md` - Orchestrator 设计
- `docs/04_knowledge_base_implementation.md` - 知识库实施

---

## 🔧 常用命令

```bash
# 代码检查
uv run ruff check src/ --fix && uv run ruff format src/
uv run pyright src/

# 测试
uv run pytest tests/00_e2e_acceptance_test.py -v

# 知识库向量化
uv run olav knowledge index
```

---

**版本**: v0.9.8
