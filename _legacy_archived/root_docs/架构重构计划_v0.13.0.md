# 🏗️ 重大架构重构计划 v0.13.0

**目标**: 简化从硬编码commands和有bug的tables到动态命令加载和简洁数据结构

**日期**: 2026-02-13

---

## 📋 需要完成的任务

### 1. 命令系统重构

**删除**: 硬编码的30个fallback_commands  
**位置**: `.olav/skills/shared/tools/sync_tools.py` 第534-548行

**当前代码**:
```python
# Fallback to basic command set if no commands in registry/whitelist
commands = [
    "show version",
    "show running-config",
    # ... 28 more
]
```

**改为**: 直接使用动态加载，无fallback

---

### 2. 表系统重构

**删除的表**:
- `raw_outputs` (已废弃, v0.10.1)
- `command_outputs` (设计有bug, 列名错误)

**新的简单表**:
```sql
CREATE TABLE parsed_outputs (
    id INTEGER PRIMARY KEY,
    device_name VARCHAR NOT NULL,
    command VARCHAR NOT NULL,
    parsed_data JSON NOT NULL,      -- TextFSM解析结果
    snapshot_date DATE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(device_name, command, snapshot_date)
)
```

**只有一个表**: parsed_outputs，只存储JSON结果，不存储原始文本

---

### 3. 代码清理清单

| 组件 | 操作 | 优先级 |
|------|------|--------|
| sync_tools.py | 删除fallback_commands (L534-548) | P0 |
| database.py | 删除raw_outputs表定义 | P0 |
| database.py | 删除command_outputs表定义 | P0 |
| database.py | 添加新的parsed_outputs表定义 | P0 |
| raw_importer.py | 完全重写为只导入JSON | P0 |
| cli_main.py | 删除SELECT COUNT(*) FROM raw_outputs | P0 |
| inspector.py | 删除SELECT FROM raw_outputs | P0 |
| 所有REFERENCE.md | 删除对raw_outputs的引用 | P1 |
| system_prompt.md | 删除对raw_outputs的查询示例 | P1 |
| SKILL.md | 更新文档引用raw_outputs的部分 | P1 |
| 3份诊断报告 | 删除过时的诊断文档 | P1 |

---

## 🔧 具体实现步骤

### Step 1: 简化数据库表结构

**文件**: `src/olav/core/database.py`

删除:
- 行 ~820-900: CREATE TABLE raw_outputs块
- 行 ~545-590: CREATE TABLE command_outputs块

添加新表:
```python
conn.execute("""
    CREATE TABLE IF NOT EXISTS parsed_outputs (
        id INTEGER PRIMARY KEY,
        device_name VARCHAR NOT NULL,
        command VARCHAR NOT NULL,
        parsed_data JSON NOT NULL,
        snapshot_date DATE NOT NULL,
        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(device_name, command, snapshot_date)
    )
""")
conn.execute("""
    CREATE INDEX IF NOT EXISTS idx_parsed_device
    ON parsed_outputs(device_name)
""")
conn.execute("""
    CREATE INDEX IF NOT EXISTS idx_parsed_command
    ON parsed_outputs(command)
""")
```

### Step 2: 重写数据导入器

**文件**: `.olav/skills/shared/tools/raw_importer.py`

删除所有内容，重новое:

```python
"""Simple JSON importer for parsed command outputs."""

import json
import logging
from pathlib import Path
import duckdb

logger = logging.getLogger(__name__)

def import_sync_data(sync_dir: Path) -> dict[str, int]:
    """Import parsed JSON files to parsed_outputs table."""
    
    from olav.core.database import get_database
    db = get_database()
    conn = db.conn
    
    parsed_dir = sync_dir / "parsed"
    if not parsed_dir.exists():
        return {"parsed_imported": 0}
    
    imported = 0
    snapshot_date = sync_dir.name  # 目录名作为快照日期
    
    for device_dir in parsed_dir.iterdir():
        if not device_dir.is_dir():
            continue
        
        device_name = device_dir.name
        
        for json_file in device_dir.glob("*.json"):
            try:
                data = json.loads(json_file.read_text(encoding="utf-8"))
                command = data.get("command", json_file.stem)
                
                conn.execute(
                    """
                    INSERT INTO parsed_outputs
                    (device_name, command, parsed_data, snapshot_date)
                    VALUES (?, ?, ?, ?)
                    ON CONFLICT DO NOTHING
                    """,
                    [device_name, command, json.dumps(data), snapshot_date]
                )
                imported += 1
                
            except Exception as e:
                logger.debug(f"Failed to import {device_name}/{json_file.name}: {e}")
    
    return {"parsed_imported": imported}
```

### Step 3: 删除硬编码命令

**文件**: `.olav/skills/shared/tools/sync_tools.py`

删除第534-548行的fallback_commands列表

改为:
```python
if commands is None:
    from olav.core.registry import get_command_registry
    
    first_host = nr_filtered.inventory.hosts[device_names[0]]
    platform = first_host.platform
    
    registry = get_command_registry()
    settings = get_settings()
    
    if settings.sync.command_mode == "whitelist":
        commands = registry.get_whitelisted_commands(platform)
        source_info = f"whitelist ({len(commands)} commands)"
    else:
        commands_dict = registry.list_commands(platform=platform)
        commands = list(commands_dict.get(platform, []))
        source_info = f"all NTC templates ({len(commands)} commands)"
    
    if not commands:
        raise ValueError(f"No commands available for platform {platform}")
    
    print(f"Using {source_info}")
```

### Step 4: 清理CLI代码

**文件**: `src/olav/cli/cli_main.py`

删除:
- 第755行: `SELECT COUNT(*) FROM raw_outputs`
- 第994行: `SELECT COUNT(*) FROM raw_outputs`

### Step 5: 清理Agent代码

**文件**: `src/olav/agents/inspector.py`

删除:
- 第34行: `SELECT DISTINCT device FROM raw_outputs`
改为: `SELECT hostname FROM devices`

### Step 6: 更新文档和提示

**文件列表**:
- `.olav/skills/network-query/REFERENCE_SIMPLE.md`
- `.olav/skills/network-query/system_prompt.md`
- `.olav/skills/network-expert/REFERENCE.md`
- `.olav/skills/network-inspection/REFERENCE.md`
- `.olav/skills/network-inspection/SKILL.md`

删除所有对raw_outputs的引用，改为parsed_outputs

**示例**:
```markdown
# ❌ 删除
SELECT device, command, output FROM raw_outputs
SELECT parsed_data FROM command_outputs

# ✅ 改为
SELECT device_name, command, parsed_data FROM parsed_outputs
```

### Step 7: 删除过时文档

```bash
rm 深度诊断_raw_outputs_command_outputs_问题分析.md
rm 代码验证报告_raw_outputs问题确认.md
rm 用户4个问题的完整答案.md
```

---

## ✅ 验证清单

完成后需要验证:

- [ ] 数据库初始化不报错
- [ ] `olav init` 命令可以运行
- [ ] 命令从registry动态加载（不使用硬编码）
- [ ] JSON解析并导入到parsed_outputs表
- [ ] 不存在对raw_outputs/command_outputs的查询
- [ ] 所有文档引用已更新

---

## 📦 修改影响范围

**直接修改文件**:
1. src/olav/core/database.py (表定义)
2. .olav/skills/shared/tools/sync_tools.py (删除fallback)
3. .olav/skills/shared/tools/raw_importer.py (重写)
4. src/olav/cli/cli_main.py (删除查询)
5. src/olav/agents/inspector.py (删除查询)
6. 6个REFERENCE*.md和SKILL.md文件 (更新文档)

**间接影响**:
- 初始化流程会更简单（无fallback）
- 数据导入会更快（设计更简单）
- SQL查询会更简单（表少了）
- 代码冗余度减少

---

## 🚀 预期结果

**代码质量**:
- ❌ 硬编码命令 → ✅ 动态命令注册
- ❌ 两个有问题的表 → ✅ 一个简单的表
- ❌ 复杂导入逻辑 → ✅ 简单JSON导入
- ❌ 50+行废弃代码 → ✅ 干净的代码库

**代码行数**:
- raw_importer.py: 110行 → 40行
- database.py: 1000+行 → 950行
- sync_tools.py: 887行 → 870行

**性能**:
- 初始化: 更快（无fallback尝试）
- 查询: 更快（表少、设计简单）
- 导入: 更快（少一个表）

