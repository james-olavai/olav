## Executive Summary

No incident clusters detected. Cross-section correlation reveals widespread OSPF adjacency failures on R1, R2, SW1, SW2 (zero FULL neighbors, Critical), likely due to shared configuration inconsistencies or interface issues; separate DR election anomalies on R3 (segment 4) and R4 (segment 3) indicate priority misconfigurations (Warning).

**Prioritized Action Items**:
1. 🔴 R2 — OSPF_FULL_NEIGHBORS_COUNT 0 — Verify OSPF enabled on expected interfaces and check configuration consistency.
2. 🔴 SW2 — OSPF_FULL_NEIGHBORS_COUNT 0 — Verify OSPF enabled on expected interfaces and check configuration consistency.
3. 🔴 SW1 — OSPF_FULL_NEIGHBORS_COUNT 0 — Verify OSPF enabled on expected interfaces and check configuration consistency.

**Network Health**: 🔴 Critical — Multiple devices lack OSPF full neighbors, risking routing instability across segments (≤30 words).
---
```



## OSPF_NON_FULL_NEIGHBORS

✅ OSPF_NON_FULL_NEIGHBORS: No anomalies detected in this window.

## OSPF FULL 状态邻居数量检查

| Device | Check Item              | Current Value | Notes                  |
|--------|-------------------------|---------------|------------------------|
| R2     | OSPF_FULL_NEIGHBORS_COUNT | 0             | 🔴 Critical           |
| SW2    | OSPPF_FULL_NEIGHBORS_COUNT | 0             | 🔴 Critical           |
| SW1    | OSPF_FULL_NEIGHBORS_COUNT | 0             | 🔴 Critical           |
| R1     | OSPF_FULL_NEIGHBORS_COUNT | 0             | 🔴 Critical           |

这些设备无 FULL 状态 OSPF 邻居。建议验证 OSPF 是否启用在预期接口，并检查配置一致性。


## OSPF_ZERO_PRIORITY_NEIGHBORS

✅ OSPF_ZERO_PRIORITY_NEIGHBORS: No anomalies detected in this window.


## OSPF_NEIGHBORS_ON_DOWN_INTERFACES

✅ OSPF_NEIGHBORS_ON_DOWN_INTERFACES: No anomalies detected in this window.

## DR/BDR 状态分布分析

| Device | Check Item       | Current Value | Notes                  |
|--------|------------------|---------------|------------------------|
| R4     | OSPF_DR_COUNT_3 | 3             | ⚠️ Warning，多 DR     |
| R3     | OSPF_DR_COUNT_4 | 4             | ⚠️ Warning，多 DR     |

检测到 segment 3 和 4 存在多个 DR，偏离理想状态（每个 segment 1 DR + 1 BDR）。建议检查 OSPF 邻居优先级配置。



---

## 🔁 Post-Check Playbook

> 以下是基于本次审计结果生成的排错建议。将对应命令复制到终端执行，Ops Agent 将使用 networkx 拓扑模拟和 SSH 验证帮助你定位并确认修复效果。


### Priority 3 — General Health Check (remaining devices)

**1.** `R1` — 通用健康确认

```bash
olav -a ops "对 R1 执行快速健康确认：show logging last 50 | show interfaces summary | show processes cpu sorted | head 10，与 DuckDB 历史快照对比，确认巡检窗口内状态稳定"
```

**2.** `R2` — 通用健康确认

```bash
olav -a ops "对 R2 执行快速健康确认：show logging last 50 | show interfaces summary | show processes cpu sorted | head 10，与 DuckDB 历史快照对比，确认巡检窗口内状态稳定"
```

**3.** `R3` — 通用健康确认

```bash
olav -a ops "对 R3 执行快速健康确认：show logging last 50 | show interfaces summary | show processes cpu sorted | head 10，与 DuckDB 历史快照对比，确认巡检窗口内状态稳定"
```

**4.** `R4` — 通用健康确认

```bash
olav -a ops "对 R4 执行快速健康确认：show logging last 50 | show interfaces summary | show processes cpu sorted | head 10，与 DuckDB 历史快照对比，确认巡检窗口内状态稳定"
```

**5.** `SW1` — 通用健康确认

```bash
olav -a ops "对 SW1 执行快速健康确认：show logging last 50 | show interfaces summary | show processes cpu sorted | head 10，与 DuckDB 历史快照对比，确认巡检窗口内状态稳定"
```


> 每条命令相互独立，可按优先级逐条执行。Ops Agent 会通过 SSH 连接设备并结合 networkx 拓扑模型给出判断，无需预先准备其他参数。
